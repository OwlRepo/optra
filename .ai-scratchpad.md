# Chat markdown + modal focus + crawl status clarity

Status: IMPLEMENTATION_READY

Deep implementation approved: Yes

## Scope
- `apps/web/app/workspaces/[id]/chat/page.tsx`
- `apps/web/app/workspaces/[id]/chat/page.spec.ts`
- `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx`
- `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.spec.ts`
- `apps/web/src/components/workspace-search.tsx`
- `apps/web/src/components/workspace-search.spec.ts`
- `apps/web/src/lib/ui/modal.spec.ts`
- `packages/ui/src/components/ui/modal.tsx`
- `apps/web/package.json`
- docs sync: `docs/ai/{module-ownership-map,testing-strategy,risk-register,file-index/repository-map}.md`

## TDD order
1. `apps/web/app/workspaces/[id]/chat/page.spec.ts`
   - markdown renders bold + bullet list
   - assistant bubble wrapper is explicit full width
2. `apps/web/src/lib/ui/modal.spec.ts`
   - rerender while typing does not steal focus from modal input
3. `apps/web/src/components/workspace-search.spec.ts`
   - cmd+k autofocuses search input
   - typing keeps focus on query input
4. `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.spec.ts`
   - scrape modal autofocuses URL input
   - typing keeps focus on URL input
   - queued/running render `In progress`
   - page counts are labeled, not bare status-like numbers
5. run targeted web/ui tests, confirm fail
6. implement deps + UI fixes
7. rerun targeted tests
8. run `bun run --cwd packages/ui test`, `bun run --cwd apps/web test`, `bun run --cwd packages/ui type-check`, `bun run --cwd apps/web type-check`

## Notes
- Keep API unchanged unless UI fix fails to resolve crawl-status confusion.
- Markdown render must stay safe: no raw HTML rendering.
- Full-width chat bubbles = row spans full width; inner text width still capped for readability.
- Shared modal fix must stop effect reruns from refocusing panel on each keystroke.
- Docs sync required in same change.
