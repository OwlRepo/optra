# Docker dev/prod hardening + Mnemra rebrand + CI/CD

Status: IMPLEMENTATION_READY

Deep implementation approved: Yes

Full plan: /Users/romeoangelesjr/.claude/plans/users-romeoangelesjr-documents-personal-precious-bonbon.md

## Scope
- `apps/api/src/health/health.controller.ts` (new), `apps/api/src/health/health.controller.spec.ts` (new), `apps/api/src/app.module.ts`
- `apps/api/Dockerfile`, `apps/web/Dockerfile` (4-stage rewrite: base/deps/dev/prod)
- `docker-compose.yml`, `docker-compose.prod.yml` (full rewrite)
- `docker/seaweedfs/s3.prod.json.example` (new), `.gitignore` (add `docker/seaweedfs/s3.prod.json`)
- `.github/workflows/deploy.yml` (new)
- `scripts/dev.sh` (delete), `scripts/stop.sh` (delete), `scripts/deploy.sh` (rebrand), `scripts/deploy-remote.sh` (rebrand)
- `package.json` (name + docker:* scripts)
- Rebrand sweep: `apps/web/app/layout.tsx`, `apps/web/app/page.tsx`, `apps/web/app/(auth)/{login,register,verify-otp}/page.tsx`, `README.md`, `docker/init-db.sql`, `.env.example`, `.env.production`, `docs/ENVIRONMENT_SETUP.md`, `docs/ROADMAP.md`, `docs/designs/elid-ticket-copilot.md`, `SUMMARY.md`, `DEPLOYMENT.md`, `DOCKER.md`
- docs sync: `docs/ai/{module-ownership-map,task-router,file-index/repository-map,risk-register,testing-strategy}.md`

## TDD order
1. `apps/api/src/health/health.controller.spec.ts` — controller defined, `GET /health` returns `{status:'ok'}` with zero deps
2. run `cd apps/api && bun run test -- health`, confirm fail (module not found)
3. implement `apps/api/src/health/health.controller.ts` + wire into `app.module.ts`
4. rerun targeted test, confirm pass
5. run full `apps/api` suite, confirm no collision
6. everything else in this task is infra/config/docs, not unit-testable — verified operationally instead (see Verification below), per the new docs/ai/testing-strategy.md section this task adds

## Notes
- 3 real bugs fixed in existing prod Docker setup: `bun.lockb` → `bun.lock` (wrong lockfile name broke every image build), missing `docker/seaweedfs/s3.prod.json` (compose referenced a file that never existed), `docker-compose.prod.yml` `env_file` pointed at the committed template `.env.production` instead of the real gitignored `.env.prod`.
- No live production data exists anywhere yet under `support_brain` — confirmed with user, safe to rename DB name/containers directly, no migration dance.
- `@repo/*` workspace package scope is intentionally NOT renamed (generic placeholder, not brand text).
- Local folder stays named `second-brain` on disk; `name: mnemra` / `name: mnemra-prod` top-level compose keys decouple volume/network naming from the folder name.
- GitHub Actions workflow structurally mirrors suki's proven deploy.yml; user configures VPS_HOST/VPS_USER/VPS_SSH_KEY/VPS_PORT/DEPLOY_DOMAIN secrets on `OwlRepo/mnemra` themselves — no live SSH deploy executed as part of this work.
- `scripts/deploy.sh`/`deploy-remote.sh` kept alongside the new GitHub Actions workflow as a manual fallback path.

## Verification
- `docker compose build` (dev) and `docker compose -f docker-compose.prod.yml build` (prod) both succeed
- `docker compose up` brings up all 5 dev services healthy; `curl localhost:3001/health` → 200; `curl localhost:3000` → 200
- edit a file under `apps/api/src` and `apps/web/app` while stack is running, confirm hot-reload log lines
- `cd apps/api && bun run test`, `cd apps/web && bun run test`, `packages/{db,ai,ui}` `bun run test` all still pass
- `bun run type-check` (root) passes
