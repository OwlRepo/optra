# Hedged partial answers instead of blunt "not enough info" refusal (Chat/RAG)

Status: IMPLEMENTATION_READY

Deep implementation approved: Yes

Task Size: Standard (2 source files, no schema/API contract shape change) — Risk kept at Deep per
docs/ai/module-ownership-map.md Chat/RAG domain designation (not downgraded).

Contract Areas:
- API: No contract shape changes. `AnswerResult{sources, stream, isFallback}` unchanged. Clarifies (does not
  change) `isFallback` semantics — see docs/ai/contracts/api-contracts.md update below.
- Database: No schema changes required.
- Permissions: No contract impact.
- External integrations: No contract impact.
- Jobs / automations: No contract impact.

Risk Register Notes: Deep (Chat/RAG domain). This changes model-facing guardrail wording in a
verified-as-of-2026-07-02 risk area (docs/ai/risk-register.md lines 193-217). The two structural hard-fallback
paths (zero-chunk branch, rewrite-exhausted `fallbackNode`) are NOT touched — they remain the only paths that
set `isFallback: true`, and the existing invariant "fallback responses never write to chat_cache" is
unaffected since hedged answers are ordinary `isFallback: false` generations that already flow through the
normal generate path. Required checks: regression tests on both hard-fallback paths must keep passing
byte-for-byte on their asserted message text; new hedge wording must not remove or alter the literal sentence
`"I don't have enough information to answer that."` used by those two structural paths' own hardcoded strings
(`FALLBACK_MESSAGE` in graph.ts, the zero-chunk stream in index.ts) — those are separate constants, not shared
with the system prompts, so no double-edit risk there, but the system-prompt copy of that same sentence (used
when the LLM decides context has nothing relevant) must be preserved verbatim too, since it's asserted by name
in tests.

Full plan: /Users/romeoangelesjr/.claude/plans/i-think-i-agree-crystalline-blossom.md

## Root cause (for context, not part of the fix)

Retrieval worked correctly (4 chunks, scores 0.60-0.67, above `RETRIEVAL_SCORE_THRESHOLD=0.35`). Request took
`packages/ai/src/chains/index.ts`'s default path because `LANGGRAPH_ENABLED` was not `'true'` — that path has
no numeric confidence gate, only a zero-chunk special case. With chunks present, the LLM was hard-instructed
to refuse unless the context "fully" answered the question, so it refused rather than synthesize a partial
answer, even though sources were correctly attached to the response the whole time.

## Scope

New: none.
Modified:
- `packages/ai/src/chains/graph.ts` — reword `ANSWER_SYSTEM_PROMPT` (lines 36-39) and
  `REGENERATE_SYSTEM_PROMPT` (lines 41-44)
- `packages/ai/src/chains/graph.spec.ts` — add prompt-content assertion test + hedged-non-fallback generate
  test; keep existing rewrite-exhausted fallback test unmodified
- `packages/ai/src/chains/index.ts` — reword `SYSTEM_PROMPT` (lines 22-25) to stay character-for-character
  identical to `ANSWER_SYSTEM_PROMPT` after the edit, exactly as it is today
- `packages/ai/src/chains/index.spec.ts` — add prompt-content assertion test + hedged-non-fallback generate
  test; keep existing zero-chunk fallback test unmodified
- `apps/api/src/chat/chat.service.spec.ts` — no edits; re-run only, regression check

### Exact prompt text

`ANSWER_SYSTEM_PROMPT` (graph.ts) and `SYSTEM_PROMPT` (index.ts) — identical, replace both with:
```
You are a helpful support assistant.
Answer using ONLY the context provided below.
If the context fully answers the question, answer directly and concisely.
If the context is only partially relevant, say what you found, be explicit about what's missing or
uncertain, and point the user to the sources below. Do not invent specifics that aren't stated in the
context.
If the context has nothing relevant to the question, say: "I don't have enough information to answer that."
Be concise, accurate, and do not make up information.
```

`REGENERATE_SYSTEM_PROMPT` (graph.ts) — replace with:
```
You are a careful support assistant.
Answer using ONLY the provided context.
If any part is unsupported, omit it.
If the context is only partially relevant, keep only the supported parts, state clearly what's missing, and
point the user to the sources below.
If context is insufficient, say: "I don't have enough information to answer that."
```

No changes to: `FALLBACK_MESSAGE`, `fallbackNode`, the zero-chunk branch (`index.ts:74-81`), `AnswerResult`
shape, retrieval, threshold, or self-grade logic.

### Docs sync (same change)
- `docs/ai/module-ownership-map.md` (Chat/RAG entry, ~line 36): add a dated note that fallback prompts now
  distinguish "no relevant context" (hard refusal, unchanged) from "partial context" (hedged, cited answer).
- `docs/ai/contracts/api-contracts.md` (~line 78): clarify `isFallback` explicitly — true only on the two
  structural paths (zero chunks / rewrites exhausted); hedged partial answers are `isFallback: false`.
- `docs/ai/testing-strategy.md` (~lines 93-98): note the new prompt-content and hedged-answer test coverage
  added to graph.spec.ts / index.spec.ts.
- `docs/ai/risk-register.md` (~lines 193-217): dated entry recording this wording change and confirming the
  cache-skip/isFallback invariant was re-verified, not altered.

## TDD order
1. `packages/ai/src/chains/graph.spec.ts` (new test) — assert `ANSWER_SYSTEM_PROMPT` and
   `REGENERATE_SYSTEM_PROMPT` contain the new hedging instruction text (e.g. "partially relevant", "point the
   user to the sources") AND still contain the literal `"I don't have enough information to answer that."`
   sentence → confirm fails against current prompt text → apply prompt edit → confirm pass.
2. `packages/ai/src/chains/graph.spec.ts` (new test) — non-empty, partial-relevance chunks + mocked
   `ChatOpenAI.stream()` returning a canned hedged-style answer → assert `isFallback === false` and `sources`
   passed through unchanged → confirm fails (or is meaningless) before the prompt/plumbing exists → confirm
   pass after.
3. `packages/ai/src/chains/graph.spec.ts` — re-run existing "falls back after max rewrites when retrieval
   stays low" (currently lines 136-159) unmodified → must still pass.
4. `packages/ai/src/chains/index.spec.ts` — repeat patterns 1-3 for `SYSTEM_PROMPT` / `answerQuestion()`;
   existing "returns fallback when retrieval empty" (currently lines 190-203) must still pass unmodified.
5. `apps/api/src/chat/chat.service.spec.ts` — re-run existing "skips cache writes for fallback miss answers"
   (currently lines 186-222) and the sources-passthrough test (currently lines 115-184) unmodified → must
   still pass (regression only, file not edited).
6. Full-suite verification (see below).

## Notes
- `AnswerResult` sources attachment (`buildSources()` in both files) already runs independently of what the
  LLM decides to say — do not add any new sources-attachment logic, it already works correctly today (this is
  why the original bug report showed 4 sources next to a blunt refusal).
- Keep `ANSWER_SYSTEM_PROMPT` (graph.ts) and `SYSTEM_PROMPT` (index.ts) textually identical after the edit,
  matching today's existing character-for-character duplication — do not let them drift.
- Do not touch `FALLBACK_MESSAGE` (graph.ts) or the zero-chunk hardcoded stream (index.ts) — these are
  separate string literals from the system prompts and are out of scope.

## Verification
- `cd packages/ai && npx vitest run` (package.json:9 test script)
- `cd apps/api && npm test -- chat.service.spec.ts` (Jest, regression only)
- `git diff --name-only` matches the Scope file list above exactly — no unlisted changes

## Manual QA
- Re-ask the exact original question ("how about the port for the REST API of biostar 2?") in a dev workspace
  chat. Expect a hedged answer mentioning 51212 / 8795 with a note that no single documented "REST API port"
  was found, plus the same 4 sources shown below.
- Ask a genuinely off-topic question (nothing in the knowledge base) and confirm the hard refusal + empty
  sources still fires unchanged.
- Confirm a normal, fully-answerable question still gets a direct answer unchanged (no regression to the
  happy path).
