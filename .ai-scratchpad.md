# Refactor: Mnemra Logo Rebrand (bloom mark)

Status: DONE (implemented directly in-session, not handed to Codex — explicit user override of the normal Claude/Codex split for this task)

## What changed

Replaced the "19A folded-page M" mark with a new six-petal bloom mark supplied
by the user (raster PNG, no vector source), everywhere it appears. Display-only,
no behavior change. Internal identifier renamed `mnemra-folded-page` →
`mnemra-bloom`.

Design decisions confirmed with the user beforehand:
- Flat presentation, no white card/fold frame (the new mark isn't letter-based).
- OG image keeps its existing text/copy/layout — only the mark artwork swapped.

Color check: new mark's dominant color `#5663e0` vs prior brand `#5661df`
(SVG) / `#525edc` (manifest theme) — within rounding, no palette/token changes made.

## Files touched

- `apps/web/public/mnemra-mark.svg` — new bloom art (raster `<image>` embed),
  `<title>Mnemra bloom mark</title>`, `data-mark="mnemra-bloom"`, viewBox `0 0 625 602`
- `apps/web/src/components/brand-mark.tsx` — `data-brand-mark="mnemra-bloom"`
- `apps/web/app/icon.png` — regenerated, 32x32, flat mark, transparent bg
- `apps/web/app/favicon.ico` — regenerated, ICO (16/32/48), flat mark, transparent bg
- `apps/web/app/apple-icon.png` — regenerated, 180x180, flat mark on solid white
- `apps/web/app/opengraph-image.png` — old icon-card region patched to plain
  white, flat mark composited in the same slot; headline/tagline/description/
  accent line untouched
- Spec files updated (`mnemra-folded-page` → `mnemra-bloom`, plus matching
  human-readable "folded-page" wording): `apps/web/src/components/brand-mark.spec.tsx`,
  `apps/web/app/brand-images.spec.ts`, `apps/web/app/page.spec.ts`,
  `apps/web/app/loading.spec.tsx`, `apps/web/app/chat/loading.spec.tsx`,
  `apps/web/app/(auth)/login/page.spec.ts`, `apps/web/app/(auth)/register/page.spec.ts`,
  `apps/web/app/(auth)/verify-otp/page.spec.ts`
- Docs synced: `docs/ai/module-ownership-map.md` (Branding Sync Notes),
  `docs/ai/risk-register.md` (Brand Assets / Logo row),
  `docs/ai/testing-strategy.md` (brand asset note),
  `docs/ai/file-index/repository-map.md` (brand-related rows)

## Verification

`bun run --cwd apps/web test` — all brand/mark-related specs pass (268/274 total;
the 6 pre-existing failures are unrelated copy/JSON-LD mismatches in
`page.spec.ts` from prior uncommitted work on that file, not touched here).

Manual QA still open: visually confirm `/`, `/loading`, `/chat/loading`,
`/login`, `/register`, `/verify-otp`, the browser tab favicon, and a social
share-preview validator once deployed.

## Rollback

Single `git revert` of this change — pure asset + identifier-string swap, no
data/schema/deploy-ordering impact.
