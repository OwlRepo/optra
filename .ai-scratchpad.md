# AI SCRATCHPAD

Status: IMPLEMENTATION_READY

Claude writes here.

Codex implements from here only when Status is `IMPLEMENTATION_READY`.

Codex validates from here only when Status is `IMPLEMENTATION_READY` or `VALIDATION_READY`.

Do not store long-term project docs here.

---

## Task Summary
Full production-style redesign for `apps/web` with modern SaaS layout, upgraded shared UI primitives, polished loading/error/empty states, toast feedback, route boundaries, and hardened chat error handling.

## Task Type
Feature

## Task Size
Standard

## Human Approval

- RCA approved: N/A
- Discovery approved: Yes
- Plan approved: Yes
- Deep implementation approved: N/A

## Confirmed Facts
- Frontend uses Next.js App Router in `apps/web/app`.
- Shared UI package lives in `packages/ui`.
- Verified scripts: `apps/web` has `build`, `type-check`, `lint`.
- Chat route exists at `apps/web/app/api/chat/route.ts` and streams via `ai` SDK.
- No verified DB/auth/billing changes required for this task.

## Files To Modify
- `packages/ui/src/globals.css`
- `packages/ui/src/components/ui/button.tsx`
- `packages/ui/src/components/ui/input.tsx`
- `packages/ui/src/components/ui/card.tsx`
- `packages/ui/src/components/ui/badge.tsx`
- `packages/ui/src/components/ui/chat-bubble.tsx`
- `packages/ui/src/components/ui/skeleton.tsx`
- `packages/ui/src/components/ui/toaster.tsx`
- `packages/ui/src/components/ui/status-banner.tsx`
- `packages/ui/src/components/ui/app-header.tsx`
- `packages/ui/src/components/ui/page-shell.tsx`
- `packages/ui/src/components/ui/page-section.tsx`
- `packages/ui/src/components/ui/stat-card.tsx`
- `packages/ui/src/components/ui/empty-state.tsx`
- `packages/ui/src/index.ts`
- `apps/web/app/layout.tsx`
- `apps/web/app/page.tsx`
- `apps/web/app/dashboard/page.tsx`
- `apps/web/app/chat/page.tsx`
- `apps/web/app/api/chat/route.ts`
- `apps/web/app/loading.tsx`
- `apps/web/app/error.tsx`
- `apps/web/app/not-found.tsx`
- `apps/web/app/chat/loading.tsx`
- `apps/web/app/dashboard/loading.tsx`

## API Contract Changes
No API contract changes required.

## Database / Schema Changes
No schema changes required.

## Contract Areas
- API: `POST /api/chat` request validation + user-safe error response
- Database: none
- Permissions: none
- External integrations: OpenAI via `@ai-sdk/openai`
- Jobs / automations: none

## Risk Register Notes
Standard FE/UI task. Avoid auth/schema/billing scope. Keep API route compatible with existing `useChat` caller.

## Guardrails
- Do not modify unlisted files.
- Do not refactor unrelated code.
- Do not rename public APIs unless explicitly listed.
- Do not change database schema unless explicitly listed.
- Do not alter auth/permissions unless explicitly listed.
- Do not infer missing backend features beyond safe UI placeholders.

## Implementation Order
1. Shared design system + primitives
2. Shared shell/feedback components
3. Root layout/toaster
4. Landing/dashboard/chat rewrites
5. API hardening
6. Route loading/error states
7. Validation

## Verification Commands
- `cd apps/web && npm run type-check`
- `cd apps/web && npm run build`
- `cd apps/web && npm run lint`

## Manual Verification Flow
1. Open `/` on mobile + desktop
2. Open `/dashboard`; trigger quick actions/toasts; inspect empty/onboarding states
3. Open `/chat`; submit prompt; verify loading, error, retry, stop, suggestion chips
4. Verify keyboard focus rings + reduced visual noise
5. Hit unknown route for 404 UI

## Rollback / Risk Notes
Rollback = restore modified UI files and route boundaries. Primary risk = chat client/API error handling mismatch; validate via build and manual chat flow.

## Done Criteria
- All public pages redesigned with cohesive SaaS shell
- Shared components exported from `@repo/ui`
- Loading/empty/error/success states present
- Toast system mounted and used
- Chat route errors surface safely in UI
- Verified build/type-check run successfully
