# API Contracts

Purpose:

Map important frontend-backend contracts.

This file is map only.

It is not proof of behavior.

Verify all conclusions against real source code, tests, types, schemas, routes, controllers, services, stores, components, API contracts, database definitions.

If this map conflicts with source code, source code wins.

Mark stale or conflicting entries as `CONTRACT DRIFT`.

Mark missing contracts as `UNMAPPED CONTRACT`.

If frontend expectation and backend response differ, mark `CONTRACT MISMATCH`.

Do not invent request or response shapes.

Unknown fields must be marked `TODO: Fill after repository analysis. Do not treat as verified.`

---

## Shared convention: offset pagination (admin tables)

Introduced 2026-07-04 (Slice 0). Admin list surfaces (KB documents, website
crawls, members, ticket drafts, chat history) are migrating from keyset cursor
(`?cursor=&limit=` → `{items, nextCursor}`) to **offset pagination** so the UI can
jump to a specific page and go first/last.

- Request DTO: `OffsetQueryDto` (`apps/api/src/common/dto/offset-query.dto.ts`) —
  `page?` (>=1), `pageSize?` (1–100), `q?`, `sort?`, `sortDir?` (`asc|desc`);
  per-domain DTOs extend it with their own filters.
- Helpers: `resolveOffsetPage()` + `buildOffsetResult()` (`@repo/db`, `packages/db/src/pagination.ts`).
- Response shape: `{ items, page, pageSize, total, totalPages }` (replaces
  `{ items, nextCursor }`). `totalPages` is `0` for an empty set.
- Frontend control: `Pagination` (`@repo/ui`, `packages/ui/src/components/ui/pagination.tsx`).
- **Chat messages ("load older") stays on keyset cursor** — infinite scroll is correct there.

Each list row below is migrated in its own slice; until a row's Notes say
"offset (Slice N)", assume it still returns `{items, nextCursor}` — verify in source.

## Contract Index

| Domain | Feature | Method | Endpoint / Route | Frontend Caller | Backend Handler | Request Shape | Response Shape | Auth / Permission | Risk | Notes |
| ------ | ------- | ------ | ---------------- | --------------- | --------------- | ------------- | -------------- | ----------------- | ---- | ----- |
| Auth | Register | POST | `/auth/register` | `apps/web/app/api/auth/register/route.ts` (proxy) → `register/page.tsx` | `auth.controller.ts#register` → `auth.service.ts#register` | `{email, password}` | `{message}`, `429`, or `500` (email send failure) | None (public) | Deep | Email normalized to lowercase before any DB write/read. OTP sent via `NotificationsService` (console log in dev, Resend in prod). Throttled: 5 req / 10 min per caller (`@Throttle`), global default 60/min applies otherwise. Fixed 2026-07-05: `resend@6.16.0` never rejects on API-level failures (bad key, unverified domain, rate limit) — it always resolves `{data, error}`. `NotificationsService.sendOtp`/`sendInvite` now check `error` and throw `InternalServerErrorException` (logged) instead of discarding it, so a failed send surfaces as `500` instead of a false-positive `201` with no email ever sent. User row + OTP row are still created before the send attempt (unchanged) — a failed send leaves an unverified user stuck until they contact support or a resend-OTP endpoint is added (`UNMAPPED RISK`, see `risk-register.md`). Verified via `auth.service.spec.ts`, `notifications.service.spec.ts`, `test/auth.e2e-spec.ts`, `test/auth-rate-limit.e2e-spec.ts`. |
| Auth | Verify OTP | POST | `/auth/verify-otp` | `.../api/auth/verify-otp/route.ts` → `verify-otp/page.tsx` | `auth.controller.ts#verifyOtp` → `auth.service.ts#verifyOtp` | `{email, code}` | `{accessToken}` + `Set-Cookie: mnemra_rt` (httpOnly), or `429` | None (public) | Deep | Marks `users.isVerified=true`. Code must be the newest live one: unused, unexpired, and under 5 wrong guesses (`otps.failed_attempts`); compared in constant time. Unknown email, verified account and wrong code all answer 401 "Invalid or expired code"; the 5th wrong guess answers "Too many wrong codes. Request a new code." (2026-09-25). Throttled: 5 req / 10 min per caller. Verified live + e2e. `accessToken` in the response body is used once by `apps/web/app/api/auth/verify-otp/route.ts` to set the `mnemra_at` httpOnly cookie server-side; as of 2026-07-01 the page no longer persists it in client-side `sessionStorage` (see `Get Current User` row) — it only calls `markLoggedIn()`, a boolean flag, not the token itself. 2026-07-05: on success the page redirects to `/chat` (was `/workspaces`) — see Auth/Permissions row in `module-ownership-map.md`. |
| Auth | Resend OTP | POST | `/auth/resend-otp` | `apps/web/app/api/auth/resend-otp/route.ts` (proxy) → `verify-otp/page.tsx` "Send a new code" | `auth.controller.ts#resendOtp` → `auth.service.ts#resendOtp` | `{email}` | always `200 {message: 'If that account is waiting for verification, a new code is on its way.'}`, `400` (bad email), `429` (address limit), `500` (email send failure) | None (public) | Deep | Added 2026-09-25. Sends only for an existing unverified account within 3 codes/email/hour (`AuthLimitsService.takeOtpResend`); expires every older live code. Throttled 3 req / 10 min per address. |
| Auth | Login | POST | `/auth/login` | `.../api/auth/login/route.ts` → `login/page.tsx` | `auth.controller.ts#login` → `auth.service.ts#login` | `{email, password}` | `{accessToken}` + `Set-Cookie: mnemra_rt`, or `429` | None (public) | Deep | Blocks unverified accounts with 403. Per account: 20 failures/hour across all addresses → 429 "Too many sign-in attempts for this account. Try again later." (2026-09-25). Throttled: 10 req / 10 min per caller. Verified live: 10×401 then 429 with `Retry-After: 600`. Same client-side handling as `Verify OTP` — `markLoggedIn()` flag only, no raw token in `sessionStorage`. 2026-07-05: on success the page redirects to `/chat` (was `/workspaces`) — see Auth/Permissions row in `module-ownership-map.md`. |
| Auth | Refresh | POST | `/auth/refresh` | `.../api/auth/refresh/route.ts` (must forward `Set-Cookie`, not optional) | `auth.controller.ts#refresh` → `auth.service.ts#refresh` | Cookie `mnemra_rt` | `{accessToken}` + new `Set-Cookie: mnemra_rt` (rotated), or `429`/`401` | Valid, non-revoked refresh cookie | Deep | Rotates on every use: old token revoked, brand-new token issued. Reusing an already-rotated (revoked) token is treated as theft — revokes ALL active refresh tokens for that user, forcing full re-login everywhere. Throttled: 20 req / 10 min per caller. Verified via `auth.service.spec.ts` (unit + theft-cascade case) and `test/auth.e2e-spec.ts` (real HTTP + cookie rotation). |
| Auth | Logout | POST | `/auth/logout` | `.../api/auth/logout/route.ts` | `auth.controller.ts#logout` → `auth.service.ts#logout` | Cookie `mnemra_rt` | `{message}` | Valid refresh cookie (no-op if missing/invalid) | Deep | Revokes the token row (`revokedAt`). No-op, does not throw, if token already revoked or unknown. Verified live. Not throttled — logging out should never be blocked. |
| Auth | Get Current User | GET | `/auth/me` | `apps/web/src/lib/api/auth.ts#getCurrentUser` → `apps/web/app/api/auth/me/route.ts` | `auth.controller.ts#getMe` | Bearer access token | `{userId, email}` or `401` | Valid access token (`JwtAuthGuard`) | Deep | Added 2026-07-01 as part of removing the client-side raw-JWT `sessionStorage` mirror (security hardening — see Notes on the `Login`/`Verify OTP` rows). Pure pass-through of `@CurrentUser()`, no DB read. Used by pages that need "who am I" client-side (e.g. hiding self-removal in the workspace members roster) instead of decoding the JWT in the browser. |
| Auth | Change Password | POST | `/auth/change-password` | `apps/web/app/workspaces/[id]/settings/page.tsx` → `apps/web/src/lib/api/auth.ts#changePassword` → `apps/web/app/api/auth/change-password/route.ts` | `auth.controller.ts#changePassword` → `auth.service.ts#changePassword` | `{currentPassword, newPassword}` (newPassword 8+ chars) | `200 {message}` (clears `mnemra_rt` cookie server-side) or `401` wrong current password / `400` weak new password / `401` unauthenticated | Valid access token (`JwtAuthGuard`), self only | Deep | Added 2026-07-04. Verifies current password via `bcrypt.compare`, re-hashes with the same `BCRYPT_ROUNDS` constant `register()`/`login()` use, and revokes ALL of the caller's active refresh tokens (same theft-cascade precedent as `refresh()`'s reuse-detection branch) — forces re-login on every other device/tab. Frontend flow on success: toast → `logout()` → redirect to `/login`; on 401 (wrong current password): inline field error only, no logout/redirect. |
| Health | Liveness | GET | `/health` | none in `apps/web`; the `api` container healthcheck in `docker-compose.yml` curls `http://127.0.0.1:3001/health` | `health/health.controller.ts#check` | none | `200 {status: 'ok'}` | None (public, no guards) | Tiny | No DB/Redis probe — it reports only that the Nest process answers. |
| Workspaces | Create Workspace | POST | `/workspaces` | `apps/web/app/workspaces/page.tsx` → `apps/web/src/lib/api/workspaces.ts#createWorkspace` → `apps/web/app/api/workspaces/route.ts` | `workspaces.controller.ts#create` → `workspaces.service.ts#create` | `{name}` | `{id, name, ownerId, createdAt}` | Logged-in user (`JwtAuthGuard`) | Deep | Same-origin proxy reads `mnemra_at` cookie and forwards Bearer; backend still owns auth/RBAC. |
| Workspaces | List My Workspaces | GET | `/workspaces/me` | `apps/web/app/workspaces/page.tsx`, `apps/web/app/workspaces/[id]/page.tsx`, `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx` → `apps/web/src/lib/api/workspaces.ts#listWorkspaces` → `apps/web/app/api/workspaces/route.ts` | `workspaces.controller.ts#listMine` → `workspaces.service.ts#listForUser` | query `cursor?`, `limit? (1-100)` | `{items: [{id, name, ownerId, createdAt, role}], nextCursor: string | null}` | Logged-in user (`JwtAuthGuard`) | Deep | Includes caller's role from `workspace_members`. After OTP verification, list returns one auto-created personal workspace. Accepts keyset pagination via `?cursor=&limit=`. |
| Workspaces | Accept Invite | POST | `/workspaces/accept-invite/:token` | `apps/web/app/invite/[token]/page.tsx` → `apps/web/src/lib/api/workspaces.ts#acceptInvite` → `apps/web/app/api/invitations/accept/[token]/route.ts` | `workspaces.controller.ts#accept` → `workspaces.service.ts#acceptInvite` | path `token` | `{id, name, ownerId, createdAt}` | Logged-in user (`JwtAuthGuard`) | Deep | Token is single-use and expires after 7 days. Logged-in email must match invite email. Marks `acceptedAt` and adds `member` row inside one transaction. |
| Workspaces | Get One | GET | `/workspaces/:workspaceId` | `apps/web/app/workspaces/[id]/page.tsx` → `apps/web/src/lib/api/workspaces.ts#getWorkspace` → `apps/web/app/api/workspaces/[id]/route.ts` | `workspaces.controller.ts#getOne` → `workspaces.service.ts#getOne` | path `workspaceId` | `{id, name, ownerId, createdAt}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Non-members get 403 from guard before service lookup. Verified in `test/workspaces.e2e-spec.ts`. |
| Workspaces | Rename Workspace | PATCH | `/workspaces/:workspaceId` | `apps/web/app/workspaces/[id]/settings/page.tsx` → `apps/web/src/lib/api/workspaces.ts#updateWorkspace` → `apps/web/app/api/workspaces/[id]/route.ts` | `workspaces.controller.ts#update` → `workspaces.service.ts#update` | `{name}` (1–255 chars) | `{id, name, ownerId, createdAt}` or `400` invalid length | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Standard | Added 2026-07-04. Cosmetic-only mutation of `workspaces.name` — no FK, role, or tenant-boundary change, so downgraded from the Workspaces domain's blanket-Deep default (see `risk-register.md`). Role gate mirrors `Invite Member` (owner/admin), not the owner-only `Remove Member` tier, since rename is reversible/non-membership-changing. Settings page hides the Save action and disables the input for plain members. |
| Workspaces | Invite Member | POST | `/workspaces/:workspaceId/invite` | `apps/web/app/workspaces/[id]/page.tsx` → `apps/web/src/lib/api/workspaces.ts#inviteMember` → `apps/web/app/api/workspaces/[id]/invite/route.ts` | `workspaces.controller.ts#invite` → `workspaces.service.ts#invite` | `{email}` | `{message}` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Generates 64-char hex token, stores invite row, sends email via `NotificationsService` or dev console `[DEV INVITE]`. Token never returned in API body. |
| Workspaces | Remove Member | DELETE | `/workspaces/:workspaceId/members/:userId` | `apps/web/src/lib/api/workspaces.ts#removeMember` → `apps/web/app/api/workspaces/[id]/members/[userId]/route.ts` | `workspaces.controller.ts#remove` → `workspaces.service.ts#removeMember` | path `workspaceId`, `userId` | `{message}` | Owner only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Blocks removing last owner with 403. Returns 404 when target membership row does not exist. |
| Workspaces | List Members | GET | `/workspaces/:workspaceId/members` | `apps/web/app/workspaces/[id]/page.tsx` → `apps/web/src/lib/api/workspaces.ts#listMembers` → `apps/web/app/api/workspaces/[id]/members/route.ts` | `workspaces.controller.ts#listMembers` → `workspaces.service.ts#listMembers` | query `page?`, `pageSize? (1-100)`, `q?`, `role?` (`owner\|admin\|member`) | `{items: [{id, userId, email, role, joinedAt}], page, pageSize, total, totalPages}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`, no `RolesGuard` — any member can view the roster) | Deep | Joins `workspace_members` with `users` for email. **Offset paginated (Slice 2, 2026-07-04)**: sorted `joinedAt DESC, id DESC` (newest-joined first, was `joinedAt ASC`); `q` filters `users.email ILIKE`, `role` filters membership role. Migrated from keyset cursor to offset (`ListMembersQueryDto extends OffsetQueryDto`, `resolveOffsetPage`/`buildOffsetResult`). FE `members/page.tsx` uses `@repo/ui` `Pagination` + search + role `Select`; e2e asserts `items` content only so shape change is compatible. Added 2026-07-01 to close the gap where `removeMember` existed with no way to list who to remove. |
| Workspace Events | List Events | GET | `/workspaces/:workspaceId/events` | `apps/web/app/workspaces/[id]/page.tsx` → `apps/web/src/lib/api/events.ts#listEvents` → `apps/web/app/api/workspaces/[id]/events/route.ts` | `events.controller.ts#list` → `events.service.ts#list` | query `cursor?`, `limit? (1-100)` | `{items: [{id, workspaceId, type, entityId, title, detail, createdAt}], nextCursor: string | null}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Newest-first keyset pagination on `createdAt DESC, id DESC`. Feed rows come from shared terminal job events, not ad-hoc frontend polling state. |
| Workspace Events | Read Unread Count | GET | `/workspaces/:workspaceId/events/unread-count` | `apps/web/src/components/workspace-nav.tsx` → `apps/web/src/lib/api/events.ts#getUnreadCount` → `apps/web/app/api/workspaces/[id]/events/unread-count/route.ts` | `events.controller.ts#unreadCount` → `events.service.ts#unreadCount` | none | `{count: number}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Count is per caller membership row, using `workspace_members.events_seen_at`; another member's seen marker never affects your badge. |
| Workspace Events | Mark Events Seen | POST | `/workspaces/:workspaceId/events/mark-seen` | `apps/web/app/workspaces/[id]/page.tsx` → `apps/web/src/lib/api/events.ts#markEventsSeen` → `apps/web/app/api/workspaces/[id]/events/mark-seen/route.ts` | `events.controller.ts#markSeen` → `events.service.ts#markSeen` | none | `204 No Content` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Writes only caller's `workspace_members.events_seen_at`. Web proxy must preserve `204` with empty body; same-origin client treats `204` as `{}`. |
| Search | Workspace Search | GET | `/workspaces/:workspaceId/search` | `apps/web/src/components/workspace-search.tsx` → `apps/web/src/lib/api/search.ts#searchWorkspace` → `apps/web/app/api/workspaces/[id]/search/route.ts` | `search.controller.ts#searchWorkspace` → `search.service.ts#search` | query `q` | `{documents: [{documentId, knowledgeBaseId, title, snippet, score}], tickets: [{ticketId, title, snippet, score}], chatMessages: [{messageId, sessionId, snippet, score}]}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Response is grouped arrays, never one merged ranking. Document hits use vector chunk search; ticket/chat hits use Postgres full-text search. |
| Knowledge Bases | Create Knowledge Base | POST | `/workspaces/:workspaceId/knowledge-bases` | `apps/web/app/workspaces/[id]/knowledge-bases/page.tsx` → `apps/web/src/lib/api/knowledge-bases.ts#createKnowledgeBase` → `apps/web/app/api/workspaces/[id]/knowledge-bases/route.ts` | `knowledge-bases.controller.ts#create` → `knowledge-bases.service.ts#create` | `{name}` | `{id, workspaceId, name, createdAt}` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Standard | Scoped by route workspace. Verified in `knowledge-bases.service.spec.ts`, `test/knowledge-bases.e2e-spec.ts`. |
| Knowledge Bases | List Knowledge Bases | GET | `/workspaces/:workspaceId/knowledge-bases` | `apps/web/app/workspaces/[id]/knowledge-bases/page.tsx` → `apps/web/src/lib/api/knowledge-bases.ts#listKnowledgeBases` → `apps/web/app/api/workspaces/[id]/knowledge-bases/route.ts` | `knowledge-bases.controller.ts#list` → `knowledge-bases.service.ts#listForWorkspace` | query `cursor?`, `limit? (1-100)` | `{items: [{id, workspaceId, name, createdAt}], nextCursor: string | null}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Workspace membership enforced before list. Non-members get 403. Accepts keyset pagination via `?cursor=&limit=`. |
| Knowledge Bases | Delete Knowledge Base | DELETE | `/workspaces/:workspaceId/knowledge-bases/:kbId` | `apps/web/app/workspaces/[id]/knowledge-bases/page.tsx` → `apps/web/src/lib/api/knowledge-bases.ts#deleteKnowledgeBase` → `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/route.ts` | `knowledge-bases.controller.ts#remove` → `knowledge-bases.service.ts#remove` | path `workspaceId`, `kbId` | `{message}` or `409`/`404` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Standard | Returns 409 when `documents` still reference KB. Cross-workspace `kbId` returns 404 to avoid leaking existence. |
| Documents | Upload Document | POST | `/workspaces/:workspaceId/knowledge-bases/:kbId/documents` | `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx` → `apps/web/src/lib/api/documents.ts#uploadDocument` → `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/route.ts` | `documents.controller.ts#upload` → `documents.service.ts#upload` | `multipart/form-data` field `file` | `{id, title, status}` where `status='pending'`; `400` when extension/MIME is unsupported or file missing; `413` when payload exceeds `MAX_UPLOAD_MB` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Proxy forwards `FormData` without setting `Content-Type` so browser/fetch keeps the multipart boundary intact. Uploads are bounded by a server-side size limit and a supported-type allowlist before storage/ingest. If queue enqueue fails after insert, the backend now marks the row terminal `failed` with `lastError` instead of leaving it stuck `pending`. |
| Documents | List Documents | GET | `/workspaces/:workspaceId/knowledge-bases/:kbId/documents` | `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx` → `apps/web/src/lib/api/documents.ts#listDocuments` → `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/route.ts` | `documents.controller.ts#list` → `documents.service.ts#listForKnowledgeBase` | query `page?`, `pageSize? (1-100)`, `q?`, `status?` (`pending\|processing\|done\|failed`) | `{items: [{id, title, status, createdAt, updatedAt}], page, pageSize, total, totalPages}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Slice 1 (2026-07-04) migrated this row from keyset cursor to offset pagination. Service re-validates KB belongs to route workspace, scopes docs to both `workspaceId` and `knowledgeBaseId`, searches `title ILIKE`, filters by `status`, and sorts `updatedAt DESC, id DESC` (newest indexed/updated first). FE uses search, status filter, row selection, and shared `Pagination`. |
| Documents | Download Document | GET | `/workspaces/:workspaceId/knowledge-bases/:kbId/documents/:documentId/download` | `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx` → `apps/web/src/lib/api/documents.ts#downloadDocument` → `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/[docId]/download/route.ts` | `documents.controller.ts#download` → `documents.service.ts#getDownloadable` → `storage.service.ts#getBuffer` | path `workspaceId`, `kbId`, `documentId` | `200 application/octet-stream` with `Content-Disposition: attachment; filename="..."`; `404` when document is outside route scope or has no stored file | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Slice 1 (2026-07-04) added member-readable file download to match member-readable list. Web proxy uses raw stream passthrough, backend scopes by workspace + KB + document before loading `storageKey`, and filenames are header-sanitized. |
| Documents | Download Selected Documents | POST | `/workspaces/:workspaceId/knowledge-bases/:kbId/documents/download` | `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx` → `apps/web/src/lib/api/documents.ts#downloadDocuments` → `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/download/route.ts` | `documents.controller.ts#downloadMany` → `documents.service.ts#getManyDownloadable` → `storage.service.ts#getBuffer` | `{documentIds: string[]}` | `200 application/zip` with `Content-Disposition: attachment; filename="documents.zip"`; `404` when no requested document can be downloaded | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Bulk download skips missing/out-of-scope ids and stored-object failures, then zips remaining files with duplicate names disambiguated. Empty usable set returns `404`. |
| Documents | Delete Selected Documents | POST | `/workspaces/:workspaceId/knowledge-bases/:kbId/documents/delete` | `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx` → `apps/web/src/lib/api/documents.ts#deleteDocuments` → `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/delete/route.ts` | `documents.controller.ts#deleteMany` → `documents.service.ts#removeMany` | `{documentIds: string[]}` (1–200 UUID v4 ids) | `{deleted: number, skipped: number}` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Best-effort selected-row delete. Service re-validates KB workspace scope, deletes only documents inside route workspace+KB, skips stale/foreign ids, attempts object deletion before DB delete, logs storage-delete failures without blocking DB delete, cascades chunks via FK, and bumps workspace chat-cache version once when at least one row is deleted. |
| Documents | Delete Document | DELETE | `/workspaces/:workspaceId/knowledge-bases/:kbId/documents/:documentId` | `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx` → `apps/web/src/lib/api/documents.ts#deleteDocument` → `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/[docId]/route.ts` | `documents.controller.ts#remove` → `documents.service.ts#remove` | path `workspaceId`, `kbId`, `documentId` | `{message}` or `404` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Cross-workspace or cross-KB ids return 404. Service attempts object delete first, logs-and-continues if object already missing, then deletes row so chunk cascade fires. |
| Chat / RAG | Stream Answer | POST | `/workspaces/:workspaceId/chat` | `apps/web/app/workspaces/[id]/chat/page.tsx` → `useChat({ api: '/api/workspaces/:id/chat' })` → `apps/web/app/api/workspaces/[id]/chat/route.ts` | `chat.controller.ts#chat` → `ChatRateLimitGuard` → `chat.service.ts#answer` → `CacheService` / `UsageService` → `@repo/ai#answerQuestion` on miss | web proxy accepts AI SDK `{messages, sessionId?}` and forwards backend `{message, sessionId?}` | `201 text/plain` stream, headers `X-Chat-Sources=<urlencoded-json>` + `X-Chat-Session-Id` + optional `X-Chat-Cache=exact|semantic|miss`; `429` on per-user/per-workspace chat rate limit; `402` when miss-path monthly workspace token budget is exhausted | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `ChatRateLimitGuard`) | Deep | Verified source + automated as of 2026-07-02. Proxy extracts last user message from AI SDK payload, keeps stream unbuffered, and passes session/source/cache headers back to browser. Controller persists user/assistant turns around streamed response for both cache hits and misses; assistant sources are stored on persisted assistant message. Citation items may now be either `{sourceType:'document', documentId, title, sourceUrl, score, snippet}` or `{sourceType:'ticket', ticketId, title, score, snippet}`. Rate limits hit all POSTs, including cache hits. Token budget counts miss-path generations only and fails open on Redis errors. 2026-07-03 RAG perf pass: in graph mode, confident answers (top score ≥ `SELF_GRADE_MIN_SCORE`) stream token-by-token and skip self-grade; only low-confidence answers take the buffered generate→grade→regenerate path. Simple lookups route to a lighter path (`SIMPLE_QUERY_CHUNK_LIMIT` chunks, no rewrite/grade). Miss path reuses the semantic-cache embedding for retrieval (no double embed). Context is a token-budgeted compact evidence pack (`RAG_CONTEXT_TOKEN_BUDGET`). Rewrite mutates only the retrieval query; generation answers the original question. Per-task models via `OPENAI_ANSWER/REWRITE/GRADE/EXTRACTION_MODEL`. Retrieval metadata filters exist in `@repo/ai` but are NOT yet exposed via `ChatDto` (cache key would need to include them). `isFallback` is true only on the two structural hard-fallback paths: zero retrieved chunks in `packages/ai/src/chains/index.ts`, or rewrite exhaustion in `packages/ai/src/chains/graph.ts`. Hedged partial-context answers remain `isFallback: false`, keep sources, and may be cached. 2026-07-05 history-aware chat: request/response wire shape is unchanged (still `{message, sessionId?}` in, same stream/headers out) — this is a pure answer-quality change. `chat.service.ts#answer` now resolves history-dependent follow-ups (e.g. "find more like that") into a standalone question via `condenseQuestion()` before caching/retrieval, and threads bounded recent session history into the final generation prompt, both gated by `HISTORY_CONDENSE_ENABLED`/`HISTORY_IN_ANSWER_ENABLED` (default `true`). See `module-ownership-map.md` Chat/RAG row and `risk-register.md` for full detail. **2026-09-20 (S0c):** condense (follow-up questions) and the structured/dataset path are budget-checked before their model call via `UsageService.metered` and charged from the provider's reported `usage_metadata`; a workspace over budget now gets `402` on those paths before the cache lookup. The main answer stream is still charged by the local `countTokens` estimate. |
| Chat / RAG | List Sessions | GET | `/workspaces/:workspaceId/chat/sessions` | `apps/web/app/workspaces/[id]/chat/page.tsx` → `apps/web/src/lib/api/chat.ts#listChatSessions` → `apps/web/app/api/workspaces/[id]/chat/sessions/route.ts` | `chat.controller.ts#listSessions` → `chat.service.ts#listSessions` | query `cursor?`, `limit? (1-100)` | `{items: [{id, title, createdAt, updatedAt}], nextCursor: string | null}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`); results scoped to caller `userId` | Deep | Newest first by `updatedAt`. Another workspace member does not see your sessions. Accepts keyset pagination via `?cursor=&limit=`. |
| Chat / RAG | List Messages | GET | `/workspaces/:workspaceId/chat/sessions/:sessionId/messages` | `apps/web/app/workspaces/[id]/chat/page.tsx` → `apps/web/src/lib/api/chat.ts#getChatMessages` → `apps/web/app/api/workspaces/[id]/chat/sessions/[sessionId]/messages/route.ts` | `chat.controller.ts#getMessages` → `chat.service.ts#getMessages` | path `sessionId`; query `cursor?`, `limit? (1-100)` | `{items: [{id, role, content, sources, createdAt}], nextCursor: string | null}` | Member only + session owner (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Returns 404 when `sessionId` is not owned by caller in that workspace. Message order is ascending `createdAt`. Persisted `sources` may now include ticket citations with `sourceType:'ticket'`; legacy rows without `sourceType` still exist and FE treats them as document citations. Accepts keyset pagination via `?cursor=&limit=`. |
| Chat / RAG | Refine Message | POST | `/workspaces/:workspaceId/refine` | `apps/web/app/workspaces/[id]/chat/page.tsx` → `apps/web/src/lib/api/refine.ts#refineMessage` → `apps/web/app/api/workspaces/[id]/refine/route.ts` | `refine.controller.ts#refine` → `RefineRateLimitGuard` → `refine.service.ts#refine` → `@repo/ai#refineMessage` | `{text}` (1–4000 chars) | `200 {original, refined}`; `400` empty/oversized text; `429` daily limit; `422` when `@repo/ai`'s `RefineEmptyError`/`RefineRefusalError` fire, with copy telling the user to try rephrasing | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RefineRateLimitGuard`) | Standard | Added 2026-07-04. Caveman-compression rewrite of a rough chat draft via `packages/ai/src/chains/refine.ts` (`OPENAI_REFINE_MODEL`, falls back to `OPENAI_CHAT_MODEL`/`gpt-4-turbo`). Daily cap per user via `RateLimitService.checkRefineDaily` (Redis key `rl:refine:${userId}:${utcDateBucket()}`, default 20/day via `REFINE_DAILY_LIMIT_PER_USER`, fails open on Redis error). Does not persist anything itself — saving is a separate, deliberate action (see `Save Refined Message` row). FE replaces the chat textarea content in place and shows an "Undo refine" affordance restoring the pre-refine text. |
| Chat / RAG | Refine Status | GET | `/workspaces/:workspaceId/refine/status` | `apps/web/app/workspaces/[id]/chat/page.tsx` → `apps/web/src/lib/api/refine.ts#getRefineStatus` → `apps/web/app/api/workspaces/[id]/refine/status/route.ts` | `refine.controller.ts#status` → `RateLimitService.getRefineStatus` | none | `200 {used, limit, remaining}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Added 2026-07-04. Read-only Redis `GET` on the same day-bucketed key `checkRefineDaily` writes to — never `INCR`s, so polling this endpoint (e.g. to render a "N refines left today" badge) never itself consumes quota. Fails open to `{used:0, limit, remaining:limit}` on Redis error, matching the fail-open posture of the daily-limit check itself. Implemented as a dedicated GET rather than a response header on the refine POST because the web proxy's `proxyJson()` helper does not forward arbitrary backend response headers. |
| Chat / RAG | Save Refined Message | POST | `/workspaces/:workspaceId/refine/saved` | `apps/web/app/workspaces/[id]/chat/page.tsx` → `apps/web/src/lib/api/refine.ts#saveRefinedMessage` → `apps/web/app/api/workspaces/[id]/refine/saved/route.ts` | `refine.controller.ts#saveRefinedMessage` → `refine.service.ts#saveRefinedMessage` | `{originalText, refinedText}` (1–4000 chars each) | `201 {id, originalText, refinedText, createdAt}` or `400` validation | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Added 2026-07-04. Explicit user action, deliberately not auto-triggered by every refine call (design choice — see chat page "Save refined message" button, enabled only right after a successful refine and cleared if the user hand-edits the textarea afterward). No uniqueness constraint; duplicate saves are allowed. |
| Chat / RAG | List Saved Refined Messages | GET | `/workspaces/:workspaceId/refine/saved` | `apps/web/app/workspaces/[id]/chat/page.tsx` → `apps/web/src/lib/api/refine.ts#listSavedRefinedMessages` → `apps/web/app/api/workspaces/[id]/refine/saved/route.ts` | `refine.controller.ts#listSavedRefinedMessages` → `refine.service.ts#listSavedRefinedMessages` | none | `200 {items: [{id, originalText, refinedText, createdAt}]}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`); results scoped to both route `workspaceId` AND caller `userId` | Standard | Added 2026-07-04. **Personal, not workspace-shared** — another workspace member does not see your saved messages, matching the `List Sessions` row's privacy precedent. `ORDER BY createdAt DESC LIMIT 20`, no pagination for v1 (saving is deliberate, so realistic volume is low); revisit with the shared offset-pagination convention if usage grows. |
| Web Sources | Start Crawl | POST | `/workspaces/:workspaceId/knowledge-bases/:kbId/scrape` | `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx` → `apps/web/src/lib/api/scrape.ts#scrapeSite` → `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/scrape/route.ts` | `scrape.controller.ts#start` → `scrape.service.ts#startScrape` | `{url, maxDepth?, maxPages?, includePrefixes?}` | `{runId, status}` with `202 Accepted` for a newly queued crawl or `200 OK` when reusing an existing in-flight run for the same workspace + KB + seed URL; `400` when seed URL is non-public / private / internal after hostname or DNS resolution checks | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Backend clamps `maxPages` to remaining workspace doc quota, rejects blocked seed URLs before queueing, defaults omitted `includePrefixes` to the seed subtree (widening `/.../home` to its parent section), and enqueues one `scrape-queue` job with `attempts:1` plus a 5-minute timeout. Duplicate in-flight requests are idempotent and return the existing run instead of inserting another queued row. UI hides control for members, disables submit while POST is in flight, and shows duplicate-run feedback when `200` is returned. |
| Web Sources | List Crawl Runs | GET | `/workspaces/:workspaceId/knowledge-bases/:kbId/scrape-runs` | `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx` → `apps/web/src/lib/api/scrape.ts#listScrapeRuns` → `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/scrape-runs/route.ts` | `scrape.controller.ts#list` → `scrape.service.ts#listRuns` | query `page?`, `pageSize? (1-100, default 5)`, `q?`, `status?` (`queued\|running\|completed\|failed`) | `{items: [{id, seedUrl, status, maxDepth, maxPages, pagesFound, pagesSucceeded, pagesFailed, error, startedAt, finishedAt, createdAt}], page, pageSize, total, totalPages}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Slice 1 (2026-07-04) migrated this row from keyset cursor to offset pagination. Frontend polls every 3 seconds while any run is `queued` or `running`. Runs are scoped to both workspace and KB, search `seedUrl ILIKE`, filter by status, sort `createdAt DESC, id DESC`, default to `pageSize=5`, and show live `pagesFound/pagesSucceeded/pagesFailed` progress as crawl pages stream in. |
| Datasets | Upload Dataset | POST | `/workspaces/:workspaceId/datasets` | `apps/web/app/workspaces/[id]/datasets/page.tsx` → `apps/web/src/lib/api/datasets.ts#uploadDataset` → `apps/web/app/api/workspaces/[id]/datasets/route.ts` (POST, `proxyMultipart`) | `datasets/datasets.controller.ts#upload` → `datasets.service.ts#upload` | `multipart/form-data` field `file` (`.csv`/`.xlsx`) | `{id, name, status='pending'}`; `400` `Only CSV or XLSX files are supported for datasets` or `file is required`; `413` over `MAX_UPLOAD_MB` (`UploadExceptionFilter`) | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Storage key `${workspaceId}/datasets/${uuid}-${name}`. A failed row insert deletes the stored object; a failed enqueue (`DatasetProfilingService.queueDataset`) marks the row `failed` with `lastError: Queue enqueue failed: …` and rethrows. XLSX is converted to CSV during profiling so DuckDB only reads CSV. |
| Datasets | List Datasets | GET | `/workspaces/:workspaceId/datasets` | `apps/web/app/workspaces/[id]/datasets/page.tsx` → `apps/web/src/lib/api/datasets.ts#listDatasets` → `apps/web/app/api/workspaces/[id]/datasets/route.ts` (GET) | `datasets.controller.ts#list` → `datasets.service.ts#list` | none | `[{id, name, status, rowCount, description, lastError, createdAt}]` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Newest first (`createdAt DESC`), workspace-scoped, not paginated. |
| Datasets | Delete Dataset | DELETE | `/workspaces/:workspaceId/datasets/:datasetId` | `apps/web/app/workspaces/[id]/datasets/page.tsx` → `apps/web/src/lib/api/datasets.ts#deleteDataset` → `apps/web/app/api/workspaces/[id]/datasets/[datasetId]/route.ts` | `datasets.controller.ts#remove` → `datasets.service.ts#remove` | path `datasetId` (UUID, `ParseUUIDPipe`) | `{message: 'Dataset deleted'}`; `404` `Dataset not found` (unknown or other workspace); `400` non-UUID | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Storage delete is attempted first and a failure is logged, not thrown; the row is then deleted. |
| Insights | List Freshness Flags | GET | `/workspaces/:workspaceId/insights/freshness-flags` | `apps/web/app/workspaces/[id]/insights/page.tsx` → `apps/web/src/lib/api/insights.ts#listFreshnessFlags` → `apps/web/app/api/workspaces/[id]/insights/route.ts` (BFF path is `/api/workspaces/:id/insights`) | `insights/insights.controller.ts#listFreshnessFlags` → `insights.service.ts#listFreshnessFlags` | none | `[{id, documentId, documentTitle, ticketId, score, reason, status, createdAt}]` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`, class-level) | Standard | Open `document_review_flags` only, joined to `documents` for the title, newest first, not paginated. |
| Insights | Coverage Dashboard | GET | `/workspaces/:workspaceId/insights/coverage` | `apps/web/app/workspaces/[id]/insights/page.tsx` → `apps/web/src/lib/api/insights.ts#getCoverage` → `apps/web/app/api/workspaces/[id]/insights/coverage/route.ts` | `insights.controller.ts#getCoverage` → `coverage-dashboard.service.ts#getSummary` + `#getLowScoreQueries` + `#getTopicGaps` | none | `{summary: {totalQueries, fallbackRate, cacheHitRate, avgTopScore}, lowScoreQueries: [{id, question, topScore, isFallback, createdAt}], topicGaps: [{label, questionCount, exampleQuestion}]}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | `summary` and `lowScoreQueries` are computed live from `chat_query_metrics` (window `COVERAGE_SUMMARY_WINDOW_DAYS`, default 30; low score = fallback or `topScore < COVERAGE_LOW_SCORE_THRESHOLD`, default 0.4; max 20 rows). `topicGaps` is read from Redis key `insights:topic-gaps:<workspaceId>` written by the weekly `TopicGapProcessor`; empty until it runs. |
| Insights | Dismiss Freshness Flag | PATCH | `/workspaces/:workspaceId/insights/freshness-flags/:flagId/dismiss` | `apps/web/app/workspaces/[id]/insights/page.tsx` → `apps/web/src/lib/api/insights.ts#dismissFreshnessFlag` → `apps/web/app/api/workspaces/[id]/insights/[flagId]/dismiss/route.ts` | `insights.controller.ts#dismissFlag` → `insights.service.ts#dismissFlag` | path `flagId` (no UUID pipe) | `{id, dismissed: true}`; `404` `Freshness flag not found` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`; no `RolesGuard`) | Standard | Update is scoped by `id` AND `workspaceId`; sets `status='dismissed'`, `dismissedAt`, `dismissedBy`. |
| Insights | List FAQ Drafts | GET | `/workspaces/:workspaceId/insights/faq-drafts` | `apps/web/app/workspaces/[id]/insights/page.tsx` → `apps/web/src/lib/api/insights.ts#listFaqDrafts` → `apps/web/app/api/workspaces/[id]/insights/faq-drafts/route.ts` | `insights/faq-drafts.controller.ts#list` → `faq-drafts.service.ts#list` | none | full `faq_drafts` rows with `status='pending'`, newest first | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Drafts are written by the FAQ clustering job (`faq-cluster.processor.ts`). |
| Insights | Approve FAQ Draft | PATCH | `/workspaces/:workspaceId/insights/faq-drafts/:draftId/approve` | `apps/web/app/workspaces/[id]/insights/page.tsx` → `apps/web/src/lib/api/insights.ts#approveFaqDraft` → `apps/web/app/api/workspaces/[id]/insights/faq-drafts/[draftId]/approve/route.ts` | `faq-drafts.controller.ts#approve` → `faq-drafts.service.ts#approve` | path `draftId` | `{id, status: 'approved', documentId}`; `404` `FAQ draft not found or already reviewed` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Human approval gate for LLM-authored content entering the corpus. Writes `Q: …\n\nA: …` as a `.txt` object, inserts a `documents` row in the workspace's `Generated FAQs` knowledge base (created on first use), queues it through `IngestService.queueDocument`, then marks the draft approved with `documentId`. |
| Insights | Reject FAQ Draft | PATCH | `/workspaces/:workspaceId/insights/faq-drafts/:draftId/reject` | `apps/web/app/workspaces/[id]/insights/page.tsx` → `apps/web/src/lib/api/insights.ts#rejectFaqDraft` → `apps/web/app/api/workspaces/[id]/insights/faq-drafts/[draftId]/reject/route.ts` | `faq-drafts.controller.ts#reject` → `faq-drafts.service.ts#reject` | path `draftId` | `{id, status: 'rejected'}`; `404` `FAQ draft not found or already reviewed` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Standard | Only a `pending` draft in the route workspace can be rejected; stamps `reviewedBy`/`reviewedAt`. |
| Insights | Get Digest Settings | GET | `/workspaces/:workspaceId/digest-settings` | `apps/web/app/workspaces/[id]/settings/page.tsx` → `apps/web/src/lib/api/digest-settings.ts#getDigestSettings` → `apps/web/app/api/workspaces/[id]/digest-settings/route.ts` (GET) | `insights/digest-settings.controller.ts#get` → `digest-settings.service.ts#get` | none | the `workspace_digest_settings` row, or defaults `{workspaceId, emailEnabled: true, slackWebhookUrl: null, slackEnabled: false}` when none saved | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`, class-level) | Standard | Read is owner/admin too: the Slack webhook URL is an external destination for workspace data. |
| Insights | Update Digest Settings | PATCH | `/workspaces/:workspaceId/digest-settings` | `apps/web/app/workspaces/[id]/settings/page.tsx` → `apps/web/src/lib/api/digest-settings.ts#updateDigestSettings` → `apps/web/app/api/workspaces/[id]/digest-settings/route.ts` (PATCH) | `digest-settings.controller.ts#update` → `digest-settings.service.ts#update` | `UpdateDigestSettingsDto`: `{emailEnabled?: boolean, slackWebhookUrl?: https URL \| null}` | the saved row; `400` validation or `Slack webhook URL rejected: …` | Owner/admin only | Deep | PATCH semantics: only present fields change. A webhook URL passes `assertPublicUrl` (`@repo/ai` SSRF guard) before it is persisted; `slackEnabled` is derived as `Boolean(slackWebhookUrl)`. Upserts by `workspaceId`. |
| Insights | Preview Digest | GET | `/workspaces/:workspaceId/digest-settings/preview` | `apps/web/app/workspaces/[id]/settings/page.tsx` → `apps/web/src/lib/api/digest-settings.ts#previewDigest` → `apps/web/app/api/workspaces/[id]/digest-settings/preview/route.ts` | `digest-settings.controller.ts#preview` → `digest-content.service.ts#build` → `digest-renderers.ts#renderDigestEmailHtml` / `#renderDigestSlackPayload` | none | `{emailHtml, slackPayload}` | Owner/admin only | Standard | Renders the next digest without sending anything. |
| Tickets | Create Ticket Draft | POST | `/workspaces/:workspaceId/tickets` | `apps/web/app/workspaces/[id]/tickets/page.tsx` → `apps/web/src/lib/api/tickets.ts#createTicket` → `apps/web/app/api/workspaces/[id]/tickets/route.ts` | `tickets.controller.ts#create` → `tickets.service.ts#create` | `{transcript}` | `202 {id, status='pending', title?}` for new extraction; `200 {id, status, title?}` for exact transcript-hash reuse in same workspace | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Transcript is hashed with SHA-256 per workspace for exact dedup. New rows queue `ticket-extraction-queue`; dedup hit skips enqueue and returns the existing draft. |
| Tickets | List Ticket Drafts | GET | `/workspaces/:workspaceId/tickets` | `apps/web/app/workspaces/[id]/tickets/page.tsx` → `apps/web/src/lib/api/tickets.ts#listTickets` → `apps/web/app/api/workspaces/[id]/tickets/route.ts` | `tickets.controller.ts#list` → `tickets.service.ts#list` | query `page?`, `pageSize? (1-100)`, `q?`, `status?`, `severity?`, `usefulness?`, `indexed?` (`true\|false`) | `{items: [{id, title, status, severity, usefulness, indexed, createdAt, updatedAt}], page, pageSize, total, totalPages}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | **Offset paginated (Slice 4, 2026-07-04)**: sorted `updatedAt DESC, createdAt DESC, id DESC` (unchanged sort, migrated from keyset cursor to offset — `ListTicketsQueryDto extends OffsetQueryDto`). `q` filters `title ILIKE`; `status`/`severity`/`usefulness` are exact-match filters; `indexed` filters on the same predicate that drives ticket embedding (`done` + `reviewedBy` + `usefulness='useful'`, computed via a null-safe `COALESCE` SQL expression, exposed per-row as `indexed: boolean`). FE `tickets/page.tsx` moved the review/refine editor into an `@repo/ui` `Modal` (was always-rendered inline), stacked the transcript-input and workspace-drafts cards into a single column, added an "Upload files" button routing to the knowledge-bases page, and added a "Reference" column + filter showing the `indexed` badge. Frontend polls every 3 seconds while any draft is `pending` or `processing`. |
| Tickets | Get Ticket Draft | GET | `/workspaces/:workspaceId/tickets/:ticketId` | `apps/web/app/workspaces/[id]/tickets/page.tsx` → `apps/web/src/lib/api/tickets.ts#getTicket` → `apps/web/app/api/workspaces/[id]/tickets/[ticketId]/route.ts` | `tickets.controller.ts#getOne` → `tickets.service.ts#getOne` | path `ticketId` | ticket detail body including `transcript`, extracted fields, feedback fields, status, `fieldConfidence`, `lastError`, and audit columns; excludes internal queue plumbing (`queueJobId`, `transcriptHash`, `enqueuedAt`, `processingStartedAt`) | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Cross-workspace ids return `404` after workspace membership passes, so route membership never leaks other-workspace row existence. |
| Tickets | Save Ticket Review | PATCH | `/workspaces/:workspaceId/tickets/:ticketId` | `apps/web/app/workspaces/[id]/tickets/page.tsx` → `apps/web/src/lib/api/tickets.ts#updateTicket` → `apps/web/app/api/workspaces/[id]/tickets/[ticketId]/route.ts` | `tickets.controller.ts#update` → `tickets.service.ts#update` | partial editable fields: `title` (<=500), `issueSummary`/`reproSteps`/`hypothesizedRootCause`/`nextAction` (<=4000 each), `severity`, `productArea` (<=120), `usefulness`, `editState`, `feedbackNote` (<=2000) | updated full ticket row | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Save stamps `reviewedBy` + `reviewedAt`, merges per-field confidence for edited fields, and powers usefulness-rate observability. |
| Tickets | Download Transcript PDF | GET | `/workspaces/:workspaceId/tickets/:ticketId/transcript.pdf` | `apps/web/app/workspaces/[id]/chat/page.tsx` → `apps/web/src/lib/api/tickets.ts#downloadTicketTranscript` (`fetchDownload`, fallback name `transcript.pdf`) → `apps/web/app/api/workspaces/[id]/tickets/[ticketId]/transcript.pdf/route.ts` (`proxyRaw`) | `tickets.controller.ts#downloadTranscript` → `tickets.service.ts#getTranscriptPdf` | path `ticketId` | `200 application/pdf` with `Content-Disposition` from `attachmentDisposition(title)` and `Content-Length`; `404` when the ticket is not in the route workspace (via `getOne`) | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | PDF is rendered on request with `pdf-lib` (`PDFDocument`, Helvetica) from the ticket title and transcript; nothing is stored. Declared before `@Get(':ticketId')` in the controller so the literal suffix wins. |
| Procurement | Upload Purchase Order | POST | `/workspaces/:workspaceId/procurement/purchase-orders` | `apps/web/app/workspaces/[id]/procurement/page.tsx` via `@/lib/api/procurement#uploadPurchaseOrder` — added 2026-07-10 | `procurement.controller.ts#uploadPurchaseOrder` → `procurement-documents.service.ts#upload` | `multipart/form-data`: file field `file` (.csv/.xlsx/.pdf) **plus required text fields `vendorId` (UUID), `poNumber` (1–200), `currency` (ISO 4217)** — S3b | `{id, name, status='pending'}`; `400` unsupported type/missing file/PDF disabled (`PROCUREMENT_PDF_EXTRACTION_ENABLED!=='true'`) **or per-field validation errors as a `message` array**; `404` `vendorId` not in this workspace; `413` over `MAX_UPLOAD_MB` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-07-09. Mirrors `Upload Document`'s guard/Multer/exception-filter pattern exactly. Storage key `${workspaceId}/procurement/purchase_order/${uuid}-${name}`. `.pdf` accepted since A2 (2026-07-10, text-only — scanned/image PDFs upload fine but fail at parse with a clear error, see `risk-register.md`). **2026-09-20 (S0b):** header `sourceKind` is `csv`/`xlsx`/`pdf` by extension; if the row insert fails after the object is stored, the object is deleted. **2026-09-21 (S3b):** header metadata is now supplied by the user at upload (nothing extracts document-level fields, and POLICY v1 #2/#3 want linkage explicit). The linked id is validated against the caller's workspace **before** `storage.save`, so a rejected header leaves no orphan object, and a foreign id is a `404` rather than a `403` so it cannot be used to enumerate another workspace. Text fields must be sent **before** the file part — multer only populates `req.body` from parts it reads first. `currency` is uppercased server-side. **2026-09-23 (S9 commit 4):** optional text field `orderedAt` (ISO 8601) — when the order was PLACED, distinct from `createdAt`, which is when the file reached Optra. Optional unlike the other three: an order date is not always known and refusing the upload over it would trade a real document for a missing field. Returned by the list read; null means not stated, and contract applicability then falls back to the upload date and says so in the flag rather than passing one off as the other. |
| Procurement | List Purchase Orders | GET | `/workspaces/:workspaceId/procurement/purchase-orders` | `apps/web/app/workspaces/[id]/procurement/page.tsx` via `@/lib/api/procurement#listPurchaseOrders` — added 2026-07-10 | `procurement.controller.ts#listPurchaseOrders` → `procurement-documents.service.ts#listPurchaseOrders` | none | `[{id, name, status, rowCount, lastError, createdAt, hasSourceFile, poNumber, currency, vendorId, vendorName}]` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Added 2026-07-09. Newest-first, workspace-scoped. **2026-09-20 (S4):** `hasSourceFile` is derived from `storageKey != null` so the UI can hide the download control for a header with no object behind it; the storage key itself is never returned. **2026-09-20 (S0b):** `lastError` is client-safe: an authored document message (e.g. `PDF extraction is not enabled`, `The CSV file is malformed near row N…`, the extraction chain's typed messages) or `Parsing failed. Reference: <8-hex>` matching the server log. A row stays `processing` while Bull retries a transient failure. |
| Procurement | Upload Invoice | POST | `/workspaces/:workspaceId/procurement/invoices` | `apps/web/app/workspaces/[id]/procurement/page.tsx` via `@/lib/api/procurement#uploadInvoice` — added 2026-07-10 | `procurement.controller.ts#uploadInvoice` → `procurement-documents.service.ts#upload` | `multipart/form-data`: file field `file` (.csv/.xlsx/.pdf) **plus required text fields `purchaseOrderId` (UUID), `invoiceNumber` (1–200), `currency` (ISO 4217)** — S3b | `{id, name, status='pending'}`; `400`/`413` same as PO upload; `404` `purchaseOrderId` not in this workspace | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-07-09. Same service, `kind='invoice'` discriminator. `.pdf` accepted since A2 (2026-07-10), same rules as PO upload. **2026-09-20 (S0b):** header `sourceKind` is `csv`/`xlsx`/`pdf` by extension; if the row insert fails after the object is stored, the object is deleted. **2026-09-21 (S3b):** header metadata is now supplied by the user at upload (nothing extracts document-level fields, and POLICY v1 #2/#3 want linkage explicit). The linked id is validated against the caller's workspace **before** `storage.save`, so a rejected header leaves no orphan object, and a foreign id is a `404` rather than a `403` so it cannot be used to enumerate another workspace. Text fields must be sent **before** the file part — multer only populates `req.body` from parts it reads first. `currency` is uppercased server-side. |
| Procurement | List Invoices | GET | `/workspaces/:workspaceId/procurement/invoices` | `apps/web/app/workspaces/[id]/procurement/page.tsx` via `@/lib/api/procurement#listInvoices` — added 2026-07-10 | `procurement.controller.ts#listInvoices` → `procurement-documents.service.ts#listInvoices` | none | `[{id, name, status, rowCount, lastError, createdAt, hasSourceFile, invoiceNumber, currency, purchaseOrderId}]` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Added 2026-07-09. **2026-09-20 (S0b):** same `lastError` semantics as List Purchase Orders. |
| Procurement | Upload Goods Receipt | POST | `/workspaces/:workspaceId/procurement/goods-receipts` | `apps/web/app/workspaces/[id]/procurement/page.tsx` via `@/lib/api/procurement#uploadGoodsReceipt` → `apps/web/app/api/workspaces/[id]/procurement/goods-receipts/route.ts` (POST) | `procurement.controller.ts#uploadGoodsReceipt` → `procurement-documents.service.ts#upload` | `multipart/form-data`: file field `file` (**.csv/.xlsx only**) plus required text fields `purchaseOrderId` (UUID), `grnNumber` (1–200) | `{id, name, status='pending'}`; `400` non-CSV/XLSX — a `.pdf` returns `Goods receipts must be CSV or XLSX; PDF is not supported yet` — or per-field validation errors as a `message` array; `404` `purchaseOrderId` not in this workspace; `413` over `MAX_UPLOAD_MB` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-09-22 (S5). No `currency` field — a receipt records what arrived, not what it cost. PDF is refused by a dedicated `spreadsheetOnlyFileFilter` **and** re-checked in the processor, because the extraction chain has no received/accepted/rejected in its result shape and would silently drop the acceptance data. Same workspace-scoped id check as the S3b uploads: validated before `storage.save`, 404 rather than 403. |
| Procurement | List Goods Receipts | GET | `/workspaces/:workspaceId/procurement/goods-receipts` | `apps/web/app/workspaces/[id]/procurement/page.tsx` via `@/lib/api/procurement#listGoodsReceipts` → `apps/web/app/api/workspaces/[id]/procurement/goods-receipts/route.ts` (GET) | `procurement.controller.ts#listGoodsReceipts` → `procurement-documents.service.ts#listGoodsReceipts` | none | `[{id, name, status, rowCount, lastError, createdAt, hasSourceFile, grnNumber, purchaseOrderId}]` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Added 2026-09-22 (S5). Newest-first, workspace-scoped. |
| Procurement | Download Goods Receipt | GET | `/workspaces/:workspaceId/procurement/goods-receipts/:docId/download` | `apps/web/app/workspaces/[id]/procurement/page.tsx` → `@/lib/api/procurement#downloadProcurementDocument` → `apps/web/app/api/workspaces/[id]/procurement/goods-receipts/[docId]/download/route.ts` | `procurement.controller.ts#downloadGoodsReceipt` → `#sendSourceDocument` | path `docId` (UUID) | Identical header contract to the PO/invoice download: `application/octet-stream`, `attachment`, `nosniff`, sanitized filename | Member only | Standard | Added 2026-09-22 (S5). |
| Procurement | Download Source Document | GET | `/workspaces/:workspaceId/procurement/{purchase-orders,invoices}/:docId/download` | `apps/web/app/workspaces/[id]/procurement/page.tsx` → `@/lib/api/procurement#downloadProcurementDocument` → `apps/web/app/api/workspaces/[id]/procurement/{purchase-orders,invoices}/[docId]/download/route.ts` | `procurement.controller.ts#downloadPurchaseOrder`/`#downloadInvoice` → `procurement-documents.service.ts#getDownloadable` → `storage.service.ts#getBuffer` | path `docId` (UUID) | `200 application/octet-stream` with `Content-Disposition: attachment; filename="<original name>"`, `Content-Length`, `X-Content-Type-Options: nosniff`; `404` unknown document/foreign workspace, and a distinct `404` when the row has no stored file; `400` non-UUID `docId` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Added 2026-09-20 (S4, closes D4). **Always an attachment and never the stored content type** — these are user-uploaded bytes served from the web app's own origin through the BFF, so anything renderable inline could execute there. The caller supplies an id, never a storage key; the row's workspace is asserted before `storageKey` is read. Filename sanitized by `common/http/content-disposition.ts`. |
| Procurement | Compare PO ↔ Invoice | POST | `/workspaces/:workspaceId/procurement/discrepancies/compare` | `apps/web/app/workspaces/[id]/procurement/page.tsx` via `@/lib/api/procurement#compareDocuments` — added 2026-07-10; on success navigates to `/workspaces/:id/discrepancies?purchaseOrderId=&invoiceId=` | `procurement.controller.ts#compare` → `comparison.service.ts#compare` | `{purchaseOrderId, invoiceId}` (both UUID) | `{comparedAt, counts: {quantity_mismatch, price_mismatch, missing_on_invoice, missing_on_po}, flags: [...]}`; `404` document not found/foreign workspace; `400` document not yet parsed or has zero line items; `503` `Comparison engine failed. Reference: <8-hex>` (engine text never returned, 2026-09-20) | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-07-09. Synchronous — exports both documents' parsed line items to temp CSVs and runs a **fixed, hand-written** SQL template (never LLM-generated) via `DuckDbQueryService.runReadOnlyMultiTableQuery()`. Idempotent: deletes that exact PO/invoice pair's prior flags before inserting the fresh set, so repeat comparisons never accumulate stale flags. Zero OpenAI calls. **2026-09-20 (S0a):** delete+insert run in one `db.transaction` behind a `FOR UPDATE` lock on the PO row (a failed insert keeps the prior set; concurrent compares yield one set); child line reads filter by `workspaceId`; match key built in TypeScript (`sku::`/`desc::`/`unkeyed::<id>`), duplicate keys summed per side (reason notes the line count), a line with no SKU and no description is flagged unmatchable on its own side; no 500-row cap (`maxRows` = combined line count, `total_rows` asserted). **2026-09-20 (S1):** re-compare no longer deletes anything. Each call inserts a `comparison_runs` row (returned as `runId`) and appends its flags, so a dismissal made against an earlier run survives. `delta` is now **invoice − PO** (positive = the invoice asks for more), and `missing_on_*` flags carry the present side's value with the absent side counted as zero, instead of three NULLs. The response gained `runId`. **2026-09-22 (S6):** `counts` gained four keys (`short_receipt`, `invoice_exceeds_received`, `uom_mismatch`, `currency_mismatch`), additive for any reader that indexes by name. A run is labelled `three_way` when at least one goods receipt linked to the PO **has finished parsing** (`status = 'done'`) and has parsed lines, and `two_way` otherwise (POLICY v1 #14, §7.4 "no false three-way claim"); flags produced by a three-way run carry `receivedValue` alongside `poValue`/`invoiceValue`. **2026-09-22 (S6 commit 2):** the two needs-review types landed and both carry `delta: null` — POLICY v1 #4 and #6 compute no difference, and a `0` would read as "agreed". `uom_mismatch` is per line: it fires when two of the three sides state different units, OR when one document's own duplicate lines for an item disagree (the engine sums them, so mixed units make the total meaningless); units are compared trimmed and case-folded, and never converted. It outranks every numeric branch, because a quantity difference measured across different units is not a number anybody can act on. Its three value columns carry the **units**, not quantities. `currency_mismatch` is the one flag not derived from a matched line: header-level, computed in TypeScript before the engine runs, written **once per run** with `sku`, `poLineItemId`, `invoiceLineItemId` and `goodsReceiptLineItemId` all null, and only when both documents state a currency and they differ (case-folded, so pre-0025 lowercase rows do not manufacture one). Line-level flags are **not** suppressed when it fires — a quantity disagreement is true whatever the documents are billed in. **2026-09-23 (S9 commit 5):** a run now also asks whether the ORDER was placed at the vendor's agreed price — a second, independent question from PO-vs-invoice, so a line can carry two flag rows and the review queue gets longer. Computed in TypeScript beside `currencyMismatchFlag` and over the purchase order's own lines, not the engine's result rows: `COMPARISON_SQL` ends `WHERE flag_type <> 'match'`, so a line the invoice matches perfectly emits no row, and that is exactly when "we ordered off contract" still matters. **`COMPARISON_SQL` is unedited across all of S9.** Flags gained `contractUnitPrice` and `contractTermId`. Silence is the default: no term for an item means no flag. Applicability uses `ordered_at ?? created_at`, and the reason says which. `COMPARISON_STRATEGY_VERSION` → 3. **2026-09-23 (S9 commit 3):** `counts` is keyed by all **ten** `discrepancy_flag_type` values, zero-filled — additive for any reader indexing by name, breaking only for one that pins the key set. The two new keys have no writer yet. **2026-09-23 (S9 commit 1):** every flag in `flags` (and in `GET .../discrepancies`) gained `poUnitPrice` and `invoiceUnitPrice`, additive. Previously the engine computed both on every row and `toFlagValues` read them only when the flag was a `price_mismatch`, so a line whose quantity **and** unit price both disagreed was labelled by the quantity and the price was recorded nowhere — the `CASE` returns its first match and `quantity_mismatch` is tested first. The label is unchanged and `delta` is unchanged; only the discarded evidence is kept. `COMPARISON_SQL` was not edited. `COMPARISON_STRATEGY_VERSION` → 2, so S8's freshness check recompares every pair once. **2026-09-23 (S8 commit 1):** receipt discovery gained `status = 'done'`, the same guard `loadReadyPo`/`loadReadyInvoice` already applied to the other two sides. Line rows exist independently of the header status — a re-parse replaces them wholesale, and a **failed** parse leaves the previous attempt's rows in place — so a mid-parse or failed receipt used to contribute stale accepted quantities. The direction of that error was an accusation: a stale low quantity becomes `short_receipt` or `invoice_exceeds_received` against a supplier who delivered correctly. Narrowing only, and it changes no response shape. **2026-09-21 (S3b):** `400 Invoice is linked to a different purchase order` when the invoice's `purchaseOrderId` is non-null and is not the PO being compared; a null link is the pre-0025 legacy path and still compares freely. |
| Procurement | List Comparison Runs | GET | `/workspaces/:workspaceId/procurement/comparison-runs` | `apps/web/src/components/procurement/discrepancy-review-modal.tsx` via `@/lib/api/procurement#listComparisonRuns` | `procurement.controller.ts#listComparisonRuns` → `comparison.service.ts#listRuns` | query `purchaseOrderId?`, `invoiceId?`, `status?` (`queued`\|`running`\|`succeeded`\|`failed`), `page?`, `pageSize?` | `{items: [{id, purchaseOrderId, invoiceId, mode, strategyVersion, status, initiatedBy, initiatedByEmail, poLineCount, invoiceLineCount, goodsReceiptLineCount, flagCount, startedAt, finishedAt, lastError, createdAt}], page, pageSize, total, totalPages}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Added 2026-09-23 (S7). Runs have been written since S1 and were never readable: a `runId` could be **used** (`GET discrepancies?runId=`) but never **discovered**, so "what did the previous comparison say?" had no answer. Ordered `created_at DESC, id DESC`; the id tiebreak matters for the same reason as the flag list. Served by the existing `comparison_runs_workspace_created_idx` and `comparison_runs_pair_status_idx` — no migration. `lastError` is returned deliberately: S0a made it a client-safe reference (`Comparison engine failed. Reference: <8-hex>`) with the engine's own text, which can quote cell values, never stored — and a failed run is the row a reviewer most needs to see. `initiatedByEmail` is a LEFT join on `users`, because `initiated_by` is `ON DELETE set null` and an orphaned run must stay in its own history. |
| Procurement | List Discrepancy Flags | GET | `/workspaces/:workspaceId/procurement/discrepancies` | `apps/web/app/workspaces/[id]/discrepancies/page.tsx` via `@/lib/api/procurement#listDiscrepancies` → `apps/web/app/api/workspaces/[id]/procurement/discrepancies/route.ts` | `procurement.controller.ts#listDiscrepancies` → `comparison.service.ts#listFlags` | query `purchaseOrderId?`, `invoiceId?`, `status?` (`open`\|`dismissed`), `runId?`, `page?`, `pageSize?` | `{items: [<full discrepancy_flags row: id, workspaceId, comparisonRunId, purchaseOrderId, invoiceId, poLineItemId, invoiceLineItemId, goodsReceiptLineItemId, sku, flagType, poValue, invoiceValue, receivedValue, poUnitPrice, invoiceUnitPrice, contractUnitPrice, contractTermId, delta, reason, status, dismissedAt, dismissedBy, createdAt>], page, pageSize, total, totalPages, counts}` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Added 2026-07-09. All filters optional and combinable. **2026-09-20 (S1):** omitting `runId` returns the **current** flags — those of the latest succeeded run for each PO/invoice pair, plus pre-S1 flags whose pair has no succeeded run yet. Without that default the list would grow by a full flag set on every re-compare. Supply `runId` to read one run's flags; a non-UUID `runId` is a 400. **2026-09-23 (S7):** the response is now the offset envelope from the shared convention above — a **breaking shape change** from the bare array, closing gap D6. `counts` is keyed by all ten `discrepancy_flag_type` values (`EMPTY_FLAG_COUNTS`, `comparison.service.ts`), zero-filled, and describes the **whole filtered set**, not the page: the six stat cards read it, and computing them from `items` would report whatever landed on screen. `total` is derived by summing `counts`, so the cards can never disagree with the list. Ordering is `created_at ASC, id ASC` — the id is load-bearing, not tidiness: `compare()` writes every flag of a run in one transaction, so they share a `created_at` exactly, and ordering on that alone is not a total order, which lets OFFSET repeat some rows and skip others. The "current run" scope also moved from a map-plus-in-memory-filter into a SQL predicate (`currentFlagScope`), because a filter applied after the query cannot be paginated; the workspace-wide read of every succeeded run went with it. |
| Procurement | Dismiss Discrepancy Flag | PATCH | `/workspaces/:workspaceId/procurement/discrepancies/:flagId/dismiss` | `apps/web/app/workspaces/[id]/discrepancies/page.tsx` via `@/lib/api/procurement#dismissDiscrepancy` → `apps/web/app/api/workspaces/[id]/procurement/discrepancies/[flagId]/dismiss/route.ts` | `procurement.controller.ts#dismiss` → `comparison.service.ts#dismissFlag` | path `flagId` | updated flag row with `status='dismissed'`, `dismissedAt`, `dismissedBy`; `404` foreign workspace/unknown flag; `400` non-UUID `flagId` (`ParseUUIDPipe`, 2026-09-20); re-dismissing an already-dismissed flag returns the stored row unchanged (first dismisser kept) | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-07-09. Audited, not a silent boolean flip — mirrors the `document_review_flags` dismiss pattern. **2026-09-20 (S2):** also writes one `false_positive` decision with an authored system note, in the same transaction as the status change. Signature is unchanged (no body), so existing callers are unaffected; a re-dismiss still returns the stored row and appends no second decision. |
| Procurement | Record Discrepancy Decision | POST | `/workspaces/:workspaceId/procurement/discrepancies/:flagId/decisions` | `apps/web/src/lib/api/procurement#recordDiscrepancyDecision` | `procurement.controller.ts#recordDecision` → `comparison.service.ts#recordDecision` | `{outcome: 'false_positive'\|'approved_exception'\|'vendor_dispute'\|'resolved', note}` — note required, 1..2000 chars | the created decision row; `400` blank note or unknown outcome or non-UUID `flagId`; `404` unknown flag/foreign workspace; `403` member | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-09-20 (S2). Append-only — there is no update or delete path. Recording a decision closes the flag; `actorRole` is captured at decision time. |
| Procurement | List Discrepancy Decisions | GET | `/workspaces/:workspaceId/procurement/discrepancies/:flagId/decisions` | `apps/web/src/lib/api/procurement#listDiscrepancyDecisions` | `procurement.controller.ts#listDecisions` → `comparison.service.ts#listDecisions` | path `flagId` | `[{id, discrepancyFlagId, comparisonRunId, actorUserId, actorRole, outcome, note, createdAt}]` oldest-first; `400` non-UUID `flagId` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Added 2026-09-20 (S2). Members can read why a flag was closed without being able to close one. **2026-09-23 (S7):** each row gains `actorEmail`, a LEFT join on `users` (null when the account was deleted — the decision stays in the trail regardless). `actorRole` is unchanged and stays *captured at decision time*, because memberships change and the record should not. `users` carries no display name, and members already see each other's emails through the members list, so this exposes nothing new. |
| Vendor Catalog | Create Vendor | POST | `/workspaces/:workspaceId/vendors` | `apps/web/app/workspaces/[id]/vendors/page.tsx` via `@/lib/api/catalog#createVendor` → `apps/web/app/api/workspaces/[id]/vendors/route.ts` (POST) | `catalog.controller.ts#createVendor` → `vendors.service.ts#create` | `{name, contactInfo?}` | `{id, name}` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-07-10 (A3). |
| Vendor Catalog | List Vendors | GET | `/workspaces/:workspaceId/vendors` | `apps/web/app/workspaces/[id]/vendors/page.tsx` (also `vendors/[vendorId]/page.tsx`, `catalog-matches/page.tsx`, `procurement/page.tsx`) via `@/lib/api/catalog#listVendors` → `apps/web/app/api/workspaces/[id]/vendors/route.ts` (GET) | `catalog.controller.ts#listVendors` → `vendors.service.ts#list` | none | `[{id, name, contactInfo, createdAt}]` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Added 2026-07-10 (A3). Newest-first, workspace-scoped. |
| Vendor Catalog | Get Vendor | GET | `/workspaces/:workspaceId/vendors/:vendorId` | `apps/web/app/workspaces/[id]/vendors/[vendorId]/page.tsx` | `catalog.controller.ts#getVendor` → `vendor-history.service.ts#get` | none | `{id, name, contactInfo, createdAt}`; `404` not in this workspace | Member only | Standard | Added 2026-09-23 (S9 commit 7). Until now the vendor detail page found its vendor by scanning the whole `listVendors` array client-side. |
| Vendor Catalog | Vendor Price History | GET | `/workspaces/:workspaceId/vendors/:vendorId/price-history` | `apps/web/app/workspaces/[id]/vendors/[vendorId]/page.tsx` | `catalog.controller.ts#vendorPriceHistory` → `vendor-history.service.ts#priceHistory` | query `sku?`, `page?`, `pageSize?` | `{items: [{poLineItemId, purchaseOrderId, poNumber, poName, currency, orderedAt, recordedAt, sku, uom, quantity, unitPrice, contractUnitPrice}], page, pageSize, total, totalPages, skus}`; `404` vendor not in this workspace | Member only | Deep | Added 2026-09-23 (S9 commit 7). One row per purchase-order line, newest order first, ordered `(orderedAt, createdAt, id)` — the id is the tiebreak that keeps OFFSET from repeating and dropping rows when orders share a date. `contractUnitPrice` is the price agreed for **that order's** date, not whichever term is open today, resolved with the same half-open window and the same refusal to choose between overlapping terms as `contractPriceFlags`; null means no single agreed price applied. `skus` describes the vendor, not the filter, so a filter can be changed without clearing it. **No `sum()`/`avg()`/`date_trunc`** — an average unit price across quarters would silently mix units and currencies, which POLICY v1 #4/#6 forbid; first/latest/change are computed by the caller from these rows. |
| Vendor Catalog | Vendor Exception Summary | GET | `/workspaces/:workspaceId/vendors/:vendorId/exception-summary` | `apps/web/app/workspaces/[id]/vendors/[vendorId]/page.tsx` | `catalog.controller.ts#vendorExceptionSummary` → `vendor-history.service.ts#exceptionSummary` | none | `{counts (all ten flag types, zero-filled), openTotal, purchaseOrderCount}`; `404` vendor not in this workspace | Member only | Standard | Added 2026-09-23 (S9 commit 7). Single-column `groupBy` + `count()`, the shape the repo already had twice. A clean vendor reports zeroes rather than an absence — Phase 3's rule that missing data is distinct from good performance. |
| Vendor Catalog | Record Price Term | POST | `/workspaces/:workspaceId/vendors/:vendorId/price-terms` | no web client function or page caller; BFF `apps/web/app/api/workspaces/[id]/vendors/[vendorId]/price-terms/route.ts` (POST) exists | `catalog.controller.ts#createPriceTerm` → `vendor-price-terms.service.ts#create` | `{sku, uom?, unitPrice (decimal STRING), currency (ISO 4217), effectiveFrom (ISO 8601), sourceReference?}` | `201` the stored term; `400` malformed price/date, or a price already effective from that date or later; `404` vendor not in this workspace | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-09-23 (S9 commit 2). `unitPrice` is a STRING end to end — drizzle maps `numeric` to a string and a JS number would round a price on the way in. `currency` is validated `@IsISO4217CurrencyCode` and **uppercased in the service**, because the global `ValidationPipe` runs without `transform` so a `@Transform` never fires (same trap S3b hit). Writing a new price **closes the previous open term in the same transaction** and links `supersedesId`, so at most one open-ended term exists per vendor/item/unit/currency; nothing is ever updated except that closing `effective_to`, and nothing is deleted. Backdating behind a live price is a 400, because two terms covering one instant is the one thing applicability must never guess at. |
| Vendor Catalog | List Price Terms | GET | `/workspaces/:workspaceId/vendors/:vendorId/price-terms` | client `@/lib/api/catalog#listVendorPriceTerms` (no page caller) → `apps/web/app/api/workspaces/[id]/vendors/[vendorId]/price-terms/route.ts` (GET) | `catalog.controller.ts#listPriceTerms` → `vendor-price-terms.service.ts#list` | none | `[{id, sku, skuKey, uom, unitPrice, currency, effectiveFrom, effectiveTo, sourceReference, supersedesId, createdBy, createdAt}]`; `404` vendor not in this workspace | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Added 2026-09-23 (S9 commit 2). Newest window first. Not paginated: a vendor's agreed prices are a short list, and S7's offset envelope would be ceremony over a handful of rows. |
| Vendor Catalog | Upload Catalog | POST | `/workspaces/:workspaceId/vendors/:vendorId/catalogs` | `apps/web/app/workspaces/[id]/vendors/[vendorId]/page.tsx` via `@/lib/api/catalog#uploadCatalog` → `apps/web/app/api/workspaces/[id]/vendors/[vendorId]/catalogs/route.ts` (POST) | `catalog.controller.ts#uploadCatalog` → `catalog-documents.service.ts#upload` | `multipart/form-data` field `file` (.pdf/.csv/.xlsx) | `{id, name, status='pending'}`; `400` unsupported type/missing file/`CATALOG_ENABLED!=='true'`; `404` vendor not found/foreign workspace; `413` over `MAX_UPLOAD_MB` | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-07-10 (A3). Mirrors `Upload Purchase Order`'s guard/Multer/exception-filter pattern, gated blanket (not per-extension) by `CATALOG_ENABLED`. Storage key `${workspaceId}/catalogs/${catalogId}/${uuid}-${name}`. PDF pages are rasterized (page-image granularity, reuses A2's `renderPdfToImages`); CSV/XLSX rows map `sku`/`description` + optional `photo_url` (downloaded via `CatalogImageService`). |
| Vendor Catalog | Scrape Catalog | POST | `/workspaces/:workspaceId/vendors/:vendorId/catalogs/scrape` | `apps/web/app/workspaces/[id]/vendors/[vendorId]/page.tsx` via `@/lib/api/catalog#scrapeCatalog` → `apps/web/app/api/workspaces/[id]/vendors/[vendorId]/catalogs/scrape/route.ts` | `catalog.controller.ts#scrapeCatalog` → `catalog-scrape.service.ts#startScrape` | `{seedUrl, maxDepth?, maxPages?}` | `{id, status='pending'}`; `400` seed URL non-public/`CATALOG_ENABLED!=='true'`; `404` vendor not found/foreign workspace | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-07-10 (A3). Mirrors `Start Crawl`'s SSRF-check-before-queue pattern; enqueues `catalog-scrape-queue` with `attempts:1`. Writes `catalog_items` directly from `<img>` tags on crawled pages — does not create `documents` or call `IngestService`. |
| Vendor Catalog | List Catalogs | GET | `/workspaces/:workspaceId/vendors/:vendorId/catalogs` | `apps/web/app/workspaces/[id]/vendors/[vendorId]/page.tsx` via `@/lib/api/catalog#listCatalogs` → `apps/web/app/api/workspaces/[id]/vendors/[vendorId]/catalogs/route.ts` (GET) | `catalog.controller.ts#listCatalogs` → `catalog-documents.service.ts#listCatalogs` | none | `[{id, name, sourceKind, status, rowCount, lastError, createdAt}]`; `404` vendor not found/foreign workspace | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Added 2026-07-10 (A3). Newest-first, scoped to workspace + vendor. |
| Vendor Catalog | List Catalog Items | GET | `/workspaces/:workspaceId/vendors/:vendorId/catalogs/:catalogId/items` | `apps/web/app/workspaces/[id]/vendors/[vendorId]/page.tsx` via `@/lib/api/catalog#listCatalogItems` → `apps/web/app/api/workspaces/[id]/vendors/[vendorId]/catalogs/[catalogId]/items/route.ts` | `catalog.controller.ts#listCatalogItems` → `catalog-documents.service.ts#listItems` | none | `[{id, sku, description, photoStorageKey, sourcePageNumber}]`; `404` catalog not found/foreign workspace or vendor | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Added 2026-07-10 (A3). Ordered by `lineNumber`. |
| Vendor Catalog | Catalog Item Photo | GET | `/workspaces/:workspaceId/catalog-items/:itemId/photo` | `<img src>` built by `apps/web/src/lib/api/catalog.ts#catalogItemPhotoUrl` in `apps/web/app/workspaces/[id]/vendors/[vendorId]/page.tsx` and `apps/web/app/workspaces/[id]/catalog-matches/page.tsx` → `apps/web/app/api/workspaces/[id]/catalog-items/[itemId]/photo/route.ts` (`proxyRaw`) | `catalog.controller.ts#catalogItemPhoto` → `catalog-documents.service.ts#getItemPhoto` → `storage.service.ts#getObject` | path `itemId` (UUID, `ParseUUIDPipe`) | `200` raw image bytes, `Content-Type` from `resolvePhotoContentType`, `Cache-Control: private, max-age=86400`, `X-Content-Type-Options: nosniff`; `404` `Catalog item not found` / `Catalog item has no photo` / `Catalog item photo is missing`; `400` non-UUID `itemId` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Standard | Item looked up by `id` AND `workspaceId`, so a foreign id is a 404. Served inline (unlike procurement downloads) because it is an `<img>` source; `nosniff` keeps the browser to the stated raster type. |
| Vendor Catalog | Search Matches (sourcing) | POST | `/workspaces/:workspaceId/catalog-matches/search` | `apps/web/app/workspaces/[id]/catalog-matches/page.tsx` via `@/lib/api/catalog#searchCatalogMatches` → `apps/web/app/api/workspaces/[id]/catalog-matches/search/route.ts` | `catalog.controller.ts#searchMatches` → `catalog-match.service.ts#search` | `{purchaseOrderLineItemId?, invoiceLineItemId?}` (exactly one) | `{matches: [...]}`; `404` line item not found/foreign workspace; `400` neither/both ids given | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-07-10 (A3). `matchType='sourcing'` — no `vendorId` scope, searches catalog items across all vendors in the workspace. `ILIKE` prefilter capped at `CATALOG_MATCH_MAX_CANDIDATES` (default 8); vision-LLM compare per candidate; delete-prior-then-insert idempotent on repeat search. **2026-09-20 (S0f):** delete+insert run in one transaction and the delete is scoped to open rows of the same `matchType`, so dismissed matches survive a re-search; a search that finds no candidates deletes nothing; the prefilter is ordered (stable candidate set) and escapes `%`/`_`; a line with no SKU and no description judges nothing instead of sending arbitrary items to the model. **2026-09-20 (S0c):** each candidate comparison goes through `UsageService.metered` (`402` when the workspace budget is spent); at most `CATALOG_MATCH_CONCURRENCY` (default 3) comparisons run at once. |
| Vendor Catalog | Verify Matches (compliance) | POST | `/workspaces/:workspaceId/vendors/:vendorId/catalog-matches/verify` | `apps/web/app/workspaces/[id]/catalog-matches/page.tsx` via `@/lib/api/catalog#verifyCatalogMatches` → `apps/web/app/api/workspaces/[id]/vendors/[vendorId]/catalog-matches/verify/route.ts` | `catalog.controller.ts#verifyMatches` → `catalog-match.service.ts#search` | `{purchaseOrderLineItemId?, invoiceLineItemId?}` (exactly one) | `{matches: [...]}`; `404` line item or vendor not found/foreign workspace; `400` neither/both ids given | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-07-10 (A3). Same primitive as Search, with `vendorId` scope → `matchType='compliance'`. Verifies a vendor's invoice/PO line items against that vendor's own catalog only. **2026-09-20 (S0f):** `vendorId` now gets a `ParseUUIDPipe` (`400` on a malformed id, previously a `500`) and is checked against the workspace (`404`); the delete is additionally scoped to that vendor, so verifying one vendor no longer wipes another's sourcing results. **2026-09-20 (S0c):** same budget (`402`) and concurrency bound as Search Matches. |
| Vendor Catalog | List Catalog Matches | GET | `/workspaces/:workspaceId/catalog-matches` | `apps/web/app/workspaces/[id]/catalog-matches/page.tsx` via `@/lib/api/catalog#listCatalogMatches` → `apps/web/app/api/workspaces/[id]/catalog-matches/route.ts` | `catalog.controller.ts#listMatches` → `catalog-match.service.ts#listMatches` | query `vendorId?`, `status?` (`open`\|`dismissed`), `poLineItemId?`, `invoiceLineItemId?` | `[{id, matchType, queryPoLineItemId, queryInvoiceLineItemId, catalogItemId, vendorId, score, isMatch, reason, status, dismissedAt, dismissedBy, createdAt}]` | Member only (`JwtAuthGuard` + `WorkspaceMemberGuard`) | Deep | Added 2026-07-10 (A3). All filters optional and combinable. **2026-09-20 (S0f):** added the two line-item filters. The Catalog Matches page is opened from one discrepancy row but had no way to ask for that row's matches, so it listed the whole workspace; it now passes them. Omitting both keeps the previous behaviour. |
| Vendor Catalog | Dismiss Catalog Match | PATCH | `/workspaces/:workspaceId/catalog-matches/:matchId/dismiss` | `apps/web/app/workspaces/[id]/catalog-matches/page.tsx` via `@/lib/api/catalog#dismissCatalogMatch` → `apps/web/app/api/workspaces/[id]/catalog-matches/[matchId]/dismiss/route.ts` | `catalog.controller.ts#dismissMatch` → `catalog-match.service.ts#dismissMatch` | path `matchId` | updated match row with `status='dismissed'`, `dismissedAt`, `dismissedBy`; `404` foreign workspace/unknown match | Owner/admin only (`JwtAuthGuard` + `WorkspaceMemberGuard` + `RolesGuard`) | Deep | Added 2026-07-10 (A3). Audited, mirrors `Dismiss Discrepancy Flag`. **2026-09-20 (S0f):** `matchId` gets a `ParseUUIDPipe` (`400` instead of `500` on a malformed id). |
