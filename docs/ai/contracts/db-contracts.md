# Database Contracts

Purpose:

Map important database models, ownership, and invariants.

This file is map only.

It is not proof of behavior.

Verify all conclusions against schema, migrations, services, jobs, and tests.

If this map conflicts with source code, source code wins.

Mark stale or conflicting entries as `CONTRACT DRIFT`.

Mark missing contracts as `UNMAPPED CONTRACT`.

If mutation path bypasses required invariant, mark `CONTRACT MISMATCH`.

Do not invent invariants.

Unknown fields must be marked `TODO: Fill after repository analysis. Do not treat as verified.`

---

## Contract Index

| Domain | Model / Table | Owner Module | Important Fields | Invariants | Mutation Paths | Transaction / Idempotency Rules | Related APIs / Jobs | Risk | Notes |
| ------ | ------------- | ------------ | ---------------- | ---------- | -------------- | ------------------------------- | ------------------- | ---- | ----- |
| Auth | `users` | `apps/api/src/auth` | `id`, `email` (unique), `passwordHash`, `isVerified` | `email` always stored lowercase+trimmed — enforced in `auth.service.ts#normalizeEmail`, not a DB constraint | insert via `register()`; update `isVerified` via `verifyOtp()` | none (single-row writes) | `POST /auth/register`, `/auth/verify-otp`, `/auth/login` | Deep | UNVERIFIED DEPENDENCY: no DB-level case-insensitive unique index — relies entirely on the app-layer normalization. Verified via `auth.service.spec.ts`. |
| Auth | `otps` | `apps/api/src/auth` | `userId` (FK), `code`, `expiresAt`, `usedAt`, `failedAttempts` (int, default 0; migration 0034) | one-time use: valid only if `usedAt IS NULL` and `expiresAt > now` | insert via `register()`; `usedAt` set via `verifyOtp()` | none | `POST /auth/register`, `/auth/verify-otp` | Deep | A code stops matching after 5 wrong guesses (2026-09-25); resend expires older live codes. |
| Auth | `refresh_tokens` | `apps/api/src/auth` | `userId` (FK), `tokenHash` (SHA-256), `expiresAt`, `revokedAt` | raw token is never stored; valid only if `revokedAt IS NULL` and `expiresAt > now` | insert via `issueTokens()`; `revokedAt` set via `logout()` AND via `refresh()` (rotation revokes the old row every time) | `refresh()` rotation (revoke-old + insert-new) runs inside `db.transaction()` as of 2026-06-29 — atomic, a crash mid-rotation rolls back instead of leaving a dead-end session | `POST /auth/login`, `/verify-otp`, `/refresh`, `/logout` | Deep | Reuse of an already-revoked token triggers `refresh()` to revoke every other active token for that `userId` (theft response). Verified in `auth.service.spec.ts`. |
| Workspaces | `workspaces` | `apps/api/src/workspaces`, `apps/api/src/auth` (`verifyOtp` auto-create) | `id`, `name`, `ownerId`, `createdAt` | every verified user gets exactly one personal workspace during first successful `verifyOtp`; workspace create must also create at least one owner membership | insert via `workspaces.service.ts#create`; insert via `auth.service.ts#verifyOtp` auto-create | `create()` and `verifyOtp()` wrap workspace + owner membership in one `db.transaction()` | `POST /workspaces`, `GET /workspaces/me`, `POST /auth/verify-otp` | Deep | Auto-created name format is ``${email}'s workspace``. Verified in `auth.service.spec.ts`, `test/workspaces.e2e-spec.ts`. |
| Workspaces | `workspace_members` | `apps/api/src/auth/guards` (read), `apps/api/src/workspaces`, `apps/api/src/auth` (`verifyOtp` owner insert) | `workspaceId` (FK), `userId` (FK), `role` (`owner`\|`admin`\|`member`) | `(workspaceId, userId)` pair determines access; checked fresh per-request, never cached in JWT; each workspace must retain at least one owner | read via `WorkspaceMemberGuard`; insert via `workspaces.service.ts#create`; insert via `workspaces.service.ts#acceptInvite`; insert via `auth.service.ts#verifyOtp`; delete via `workspaces.service.ts#removeMember` | `verifyOtp()`, `create()`, and `acceptInvite()` perform related writes transactionally; `removeMember()` forbids deleting last owner | `GET /workspaces/:workspaceId`, `POST /workspaces`, `/accept-invite/:token`, `/invite`, `DELETE /members/:userId` | Deep | `RolesGuard` authorizes by `workspaceMember.role`; `member` can read only, `owner/admin` can invite, only `owner` can remove members. |
| Workspaces | `invitations` | `apps/api/src/workspaces` | `workspaceId` (FK), `email`, `token`, `expiresAt`, `acceptedAt`, `createdAt` | `token` unique + single-use (`acceptedAt IS NULL` before acceptance); invite expires after 7 days; invite token never returned in API body | insert via `workspaces.service.ts#invite`; update `acceptedAt` via `workspaces.service.ts#acceptInvite` | `acceptInvite()` marks `acceptedAt` and inserts membership in one transaction; duplicate existing member becomes no-op membership write but still burns invite token | `POST /workspaces/:workspaceId/invite`, `POST /workspaces/accept-invite/:token` | Deep | Invite email must match logged-in user email at acceptance time. Dev fallback logs `[DEV INVITE] <email> -> <url>`. |
| Knowledge Bases | `knowledge_bases` | `apps/api/src/knowledge-bases` | `id`, `workspaceId`, `name`, `createdAt` | every KB is scoped to one workspace; delete must be blocked while any `documents.knowledgeBaseId` references it | insert via `knowledge-bases.service.ts#create`; delete via `knowledge-bases.service.ts#remove`; read via `listForWorkspace()` | no transaction needed: create is single-row insert; delete first verifies workspace match, then counts `documents`, then deletes only when count = 0 | `POST/GET/DELETE /workspaces/:workspaceId/knowledge-bases*` | Standard | Delete returns 404 when `kb.workspaceId !== route.workspaceId` so callers cannot probe other workspaces. |
| Documents | `documents` | `apps/api/src/documents`, `apps/api/src/ingest`, `apps/api/src/scrape` | `workspaceId`, `knowledgeBaseId`, `title`, `sourceUrl`, `storageKey`, `status`, `queueJobId`, `enqueuedAt`, `processingStartedAt`, `lastError`, `updatedAt` | upload creates rows as `pending`; ingest job is the only writer that transitions `status` to `processing`, `done`, or `failed`; queue metadata must be present before or during enqueue so startup reconciliation can fail stale orphaned rows instead of leaving them stuck; requeue clears any stale `processingStartedAt`; `storageKey` preserves original extension so `loadDocument()` can choose the right loader and so download endpoints can fetch exact stored bytes; `(knowledgeBaseId, sourceUrl)` is unique when `sourceUrl IS NOT NULL` so recrawls update one page row instead of duplicating | insert via `documents.service.ts#upload`; enqueue + reconciliation via `ingest.service.ts#queueDocument` / `#reconcileDocuments`; upsert via `scrape.processor.ts#handleScrape`; list/download/delete via `documents.service.ts#listForKnowledgeBase` / `#getDownloadable` / `#getManyDownloadable` / `#remove` / `#removeMany`; update via `ingest.processor.ts#handleIngest` | upload is not fully transactional across object storage + DB + queue, so enqueue failure must mark the row terminal `failed` with `lastError`; scraped-page idempotency comes from `onConflictDoUpdate` on `(knowledgeBaseId, sourceUrl)` plus downstream `syncChunks()` hash diff; download is read-only after route-scope verification and uses `storageKey`; single and bulk delete always remove DB rows even if object storage delete fails | `/workspaces/:workspaceId/knowledge-bases/:kbId/documents*`, `/scrape`, Bull `scrape-queue`, Bull `ingest-queue` | Deep | Startup reconciliation fails stale `pending` rows after 2 minutes and stale `processing` rows after 30 minutes when Bull no longer has the job id. Ingest queue jobs now carry a 5-minute Bull timeout. Scrape writes clean page text as `.txt` objects and reuses existing ingest queue per page. Slice 1 (2026-07-04) added no schema change: document listing now reads with `updatedAt DESC, id DESC`, `title ILIKE`, and `status`; single/bulk downloads read `storageKey` through `StorageService.getBuffer()`. Bulk delete (2026-07-04) adds no schema change: it scopes selected ids to workspace+KB, skips stale/foreign ids, deletes matching rows in one DB statement, relies on chunk FK cascade, and bumps chat cache once if any row is deleted. |
| Documents | `chunks` | `packages/ai/src/vectorstore`, `apps/api/src/ingest`, `apps/api/src/tickets` | `documentId`, `ticketId`, `workspaceId`, `content`, `contentHash`, `embedding`, `metadata` | every chunk row belongs to exactly one parent via DB CHECK `((document_id IS NOT NULL) <> (ticket_id IS NOT NULL))`; document chunks enrich metadata with `workspaceId`, `knowledgeBaseId`, and `documentId`; ticket chunks enrich metadata with `workspaceId`, `ticketId`, and `source='ticket'`; deleting a parent cascades its chunks automatically; at most one chunk may point to a given ticket via unique nullable index on `ticket_id` | insert/delete via `syncChunks(...)`; insert/delete via `syncTicketChunk()` / `backfillTicketEmbeddings()`; delete via `documents.service.ts#remove` cascade | `syncChunks()` is idempotent by `contentHash` diff per `documentId`; `syncTicketChunk()` is idempotent by one-chunk-per-ticket + content-hash compare and embeds-before-delete so a failed re-embed leaves old chunk intact; both parent FKs are `ON DELETE CASCADE` | Bull `ingest-queue`, document delete path, `PATCH /workspaces/:workspaceId/tickets/:ticketId`, `scripts/backfill-ticket-embeddings.ts` | Deep | Document re-processing still does not duplicate unchanged chunks because `syncChunks()` deletes removed hashes and inserts only new hashes. Ticket embedding is one chunk per reviewed useful resolved ticket, not multi-chunk. Ticket-driven chunk changes now also drive workspace chat-cache invalidation: `TicketsService.update()` bumps cache version on `syncTicketChunk()` outcomes `embedded`/`deleted`, and backfill reports `changedWorkspaceIds` so the script can bump each touched workspace once. 2026-07-03: added indexed filter columns `source_type` ('document'|'ticket'|'web'), `doc_type` (file type), `product_area` (ticket area) via migration `0011_sticky_scream` (additive + backfill). Write paths populate them — `syncChunks()` from ingest metadata (`sourceType`, `fileType`→`docType`), `syncTicketChunk()` from ticket fields (`sourceType='ticket'`, `productArea`, `severity`). Added `chunks_embedding_hnsw_idx` (hnsw, `vector_cosine_ops`; pgvector 0.8.3) so retrieval stops sequential-scanning every workspace chunk, plus btree indexes on `(workspace_id, source_type|doc_type|product_area)`. |
| Chat / RAG | `documents` + `chunks` read path | `packages/ai/src/chains`, `apps/api/src/chat` | `chunks.workspaceId`, `chunks.metadata.documentId`, `chunks.metadata.ticketId`, `documents.title`, `documents.sourceUrl`, `tickets.title` | retrieval must stay scoped by `workspaceId`; `buildContext()` may use both document and ticket chunk content; citation hydration must discriminate `metadata.documentId` vs `metadata.ticketId`; chunks with neither parent metadata should not become citations; chat retrieval must reserve at most `TICKET_SLOT_RESERVE` qualifying ticket chunks and exclude ticket chunks below `TICKET_SLOT_MIN_SCORE`; the retrieval query passed in must already be a standalone question — `chat.service.ts` resolves history-dependent follow-ups via `condenseQuestion()` before calling retrieval, so `retrieveNode`/`similaritySearchWithTicketSlot()` never see raw conversation history | read via `similaritySearch(query, workspaceId, limit)` for search and `similaritySearchWithTicketSlot(query, workspaceId, limit)` for chat (`answerQuestion()` / `answerQuestionWithGraph()`) | read-only request path; legacy persisted chat source rows may lack `sourceType`, so FE read path must default them to document rendering | `POST /workspaces/:workspaceId/chat` | Deep | Verified source + automated as of 2026-07-02. Resolved useful reviewed tickets can now surface in chat citations alongside KB docs. Chat now reserves a small ticket slot with a relevance floor so ticket citations are not drowned out by large KB corpora, while `search.service.ts` stays on plain `similaritySearch()` and remains document-oriented. 2026-07-03: `similaritySearch()` / `similaritySearchWithTicketSlot()` now accept optional `precomputedEmbedding` (reuse the semantic-cache embedding — no double embed on miss) and `RetrievalFilters` (`sourceType`/`docType`/`productArea`/`sectionId` → indexed WHERE on the document query). `buildContext()` now delegates to `buildEvidencePack()` (token-budgeted compact pack, `RAG_CONTEXT_TOKEN_BUDGET`). Filters are plumbed through `answerQuestion()` and the graph but not yet through `ChatDto`. 2026-07-05: `answerQuestion()`/`answerQuestionWithGraph()` gained an optional trailing `history: HistoryTurn[]` param (bounded via `boundHistory()`, default `[]`) threaded into the final generation prompt only — never into `retrieveNode`/`rewriteNode`, which still operate solely on the (already-condensed) `retrievalQuery`. |
| Chat / RAG | `chat_sessions` | `apps/api/src/chat`, `packages/db/src/schema/chatSessions.ts` | `workspaceId`, `userId`, `title`, `updatedAt` | every session belongs to exactly one workspace and one owning user; session reads must filter by both | insert via `chat.service.ts#createSession`; read via `listSessions()`/`getOwnedSession()` | additive S2 table; FK cascades on workspace/user delete | `/workspaces/:workspaceId/chat*` | Deep | Title is first user message truncated to 60 chars in service code. `updatedAt` bumps when assistant turn persists so newest-active sessions sort first. |
| Chat / RAG | `chat_messages` | `apps/api/src/chat`, `packages/db/src/schema/chatSessions.ts` | `sessionId`, `role`, `content`, `sources`, `createdAt` | rows belong to one session; only `assistant` rows carry `sources`; message history order is ascending `createdAt`; `(session_id, created_at)` is indexed (`chat_messages_session_created_idx`, migration `0013_condemned_silver_surfer.sql`) | insert user row before retrieval in `chat.service.ts#answer`; insert assistant row via returned `onComplete()` callback after stream finishes; read via `getMessages()` (paginated history panel) and new `getRecentHistory()` (bounded recent-turns fetch for history-aware chat, ordered `createdAt DESC, id DESC`, excludes the just-inserted current-turn row by id) | assistant persistence is decoupled from token streaming but still tied to same session; FK cascade on session delete | `/workspaces/:workspaceId/chat`, `/sessions/:sessionId/messages` | Deep | `sources` stores JSON copy of `ChatSource[]` from retrieval so frontend can reload citations from history without replaying stream headers. 2026-07-05: added the composite index because `getRecentHistory()` now runs this same `(session_id, created_at)` read shape on every chat turn (not just the paginated history-panel endpoint) — additive index migration, no backfill, safe rollback via `DROP INDEX`. |
| Chat / RAG | `chat_cache` | `apps/api/src/cache`, `packages/db/src/schema/chatCache.ts` | `workspaceId`, `version`, `question`, `questionEmbedding`, `answer`, `sources`, `createdAt` | semantic cache must be scoped by both `workspaceId` and `version`; semantic hits also expire by `createdAt` age via `SEMANTIC_CACHE_TTL_HOURS` (default `24`); deterministic fallback responses (`AnswerResult.isFallback=true`) must never be written to either cache layer; version bump makes older rows cold immediately; exact Redis cache uses same workspace-version boundary | insert via `cache.service.ts#saveSemantic`; read via `cache.service.ts#getSemantic`; invalidation via `cache.service.ts#bumpVersion` called from `ingest.processor.ts` document `done`, `documents.service.ts#remove` / `#removeMany`, and ticket-chunk changes through `tickets.service.ts` / backfill script | additive table; no backfill; safe rollback = drop table; versioned invalidation avoids row-by-row deletes; expired semantic rows are opportunistically deleted on each `saveSemantic()` write for that workspace | `/workspaces/:workspaceId/chat`, ingest job, document delete, ticket review save, `scripts/backfill-ticket-embeddings.ts` | Deep | Redis exact cache key format is `chat:ans:${workspaceId}:${version}:sha256(normalizedMessage)`. `getVersion()` bootstraps missing workspace versions at `1`, so first invalidation advances to `2` and makes previous exact keys unreachable. `getSemantic()` now refuses rows older than the TTL even if version still matches, so missed version bumps no longer create unbounded semantic-cache staleness. Cache writes from live generation now happen only when `AnswerResult.isFallback` is false, so deterministic fallback/non-answer text is not persisted as reusable cache content. |
| Chat / RAG | `saved_refined_messages` | `apps/api/src/refine` | `workspaceId` (FK), `userId` (FK), `originalText`, `refinedText`, `createdAt` | every row belongs to exactly one workspace and one user; reads must filter by both so saved messages stay personal — not workspace-shared — mirroring the `chat_sessions` privacy invariant; no unique constraint, duplicate `(originalText, refinedText)` pairs are allowed; no DB-level ordering, service enforces `createdAt DESC` capped at 20 | insert via `refine.service.ts#saveRefinedMessage`; read via `refine.service.ts#listSavedRefinedMessages` | additive table, migration `0012_little_blink.sql`; both FKs `ON DELETE CASCADE` (workspace or user delete removes saved messages); single-row insert needs no transaction; safe rollback = drop table | `POST/GET /workspaces/:workspaceId/refine/saved` | Standard | Added 2026-07-04. Schema/migration and the three hard components this feature depends on (chain, rate limiter, schema) were already built and tested before the endpoint wiring landed — see `risk-register.md` for the reasoning behind classifying this Standard rather than the repo's migrations-Deep-by-default rule. |
| Web Sources | `scrape_runs` | `apps/api/src/scrape` | `workspaceId`, `knowledgeBaseId`, `seedUrl`, `status`, `queueJobId`, `enqueuedAt`, `maxDepth`, `maxPages`, `pagesFound`, `pagesSucceeded`, `pagesFailed`, `error`, `startedAt`, `lastProgressAt`, `finishedAt` | each run belongs to one workspace + KB; lifecycle is `queued -> running -> completed|failed`; queue metadata must be present before or during enqueue so startup reconciliation can fail stale orphaned rows; omitted `includePrefixes` defaults to the seed subtree (with `/.../home` widened to its parent section); page-level storage failures do not fail whole run, they increment `pagesFailed`; crawl-level failure stores terminal `failed` plus `error`; duplicate in-flight requests for the same workspace + KB + seed URL must reuse the existing run instead of inserting another queued row | insert + enqueue / duplicate reuse / reconciliation via `scrape.service.ts#startScrape` / `#reconcileRuns`; offset list via `scrape.service.ts#listRuns`; live page updates via `scrape.processor.ts#handleScrape` | run creation and queue enqueue are separate, so enqueue failure must mark the row terminal `failed`; whole-crawl auto-retry disabled (`attempts:1`) because recrawl side effects are bounded inside processor/page-level handling | `/workspaces/:workspaceId/knowledge-bases/:kbId/scrape*`, Bull `scrape-queue` | Deep | Startup reconciliation fails stale `queued` rows after 2 minutes, stale `running` rows after 30 minutes when Bull no longer has the job id, and `running` rows after 5 minutes with no progress heartbeat. FKs to `workspaces` and `knowledge_bases` cascade on delete. Slice 1 (2026-07-04) added no schema change: newest-first listing now uses offset pagination with default `pageSize=5`, `seedUrl ILIKE`, and `status`. |
| Tickets | `tickets` | `apps/api/src/tickets`, `packages/ai/src/chains/ticket-extraction.ts`, `packages/ai/src/vectorstore/index.ts` | `workspaceId`, `transcript`, `transcriptHash`, extracted fields (`title`, `issueSummary`, `reproSteps`, `severity`, `productArea`, `hypothesizedRootCause`, `nextAction`), lifecycle fields (`status`, `queueJobId`, `enqueuedAt`, `processingStartedAt`, `lastError`), `fieldConfidence`, feedback fields (`usefulness`, `editState`, `feedbackNote`), audit fields (`reviewedBy`, `reviewedAt`) | each row belongs to one workspace; exact transcript dedup is enforced by unique `(workspaceId, transcriptHash)` plus app pre-check/re-fetch on race; extraction worker is only writer for `status -> processing|done|failed` and must not clobber rows that already left `pending|processing`; startup reconciliation fails stale orphaned `pending`/`processing` rows when Bull job is missing; review saves overwrite `reviewedBy`/`reviewedAt` and merge edited-field confidence; qualifying reviewed tickets (`done` + `reviewedBy` + `usefulness='useful'`) own exactly one vector chunk | insert + enqueue / dedup / reconciliation via `tickets.service.ts#create` / `#queueTicket` / `#reconcileTickets`; extraction writes via `ticket-extraction.processor.ts#handleExtraction`; review edits via `tickets.service.ts#update`, which now calls `syncTicketChunk()` with caught/logged failures only and bumps workspace chat-cache version on `embedded`/`deleted`; backfill via `backfillTicketEmbeddings()` | queue enqueue failure marks row terminal `failed`; extraction queue uses deterministic job id `ticket-extraction:{ticketId}`; pending-stale grace is 10 minutes so it exceeds the 5-minute Bull timeout; ticket-embedding sync is synchronous in PATCH, idempotent, and safe to re-run via backfill; additive table so rollback = drop ticket-authored chunks then remove schema/module | `/workspaces/:workspaceId/tickets*`, Bull `ticket-extraction-queue`, `scripts/backfill-ticket-embeddings.ts` | Deep | `transcript` is stored before enqueue so jobs carry only `ticketId`. Feedback fields drive usefulness-rate observability in dashboard and future alerting. Ticket chunk sync failures never fail the review-save request. Cosmetic review saves that leave ticket chunk state unchanged do not bump cache version. |
| Procurement / Discrepancy | `purchase_orders` / `invoices` | `apps/api/src/procurement` | `workspaceId`, `name`, `poNumber`/`invoiceNumber`, `currency`, `storageKey` (nullable), `sourceKind` (default `'csv'`), lifecycle fields (`status`, `queueJobId`, `enqueuedAt`, `processingStartedAt`, `rowCount`, `lastError`) | each row belongs to one workspace; parse worker is only writer for `status -> processing\|done\|failed`; `storageKey` nullable + `sourceKind` were A2 attach points, now live: `sourceKind='pdf'` on headers for `.pdf` uploads (`'csv'` default unchanged for CSV/XLSX). **2026-09-20 (S0b):** header `sourceKind` is `'csv'`/`'xlsx'`/`'pdf'` by extension; `lastError` is client-safe (authored message or `Parsing failed. Reference: <id>`); `queueJobId` gains an `:r<n>` suffix when reconcile requeues (max 2) | insert + enqueue via `procurement-documents.service.ts#upload`; enqueue/reconcile via `procurement-parse.service.ts#queueDoc`/`#reconcile`; status transitions + delete-then-insert of child line items via `procurement-parse.processor.ts#handleParse` | queue enqueue failure marks row terminal `failed`; deterministic job id `procurement-parse:{kind}:{id}`; re-parse deletes existing line items before inserting fresh ones (idempotent on retry); additive tables, no vector column, rollback = drop tables + enum | `/workspaces/:workspaceId/procurement/{purchase-orders,invoices}`, Bull `procurement-parse-queue` | Deep | Added 2026-07-09, migration `0020_bumpy_energizer.sql`. Mirrors `datasets` header lifecycle exactly minus the embedding step — no OpenAI call anywhere in the CSV/XLSX path (verified). **Updated 2026-07-10 (A2):** `.pdf` uploads (still no migration — same columns) now go through an LLM extraction chain instead of `Papa.parse`; see `module-ownership-map.md`'s A2 note. **Updated 2026-09-21 (S3b), migration `0025_broad_invisible_woman.sql`:** `purchase_orders.vendor_id` -> `vendors.id` and `invoices.purchase_order_id` -> `purchase_orders.id`, both **nullable**, both `ON DELETE set null`, indexed `(workspace_id, vendor_id)` and `(workspace_id, purchase_order_id)`. **2026-09-23 (S9 commit 4), migration `0032`:** `purchase_orders.ordered_at`, nullable timestamp — when the order was placed, as stated by the uploader, distinct from `created_at`. Contract applicability reads `ordered_at ?? created_at`; without it a January order uploaded in June is judged against June's contract. Nullable and optional at upload because every pre-0032 row has none and a genuine "I don't know" must stay uploadable. `updated_at` was never a candidate: it moves on re-parse, so the same order would match a different term tomorrow with no document change. `poNumber`/`invoiceNumber`/`currency` stop being seed-only and are now written on every upload from a user-supplied header (POLICY v1 #2, #3) — `currency` is validated ISO 4217 and **uppercased in the service** before insert, because `IsISO4217CurrencyCode` is case-insensitive and S6 compares currencies. Both new columns are nullable on purpose: migrations run at API start against the previous image's database, so `NOT NULL` with no default would fail the deploy; a null means "uploaded before 0025" and every reader must allow it. `ComparisonService.compare` refuses when a **non-null** `invoices.purchase_order_id` disagrees with the PO being compared, and allows null as the legacy path. |
| Procurement / Receiving | `goods_receipts` / `goods_receipt_line_items` | `apps/api/src/procurement` | Header: `workspaceId`, **`purchaseOrderId` (NOT NULL, FK cascade)**, `name`, `grnNumber`, `storageKey` (nullable), `sourceKind`, lifecycle fields (`status` reusing `procurement_doc_status`, `queueJobId`, `enqueuedAt`, `processingStartedAt`, `rowCount`, `lastError`). Lines: `sku`, `description`, **`quantityReceived` / `quantityAccepted` / `quantityRejected` (all nullable `numeric`)**, `uom`, `rawRow`, `sourceSheet`, `sourceRow`, `sourceKind` | **NULL IS NOT ZERO** on the three quantity columns — §1B and POLICY v1 #14 both require that missing receiving data is never read as "nothing received/accepted". No DEFAULT may ever be added to them; S6 must treat null as "not stated". `purchaseOrderId` is NOT NULL (unlike `invoices.purchase_order_id`, which had to tolerate pre-0025 rows) because the table is new and POLICY v1 #2 requires the link — and it is **not unique**, since POLICY v1 #14 allows many receipts per PO. No `vendorId` (POLICY v1 #3: inherited via the linked PO) and no `currency` (a receipt records what arrived, not what it cost). No `extraction_confidence`/`extractor_version`: those are PDF-only and PDF receipts are deferred, so shipping them would mean permanently-null columns | insert + enqueue via `procurement-documents.service.ts#upload` (kind `'goods_receipt'`); lines written by `procurement-parse.processor.ts#replaceLineItemsAndFinish` | Shares `procurement-parse-queue` and the whole upload→parse→queue template; job id namespaces by kind. **`reconcile()` needed a third scan added by hand** — it is a sequence of per-table scans, not a branch, so `assertUnreachable` cannot catch an omission there | `/workspaces/:workspaceId/procurement/goods-receipts` | Deep | Added 2026-09-22 (S5), migration `0026_nostalgic_black_widow.sql`. Two new tables, four indexes, four FKs; no new enum — `procurement_doc_status` is reused, and the repo still has no `ALTER TYPE` precedent. `comparison_runs`/`discrepancy_flags` are deliberately untouched: the three-way match is S6, and a column with no writer is the defect S3a/S3b removed. |
| Procurement / Three-way | `comparison_run_goods_receipts` | `apps/api/src/procurement` | `comparison_run_id` + `goods_receipt_id`, both NOT NULL cascade, composite PK, index on `goods_receipt_id` | A join table rather than a column on `comparison_runs`, because POLICY v1 #14 sums accepted quantity across **every** receipt linked to the PO — one column could not say which. Recorded rather than re-derived so a receipt uploaded later cannot change what an earlier run says it compared (§1E "source document IDs") | written inside `compare()`'s transaction when `mode = 'three_way'`; **since 2026-09-23 (S8 commit 1) only receipts with `status = 'done'` are read at all**, so a mid-parse or failed receipt contributes neither lines nor a join row | Read by POLICY v1 #9's retention guard (`runReferencePredicate`) to know a receipt is now evidence | — | Deep | Added 2026-09-22 (S6), migration `0027_common_darkstar.sql`. Same migration adds four values to `discrepancy_flag_type` (`short_receipt`, `invoice_exceeds_received`, `uom_mismatch`, `currency_mismatch`) — the repo's first `ALTER TYPE … ADD VALUE`, hand-guarded with `IF NOT EXISTS`; plus `discrepancy_flags.received_value` + `goods_receipt_line_item_id` and `comparison_runs.goods_receipt_line_count`, all nullable. **The migration adds enum values and never writes one** — see the risk register for why that distinction is load-bearing. |
| Procurement / Discrepancy | `po_line_items` / `invoice_line_items` | `apps/api/src/procurement` | `workspaceId` (denormalized), parent FK, `lineNumber`, `sku`, `description`, `quantity`/`unitPrice`/`lineTotal` (all nullable, stored as `numeric` strings), `rawRow` (jsonb, original CSV row or extracted PDF item incl. confidence), `sourceKind` (`'csv'`, `'xlsx'` since 2026-09-20, or `'pdf-extraction'`) | `workspaceId` denormalized onto the row (not derived via parent join) so every read is a single indexed WHERE, same pattern as `chunks`; parent FK `ON DELETE CASCADE` | insert/delete via `procurement-parse.processor.ts#handleParse` (delete-then-bulk-insert per parse run); read via `comparison.service.ts#compare` (exported to temp CSV) | delete-then-insert per parse run is the idempotency mechanism — a Bull retry never doubles rows; no direct API mutation path, only via re-parsing the parent document | Bull `procurement-parse-queue`, `comparison.service.ts` (read-only) | Deep | Added 2026-07-09, migration `0020_bumpy_energizer.sql`. `rawRow` preserves the full original row for audit even when `column-mapping.ts`'s alias resolution only partially maps a vendor's header names. **Updated 2026-07-10 (A2):** PDF-sourced rows get `sourceKind='pdf-extraction'` and `rawRow` holding the LLM's raw extracted item (sku/description/quantity/unitPrice/lineTotal/confidence) instead of a CSV row — same columns, no migration. Numeric fields are validated at the LLM-output boundary (`Number.isFinite`) before insert, since a hallucinated non-numeric string would otherwise fail the whole bulk insert at the Postgres `numeric` column. **2026-09-20 (S3a):** five nullable provenance columns — `sourceSheet`, `sourceRow`, `uom`, `extractionConfidence`, `extractorVersion`. Which are populated depends on the source and that is the point: CSV/XLSX rows carry `sourceRow` (the real file line — Papa's `skipEmptyLines` is off so the index still maps to the file) and `uom`; XLSX additionally carries `sourceSheet`; PDF rows carry `extractionConfidence` (promoted out of `rawRow`, where it was unqueryable) and `extractorVersion`. `uom` is **captured, never converted** (POLICY v1 #4) — S6 turns a mismatch into `needs_review`. `source_page` is deliberately absent: the extraction chain prompts over the whole document, so no honest page number exists without per-page calls. Migration `0024` backfills `extractionConfidence` from `raw_row->>'confidence'`, guarded by `jsonb_typeof` so a non-numeric value is skipped rather than aborting. **2026-09-20 (S0b):** every path (CSV, XLSX, PDF) runs `column-mapping.ts#validateLineItem` — a non-decimal numeric cell or a SKU over 200 chars is stored as `null` with the original kept in `rawRow`; all-empty rows are skipped; delete + 1,000-row chunked inserts + the parent's `done` update run in one transaction (`procurement-parse.processor.ts#replaceLineItemsAndFinish`). |
| Procurement / Discrepancy | `discrepancy_decisions` | `apps/api/src/procurement` | `workspaceId`, `discrepancyFlagId`, `comparisonRunId` (nullable), `actorUserId`, `actorRole` (captured at decision time), `outcome` (`false_positive`\|`approved_exception`\|`vendor_dispute`\|`resolved`), `note` (required), `createdAt` | **Append-only**: no code path updates or deletes a row. A wrong call is corrected by recording another decision, so the trail stays readable. Every outcome closes the flag (`status='dismissed'`) — the nuance lives in `outcome`, so `discrepancy_flag_status` stays two-state and needs no `ALTER TYPE`. `actorRole` is stored rather than looked up later, because memberships change and the record should not. | insert via `comparison.service.ts#recordDecision` and `#dismissFlag`; read via `#listDecisions` | Decision insert and the flag's status change share one transaction. `dismissFlag` only writes a decision when it actually transitions an open flag, so a re-dismiss appends nothing. | `POST`/`GET .../discrepancies/:flagId/decisions`, `PATCH .../dismiss` | Deep | Added 2026-09-20 (S2), migration `0023_zippy_micromacro.sql`. POLICY v1 #7. |
| Procurement / Discrepancy | `comparison_runs` | `apps/api/src/procurement` | `workspaceId`, `purchaseOrderId`, `invoiceId`, `mode` (varchar, default `'two_way'`), `strategyVersion` (default 1), `status` (`queued`\|`running`\|`succeeded`\|`failed`), `initiatedBy`, `poLineCount`/`invoiceLineCount`/`flagCount`, `startedAt`/`finishedAt`, `lastError` | One row per comparison attempt, append-only — never updated except to close it (`succeeded`/`failed`) and never deleted. "Current" for a PO/invoice pair means its latest **succeeded** run. `mode` is varchar not an enum so S6's `three_way` needs no migration; `status` carries an unused `'queued'`. **2026-09-23 (S9 commit 5), migration `0033`:** `contract_term_count`, nullable integer — how many agreed prices the run had to judge against. Null when the PO has no vendor or the vendor has no term for any of its items, which is NOT the same as zero, and zero is not "we did not look". A count rather than a join table (unlike `comparison_run_goods_receipts`) because terms are workspace rows, not documents, so POLICY v1 #9's retention guard has nothing to protect — and each flag already names the exact term it used. **No new `mode` value** despite the plan naming `contract-price`: `mode` says which document *sides* were read, and a contract-checked run is still two-way or three-way. **2026-09-23 (S8 commit 3):** `workspace_event_type` gained `comparison_flagged` and `comparison_failed` (migration `0028`, values only, `IF NOT EXISTS` hand-added — verified on a fresh database and on one already at `0027`). `comparison_flagged`, not `comparison_completed`: the event fires only when a run found something, and a name promising every completion would be false. Written fire-and-forget with `entity_id` = the run id, the same split `scrape_completed` uses. **2026-09-23 (S8):** it is still unused, deliberately — automation writes no row until the job actually starts, so there is no queued state to record, and adding one would have meant a lifecycle migration (`queue_job_id`/`enqueued_at`) to carry information the sweep already recovers from `started_at`. `strategy_version` stopped being a DB default with no writer: `compare()` now writes `COMPARISON_STRATEGY_VERSION` explicitly, and S8's freshness check requires a match, so bumping it recompares every pair instead of trusting a verdict the current rules would no longer produce. `started_at` is the freshness anchor — **not** `finished_at`, because a run that started before a re-parse and finished after it read the old lines. | insert + close via `comparison.service.ts#compare`; read via `#listFlags` and `procurement-documents.service.ts#remove`'s guard | The run row is written **before** the DuckDB call so a failed attempt still leaves evidence; flag insert + run close share one transaction behind a `FOR UPDATE` lock on the PO row, which now orders runs rather than protecting a delete | `POST .../discrepancies/compare`, `GET .../discrepancies?runId=` | Deep | Added 2026-09-20 (S1), migration `0022_previous_wendell_vaughn.sql`. A document referenced by a run cannot be hard-deleted (POLICY v1 #9). |
| Vendor Catalog / Price | `vendor_price_terms` | `apps/api/src/catalog` | `workspaceId`, `vendorId` (NOT NULL cascade), `sku` (as written) + `skuKey` (lower-cased, what matching uses), `uom` (nullable — null is "unstated", never a unit), `unitPrice` (`numeric`, NOT NULL), `currency` (NOT NULL), `effectiveFrom` (NOT NULL) / `effectiveTo` (nullable = open-ended), `sourceReference` (free text), `supersedesId` (self FK, set null), `createdBy`, timestamps | Half-open windows `[from, to)` throughout — the strict upper bound is what leaves exactly one live term at the instant one supersedes another. `currency` is NOT NULL unlike `purchase_orders.currency`: that column had to tolerate pre-0025 rows, this table is new and has none. No `price_type`, no quantity tiers, no tax/freight flags — POLICY v1 #6 puts them out of v1 comparison, so they would have no writer (the rule S5 set refusing `extraction_confidence` on receipts). `skuKey` NOT NULL means a PO line keyed only by description is **never** contract-checked | insert + supersede via `vendor-price-terms.service.ts#create`, in one transaction; seeded by `scripts/seed/data/price-terms.ts` | Append-only: never edited except the closing `effective_to`, never deleted. **"At most one open term" is enforced in the service, not by an index** — drizzle-kit 0.20 accepts `.where()` on a unique index and then drops it, emitting a FULL unique index that would reject exactly the superseded rows this table exists to keep | `POST`/`GET .../vendors/:vendorId/price-terms` | Deep | Added 2026-09-23 (S9 commit 2), migration `0030_chilly_mastermind.sql`. One table, two indexes, four FKs, no enum. Nothing reads it yet — the same way S1 shipped `comparison_runs` before it had a reader. |
| Procurement / Discrepancy | `discrepancy_flags` | `apps/api/src/procurement` | **2026-09-23 (S9 commit 5), migration `0033`:** `contract_unit_price` (nullable `numeric`) and `contract_term_id` (nullable FK → `vendor_price_terms`, `set null`) — the agreed price a line was judged against and which term said so, filled only by the two `contract_price_*` types. Their own columns rather than a reuse of `invoice_value`, because no invoice is involved in a contract finding and a column named for one holding a contract price would be a lie a later reader has to disprove. `contract_unit_price` is null on an `unavailable` finding: the point of that type is that the system will not name a price. **2026-09-23 (S9 commit 3), migration `0031`:** `discrepancy_flag_type` gains `contract_price_variance` and `contract_price_unavailable` — values only, `IF NOT EXISTS` hand-added, **no writer in that migration or that commit**, verified on a fresh database and on one already at `0030`. Both are about the AGREED price and never about the invoice: `price_mismatch` keeps exactly its prior meaning and POLICY v1 #5 is untouched. **2026-09-23 (S9 commit 1), migration `0029`:** `po_unit_price` + `invoice_unit_price`, both nullable `numeric`, filled on **every** flag that has a line behind it — not only on `price_mismatch`. They are separate columns rather than a reuse of `po_value`/`invoice_value` because those are `text` and already carry units on a `uom_mismatch` row and currency codes on a `currency_mismatch` row; nothing can be aggregated over a column that sometimes holds `"EA"`. Null means the side stated no single price (no line, or its own duplicate lines disagree) — never a price the system chose. | `comparisonRunId` (nullable — see note), `workspaceId`, `purchaseOrderId`, `invoiceId`, `poLineItemId`/`invoiceLineItemId` (nullable — null on the missing side), `sku`, `flagType` (`quantity_mismatch`\|`price_mismatch`\|`missing_on_invoice`\|`missing_on_po`), `poValue`/`invoiceValue`/`delta`, `reason`, `status` (`open`\|`dismissed`), audit fields (`dismissedAt`, `dismissedBy`) | N-per-(PO,invoice) with provenance, modeled on `document_review_flags` — a comparison run can surface several independent mismatches; dismissal is an audited per-flag action, never a boolean cleared elsewhere | insert (bulk) + delete via `comparison.service.ts#compare` (deletes that PO/invoice pair's prior flags before inserting fresh ones); update via `comparison.service.ts#dismissFlag` | **2026-09-20 (S1): the delete is gone.** A re-compare appends a new run and inserts its flags; prior flags are retained, so a dismissal is never destroyed (B1). Duplication is prevented at read time instead — `listFlags` defaults to the latest succeeded run per pair. `comparisonRunId` is nullable because every flag written before S1 predates the table; those count as current for their pair until it is compared again, which is why no data migration was needed. `set null` rather than cascade: a flag losing its run degrades to legacy, it does not vanish. Additive table, rollback = drop table + 2 enums | `POST .../discrepancies/compare`, `GET .../discrepancies`, `PATCH .../discrepancies/:flagId/dismiss` | Deep | Added 2026-07-09, migration `0020_bumpy_energizer.sql`. Comparison SQL is a fixed hand-written template (never LLM-generated, never built from row data) executed via `DuckDbQueryService.runReadOnlyMultiTableQuery()` — see `risk-register.md`'s new note for the full safety-boundary detail. |
| Vendor Catalog | `vendors` | `apps/api/src/catalog` | `workspaceId`, `name`, `contactInfo` (nullable) | each row belongs to one workspace; referenced by `catalogs`/`catalog_matches` via cascade FK | insert via `vendors.service.ts#create`; read via `#list` | additive table, no vector column, rollback = drop table | `POST/GET .../vendors` | Deep | Added 2026-07-10, migration `0021_dear_speed_demon.sql`. First-class vendor entity — new domain, no prior concept existed. |
| Vendor Catalog | `catalogs` | `apps/api/src/catalog` | `workspaceId`, `vendorId`, `name`, `sourceKind` (default `'upload'`, or `'scrape'`), `storageKey`/`seedUrl` (nullable, one per source kind), lifecycle fields (`status`, `queueJobId`, `enqueuedAt`, `processingStartedAt`, `rowCount`, `lastError`), scrape-only progress (`pagesFound`/`pagesSucceeded`/`pagesFailed`/`lastProgressAt`, nullable — only the scrape path writes these) | each row belongs to one workspace + one vendor; parse/scrape worker is only writer for `status -> processing\|done\|failed`; same 4-state `catalog_doc_status` enum shared by both `upload` and `scrape` source kinds | insert + enqueue via `catalog-documents.service.ts#upload` or `catalog-scrape.service.ts#startScrape`; status transitions via `catalog-parse.processor.ts#handleParse` or `catalog-scrape.processor.ts#handleScrape` | queue enqueue failure marks row terminal `failed`; deterministic job ids `catalog-parse:{id}` / `catalog-scrape:{id}`; re-parse deletes existing `catalog_items` before inserting fresh ones (idempotent on retry); additive table + 1 enum, rollback = drop table + enum | `/workspaces/:workspaceId/vendors/:vendorId/catalogs[/scrape]`, Bull `catalog-parse-queue`, Bull `catalog-scrape-queue` | Deep | Added 2026-07-10, migration `0021_dear_speed_demon.sql`. Schema designed up front for both source kinds — scrape progress columns exist even though Phase 1 (schema) predates Phase 5 (scrape implementation). |
| Vendor Catalog | `catalog_items` | `apps/api/src/catalog` | `workspaceId` (denormalized, not derived via a `catalogs` join — same isolation shape as `chunks`/`poLineItems`), `catalogId`, `lineNumber`/`sku`/`description`/`photoStorageKey`/`sourcePageNumber` (all nullable), `rawRow` (jsonb, nullable) | every item belongs to exactly one catalog and (denormalized) one workspace; `photoStorageKey` is the S3 key of either the PDF page image (page-granularity, shared across every item on that page) or a downloaded scrape/CSV `photo_url` image — never a live external URL | delete-then-insert (bulk) via `catalog-parse.processor.ts#handleParse` (upload path) or per-image insert via `catalog-scrape.processor.ts#handleScrape` (scrape path) | re-parse for the same catalog is idempotent — prior items deleted before the fresh set is inserted; additive table, rollback = drop table | `GET .../catalogs/:catalogId/items` | Deep | Added 2026-07-10, migration `0021_dear_speed_demon.sql`. First table in this app to persist a reference to an actual stored image (not just text) — see `risk-register.md`'s A3 note. |
| Vendor Catalog | `catalog_matches` | `apps/api/src/catalog` | `workspaceId`, `matchType` (`sourcing`\|`compliance`), `queryPoLineItemId`/`queryInvoiceLineItemId` (nullable, FK `set null` — exactly one populated per row), `catalogItemId` (FK cascade), `vendorId` (denormalized, FK cascade — enables per-vendor filtering without a join), `score` (nullable, 0..1), `isMatch` (boolean, not null), `reason` (not null), `status` (`open`\|`dismissed`), audit fields (`dismissedAt`, `dismissedBy`) | one query (PO or invoice line item) + candidate (catalog item) pair per row; `matchType` is derived, not independently settable — `'compliance'` iff the search request supplied a `vendorId`, else `'sourcing'` | insert (bulk) + delete via `catalog-match.service.ts#search` (deletes this query's prior matches before inserting the fresh set); update via `#dismissMatch` | re-search for the same query line item is idempotent — prior matches for that exact query are deleted before the fresh set is inserted; additive table + 2 enums, rollback = drop table + 2 enums | `POST .../catalog-matches/search`, `POST .../vendors/:vendorId/catalog-matches/verify`, `GET .../catalog-matches`, `PATCH .../catalog-matches/:matchId/dismiss` | Deep | Added 2026-07-10, migration `0021_dear_speed_demon.sql`. Match mechanism is a vision-LLM comparator (`compareLineItemToCatalogImage`), not embedding similarity — no vector column, no new embedding dimension. |
