# Repository Map

Purpose:

Dense file ledger.

This file is map only.

It is not proof of behavior.

Verify all conclusions against real source code.

If this map conflicts with source code, source code wins.

Mark stale or conflicting entries as `CONTEXT DRIFT`.

---

## File Index

TODO: Fill after repository analysis. Do not treat as verified. (Auth rows below are verified; everything else still TODO.)

| Path | Purpose | Domain | Risk | Notes |
| ---- | ------- | ------ | ---- | ----- |
| `apps/api/src/auth/auth.service.ts` | Core auth logic: register/verifyOtp/login/refresh/logout/changePassword, email normalization, bcrypt hashing, JWT + refresh token issuance | Auth | Deep | Tested in `auth.service.spec.ts`; `changePassword()` added 2026-07-04 |
| `apps/api/src/auth/auth.controller.ts` | HTTP routes for `/auth/*`, sets/clears the `mnemra_rt` httpOnly cookie | Auth | Deep | Tested in `test/auth.e2e-spec.ts` and `test/change-password.e2e-spec.ts`; `POST /auth/change-password` added 2026-07-04 |
| `apps/api/src/auth/dto/change-password.dto.ts` | Validation DTO for `POST /auth/change-password` | Auth | Express | `newPassword` reuses `RegisterDto`'s `@MinLength(8)` rule; added 2026-07-04 |
| `apps/web/app/api/auth/change-password/route.ts` | Same-origin proxy for change-password, Bearer-authenticated via `proxyJson()` (mirrors `/auth/me`'s pattern, not the cookie-forwarding pattern used by login/register/refresh) | Auth Frontend | Standard | Added 2026-07-04 |
| `apps/web/src/lib/api/auth.ts` | Client API lib for register/verifyOtp/login/refresh/logout/getCurrentUser/changePassword | Auth Frontend | Standard | Tested in `auth.spec.ts`; `changePassword()` added 2026-07-04 |
| `apps/web/app/(auth)/login/page.tsx` | Login form page | Auth Frontend | Standard | Uses shared `BrandMark` for the Mnemra brand link as of 2026-07-05; login behavior unchanged |
| `apps/web/app/(auth)/register/page.tsx` | Register form page | Auth Frontend | Standard | Uses shared `BrandMark` for the Mnemra brand link as of 2026-07-05; registration behavior unchanged |
| `apps/web/app/(auth)/verify-otp/page.tsx` | OTP verification form page | Auth Frontend | Standard | Uses shared `BrandMark` for the Mnemra brand link as of 2026-07-05; OTP behavior unchanged |
| `apps/api/src/auth/decorators/current-user.decorator.ts` | Param decorator that reads `req.user` as `{userId, email}` for authenticated handlers | Auth / Workspaces | Express | Used by `workspaces.controller.ts` |
| `apps/api/src/auth/guards/workspace-member.guard.ts` | Per-request check: is this user a member of the workspace in the route param; attaches `{workspaceId, role}` to the request | Auth / Workspaces | Deep | Tested in `workspace-member.guard.spec.ts` |
| `apps/api/src/auth/guards/roles.guard.ts` | RBAC guard that enforces `@Roles(...)` against `req.workspaceMember.role` after membership lookup | Auth / Workspaces | Deep | Tested in `roles.guard.spec.ts`; must run after `WorkspaceMemberGuard` |
| `apps/api/src/auth/decorators/roles.decorator.ts` | Metadata decorator for workspace RBAC (`owner`, `admin`, `member`) | Auth / Workspaces | Express | Consumed by `RolesGuard` |
| `apps/api/src/auth/decorators/current-workspace-member.decorator.ts` | Param decorator that reads `req.workspaceMember` for controller handlers | Auth / Workspaces | Express | No tests needed — trivial passthrough |
| `apps/api/src/workspaces/workspaces.controller.ts` | HTTP routes for create/list/get/invite/accept/remove/rename workspace flows | Workspaces | Deep | Guard order matters: `JwtAuthGuard`, `WorkspaceMemberGuard`, then `RolesGuard` where applicable. `PATCH :workspaceId` (rename, added 2026-07-04) is owner/admin, classified Standard for that route specifically — see `risk-register.md` |
| `apps/api/src/workspaces/workspaces.service.ts` | Workspace creation, member listing, invite issuance/acceptance, and last-owner enforcement | Workspaces | Deep | Tested in `workspaces.service.spec.ts` |
| `apps/api/src/workspaces/workspaces.module.ts` | Wires workspaces controller/service plus auth/notification guard dependencies | Workspaces | Standard | Imported by `app.module.ts` |
| `apps/api/src/workspaces/dto/create-workspace.dto.ts` | Validation DTO for `POST /workspaces` | Workspaces | Express | `name` length 1..255 |
| `apps/api/src/workspaces/dto/update-workspace.dto.ts` | Validation DTO for `PATCH /workspaces/:workspaceId` (rename) | Workspaces | Express | `name` length 1..255, same shape as `CreateWorkspaceDto`; added 2026-07-04 |
| `apps/api/src/workspaces/dto/invite-member.dto.ts` | Validation DTO for `POST /workspaces/:workspaceId/invite` | Workspaces | Express | Validates invitee email |
| `apps/api/src/knowledge-bases/knowledge-bases.controller.ts` | Nested workspace routes for create/list/delete knowledge bases | Knowledge Bases | Standard | Reuses `JwtAuthGuard`, `WorkspaceMemberGuard`, `RolesGuard` |
| `apps/api/src/knowledge-bases/knowledge-bases.service.ts` | KB create/list/delete logic, including non-empty delete guard via `documents` count | Knowledge Bases | Standard | Tested in `knowledge-bases.service.spec.ts` |
| `apps/api/src/knowledge-bases/knowledge-bases.module.ts` | Wires KB controller/service and auth guard dependencies | Knowledge Bases | Standard | Imported by `app.module.ts` |
| `apps/api/src/knowledge-bases/dto/create-knowledge-base.dto.ts` | Validation DTO for `POST /workspaces/:workspaceId/knowledge-bases` | Knowledge Bases | Express | `name` length 1..255 |
| `apps/api/src/cache/cache.module.ts` | Wires singleton Redis client + `CacheService` for chat answer caching | Chat / RAG | Standard | Uses `REDIS_HOST` / `REDIS_PORT` via `ConfigService`; imported by chat/documents/ingest modules |
| `apps/api/src/cache/cache.service.ts` | Workspace-scoped answer cache: exact Redis keys, semantic `chat_cache` lookup, version invalidation, fail-soft behavior | Chat / RAG | Deep | Exact key = `chat:ans:${workspaceId}:${version}:sha256(normalizedMessage)`; semantic hits require score >= env threshold |
| `apps/api/src/limits/limits.module.ts` | Wires Redis-backed chat rate-limit and usage-budget services/guard | Chat / RAG | Standard | Imports `CacheModule` to reuse exported `REDIS_CLIENT` |
| `apps/api/src/limits/rate-limit.service.ts` | Fixed-window per-minute Redis counters for per-user and per-workspace chat POST limits, plus a per-user daily refine cap (`checkRefineDaily`) and its read-only counterpart (`getRefineStatus`, added 2026-07-04) | Chat / RAG | Deep | Fail-open on Redis errors; throws `429` only when counters exceed configured caps; `getRefineStatus` never `incr`s so status polling cannot itself consume quota |
| `apps/api/src/limits/usage.service.ts` | Monthly Redis token budget tracker for miss-path workspace generations | Chat / RAG | Deep | Fail-open on Redis errors; throws `402` only when monthly counter reaches configured cap |
| `apps/api/src/limits/chat-rate-limit.guard.ts` | Nest guard that enforces chat rate limits from `req.user.userId` + `req.params.workspaceId` | Chat / RAG | Deep | Applied after auth + workspace membership guards on chat POST |
| `apps/api/src/documents/documents.controller.ts` | Nested workspace+KB routes for multipart upload, offset list, single download, bulk zip download, single delete, and selected-documents delete flows | Documents | Deep | Uses `FileInterceptor('file')` with server-side size limit + supported-type allowlist and maps oversized uploads to `413`; member download endpoints stream `getBuffer()` bytes or zip via `archiver`; selected delete is owner/admin only |
| `apps/api/src/documents/documents.service.ts` | Saves uploaded bytes to object storage, inserts pending docs, queues ingest, scopes offset list/download/delete to workspace+KB, invalidates chat cache on delete | Documents | Deep | Tested in `documents.service.spec.ts`; list sorts `updatedAt DESC, id DESC` with title search + status filter; `removeMany()` skips stale/foreign ids and bumps cache once when rows are deleted |
| `apps/api/src/documents/documents.module.ts` | Wires documents controller/service with auth guards, storage, and ingest dependencies | Documents | Standard | Imported by `app.module.ts` |
| `apps/api/src/documents/dto/delete-many.dto.ts` | Validation DTO for selected-documents delete | Documents | Standard | Requires non-empty UUID v4 array, max 200 ids |
| `apps/api/src/chat/chat.controller.ts` | Workspace-scoped streaming chat endpoint that writes plain-text answer tokens and emits sources via `X-Chat-Sources` | Chat / RAG | Deep | Guarded by `JwtAuthGuard` + `WorkspaceMemberGuard` + `ChatRateLimitGuard`; persists/cache-writes before request completes |
| `apps/api/src/chat/chat.service.ts` | Chat session/message persistence plus answer-cache orchestration for workspace-scoped streamed answers | Chat / RAG | Deep | Creates/reuses owned sessions, inserts user/assistant turns, checks exact then semantic cache, enforces miss-path workspace budget, and stores assistant sources/history identically for hits and misses. 2026-07-05: new private `getRecentHistory()` fetches bounded recent turns (excludes the just-inserted current-turn row by id) before any cache/embed/classify/retrieval step; when history exists, `condenseQuestion()` resolves the follow-up into a standalone question used for cache keys, `embedQuery()`, and `answerQuestion()`'s `question` arg (raw `message` is still persisted as-is for display) — skipped (zero cost) on a session's first turn or when `HISTORY_CONDENSE_ENABLED=false`; gated overall by `historyCondenseEnabled()`/`historyInAnswerEnabled()` so both flags off reproduces prior behavior exactly. |
| `apps/api/src/chat/chat.module.ts` | Wires chat controller/service and imports auth + cache + limits dependencies | Chat / RAG | Standard | Imported by `app.module.ts` |
| `apps/api/src/chat/dto/chat.dto.ts` | Validation DTO for `POST /workspaces/:workspaceId/chat` | Chat / RAG | Express | `message` must be non-empty string max 4000; `sessionId` reserved for S2 |
| `apps/api/src/refine/refine.controller.ts` | HTTP routes for refine, refine status, save/list saved refined messages, all nested under `:workspaceId/refine` | Chat / RAG | Standard | Guarded by `JwtAuthGuard` + `WorkspaceMemberGuard`; refine POST additionally guarded by `RefineRateLimitGuard`; maps `RefineEmptyError`/`RefineRefusalError` to `422` |
| `apps/api/src/refine/refine.service.ts` | Calls `@repo/ai#refineMessage`, persists/reads `saved_refined_messages` scoped to workspace+user | Chat / RAG | Standard | Tested in `refine.service.spec.ts` (added 2026-07-04) |
| `apps/api/src/refine/refine.module.ts` | Wires refine controller/service, imports `AuthModule` + `LimitsModule` | Chat / RAG | Express | Imported by `app.module.ts` |
| `apps/api/src/refine/refine.rate-limit.guard.ts` | Nest guard enforcing `RateLimitService.checkRefineDaily` from `req.user.userId` | Chat / RAG | Standard | Applied only to the refine POST route, mirrors `chat-rate-limit.guard.ts`; covered via `refine.e2e-spec.ts`, no dedicated guard spec (matches house convention) |
| `apps/api/src/refine/dto/refine.dto.ts` | Validation DTO for `POST /workspaces/:workspaceId/refine` | Chat / RAG | Express | `text` non-empty, max 4000 |
| `apps/api/src/refine/dto/save-refined-message.dto.ts` | Validation DTO for `POST /workspaces/:workspaceId/refine/saved` | Chat / RAG | Express | `originalText`/`refinedText` non-empty, max 4000 each |
| `apps/api/src/ingest/ingest.processor.ts` | Bull worker for `ingest-queue`: document load, chunk, embed, sync, status transitions, temp cleanup, chat-cache invalidation on success | Documents / Ingestion | Deep | Lazy-imports `@repo/ai` so non-ingest app startup paths do not eagerly load document parsers |
| `apps/api/src/ingest/ingest.service.ts` | Enqueues document ingest jobs with retry/backoff/remove-on-complete policy, clears stale processing markers on requeue, and reconciles missing jobs on startup | Documents / Ingestion | Standard | Retry-safe because `syncChunks()` is content-hash diff based |
| `apps/api/src/ingest/ingest.module.ts` | Registers ingest Bull queue and exposes queueing + processor providers | Documents / Ingestion | Standard | Imports `StorageModule` for worker temp-file fetches |
| `apps/api/src/scrape/scrape.service.ts` | Creates scrape runs, derives default crawl scope, enforces workspace document quota, rejects blocked seed URLs, enqueues crawl jobs, and offset-lists runs | Web Sources / Scraping | Deep | KB must belong to route workspace; owner/admin only via controller guards; list defaults to `pageSize=5`, searches `seedUrl`, filters `status`, sorts `createdAt DESC, id DESC` |
| `apps/api/src/scrape/scrape.processor.ts` | Bull worker for `scrape-queue`: stream crawl pages, save page text, upsert `documents`, enqueue ingest per page, and persist live run counters/status | Web Sources / Scraping | Deep | Lazy-loads crawler submodule so app bootstrap avoids crawler dependency fan-out in tests |
| `apps/api/src/scrape/scrape.controller.ts` | Nested workspace+KB routes for start-crawl and offset list-runs endpoints | Web Sources / Scraping | Standard | `POST /scrape` = owner/admin, `GET /scrape-runs` = member |
| `apps/api/src/scrape/scrape.module.ts` | Wires scrape controller/service/processor with Bull queue, auth guards, storage, and ingest | Web Sources / Scraping | Standard | Registers `scrape-queue` |
| `apps/api/src/scrape/dto/scrape.dto.ts` | Validation DTO for crawl requests | Web Sources / Scraping | Express | Validates URL, depth 0..5, pages 1..2000 |
| `apps/api/src/scrape/dto/list-scrape-runs-query.dto.ts` | Validation DTO for scrape-run offset pagination, search, and status filtering | Web Sources / Scraping | Express | Extends `OffsetQueryDto`; status must be `queued`, `running`, `completed`, or `failed` |
| `apps/api/src/storage/storage.service.ts` | S3-compatible object storage adapter: ensure bucket, save object bytes, read object bytes, download to temp file, delete by key | Storage | Deep | Tested in `storage.service.spec.ts`; SeaweedFS-backed locally; `getBuffer()` backs document downloads |
| `apps/api/src/storage/storage.module.ts` | Exports `StorageService` and boots bucket creation on API startup | Storage | Standard | Imported by `app.module.ts` in Slice 3A |
| `apps/api/src/cache/cache.service.spec.ts` | Unit coverage for exact cache versioning, workspace isolation, and semantic thresholding | Chat / RAG | Standard | Mocks Redis + DB query seam |
| `apps/api/src/documents/documents.service.spec.ts` | Real-DB unit coverage for upload/list/download/single-delete/bulk-delete document service behavior | Documents | Standard | Mocks `StorageService`, `IngestService`, and cache invalidation |
| `apps/api/src/documents/documents.controller.spec.ts` | Controller-level coverage for document download headers, bulk zip streaming, and bulk delete delegation | Documents | Standard | Uses direct controller instance with mocked service |
| `apps/api/src/ingest/ingest.processor.spec.ts` | Unit coverage for status transitions, metadata injection, sync call shape, and temp cleanup | Documents / Ingestion | Standard | Mocks `@repo/ai` and `StorageService` |
| `apps/api/src/chat/chat.service.spec.ts` | Unit coverage for exact-hit, semantic-hit, miss-to-cache, single-embed chat behavior plus miss-path budget/usage accounting | Chat / RAG | Standard | Mocks `@repo/ai` + `CacheService` + `UsageService`. 2026-07-05: added coverage for history-driven condensation (cache/embed/generation use the condensed question, prior turns passed through, usage accounting includes condensed-question tokens only when condensing changed the text) and the both-flags-disabled regression guard. |
| `apps/api/src/limits/rate-limit.service.spec.ts` | Unit coverage for per-user/per-workspace rate-limit counters, minute buckets, and fail-open behavior | Chat / RAG | Standard | Pure Redis mock seam |
| `apps/api/src/limits/usage.service.spec.ts` | Unit coverage for monthly token budget keys, cap enforcement, and fail-open behavior | Chat / RAG | Standard | Pure Redis mock seam |
| `apps/api/test/chat.e2e-spec.ts` | End-to-end RBAC + streaming + session/message history + cache-hit/invalidation + rate-limit coverage for workspace chat endpoints | Chat / RAG | Deep | Mocks `@repo/ai` so no live OpenAI call. 2026-07-05: first test extended with a same-session follow-up asserting the second `answerQuestion()` call receives the first turn as history while the first call's history is empty — added inline to the existing test (not a new `it()`) to avoid tipping the file's cumulative `/auth/register` calls over the endpoint's 5-per-window throttle, which every existing test in this file already sits at exactly. |
| `apps/api/test/documents.e2e-spec.ts` | End-to-end RBAC/storage verification for document upload/list/download/delete plus upload limit/type rejection | Documents | Deep | Overrides `IngestService` and `StorageService` so test stays independent of live OpenAI embeddings/S3; covers member bulk-delete denial, invalid id validation, and best-effort owner bulk delete |
| `apps/api/tsconfig.json` | API compile/runtime tsconfig for Nest + SWC | Platform Build | Deep | `@repo/*` source-path aliases removed 2026-07-01 so runtime resolves built workspace packages instead of broken `packages/*/src` paths |
| `apps/api/test/jest-e2e.json` | API e2e Jest config | Platform Build | Standard | Maps `@repo/*` to package `dist` and transpiles ESM-only `p-limit`/`yocto-queue` for app-module boot under Jest |
| `apps/api/src/notifications/notifications.service.ts` | Sends OTP and invite email via Resend, or console-logs in dev when `EMAIL_OTP_ENABLED!=='true'` | Auth / Workspaces | Standard | Covered by `auth.service.spec.ts`, `workspaces.service.spec.ts`, `test/workspaces.e2e-spec.ts` |
| `apps/web/src/lib/http/auth-proxy.ts` | Shared Next proxy helper that converts `mnemra_at` cookie into backend `Authorization: Bearer` and handles JSON/multipart/raw passthrough | Web API Proxy | Deep | Returns 401 when access-token cookie is missing; never exposes token to client JS; `proxyRaw()` preserves download streams/headers |
| `apps/web/src/lib/api/handle-unauthorized.ts` | Small client helper that normalizes 401 detection from parsed proxy errors | Web API Client | Express | Covered by `handle-unauthorized.spec.ts` and reused by workspace/document pages |
| `apps/web/app/api/workspaces/route.ts` | Same-origin proxy for list-my-workspaces and create-workspace web calls | Workspaces Frontend | Standard | Covered by `app/api/workspaces/route.spec.ts` |
| `apps/web/app/api/workspaces/[id]/route.ts` | Same-origin proxy for get-workspace and rename-workspace | Workspaces Frontend | Standard | `PATCH` export added 2026-07-04; covered by `route.spec.ts` |
| `apps/web/app/api/workspaces/[id]/invite/route.ts` | Same-origin proxy for invite-member | Workspaces Frontend | Standard | — |
| `apps/web/app/api/workspaces/[id]/members/[userId]/route.ts` | Same-origin proxy for remove-member | Workspaces Frontend | Standard | — |
| `apps/web/app/api/invitations/accept/[token]/route.ts` | Same-origin proxy for accept-invite | Workspaces Frontend | Standard | — |
| `apps/web/app/api/workspaces/[id]/knowledge-bases/route.ts` | Same-origin proxy for list/create knowledge bases | Knowledge Bases Frontend | Standard | — |
| `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/route.ts` | Same-origin proxy for delete knowledge base | Knowledge Bases Frontend | Standard | — |
| `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/route.ts` | Same-origin proxy for offset-list/upload documents, including multipart passthrough | Documents Frontend | Deep | Covered by `documents/route.spec.ts` for multipart behavior and query passthrough |
| `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/scrape/route.ts` | Same-origin proxy for starting a scrape run | Web Sources Frontend | Standard | Covered by `scrape/route.spec.ts`; forwards JSON body + Bearer |
| `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/scrape-runs/route.ts` | Same-origin proxy for listing scrape runs | Web Sources Frontend | Standard | Covered by `scrape-runs/route.spec.ts` |
| `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/[docId]/route.ts` | Same-origin proxy for delete document | Documents Frontend | Standard | — |
| `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/[docId]/download/route.ts` | Same-origin raw proxy for single document download | Documents Frontend | Standard | Preserves backend download stream and `Content-Disposition`; covered by route spec |
| `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/download/route.ts` | Same-origin raw proxy for selected-documents zip download | Documents Frontend | Standard | Preserves backend zip stream and `Content-Disposition`; covered by route spec |
| `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/delete/route.ts` | Same-origin JSON proxy for selected-documents delete | Documents Frontend | Standard | Forwards `{documentIds}` with Bearer auth and returns `{deleted, skipped}`; covered by route spec |
| `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/delete/route.spec.ts` | Vitest coverage for selected-documents delete same-origin proxy | Documents Frontend | Standard | Asserts Bearer auth, JSON body forwarding, and deletion-count response |
| `apps/web/app/api/workspaces/[id]/chat/route.ts` | Same-origin streaming proxy that converts AI SDK `messages` payload into backend `{message, sessionId?}` and passes text stream/headers through | Chat Frontend | Deep | Node runtime; forwards `X-Chat-Sources` + `X-Chat-Session-Id` |
| `apps/web/app/api/workspaces/[id]/chat/sessions/route.ts` | Same-origin proxy for listing owned chat sessions in a workspace | Chat Frontend | Standard | Reuses `proxyJson()` |
| `apps/web/app/api/workspaces/[id]/chat/sessions/[sessionId]/messages/route.ts` | Same-origin proxy for loading owned session history + persisted sources | Chat Frontend | Standard | Reuses `proxyJson()` |
| `apps/web/app/api/workspaces/[id]/refine/route.ts` | Same-origin proxy for the refine POST | Chat Frontend | Standard | Reuses `proxyJson()`; added 2026-07-04 |
| `apps/web/app/api/workspaces/[id]/refine/status/route.ts` | Same-origin proxy for refine remaining-count GET | Chat Frontend | Express | Reuses `proxyJson()`; added 2026-07-04 |
| `apps/web/app/api/workspaces/[id]/refine/saved/route.ts` | Same-origin proxy for save/list saved refined messages | Chat Frontend | Standard | Reuses `proxyJson()`; added 2026-07-04 |
| `apps/web/src/lib/api/client.ts` | Shared browser-side fetch helpers for JSON and file upload against same-origin web proxies | Web API Client | Standard | Reused by auth/workspaces/KB/documents libs |
| `apps/web/src/lib/api/chat.ts` | Client API lib for chat session/message history endpoints | Chat Frontend | Standard | Streaming POST still handled by `useChat` directly |
| `apps/web/src/lib/api/refine.ts` | Client API lib for refine + refine status + saved-refined-message endpoints | Chat Frontend | Standard | Tested in `refine.spec.ts`; added 2026-07-04 |
| `apps/web/src/lib/api/workspaces.ts` | Client API lib for workspace flows | Workspaces Frontend | Standard | Calls `/api/workspaces*` proxies only; `updateWorkspace()` added 2026-07-04 |
| `apps/web/src/lib/api/knowledge-bases.ts` | Client API lib for knowledge-base flows | Knowledge Bases Frontend | Standard | Calls `/api/workspaces/:id/knowledge-bases*` proxies |
| `apps/web/src/lib/api/documents.ts` | Client API lib for document offset list/upload/delete/download flows | Documents Frontend | Standard | Uses `uploadFile()` for multipart; `downloadDocument()`/`downloadDocuments()` save browser blobs from raw proxies; `deleteDocuments()` posts selected ids to the bulk-delete proxy |
| `apps/web/src/lib/api/scrape.ts` | Client API lib for crawl-start and offset run-list flows | Web Sources Frontend | Standard | Calls same-origin scrape proxies only; serializes `page`, `pageSize`, `q`, and `status` |
| `apps/web/src/components/workspace-search.tsx` | Workspace-scoped `⌘K` search modal with grouped document/ticket/chat results and autofocus-on-open behavior | Search Frontend | Standard | Uses shared `Modal`; keeps query input focused while result rerenders occur |
| `apps/web/src/components/brand-mark.tsx` | Shared folded-page Mnemra brand mark component | Marketing / Shared Frontend | Tiny | Renders `/mnemra-mark.svg`; decorative mode is used inside already-labelled links |
| `apps/web/src/components/brand-mark.spec.tsx` | Unit coverage for shared brand mark rendering | Marketing / Shared Frontend | Tiny | Asserts the SVG path and decorative alt/aria behavior |
| `apps/web/public/mnemra-mark.svg` | Source SVG for the 19A folded-page Mnemra mark | Marketing / Brand Assets | Tiny | Used by `BrandMark`; `brand-images.spec.ts` asserts title and `data-mark` |
| `apps/web/app/page.tsx` | Public marketing landing page | Marketing / Landing | Tiny | Header logo uses shared `BrandMark` as of 2026-07-05; final CTA still uses a decorative Sparkles icon, not the logo |
| `apps/web/app/loading.tsx` | Root route loading skeleton | Marketing / Shared Frontend | Tiny | Header brand uses shared `BrandMark`; covered by `loading.spec.tsx` |
| `apps/web/app/chat/loading.tsx` | Chat route loading skeleton | Chat Frontend / Brand Assets | Tiny | Header brand uses shared `BrandMark`; covered by `chat/loading.spec.tsx` |
| `apps/web/app/icon.png` | Static 32x32 favicon image generated from the folded-page mark | Marketing / Brand Assets | Tiny | Tested by `brand-images.spec.ts`; referenced by `manifest.ts` |
| `apps/web/app/favicon.ico` | Static ICO favicon fallback generated from the folded-page mark | Marketing / Brand Assets | Tiny | Tested by `brand-images.spec.ts`; contains multiple sizes |
| `apps/web/app/apple-icon.png` | Static 180x180 Apple touch icon generated from the folded-page mark | Marketing / Brand Assets | Tiny | Tested by `brand-images.spec.ts` |
| `apps/web/app/opengraph-image.png` | Static 1200x630 share card using the folded-page mark | Marketing / Brand Assets | Tiny | Tested by `brand-images.spec.ts`; visually reviewed after regeneration |
| `apps/web/app/workspaces/page.tsx` | Client page for listing accessible workspaces and creating a new workspace through a modal form | Workspaces Frontend | Standard | Redirects to `/login` on proxy 401; uses `Table`, `Modal`, `useToast` |
| `apps/web/app/workspaces/[id]/page.tsx` | Client page for workspace detail, invite form, and knowledge-base create/delete management | Workspaces Frontend | Standard | Derives caller role from `listWorkspaces()` to hide owner/admin controls when known |
| `apps/web/app/workspaces/[id]/settings/page.tsx` | Client settings page: editable workspace-name rename form (role-gated, owner/admin editable, member disabled/no save button) plus a change-password form (current/new/confirm, client-side match+length validation, logs out and redirects to `/login` on success, inline error on wrong current password) | Workspaces Frontend | Standard (rename) / Deep (password) | Replaced the "more settings coming soon" placeholder 2026-07-04; derives role the same way as `workspaces/[id]/page.tsx`; change-password added 2026-07-04 after explicit Deep approval |
| `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx` | Client page for drag/drop document upload, document/crawl search-filter-pagination, crawl-run management, downloads, live queue summaries, status polling, and delete confirmation within a knowledge base | Documents Frontend / Web Sources | Deep | Polls docs while ingest active and scrape runs while crawl active; document order now comes from backend `updatedAt DESC`; owner/admin can start crawls via modal. Slice 1 (2026-07-04) added drag/drop upload, document row selection, single/bulk download, document search/status filter/pagination, and crawl search/status filter/pagination (default 5). 2026-07-04 bulk delete adds an owner/admin-only Delete selected action with confirmation, best-effort result toast, selection clearing, and document reload. 2026-07-03: scrape modal autofocuses URL input, crawl rows map `queued`/`running` to `In progress`, and page counts are labeled (`Found`, `Queued`, `Page errors`) instead of bare numbers. |
| `apps/web/app/workspaces/[id]/chat/page.tsx` | Client workspace chat page with streamed answers, source panel, history sidebar, session reloads, and mixed document/ticket citation rendering | Chat Frontend | Deep | Uses `useChat` against workspace proxy and reloads persisted message history after each answer; legacy persisted sources with no `sourceType` still render as documents. 2026-07-03: message bodies render safe Markdown (`react-markdown` + `remark-gfm`) and rows span full width while inner content stays capped for readability. 2026-07-04: added "Refine with Mnemra" (replaces textarea content in place, "Undo refine" restores original), "Save refined message" (separate deliberate action tied to the last refine result, cleared on hand-edit), a remaining-refines-today badge (polls `GET .../refine/status`), and a "Saved messages" modal cloned from the existing message-templates modal pattern. |
| `apps/web/app/chat/page.tsx` | Legacy `/chat` redirect page that resolves first workspace then forwards user to `/workspaces/:id/chat` | Chat Frontend | Standard | Keeps old links working after workspace-scoped chat launch |
| `packages/db/src/schema/chunks.ts` | Drizzle schema for shared vector chunks spanning document-backed and ticket-backed embeddings with exactly-one-parent enforcement | Documents / Tickets / Chat / RAG | Deep | `document_id` is now nullable, `ticket_id` is nullable+unique, and CHECK `chunks_exactly_one_parent_check` enforces exactly one parent per chunk row |
| `packages/db/src/schema/tickets.ts` | Drizzle schema for workspace-scoped transcript ticket drafts, extraction lifecycle, feedback, and review audit fields | Tickets | Deep | Added in migrations `0007_sudden_the_phantom.sql` + `0008_clumsy_pepper_potts.sql`; unique `(workspace_id, transcript_hash)` closes dedup races |
| `apps/api/src/tickets/tickets.service.ts` | Ticket create/list/get/update service plus queue enqueue, stale-job reconciliation, dedup by transcript hash, projected detail reads, review-confidence merging, and review-save vector sync | Tickets | Deep | Uses Bull queue `ticket-extraction-queue`; any workspace member can create/review; caught `syncTicketChunk()` failures never fail PATCH |
| `apps/api/src/tickets/ticket-extraction.processor.ts` | Bull worker that runs transcript extraction and writes extracted fields / terminal failure state | Tickets | Deep | Imports `extractTicketFromTranscript` from `@repo/ai`; job payload is `{ticketId}` only; completion write is guarded to avoid clobbering reviewed rows |
| `apps/api/src/tickets/tickets.controller.ts` | Workspace-scoped ticket HTTP routes for create/list/get/update | Tickets | Deep | `POST` returns `202` for new extraction and `200` for exact dedup hit |
| `apps/api/src/tickets/tickets.module.ts` | Wires ticket controller/service/processor with Bull queue and auth guards | Tickets | Standard | Registers `ticket-extraction-queue` |
| `apps/api/src/tickets/dto/{create-ticket,update-ticket}.ts` | Validation DTOs for transcript create and review-save payloads | Tickets | Express | `transcript` max 50k chars; review fields are partial |
| `apps/api/src/tickets/tickets.service.spec.ts` | Real-DB unit coverage for ticket create/dedup/failure/update behavior | Tickets | Standard | Mocks Bull queue only |
| `apps/api/src/tickets/ticket-extraction.processor.spec.ts` | Unit coverage for extraction success/failure status transitions | Tickets | Standard | Mocks `@repo/ai` extraction chain |
| `apps/api/test/tickets.e2e-spec.ts` | End-to-end workspace ticket flow and IDOR coverage | Tickets | Deep | Covers `403` non-member and `404` cross-workspace ticket id |
| `packages/ai/src/chains/ticket-extraction.ts` | Direct transcript-to-ticket extraction chain using `ChatOpenAI`, strict JSON parsing, timeout retry, and typed extraction errors | Tickets | Deep | Separate from chat LangGraph path by design |
| `packages/ai/src/chains/ticket-extraction.spec.ts` | Vitest coverage for extraction happy path, empty/refusal/parse/timeout/injection cases | Tickets | Standard | No live OpenAI call |
| `packages/ai/src/vectorstore/index.ts` | Shared vector-store write/read helpers for document chunks, ticket chunks, similarity search, and one-off ticket embedding backfill | Tickets / Chat / RAG | Deep | `syncTicketChunk()` embeds one chunk per qualifying reviewed useful resolved ticket; `backfillTicketEmbeddings()` replays whole table safely. 2026-07-03: search fns accept optional `precomputedEmbedding` (no double embed) + `RetrievalFilters` (`buildFilterSql` → indexed WHERE); write paths populate `source_type`/`doc_type`/`product_area` columns |
| `packages/ai/src/vectorstore/index.spec.ts` | Real-DB ticket-embedding coverage for `syncTicketChunk()`, backfill tallying, and chunks parent CHECK enforcement | Tickets / Chat / RAG | Standard | Mocks embeddings only; exercises live Postgres constraint |
| `apps/web/src/lib/api/tickets.ts` | Browser-side client for ticket queue/detail/review endpoints | Tickets Frontend | Standard | Calls same-origin ticket proxies only |
| `apps/web/app/api/workspaces/[id]/tickets/route.ts` | Same-origin proxy for ticket list/create | Tickets Frontend | Standard | Reuses `proxyJson()` |
| `apps/web/app/api/workspaces/[id]/tickets/[ticketId]/route.ts` | Same-origin proxy for ticket detail/update | Tickets Frontend | Standard | Reuses `proxyJson()` |
| `apps/web/app/workspaces/[id]/tickets/page.tsx` | Client workspace ticket-copilot page for transcript intake, polling, review editing, failure banner, and copy-to-clipboard handoff | Tickets Frontend | Deep | Polls every 3 seconds while extraction in flight |
| `apps/web/app/workspaces/[id]/tickets/page.spec.ts` | jsdom coverage for ticket polling, review save, copy, and failure banner | Tickets Frontend | Standard | Mocks ticket client API + clipboard |
| `apps/web/app/api/workspaces/[id]/tickets/**/*.spec.ts` | Vitest coverage for ticket same-origin proxy auth and passthrough | Tickets Frontend | Standard | Collection + detail routes |
| `scripts/eval/extraction-eval-dataset.json` | Seed dataset for transcript-extraction accuracy checks | Tickets | Standard | Includes actionable and non-actionable transcripts |
| `scripts/eval/evaluate_extraction.py` | Manual/live field-accuracy evaluator for extraction chain | Tickets | Standard | Requires `OPENAI_API_KEY`; compares predicted fields against labeled rows |
| `scripts/eval/test_extraction_dataset_schema.py` | Offline schema/import test for extraction eval harness | Tickets | Express | No network required |
| `apps/web/app/invite/[token]/page.tsx` | Client page that accepts a workspace invite token and redirects into the joined workspace | Workspaces Frontend | Standard | Surfaces backend invite error messages directly |
| `apps/web/app/workspaces/page.spec.ts` | jsdom coverage for workspace list/create flows and unauthorized redirect | Workspaces Frontend | Standard | Mocks `next/navigation` and workspace client API |
| `apps/web/app/workspaces/[id]/page.spec.ts` | jsdom coverage for workspace detail invite/create/delete knowledge-base flows | Workspaces Frontend | Standard | Mocks workspace and knowledge-base client APIs |
| `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.spec.ts` | jsdom coverage for document list/upload/drop/download/poll/delete flows plus crawl-status and scrape-modal regressions | Documents Frontend | Standard | Uses fake timers to verify polling cleanup; covers document/crawl offset pagination, search/filter controls, selected download/delete controls, scrape autofocus/focus retention, and `In progress` + labeled crawl counts |
| `apps/web/app/api/workspaces/[id]/chat/route.spec.ts` | Vitest coverage for streaming workspace chat proxy header/body passthrough and auth enforcement | Chat Frontend | Standard | Validates AI SDK payload normalization |
| `apps/web/app/api/workspaces/[id]/chat/sessions/route.spec.ts` | Vitest coverage for chat session proxy GET path | Chat Frontend | Express | — |
| `apps/web/app/api/workspaces/[id]/chat/sessions/[sessionId]/messages/route.spec.ts` | Vitest coverage for chat message-history proxy GET path | Chat Frontend | Express | — |
| `apps/web/app/workspaces/[id]/chat/page.spec.ts` | jsdom coverage for chat history loading, source rendering, and Markdown/full-width message rendering | Chat Frontend | Standard | Mocks `useChat` and chat history APIs |
| `apps/web/app/chat/page.spec.ts` | jsdom coverage for `/chat` redirect behavior | Chat Frontend | Express | Redirects to first workspace chat |
| `packages/ai/src/chains/index.ts` | Workspace-scoped RAG answer generation entrypoint; straight-line path by default, conditional LangGraph path behind env flag | Chat / RAG | Deep | `answerQuestion()` now hydrates both document and ticket citations from shared chunk retrieval; `askQuestion()` remains back-compat wrapper. 2026-07-03: routes simple vs complex (`classifyQuery`, `SIMPLE_QUERY_CHUNK_LIMIT`) — simple queries take the light path (fewer chunks, no rewrite/grade) even when graph is on; accepts + threads `precomputedEmbedding` and `RetrievalFilters`; context via `buildEvidencePack()`; answer model via `resolveModel('answer')`. 2026-07-05: `answerQuestion()` gains an optional trailing `history: HistoryTurn[] = []` param, gated once via `historyInAnswerEnabled()` and threaded into the light-path prompt (`toMessages(effectiveHistory)` spliced before the final Context+Question `HumanMessage`) and into the `answerQuestionWithGraph()` call. |
| `packages/ai/src/chains/graph.ts` | Conditional LangGraph miss-path orchestration: retrieve, score-route, rewrite loop, fallback, optional self-grade/regenerate | Chat / RAG | Deep | Uses retrieval score threshold + rewrite cap; feature flag defaults off; shares mixed document/ticket citation hydration with straight path. 2026-07-03: confident answers stream via `prepareStream`→`streamAnswer` (no buffering); self-grade gated by `SELF_GRADE_MIN_SCORE`; state splits `originalQuestion`/`retrievalQuery`; reuses `precomputedEmbedding` on first retrieval; per-role LLMs (`resolveModel`). 2026-07-05: `GraphState` gains a write-once `history` field; `collectAnswer()`/`streamAnswer()` gain an optional `history` param spliced into the prompt; `generateNode`/`regenerateNode`/the confident-stream branch pass `state.history` through — `retrieveNode`/`rewriteNode`/`gradeAnswerNode` deliberately do not receive it. `answerQuestionWithGraph()` gains the same optional trailing `history` param as `answerQuestion()`, seeded once at graph invocation. |
| `packages/ai/src/chains/models.ts` | Per-task model resolver: `OPENAI_ANSWER/REWRITE/GRADE/EXTRACTION/CONDENSE_MODEL` → `OPENAI_CHAT_MODEL` → `gpt-4-turbo` | Chat / RAG | Standard | Added 2026-07-03; unit-tested in `models.spec.ts`. `'condense'` role added 2026-07-05 for history-aware follow-up resolution — kept separate from `'rewrite'` since the two solve different problems (coreference resolution vs. retrieval-vocabulary recall). |
| `packages/ai/src/chains/condense.ts` | `condenseQuestion(question, history)` — resolves a history-dependent follow-up into a standalone question via a cheap model; skips the LLM call entirely when history is empty or `HISTORY_CONDENSE_ENABLED=false` | Chat / RAG | Standard | Added 2026-07-05, mirrors `refine.ts`'s single-purpose-chain shape. Barrel-exported from `packages/ai/src/index.ts`. |
| `packages/ai/src/chains/condense.spec.ts` | Vitest coverage for skip-when-no-history, skip-when-flag-off, condense happy path, empty-output fallback, and history bounding before prompt construction | Chat / RAG | Standard | Added 2026-07-05 |
| `packages/ai/src/chains/history.ts` | `HistoryTurn` type, `boundHistory()` (token-budget trim, oldest-first drop), `toMessages()` (role → `HumanMessage`/`AIMessage`), and the two rollout-flag readers (`historyCondenseEnabled`/`historyInAnswerEnabled`) plus `historyMaxMessages`/`historyTokenBudget` | Chat / RAG | Standard | Added 2026-07-05, mirrors `context.ts`'s token-budget pattern. Barrel-exported from `packages/ai/src/index.ts`. |
| `packages/ai/src/chains/history.spec.ts` | Vitest coverage for `boundHistory()` trimming, `toMessages()` role mapping, and flag/env default+override behavior | Chat / RAG | Standard | Added 2026-07-05 |
| `packages/ai/src/chains/context.ts` | `buildEvidencePack()` — ranked, token-budgeted compact context (`RAG_CONTEXT_TOKEN_BUDGET`), replaces raw-chunk join | Chat / RAG | Standard | Added 2026-07-03; unit-tested in `context.spec.ts`; reuses `tokens.ts#countTokens` |
| `packages/ai/src/chains/classify.ts` | `classifyQuery()` heuristic simple/complex router for query routing | Chat / RAG | Standard | Added 2026-07-03; unit-tested in `classify.spec.ts`; LLM-free |
| `packages/ai/src/vectorstore/embedding-reuse.spec.ts` | Unit coverage that retrieval reuses a `precomputedEmbedding` and does not re-embed | Chat / RAG | Standard | Added 2026-07-03; mocks `@repo/db` + embeddings |
| `packages/ai/src/chains/index.spec.ts` | Unit coverage for deduped source mapping and empty-retrieval fallback in straight-line chat chain | Chat / RAG | Standard | Mocks vector search, DB lookup, and LLM stream; covers mixed document/ticket citations. 2026-07-05: added history-in-prompt coverage (present when `HISTORY_IN_ANSWER_ENABLED=true`, absent when `false`) and a fallback-with-history regression case. |
| `packages/ai/src/chains/graph.spec.ts` | Unit coverage for LangGraph routing: direct generate, rewrite retry, fallback, and self-grade regenerate | Chat / RAG | Standard | Mocks vector search, DB lookup, and LLM calls; covers ticket citation hydration. 2026-07-05: added history-threading coverage for all three generation call sites (confident-stream, generate, regenerate) plus a fallback-with-history regression case. |
| `packages/ai/src/loaders/pdf.ts` | PDF loader used by ingest/document parsing | Documents / Ingestion | Standard | Switched 2026-07-01 from internal `pdf-parse/lib/pdf-parse.js` require to exported `PDFParse` API; lazy-imports `pdf-parse` inside `loadPDF()` as of 2026-07-04 so API boot does not load pdfjs/DOMMatrix code unless a PDF is ingested |
| `packages/ai/src/loaders/pdf.spec.ts` | Unit coverage for PDF loader parser integration seam | Documents / Ingestion | Standard | Mocks `pdf-parse` + `fs/promises`; asserts extracted text/page count mapping and no `pdf-parse` import at module import time |
| `packages/ai/src/tokens.ts` | Token-count helper using `tiktoken` `cl100k_base` for workspace usage estimation | Chat / RAG | Express | Used by API usage accounting only |
| `packages/ai/src/tokens.spec.ts` | Unit coverage for token counting + encoder cleanup | Chat / RAG | Express | Mocks `tiktoken` |
| `scripts/eval/requirements.txt` | Python deps for offline RAGAS evaluation harness | Chat / RAG | Standard | Not loaded by Node runtime |
| `scripts/eval/eval-dataset.json` | Seed offline RAGAS dataset rows with `question/answer/contexts/ground_truth` schema | Chat / RAG | Standard | Expand from templates toward real support cases |
| `scripts/eval/evaluate.py` | Offline weekly RAGAS runner that prints four metrics, lowest metric, and writes timestamped JSON | Chat / RAG | Standard | Requires Python deps + `OPENAI_API_KEY` |
| `scripts/eval/capture_from_langsmith.py` | Optional helper to export recent LangSmith runs into mergeable dataset rows | Chat / RAG | Standard | Emits blank `ground_truth` for manual fill-in |
| `scripts/eval/README.md` | Runbook for offline eval harness setup, cadence, and metric interpretation | Chat / RAG | Express | Documents "fix lowest metric first" loop |
| `scripts/eval/test_dataset_schema.py` | Offline schema/import test for eval dataset + metric list | Chat / RAG | Express | No network required |
| `scripts/backfill-ticket-embeddings.ts` | One-off manual script that replays `backfillTicketEmbeddings()` across all workspaces after deploy | Tickets / Chat / RAG | Deep | Run only after schema + app code deploy; safe to rerun because sync is idempotent |
| `packages/db/src/schema/chatSessions.ts` | Drizzle schema for `chat_sessions`, `chat_messages`, and `chat_message_role` enum | Chat / RAG | Deep | Added in migration `0003_shallow_iron_patriot.sql`; assistant `sources` stored as jsonb. 2026-07-05: `chat_messages` gained a composite index on `(session_id, created_at)` (migration `0013_condemned_silver_surfer.sql`) since history-aware chat now reads this shape on every chat turn, not just the paginated history-panel endpoint. |
| `packages/db/src/schema/chatCache.ts` | Drizzle schema for semantic answer cache rows keyed by workspace + version | Chat / RAG | Deep | Added in migration `0004_exotic_roughhouse.sql`; stores question embedding, answer, and sources |
| `packages/db/src/schema/savedRefinedMessages.ts` | Drizzle schema for personal (workspace+user scoped) saved refined-message drafts | Chat / RAG | Standard | Added migration `0012_little_blink.sql` (2026-07-04); both FKs `ON DELETE CASCADE` |
| `packages/ai/src/chains/refine.ts` | `refineMessage(rawText)` caveman-compression chain; `RefineEmptyError`/`RefineRefusalError` | Chat / RAG | Standard | Tested in `refine.spec.ts`; barrel-exported from `packages/ai/src/index.ts` as of 2026-07-04 (closed a real gap — it existed but wasn't reachable via `@repo/ai`) |
| `packages/types/src/index.ts` | Shared legacy app types; chat-adjacent document/chunk fields now use `workspaceId` naming | Shared Types | Express | S1 removed remaining `tenantId` fields from live type surface |
| `packages/ai/src/web/ssrf.ts` | Shared SSRF guard for scrape seeds and crawler fetch targets using hostname + IP + DNS-resolution checks | Web Sources / Scraping | Deep | Blocks localhost/internal hostnames, private/link-local/loopback IPs, metadata IP, and DNS rebinding to private addresses |
| `packages/ai/src/web/ssrf.spec.ts` | Vitest coverage for SSRF guard hostname/IP/rebinding rules | Web Sources / Scraping | Standard | No live DNS/network |
| `packages/ai/src/web/crawl.ts` | Production crawler core: canonicalize URLs, enforce same-origin/path scope, block SSRF targets, honor robots, extract main article text, bounded BFS crawl, and stream accepted-page progress callbacks | Web Sources / Scraping | Deep | Tested with injected fetch + lookup mocks only; exports helpers for scope/content/link tests; lazy-imports ESM-only `p-limit` inside `crawlSite()` so `@repo/ai` CommonJS package import does not fail at API boot |
| `packages/ai/src/web/crawl.spec.ts` | Vitest coverage for crawler canonicalization, scope, depth/page caps, robots, SSRF blocking, UA, readability extraction, resilience, and live progress callbacks | Web Sources / Scraping | Standard | No live network or DNS |
| `packages/db/src/schema/scrapeRuns.ts` | Drizzle schema for `scrape_runs` and `scrape_run_status` | Web Sources / Scraping | Deep | Added with migration `0002_ordinary_punisher.sql`; `last_progress_at` added in `0006_needy_harrier.sql` |
| `apps/web/app/invite/[token]/page.spec.ts` | jsdom coverage for invite acceptance redirect flow | Workspaces Frontend | Standard | Mocks `acceptInvite()` and router push |
| `apps/web/src/lib/api/handle-unauthorized.spec.ts` | Unit coverage for normalized 401 detection helper | Web API Client | Express | Verifies `statusCode` and `message` paths |
| `apps/web/src/lib/ui/modal.spec.ts` | jsdom render test for `Modal` open/close behavior and rerender focus retention | UI | Standard | Uses `@testing-library/react` |
| `packages/ui/src/components/ui/modal.tsx` | Minimal accessible dialog primitive with overlay click, Escape close, and stable focus across rerenders | UI | Standard | Exported from `@repo/ui` |
| `packages/ui/src/components/ui/select.tsx` | Styled native `<select>` primitive matching input styles | UI | Express | Exported from `@repo/ui` |
| `packages/ui/src/components/ui/textarea.tsx` | Styled `<textarea>` primitive matching input styles | UI | Express | Exported from `@repo/ui` |
| `packages/ui/src/components/ui/table.tsx` | Thin table wrapper primitives for headers/body/rows/cells | UI | Express | Exported from `@repo/ui` |
| `docker/seaweedfs/s3.json` | Local SeaweedFS S3 identity file with dev-only credentials matching `.env.example` | Storage Infra | Deep | Never reuse for production secrets |
| `packages/db/src/db/index.ts` | Drizzle client + exported `pg.Pool` (`pool` exported specifically so tests can close the connection cleanly) | DB infra | Standard | — |
| `.env.example` | Single env template for local Docker defaults plus production-required values | Infrastructure / Deployment | Standard | Local Docker host web/API defaults are `MNEMRA_WEB_PORT=3100` and `MNEMRA_API_PORT=3101` so Mnemra can run beside Suki's `3000`/`3001`; production still sets `DOMAIN`, `POSTGRES_PASSWORD`, and secrets in a gitignored `.env`; bundled prod Caddy remains disabled unless `.env` opts into `COMPOSE_PROFILES=public` |
| `package.json` | Root scripts for build/test/type-check plus Docker and deploy entrypoints | Infrastructure / Deployment | Standard | `docker:prod:up` uses `--remove-orphans` so renamed prod services do not leave stale containers behind |
| `docker-compose.yml` | Full local dev stack: postgres/redis/seaweedfs + api/web (`target: dev`, bind-mounted, hot reload) | Infrastructure / Deployment | Deep | `docker compose up` is now the single local dev command; top-level `name: mnemra` pins the Compose project name regardless of the on-disk folder name; api/web have real dev healthchecks and web waits for api healthy; host web/API ports default to `3100`/`3101` to avoid Suki's `3000`/`3001`; API service overrides `S3_*` to match local `docker/seaweedfs/s3.json` even when `.env` contains production S3 keys |
| `docker-compose.prod.yml` | Full prod stack: postgres/redis/seaweedfs/api/web plus opt-in Caddy, no bind mounts, healthchecked | Infrastructure / Deployment | Deep | `env_file: .env` is scoped to `api`/`web` only; infra services receive explicit Compose-interpolated env only; prod API builds `prod`, prod web builds final `runner`; app/internal service ports are not host-published; Caddy is behind profile `public` so shared VPS hosts can deploy without binding `80`/`443` |
| `docker/Caddyfile` | Optional Caddy public reverse-proxy and security-header config | Infrastructure / Deployment | Deep | Used only with `COMPOSE_PROFILES=public`; public traffic goes to `web:3000`; Next.js owns same-origin `/api/*` proxy route handlers and forwards server-side to `api:3001`, avoiding a broken `/api` prefix pass-through to Nest |
| `apps/api/Dockerfile` | 4-stage build (`base`/`deps`/`dev`/`prod`) for the NestJS API | Infrastructure / Deployment | Deep | Installs real Node in the Bun Alpine base so Node-shebang CLIs (`nest`, `swc`, `tsc`) do not hang under Bun's shim; filtered Bun install includes only root + `@repo/api` + `@repo/ai` + `@repo/db`; install/build stages use locked app-specific BuildKit cache mounts; dev target delegates package rebuild + `db:migrate` to `docker/api-dev-entrypoint.sh`; prod `HEALTHCHECK` hits `/health` |
| `apps/web/Dockerfile` | 5-stage build (`base`/`deps`/`dev`/`prod`/`runner`) for the Next.js web app | Infrastructure / Deployment | Deep | Installs real Node for Next/Turbo CLIs and runs the standalone server with `node server.js` from the copied Next standalone root; filtered Bun install includes only root + `@repo/web` + `@repo/ui`; runner also copies that filtered `node_modules` because Bun workspace layout does not leave `next` inside `.next/standalone`; install/build stages use locked app-specific BuildKit cache mounts; dev target delegates stale dependency-volume repair and startup to `docker/web-dev-entrypoint.sh`; final runnable image tolerates apps with no `public/` directory |
| `docker/api-dev-entrypoint.sh` | Local API dev entrypoint for bind-mounted Docker runtime | Infrastructure / Deployment | Deep | Repairs stale `api_node_modules` volume with filtered `bun install` when required deps are missing or manifest/lockfile inputs are newer than `.mnemra-api-deps.stamp`; rebuilds `@repo/db`/`@repo/ai` only when `dist/index.js` is missing or older than source, then runs `db:migrate` before `bun run dev` |
| `docker/web-dev-entrypoint.sh` | Local Web dev entrypoint for bind-mounted Docker runtime | Infrastructure / Deployment | Deep | Repairs stale `web_node_modules` volume with filtered `bun install` when required deps are missing or manifest/lockfile inputs are newer than `.mnemra-web-deps.stamp`, then starts Next dev |
| `apps/api/src/health/health.controller.ts` | `GET /health` liveness endpoint, zero dependencies | Infrastructure / Deployment | Express | Tested in `health.controller.spec.ts`; consumed by Docker `HEALTHCHECK` and the CI health-poll steps; deliberately not DB/Redis-backed so it never false-fails during a startup race |
| `.github/workflows/deploy.yml` | Push-to-`main` auto-deploy: SSH to VPS, backup Postgres, build+up prod stack, internal healthcheck-poll, S3 round-trip, optional public smoke | Infrastructure / Deployment | Deep | Requires `VPS_HOST`/`VPS_USER`/`VPS_SSH_KEY`/`VPS_PORT` GitHub Secrets; reads `DOMAIN`/`COMPOSE_PROFILES` from `.env` on the VPS without shell-sourcing secrets; backs up from postgres container env; runs `scripts/ensure-seaweedfs-s3-config.sh` after `git pull`; removes stale `mnemra-prod-caddy` unless `COMPOSE_PROFILES=public`; starts prod with `--force-recreate`; api/web healthchecks and S3 round-trip run via `docker compose exec -T` because app ports are not host-published |
| `docker/seaweedfs/s3.prod.json.example` | Committed placeholder template for prod S3 credentials | Infrastructure / Deployment | Standard | Template only; `scripts/ensure-seaweedfs-s3-config.sh` creates or replaces the gitignored real file from non-placeholder `.env` S3 credentials |
| `scripts/ensure-seaweedfs-s3-config.sh` | Rewrites gitignored prod SeaweedFS identity JSON from `.env` S3 credentials | Infrastructure / Deployment | Deep | Used by GitHub Actions and manual deploy so deploys do not fail silently on a missing/stale `docker/seaweedfs/s3.prod.json`; refuses empty or placeholder credentials; writes mode `0644` so the SeaweedFS uid inside the container can read the bind-mounted file across Docker Desktop and Linux hosts |
| `scripts/deploy.sh` | Manual on-VPS deploy script (cache-preserving app build + up against `docker-compose.prod.yml`) | Infrastructure / Deployment | Deep | Uses `docker compose build api web` without `--no-cache`, relies on API startup `db:migrate`, removes stale `mnemra-prod-caddy` unless `COMPOSE_PROFILES=public`, starts with `--remove-orphans --force-recreate`, then checks API health, Web health, and an S3 round-trip from inside the `api` container; kept alongside GitHub Actions as a manual fallback path |
| `scripts/deploy-remote.sh` | Manual local-machine-to-VPS deploy (rsync + SSH + invoke `deploy.sh`) | Infrastructure / Deployment | Deep | Kept alongside GitHub Actions as a manual fallback path |
