# Module Ownership Map

Purpose:

Map product/business domains to implementation areas.

This file is map only.

It is not proof of behavior.

Verify all conclusions against real source code, tests, types, schemas, routes, controllers, services, stores, components, API contracts, database definitions.

If this map conflicts with source code, source code wins.

Mark stale or conflicting entries as `CONTEXT DRIFT`.

Mark missing domains as `UNMAPPED DOMAIN`.

---

## Domain Index

| Domain                | Frontend                                                        | Backend                                                         | Database / Schema                                               | Jobs / Automations                                              | Integrations                                                    | Tests                                                           | Risk     | Notes                                                                                          |
| --------------------- | --------------------------------------------------------------- | --------------------------------------------------------------- | --------------------------------------------------------------- | --------------------------------------------------------------- | --------------------------------------------------------------- | --------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------- |
| Billing Requests      | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Deep     | TODO: Fill after repository analysis. Do not treat as verified.                                |
| Payments              | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Deep     | TODO: Fill after repository analysis. Do not treat as verified.                                |
| SMS Credits           | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Deep     | TODO: Fill after repository analysis. Do not treat as verified.                                |
| Plan Upgrades         | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Deep     | TODO: Fill after repository analysis. Do not treat as verified.                                |
| Marketing / Landing   | `apps/web/app/page.tsx`, `apps/web/app/loading.tsx`, `apps/web/app/layout.tsx` (global metadata), `apps/web/app/robots.ts`, `apps/web/app/sitemap.ts`, `apps/web/app/manifest.ts`, `apps/web/app/icon.png`, `apps/web/app/apple-icon.png`, `apps/web/app/opengraph-image.png` | none | none | none | none | `apps/web/app/page.spec.ts`, `apps/web/app/layout.spec.ts`, `apps/web/app/robots.spec.ts`, `apps/web/app/sitemap.spec.ts`, `apps/web/app/manifest.spec.ts`, `apps/web/app/brand-images.spec.ts` | Tiny | Public unauthenticated marketing page, no data fetching. Copy realigned 2026-07-02 to speak to a support-team buyer instead of describing the UI/design process (design-audit finding). CTAs point to `/workspaces`, not the retired `/dashboard`. Copy rewritten again 2026-07-04 to lead with support-team pain points (scattered knowledge, onboarding time, knowledge loss on attrition, senior-agent interruptions, answer consistency) instead of feature descriptions; added a Personal/Team workspace positioning section (pure copy — `workspace_members` already supports 1..N members, no new capability). Fixed `page.tsx:135` raw `rgba()` gradient literal to use Tailwind's `bg-gradient-to-br`/`via-white/40` token-based utilities (closes DESIGN.md cleanup item). SEO hardening (2026-07-05): fixed `layout.tsx`'s `metadataBase` (was pointing at the dead `mnemra.com`, corrected to real deployed `https://mnemra.tyvera.app` via `WEB_URL` env with hardcoded fallback) and rewrote description/OG/Twitter copy to match the actual hero value prop instead of generic boilerplate; added `robots.ts` (allows `/`, disallows `/chat` `/workspaces` `/invite` `/api`), `sitemap.ts` (lists the marketing homepage), `manifest.ts`, and `Organization`+`SoftwareApplication` JSON-LD on `page.tsx`. Favicon/apple-touch-icon/OG-image are **static PNGs** at `app/{icon,apple-icon,opengraph-image}.png`, not dynamic `next/og` `ImageResponse` routes — a real bug was found where this repo's Node runtime (v25) throws `ERR_MODULE_NOT_FOUND` resolving `next/dist/compiled/@vercel/og/index.node.js` through Next 14.2.35's live metadata-image route pipeline specifically (confirmed `ImageResponse` itself works fine when imported directly, e.g. in a plain script or Vitest — only Next's internal dynamic `import()` for that route type breaks). Images were pre-rendered once via a throwaway script and committed as static files, sidestepping the buggy runtime path entirely; revisit only if a future Next/Node upgrade needs per-request dynamic OG images. `apps/web/app/chat/layout.tsx` and `apps/web/app/workspaces/layout.tsx` (new, `{robots: {index: false, follow: false}}`) added as noindex backstops on top of the existing `middleware.ts` auth-redirect gating for those routes — see Auth/Permissions and Workspaces rows for the routes themselves, listed here only because the noindex metadata is SEO-motivated. |
| Auth / Permissions    | `apps/web/app/(auth)/*`, `app/api/auth/*` (now with `route.spec.ts` per route, Vitest), `middleware.ts`, `src/lib/auth.ts`, `src/lib/api/auth.ts` | `apps/api/src/auth/*` (controller/service/strategies/guards/decorators), `src/notifications/*`, global `ThrottlerGuard` via `APP_GUARD` in `app.module.ts` | `users`, `otps`, `refresh_tokens`, `workspaces`, `workspace_members` (`packages/db/src/schema/`) | none yet | Resend (email OTP/invite; console-log fallback when `EMAIL_OTP_ENABLED!=='true'`; send failures throw + log as of 2026-07-05, see `notifications.service.spec.ts`) | `auth.service.spec.ts`, `notifications.service.spec.ts`, `roles.guard.spec.ts`, `workspace-member.guard.spec.ts`, `test/auth.e2e-spec.ts`, `test/auth-rate-limit.e2e-spec.ts`, 6× `apps/web/app/api/auth/*/route.spec.ts` (Vitest, incl. new `me/route.spec.ts`), `apps/web/src/lib/auth.spec.ts`, `apps/web/src/lib/api/auth.spec.ts`, `apps/web/app/(auth)/login/page.spec.ts`, `apps/web/app/(auth)/verify-otp/page.spec.ts` | Deep     | Verified source + automated as of 2026-07-01. JWT carries `{sub, email}` only — workspace scoping is checked per-request via `WorkspaceMemberGuard`, never baked into token. `verifyOtp()` now auto-creates a personal workspace + owner membership inside one transaction before issuing tokens. Security fix 2026-07-01: `apps/web/src/lib/auth.ts` used to mirror the raw JWT into `sessionStorage` (same key as the httpOnly `mnemra_at` cookie) — JS-readable, undermining the cookie's XSS protection. Replaced with a non-sensitive boolean flag (`markLoggedIn`/`isLoggedIn`/`clearLoggedIn`, key `mnemra_session_active`) for the dashboard's bfcache recheck, and a new `GET /auth/me` endpoint (`auth.controller.ts#getMe`, `JwtAuthGuard` + `@CurrentUser()`, no service/DB logic) for any "who am I" need client-side — first consumer is the workspace members roster's self-removal check. No raw token is persisted client-side anywhere anymore. Post-auth redirects: `/dashboard` stays retired. 2026-07-05: login (`login/page.tsx:36`) and verify-otp (`verify-otp/page.tsx:40`) now push `/chat` (not `/workspaces` directly) — `/chat` (`apps/web/app/chat/page.tsx`, unchanged) auto-routes to `/workspaces/:id/chat` for the user's first workspace, falling back to `/workspaces` if they have none. 2026-07-08: fixed intermittent "stuck on /login after successful login/OTP-verify" bug — root cause was a stale Next.js client Router Cache entry for `/chat` created when a logged-out visitor's browser prefetched one of the marketing page's `<Link href="/chat">` links, hit `middleware.ts`'s no-`mnemra_rt` redirect-to-`/login`, and cached that result; a subsequent real login's soft `router.push('/chat')` could then serve that stale cached redirect instead of re-checking middleware with the freshly-set cookies. Fixed by adding `router.refresh()` immediately before `router.push('/chat')` in both `login/page.tsx` and `verify-otp/page.tsx` to invalidate the Router Cache first. See `risk-register.md`'s "Post-Login Redirect Target" row for the full RCA. Follow-up fixed the same day: `apps/web/src/lib/api/client.ts`'s `apiFetch`/`uploadFile` now retry once through `POST /api/auth/refresh` on a 401 (excluding the unauthenticated auth endpoints and the refresh endpoint itself), sharing one in-flight refresh promise across concurrent 401s — previously a client-side data fetch from an already-open page had no refresh path at all (only page navigation via `middleware.ts` got one), so an expired 15-minute `mnemra_at` cookie caused a silent full logout even with a valid 7-day `mnemra_rt`. New `client.spec.ts` added (file had zero prior coverage). See `risk-register.md`'s "Client-Side Session Refresh" row. Invite-accept (`invite/[token]/page.tsx`) intentionally still pushes `/workspaces/:id/chat` directly since it already knows the destination workspace — see `risk-register.md`'s "Post-Login Redirect Target" row for the downgrade rationale. Auth pages (login/register/verify-otp) restyled 2026-07-02 with `@repo/ui` components (`Input`/`Button`/`Card`/`PageShell`/`StatusBanner`) instead of raw HTML — zero logic change, identical validation/redirect/error-handling behavior. 2026-07-04: added `POST /auth/change-password` (Deep, explicit approval required and given), surfaced via `apps/web/app/workspaces/[id]/settings/page.tsx` — see Settings/Onboarding row. Reuses the exact refresh-token theft-cascade revocation pattern from `refresh()`'s reuse-detection branch. Fixed 2026-07-05: `NotificationsService.sendOtp`/`sendInvite` used to discard the Resend SDK's `{data, error}` return value — `resend@6.16.0` never rejects on API-level failures, so a failed send was indistinguishable from success (no exception, no log, register() still returned `201`). Now checks `error` and throws `InternalServerErrorException` (logged via `Logger.error`). `apps/api/test/jest-e2e.setup.ts` forces `EMAIL_OTP_ENABLED=false` for all e2e specs (dotenv never overrides a pre-set `process.env` value) so the suite uses the console-log fallback instead of depending on a live Resend account. See `risk-register.md` for the still-open operational risk (the configured `tyvera.com` sending domain is confirmed live-unverified in Resend as of 2026-07-05). |
| Workspaces            | `apps/web/app/workspaces/page.tsx`, `apps/web/app/workspaces/[id]/page.tsx` (Overview quick-links only), `apps/web/app/workspaces/[id]/knowledge-bases/page.tsx`, `apps/web/app/workspaces/[id]/members/page.tsx`, `apps/web/app/workspaces/[id]/settings/page.tsx`, `apps/web/src/components/workspace-nav.tsx`, `apps/web/src/components/mobile-tab-bar.tsx`, `apps/web/src/components/workspace-brand-link.tsx`, `packages/ui/src/components/ui/app-shell.tsx`, `packages/ui/src/components/ui/mobile-nav-drawer.tsx`, `apps/web/app/invite/[token]/page.tsx`, `apps/web/app/api/workspaces*`, `apps/web/app/api/invitations/accept/[token]/route.ts`, `apps/web/src/lib/api/workspaces.ts`, `apps/web/src/lib/api/handle-unauthorized.ts`, `apps/web/middleware.ts` | `apps/api/src/workspaces/*`, `apps/api/src/auth/guards/workspace-member.guard.ts`, `apps/api/src/auth/guards/roles.guard.ts` | `workspaces`, `workspace_members`, `invitations` | none yet | Resend invite email via `NotificationsService.sendInvite()`; dev console `[DEV INVITE]` fallback | `workspaces.service.spec.ts`, `test/workspaces.e2e-spec.ts`, `roles.guard.spec.ts`, `workspace-member.guard.spec.ts`, `apps/web/app/api/workspaces/route.spec.ts`, `apps/web/app/api/workspaces/[id]/members/route.spec.ts`, `apps/web/app/workspaces/page.spec.ts`, `apps/web/app/workspaces/[id]/page.spec.ts`, `apps/web/app/workspaces/[id]/knowledge-bases/page.spec.ts`, `apps/web/app/workspaces/[id]/members/page.spec.ts`, `apps/web/app/workspaces/[id]/settings/page.spec.ts`, `apps/web/src/components/workspace-nav.spec.ts`, `apps/web/src/components/mobile-tab-bar.spec.ts`, `packages/ui/src/components/ui/app-shell.spec.tsx`, `packages/ui/src/components/ui/mobile-nav-drawer.spec.tsx`, `apps/web/app/invite/[token]/page.spec.ts`, `apps/web/src/lib/api/handle-unauthorized.spec.ts` | Deep     | Frontend now includes actual workspace list/detail/invite pages. Proxy turns `mnemra_at` cookie into Bearer but does not own auth/RBAC decisions. Server-side RBAC still: member = read, owner/admin = invite, owner = remove member. `GET :workspaceId/members` (added 2026-07-01) lists roster (member-readable, joins `users` for email); FE remove control is owner-only and hidden on viewer's own row via `getCurrentUser()` (`apps/web/src/lib/api/auth.ts`, backed by `GET /auth/me` — see Auth/Permissions row — not client-side JWT decode; that mechanism was removed 2026-07-01 as security fix). `/dashboard` retired 2026-07-01. Sidebar shell (`AppShell` + `WorkspaceNav`) is now shared nav model for workspace-scoped pages. Loading skeletons in root app and chat still use standalone `AppHeader` briefly by design. The leftover "P2 ready" dev-status badge was dropped from `/workspaces` on 2026-07-02 with no replacement because there is no truthful per-workspace status metric there yet. Slice 0 (2026-07-04) shared-UI foundations: `app-shell.tsx` stacks the footer logout/collapse controls into a column and centers the sidebar header initial when collapsed (fixed the collapsed-sidebar unclickable-toggle bug); new shared `Pagination` component + `Modal` `size` prop (`md`/`lg`/`xl`/`full`) with a scrollable body; `toaster.tsx` moved to neutral high-contrast text on a tinted surface (WCAG fix, was same-hue `text-primary` on `bg-primary/10`); `workspace-nav.tsx` adds bottom spacing under the search control (bumped `mb-3`→`mb-4` 2026-07-04 for a bit more breathing room). 2026-07-04: added `PATCH /workspaces/:workspaceId` (rename) — see the Settings/Onboarding row and `api-contracts.md`/`risk-register.md` for the evidenced Standard downgrade from this domain's blanket-Deep default; the click-target from `/workspaces` already went straight to `/workspaces/:id/chat` before this change (see Chat/RAG row), so no landing-page redirect work was needed this round. 2026-07-08 mobile-responsive pass: `AppShell`'s `<aside>` is now `hidden lg:flex` (persistent sidebar only at 1024px+); below `lg`, a new hand-rolled `MobileNavDrawer` (no Radix, mirrors `Modal`'s conventions) renders off-canvas, triggered by an always-present `lg:hidden` hamburger strip. Prop API unchanged, all 9 consumers unaffected. Also fixed a real overflow bug found via live 375px verification: the chat page's empty-state suggested-prompt buttons inherited `Button`'s base `whitespace-nowrap` and overflowed the message container on narrow viewports — fixed at the usage site (`chat/page.tsx`) with `whitespace-normal text-left`, not in the shared `Button` component. Dark mode tokens exist in `globals.css` but nothing currently toggles the `.dark` class (no `next-themes`, no media-query hook) — noted as a separate pre-existing gap, not addressed by this pass. **Same-day follow-up (CONTEXT DRIFT fix — these 4 commits landed right after the pass above but were missed by the same-session docs-sync commit):** `AppShell` gained two more optional props, verified in source — `mobileTabBar?: (args: {moreActive, onMoreClick}) => ReactNode` and `mobileFullBleed?: boolean`. `apps/web/src/components/mobile-tab-bar.tsx` (`MobileTabBar`) is an iOS-style bottom nav (fixed, `lg:hidden`, safe-area-inset-bottom padded) rendered via the `mobileTabBar` slot on all 9 workspace pages except chat; its trailing "More" button reuses the same `mobileNavOpen` state as the hamburger drawer (`moreActive`/`onMoreClick`), so there is exactly one mobile-nav-open source of truth, not two. When `mobileTabBar` is passed, `AppShell` adds `pb-20 lg:pb-0` to `<main>` so page content cannot render underneath the fixed bar. `apps/web/src/components/workspace-brand-link.tsx` (`WorkspaceBrandLink`) replaces each page's inline "avatar-initial + truncated name" sidebar-header markup with one shared component (fixes a truncation bug where the name wasn't actually inside a `min-w-0` flex child on several pages) and is now the `sidebarHeader` slot on all 9 pages. `mobileFullBleed` (chat page only, see Chat/RAG row) hides both the sticky title `<header>` and the tab bar/hamburger strip on mobile so the page can render its own compact custom header via the `children` function form's `openMobileNav` callback; desktop layout (`lg:` and above) is unaffected in all cases. |
| Workspace Events      | `apps/web/app/workspaces/[id]/page.tsx` (Activity feed), `apps/web/src/components/workspace-nav.tsx` (Overview unread badge), `apps/web/app/api/workspaces/[id]/events/**`, `apps/web/src/lib/api/events.ts` | `apps/api/src/events/*` | `workspace_events`, `workspace_members.events_seen_at` | `apps/api/src/{ingest,scrape,tickets}/*.processor.ts` record terminal success/failure events via fire-and-forget `.catch()` | none | `apps/api/src/events/events.service.spec.ts`, `apps/api/test/events.e2e-spec.ts`, `apps/api/src/ingest/ingest.processor.spec.ts`, `apps/api/src/scrape/scrape.processor.spec.ts`, `apps/api/src/tickets/ticket-extraction.processor.spec.ts`, `apps/web/src/lib/api/events.spec.ts`, `apps/web/app/api/workspaces/[id]/events/**/*.spec.ts`, `apps/web/app/workspaces/[id]/page.spec.ts`, `apps/web/src/components/workspace-nav.spec.ts` | Standard | One shared system for both feed + notifications. `markSeen()` only updates caller's own membership row; unread count is `workspace_events.created_at > workspace_members.events_seen_at` or all rows when null. Event-write failures are logged and swallowed so ingest/scrape/ticket jobs still reach their own terminal state. |
| Search                | `apps/web/src/components/workspace-search.tsx`, `apps/web/src/components/workspace-nav.tsx`, `apps/web/app/api/workspaces/[id]/search/route.ts`, `apps/web/src/lib/api/search.ts` | `apps/api/src/search/*` | reads `chunks`, `documents`; generated `tickets.search_vector`; generated `chat_messages.search_vector` | none | OpenAI embeddings indirectly via `@repo/ai#similaritySearch()` for document chunks; Postgres full-text search for tickets/chat | `apps/api/src/search/search.service.spec.ts`, `apps/api/test/search.e2e-spec.ts`, `apps/web/src/lib/api/search.spec.ts`, `apps/web/app/api/workspaces/[id]/search/route.spec.ts`, `apps/web/src/components/workspace-search.spec.ts`, `apps/web/src/lib/ui/modal.spec.ts` | Standard | Grouped response only: `{documents, tickets, chatMessages}`. Document results are chunk-backed but fetch `documents` rows for title/knowledge-base lookup because stored chunk metadata does not include document title. Chat search scopes by joining `chat_sessions` because `chat_messages` has no `workspace_id`. Shared modal regression fixed 2026-07-03: `⌘K` search now autofocuses query input and keeps focus while results rerender. |
| Knowledge Bases       | `apps/web/app/workspaces/[id]/knowledge-bases/page.tsx`, `apps/web/app/api/workspaces/[id]/knowledge-bases*`, `apps/web/src/lib/api/knowledge-bases.ts` | `apps/api/src/knowledge-bases/*` | `knowledge_bases`, `documents` (read-only count guard) | none yet | none | `knowledge-bases.service.spec.ts`, `test/knowledge-bases.e2e-spec.ts`, `apps/web/app/workspaces/[id]/knowledge-bases/page.spec.ts` | Standard | Nested under workspace. owner/admin can create/delete; member can list. Delete blocked while documents reference KB. |
| Documents / Ingestion | `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx`, `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents*`, `apps/web/src/lib/api/documents.ts`, `apps/web/src/lib/http/auth-proxy.ts`, `packages/ui/src/components/ui/{modal,select,textarea,table}.tsx` | `apps/api/src/documents/*`, `apps/api/src/ingest/*`, `apps/api/src/storage/*` | `documents`, `chunks` | Bull `ingest-queue` | SeaweedFS/S3 via `StorageService`, OpenAI embeddings via `@repo/ai` | `documents.service.spec.ts`, `documents.controller.spec.ts`, `ingest.service.spec.ts`, `ingest.processor.spec.ts`, `test/documents.e2e-spec.ts`, `storage.service.spec.ts`, `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/route.spec.ts`, `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/[docId]/download/route.spec.ts`, `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/download/route.spec.ts`, `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/documents/delete/route.spec.ts`, `apps/web/src/lib/api/documents.spec.ts`, `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.spec.ts`, `apps/web/src/lib/ui/modal.spec.ts` | Deep | Upload path is owner/admin only; list/download are member-readable; delete is owner/admin only. Uploads are bounded by `MAX_UPLOAD_MB` and a supported-type allowlist before storage/ingest. `documents` now persist queue metadata (`queueJobId`, `enqueuedAt`, `processingStartedAt`, `lastError`) so enqueue failures and stale orphaned jobs reconcile to terminal `failed` instead of hanging forever. Requeue clears stale `processingStartedAt` and applies a 5-minute Bull timeout. Slice 1 (2026-07-04) changed the KB document table to backend offset pagination, `title ILIKE` search, status filter, `updatedAt DESC, id DESC` order, drag/drop upload, row selection, and single/bulk download through raw web proxies backed by `StorageService.getBuffer()`. Bulk delete (2026-07-04) reuses selected rows, owner/admin RBAC, best-effort scoped deletion, and one workspace chat-cache bump after any successful row delete. Upload proxy forwards multipart untouched; ingest worker lazily imports `@repo/ai`, writes document lifecycle state, injects tenant metadata into chunks, and relies on `syncChunks()` hash diff for retry-safe reprocessing. |
| Chat / RAG | `apps/web/app/workspaces/[id]/chat/page.tsx`, `apps/web/app/api/workspaces/[id]/chat/**`, `apps/web/src/lib/api/chat.ts`, `apps/web/app/chat/page.tsx` redirect, `apps/web/app/api/workspaces/[id]/refine/**`, `apps/web/src/lib/api/refine.ts` | `apps/api/src/chat/*`, `apps/api/src/cache/*`, `apps/api/src/limits/*`, `apps/api/src/auth/*` guards, `packages/ai/src/chains/index.ts`, `packages/ai/src/chains/graph.ts`, `packages/ai/src/chains/condense.ts`, `packages/ai/src/chains/history.ts`, `apps/api/src/refine/*` | `chat_sessions`, `chat_messages`, `chat_cache`, `chat_query_metrics`, `saved_refined_messages`, reads `documents`, `chunks`; `@repo/types` now uses `workspaceId` | ingest bumps workspace cache version on document `done`; document delete bumps too | OpenAI chat model via `@repo/ai`, Redis exact cache + usage/rate counters, workspace-scoped retrieval via `similaritySearch()`, optional LangGraph miss path | `cache.service.spec.ts`, `rate-limit.service.spec.ts`, `usage.service.spec.ts`, `chat.service.spec.ts`, `test/chat.e2e-spec.ts`, `packages/ai/src/chains/index.spec.ts`, `packages/ai/src/chains/graph.spec.ts`, `apps/web/app/api/workspaces/[id]/chat/**/*.spec.ts`, `apps/web/app/workspaces/[id]/chat/page.spec.ts`, `apps/web/app/chat/page.spec.ts`, `apps/api/src/refine/refine.service.spec.ts`, `apps/api/test/refine.e2e-spec.ts`, `apps/web/src/lib/api/refine.spec.ts`, `apps/web/app/api/workspaces/[id]/refine/**/*.spec.ts`, `packages/ai/src/index.spec.ts`, `packages/ai/src/chains/condense.spec.ts`, `packages/ai/src/chains/history.spec.ts` | Deep | Verified source + automated as of 2026-07-02. Chat answer path is exact Redis cache -> semantic `chat_cache` lookup -> straight RAG or conditional LangGraph on miss. Ticket-authored vector chunks can now cite resolved useful reviewed tickets alongside KB documents; FE source rendering treats legacy rows with no `sourceType` as document citations so old history/cache rows still render. Rate limits apply to all chat POSTs; monthly token budget applies to miss-path generations only. History panel on the workspace chat page (`chat/page.tsx`) is hidden by default as of 2026-07-02 (`showHistory` state, toggled via a "Show/Hide history" action) so the chat column gets full width by default; sessions still load in the background regardless of panel visibility. 2026-07-03 chat UI pass switched message rendering to safe Markdown (`react-markdown` + `remark-gfm`, no raw HTML) and made bubbles span full column while capping inner text width for readability. 2026-07-03 RAG perf pass added `packages/ai/src/chains/{models,context,classify}.ts` (+ `.spec.ts`) and `packages/ai/src/vectorstore/embedding-reuse.spec.ts`. Graph now streams confident answers and gates self-grade by `SELF_GRADE_MIN_SCORE`; `answerQuestion()` routes simple vs complex queries; rewrite mutates only the retrieval query (`originalQuestion` vs `retrievalQuery`); per-task model config via `resolveModel()`; context via token-budgeted `buildEvidencePack()`; retrieval reuses the cache-lookup embedding and accepts metadata filters. 2026-07-04: added "Refine with Mnemra" (caveman-compression rewrite of a rough draft, `packages/ai/src/chains/refine.ts`, pre-existing/pre-tested) plus a personal (workspace+user scoped, not shared) saved-refined-messages library, surfaced via two new buttons + a saved-messages modal on the chat page. Daily per-user refine cap (`REFINE_DAILY_LIMIT_PER_USER`, default 20) enforced via `RefineRateLimitGuard` wrapping the existing `RateLimitService.checkRefineDaily`; remaining-count exposed via a dedicated `GET .../refine/status` rather than a response header, since the web proxy's `proxyJson()` helper does not forward arbitrary headers. 2026-07-05: prompt wording now explicitly distinguishes partial-context answers from true no-context fallback. Hard refusal text for zero-chunk / rewrite-exhausted structural fallback stays unchanged; partial-context generations stay non-fallback and keep citations. 2026-07-05 history-aware chat: `chat.service.ts#answer()` now fetches bounded recent session history (`getRecentHistory()`, new index-backed query) before any cache/embed/classify/retrieval step. When history exists, `packages/ai/src/chains/condense.ts#condenseQuestion()` (new `'condense'` model role in `models.ts`) resolves the follow-up into a standalone question using a cheap model — skipped entirely (zero LLM cost) on a session's first turn or when `HISTORY_CONDENSE_ENABLED=false`. Exact/semantic cache keys, `embedQuery()`, and `classifyQuery()` all now operate on the condensed standalone question, not the raw follow-up — fixes a real cache-collision risk where two unrelated conversations asking the same literal short follow-up ("find more like that") would previously have shared a cache entry. Bounded history (`packages/ai/src/chains/history.ts`, `HISTORY_MAX_MESSAGES`/`HISTORY_TOKEN_BUDGET`) is also threaded into the final answer-generation prompt as alternating `HumanMessage`/`AIMessage` turns (`answerQuestion()`/`answerQuestionWithGraph()` gain an optional trailing `history` param; `GraphState` gains a write-once `history` field read only by `generateNode`/`regenerateNode`/the confident-stream branch — `retrieveNode`/`rewriteNode`/`gradeAnswerNode` are deliberately untouched), gated independently via `HISTORY_IN_ANSWER_ENABLED`. Both flags default `true` (explicit product decision, not this repo's usual dark-ship default) since the fix directly addresses reported broken multi-turn chat. Monthly usage accounting now folds in the condensed-question token length on turns where condensing actually changed the text. 2026-07-08 (V2 slice S3, `docs/ai/planning/v2-features.md`): added write-only retrieval-quality telemetry — `chat_query_metrics` (new table, additive migration `0014`) records one row per turn (workspaceId, cacheStatus, queryClass via now-exported `classifyQuery()`, topScore/sourceCount from the returned sources, isFallback, latencyMs, and the reused question embedding — null on exact-cache hits since those never re-embed). Write point is `ChatService#recordQueryMetrics()`, called from all three `onComplete` branches as `await ...recordQueryMetrics(...).catch(...)` — awaited (not truly detached) specifically so a metrics failure can't race past the response while still never rejecting into the response path; `persistAssistant()` now returns the inserted `chatMessages.id` to feed it. No reads yet — this table is the substrate for the planned F6 digest and F7a coverage dashboard. **2026-07-08 full-bleed mobile chat UI (CONTEXT DRIFT fix, verified in source):** `chat/page.tsx` is the only page passing `AppShell`'s `mobileFullBleed` prop — below `lg`, the sticky title header and the hamburger/tab-bar strip are hidden entirely and the page instead renders its own compact custom header (drawer-open icon via the `children({openMobileNav})` function form, page title, history-toggle icon, new-chat icon) inside the chat column itself, so the message list can use the full viewport height edge-to-edge like the reference "Claude mobile" pattern. Desktop (`lg:` and above) renders the normal `AppShell` title header and sidebar unchanged — this prop only ever affects `<lg`. No RAG/streaming/session logic changed. **2026-07-09 (CONTRACT DRIFT fix + mobile UX pass):** the 2026-07-08 entry's "edge-to-edge" claim was not actually true in source (padded wrapper, bordered Card still present below `lg`) — now genuinely flat/full-bleed via `lg:`-scoped Card overrides and `100dvh`. Added stick-to-bottom auto-scroll, a floating `ScrollToBottomButton`, a `min-w-0`/`break-words` fix for a real bubble horizontal-overflow bug, hidden empty-state suggestions below `lg`, an auto-growing textarea, and `safe-area-inset-bottom` input padding. First use of the reactbits/shadcn registry in this repo (`apps/web/components.json`, `apps/web/.mcp.json`) added `gsap`/`@gsap/react`/`motion`/`ogl` as new `apps/web` dependencies — a deliberate departure from this repo's prior zero-animation-library convention. Streaming assistant text now renders via `StreamingText` (chunked GSAP SplitText reveal, only while actively streaming) instead of `ReactMarkdown`; the old `Loader2`+plain-text loading state is `ThinkingIndicator` (Strands WebGL loader + ShinyText); a mobile-only `MessageJumpRail` (`LineSidebar`-based) was added for per-turn jump navigation. New page-local files under `apps/web/src/components/chat/` and `apps/web/src/hooks/use-stick-to-bottom.ts` — see `repository-map.md` and `learnings.md`'s 2026-07-09 entry for the vendored-component bugs found and fixed (missing `React` imports, missing jsdom `matchMedia`/`scrollTo` polyfills now in `apps/web/vitest.setup.ts`). |
| Web Sources / Scraping | `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.tsx`, `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/{scrape,scrape-runs}/route.ts`, `apps/web/src/lib/api/scrape.ts` | `apps/api/src/scrape/*`, `apps/api/src/storage/*`, `apps/api/src/ingest/*`, `packages/ai/src/web/{crawl,ssrf}.ts` | `scrape_runs`, `documents` (`sourceUrl` upsert path) | Bull `scrape-queue`, Bull `ingest-queue` fan-out | outbound docs-site crawl via `@repo/ai/src/web/crawl.ts`, SeaweedFS/S3 text storage | `packages/ai/src/web/ssrf.spec.ts`, `packages/ai/src/web/crawl.spec.ts`, `scrape.service.spec.ts`, `scrape.processor.spec.ts`, `test/scrape.e2e-spec.ts`, `apps/web/app/api/workspaces/[id]/knowledge-bases/[kbId]/{scrape,scrape-runs}/route.spec.ts`, `apps/web/app/workspaces/[id]/knowledge-bases/[kbId]/page.spec.ts`, `apps/web/src/lib/api/scrape.spec.ts`, `apps/web/src/lib/ui/modal.spec.ts` | Deep | Owner/admin starts crawl; member can list runs. API rejects non-public seed URLs before queueing. `scrape_runs` now persist queue metadata (`queueJobId`, `enqueuedAt`, `lastProgressAt`) and API startup reconciles stale orphaned `queued`/`running` rows to terminal `failed`. Duplicate in-flight crawl requests for the same workspace + KB + seed URL now reuse the existing run instead of creating a second queued row. When `includePrefixes` is omitted, backend defaults scope to the seed subtree and widens `/.../home` seeds to their parent section. Crawler revalidates every fetched URL against the same SSRF guard, stays same-origin, honors robots, sets User-Agent, throttles requests, streams live per-page progress, stores clean page text as `.txt`, then reuses existing ingest pipeline per page. Workspace doc quota clamps crawl size. Slice 1 (2026-07-04) changed crawl-run list to backend offset pagination, `seedUrl ILIKE` search, status filter, default `pageSize=5`, and `createdAt DESC, id DESC` order while preserving polling. 2026-07-03 UI pass maps `queued`/`running` crawl rows to `In progress`, labels page counts (`Found`, `Queued`, `Page errors`) so page-level issues do not masquerade as run-level failure, and autofocuses scrape URL input via shared modal-focus fix. |
| Tickets | `apps/web/app/workspaces/[id]/tickets/page.tsx`, `apps/web/app/api/workspaces/[id]/tickets/**`, `apps/web/src/lib/api/tickets.ts`, `apps/web/app/dashboard/page.tsx` | `apps/api/src/tickets/*`, `apps/api/src/auth/*` guards, `packages/ai/src/chains/ticket-extraction.ts`, `packages/ai/src/vectorstore/index.ts` | `tickets`, `chunks` | Bull `ticket-extraction-queue` | OpenAI chat completion via `@repo/ai` extraction chain and embeddings via `@repo/ai#syncTicketChunk()` | `packages/ai/src/chains/ticket-extraction.spec.ts`, `packages/ai/src/vectorstore/index.spec.ts`, `tickets.service.spec.ts`, `ticket-extraction.processor.spec.ts`, `test/tickets.e2e-spec.ts`, `apps/web/app/api/workspaces/[id]/tickets/**/*.spec.ts`, `apps/web/app/workspaces/[id]/tickets/page.spec.ts`, `scripts/eval/test_extraction_dataset_schema.py` | Deep | Workspace-scoped transcript-to-ticket copilot. Any workspace member can create/review. Exact transcript hash dedups within workspace via DB-backed unique `(workspaceId, transcriptHash)`. Reviewer UI now keeps source transcript visible beside draft fields, shows `fieldConfidence` summary, and flags low-confidence root-cause guesses for verification. Reviewed tickets that are `done + reviewedBy != null + usefulness='useful'` now sync one vector chunk per ticket on review save, and a one-off backfill script can embed pre-existing qualifying tickets. Slice 4 (2026-07-04): `tickets.service.ts#list` migrated keyset cursor → offset pagination with title search + status/severity/usefulness/indexed filters (`ListTicketsQueryDto`); list rows now carry an `indexed` boolean (same predicate as vector-chunk eligibility). `tickets/page.tsx` moved the review/refine editor into a `Modal`, stacked transcript-input/workspace-drafts into one column, added an Upload-files button to Knowledge Bases, a Reference column/filter for `indexed`, and a Copy-for-Linear helper caption. |
| Customers             | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Standard | TODO: Fill after repository analysis. Do not treat as verified.                                |
| Datasets / Structured Query | `apps/web/app/workspaces/[id]/datasets/page.tsx`, `apps/web/app/api/workspaces/[id]/datasets/**`, `apps/web/src/lib/api/datasets.ts` | `apps/api/src/datasets/*`, `apps/api/src/structured-query/*`, `packages/ai/src/chains/{classify,text-to-sql,models}.ts` | `datasets` | Bull `dataset-profiling-queue` | OpenAI embeddings for dataset description (`@repo/ai#embedQuery`), `'sql'` model role text-to-SQL, ephemeral in-process DuckDB (no external service) | `datasets.service.spec.ts`, `dataset-profiling.processor.spec.ts`, `duckdb-query.service.spec.ts`, `structured-query.service.spec.ts`, `packages/ai/src/chains/text-to-sql.spec.ts`, `packages/ai/src/chains/classify.spec.ts`, `apps/api/src/chat/chat.service.spec.ts` (structured-routing cases) | Deep | Added 2026-07-08 (V2 slice S1+F1, `docs/ai/planning/v2-features.md`). CSV and XLSX (converted to CSV at profiling time, first sheet only) for v1. Datasets are workspace-scoped, NOT chunked/embedded like `documents` — only the profiling job's deterministic description (headers + inferred types + row count) is embedded, for semantic dataset selection via pgvector cosine over `datasets.description_embedding` (same hand-written `WHERE workspace_id = ...::uuid` guard as `vectorstore/index.ts`). `ChatService#answer()` decides structured-vs-RAG intent BEFORE either cache lookup (`classifyStructuredIntent()` keyword heuristic + `hasReadyDatasets()` indexed count, both cheap; structured answers bypass the exact/semantic cache entirely, same policy as never-cached fallback answers). `StructuredQueryService.answer()` returns one of four states (confident/ambiguous/correction/empty) carried over two new additive response headers (`X-Chat-Structured-State`, `X-Chat-Structured-Candidates`) — frontend renders all 4 states distinctly, including the ambiguous-candidate picker (`apps/web/app/workspaces/[id]/chat/page.tsx`) and a dedicated Datasets management page (upload/list/delete + poll-while-profiling). `DuckDbQueryService` hardening (verified empirically, not just assumed): trusted code loads exactly one workspace's CSV into an ephemeral in-memory DuckDB BEFORE `SET enable_external_access=false`; DuckDB then refuses both filesystem access and any attempt to re-enable the setting for the connection's lifetime. A keyword-based SELECT-only/single-statement/forbidden-DDL-DML filter is still required as defense-in-depth against statements that only touch the already-loaded in-memory table (CREATE/DROP/INSERT/etc. don't need filesystem access, so the DuckDB-level lock alone doesn't stop them). One repair retry on SQL execution failure (error fed back to the model); DuckDB `SUM`/aggregate results return as `bigint` from the Node driver and are sanitized to `Number` before ever leaving the service (would otherwise crash `JSON.stringify`). 2026-07-08 (V2 F2, ticket trends): `StructuredQueryService#answerFromTickets()` reuses the same DuckDbQueryService unchanged — trusted Drizzle code exports the caller's workspace tickets (mandatory `workspaceId` filter, `status='done'` only) to a temp CSV, joins `reviewedBy`/`assigneeId` to `users.email` for readable agent labels, then runs the identical generate→execute→repair→verbalize flow. `classifyTicketIntent()` (keyword heuristic) routes ticket-shaped trend questions here before the dataset-embedding selector runs. New `tickets` columns (migration `0016`): `category` (deterministically copied from the already-extracted `productArea`, not a new LLM extraction target), `resolvedAt`, `assigneeId` — all settable via the existing `PATCH .../tickets/:id` endpoint. 2026-07-08 (V2 F5, cross-file comparison): `DuckDbQueryService.runReadOnlyMultiTableQuery()` generalizes the same hardening to N tables (all loaded via trusted code before `enable_external_access=false`); `runReadOnlyQuery()` now delegates to it with a 1-table array (backward-compatible, unchanged signature). `classifyComparisonIntent()` (`packages/ai/src/chains/classify.ts`) detects comparison phrasing ("compare", "vs", "difference between"); `StructuredQueryService.answer()` checks it after candidate selection and, if 2+ candidates score above `STRUCTURED_QUERY_COMPARISON_MIN_SCORE=0.35` (looser than the single-dataset `CONFIDENT_MIN_SCORE=0.5` — a comparison question's similarity splits across topics), routes to `answerAcrossDatasets()` instead of the single-dataset flow; otherwise falls through unchanged. `generateMultiTableSql()` (new chain, same `'sql'` model role) can JOIN across the named tables. Confident multi-dataset answers set `StructuredQueryResult.datasets[]` (not `datasetId`/`datasetName`), and `ChatService#answerStructured()` cites every joined dataset as its own source. **Deviation from plan:** the plan called for a "multi-select disambiguation UX" (checkboxes); implemented instead as automatic detection from natural-language comparison phrasing plus multi-candidate scoring — the existing ambiguous-candidate picker and 4-state rendering already handle everything needed, so no new frontend surface was required. No migration. |
| Insights / Scheduler | `apps/web/app/workspaces/[id]/insights/page.tsx`, `apps/web/app/api/workspaces/[id]/insights/**`, `apps/web/src/lib/api/insights.ts` | `apps/api/src/insights/*` | `background_runs`, `document_review_flags` | Bull `freshness-tick-queue` (repeatable, weekly), `freshness-check-queue` (per-workspace fan-out) | none (embedding-distance heuristic reuses existing chunk embeddings, zero new OpenAI calls) | `ticket-doc-coverage.service.spec.ts`, `insights.service.spec.ts`, `freshness-check.processor.spec.ts` | Deep | Added 2026-07-08 (V2 S2+F3, `docs/ai/planning/v2-features.md`). S2 scheduler substrate: `background_runs` anchors status/lastError for jobs with no natural entity row; `FreshnessTickProcessor` registers ONE repeatable Bull job (`FRESHNESS_CHECK_CRON`, default weekly Monday 03:00) that fans out one `freshness-check-queue` job per workspace (jobId includes the ISO week so reruns within the same week dedupe). F3 freshness detector: `TicketDocCoverageService.findGaps()` is the shared ticket↔doc coverage primitive (also serves F4) — for each ticket-derived `chunks` row within `FRESHNESS_LOOKBACK_DAYS` (default 90), finds the nearest document-derived chunk in the SAME workspace via pgvector cosine; a score below `FRESHNESS_MIN_SCORE` (default 0.5, same threshold convention as F1's `CONFIDENT_MIN_SCORE`) inserts a `document_review_flags` row. Deliberately embedding-distance, not LLM-compare, for v1 — zero new LLM cost, reuses embeddings that already exist from `syncTicketChunk()`/document ingest; the plan explicitly left LLM-compare as a fallback posture only. `document_review_flags` is N-per-document with provenance (`ticketId`, `score`) and an audited dismiss action (`dismissedAt`/`dismissedBy`), NOT a boolean on `documents` — a doc can accrue several independent signals over time. `NOT EXISTS` guard in `findGaps()` prevents re-flagging a (document, ticket) pair that already has an open flag, so weekly reruns don't duplicate. S4 Insights shell ships here too (tab 1 only): `GET/PATCH workspaces/:workspaceId/insights/freshness-flags[/:flagId/dismiss]`, member-readable, guarded `JwtAuthGuard`+`WorkspaceMemberGuard`. Only reviewed+useful+done tickets have chunks at all (`syncTicketChunk`), so this primitive only ever sees that reviewed subset — same scope note as the shared-primitive design in the plan. 2026-07-08 (V2 F4, auto-FAQ): `TicketDocCoverageService.findUncoveredTickets()` is the ticket-centric read of the same primitive (deliberately NOT deduped against `document_review_flags` — F4 needs the full uncovered set every run, dedupes instead against tickets already covered by a pending/approved `faq_drafts` row). `FaqClusterService.cluster()` is a pure greedy single-pass clusterer (cosine vs. a fixed seed embedding, `FAQ_CLUSTER_SIMILARITY_THRESHOLD=0.75` — tighter than F3's 0.5 — `FAQ_MIN_CLUSTER_SIZE=3`). Surviving clusters go through `generateFaqDraft()` (`packages/ai/src/chains/faq-draft.ts`, new `'faq'` model role), built from already-extracted, reviewer-approved ticket fields (`title`/`issueSummary`/`nextAction`) — never the raw transcript, the plan's PII/prompt-injection mitigation for this slice. Drafts land in `faq_drafts` as `pending`; approval is owner/admin-only (`FaqDraftsController`, hard human-approval-gate invariant) and materializes the draft as a normal `documents` row through the EXISTING ingest pipeline — same pattern `scrape.processor.ts` uses for crawled pages (write `.txt` to storage, insert `documents`, `IngestService.queueDocument()`) — into a find-or-create `"Generated FAQs"` knowledge base per workspace. Zero retrieval-side changes. Insights web page gained a second tab (button-pair toggle, no new design-system Tabs component) for drafts review. 2026-07-08 (V2 F7a, coverage dashboard): `CoverageDashboardService` adds a third read-only tab — `getSummary()`/`getLowScoreQueries()` are live per-request rollups over `chat_query_metrics` (no job, no migration); `getTopicGaps()` reads a Redis-cached (`insights:topic-gaps:${workspaceId}`, 9-day TTL) result written by a new weekly `TopicGapTickProcessor`/`TopicGapProcessor` pair (same S2 substrate). `TopicGapProcessor` reuses `FaqClusterService.cluster()` as-is (field-name reuse: `ticketId` slot holds the metric's own id) to cluster recent fallback/low-score question embeddings, then labels only the `TOPIC_GAP_MAX_LABELED_CLUSTERS` largest clusters via `generateTopicLabel()` (`packages/ai`, reuses the existing cheap `'grade'` model role rather than adding a new one) — resolves this slice's "gap-cluster labeling cost cap" open point via two caps (`TOPIC_GAP_MAX_CANDIDATES` rows fetched, `TOPIC_GAP_MAX_LABELED_CLUSTERS` LLM calls). `GET workspaces/:workspaceId/insights/coverage` returns all three panels together, member-readable. No new table — a deliberate scope cut matching the plan's own "no migration" classification for this slice. 2026-07-08 (V2 F6, digest): `workspace_digest_settings` (new table, migration `0019`) holds one row per workspace (emailEnabled, slackWebhookUrl, slackEnabled). `DigestSettingsService.update()` validates any Slack webhook URL with `assertPublicUrl()` (`packages/ai/src/web/ssrf.ts`, the same SSRF guard the crawler uses) BEFORE persisting; `DigestProcessor` re-validates at send time (defense in depth). Weekly `DigestTickProcessor`/`DigestProcessor` (same S2 substrate) build `DigestContent` (`DigestContentService`, aggregates `workspace_events` counts, `CoverageDashboardService.getSummary()`, new freshness-flag/FAQ-draft/ticket counts — zero LLM calls) and send it over email (`NotificationsService#sendDigestEmail()`, to the workspace owner only in v1 — per-member preferences are a documented scope cut) and/or Slack (first outbound HTTP POST from `apps/api`, plain `fetch()`, no new dependency). Email and Slack are independent best-effort channels — one failing never blocks the other or fails the whole run; both outcomes land in `background_runs.stats` (`emailSent`/`emailError`/`slackSent`/`slackError`). Digest content renderers (`digest-renderers.ts`) treat a week with nothing notable as a first-class "quiet week" message, not an empty/broken state. `GET/PATCH workspaces/:workspaceId/digest-settings` and `GET .../digest-settings/preview` (renders without sending) are owner/admin only — the Slack webhook is workspace-sensitive external configuration, unlike the member-readable freshness/FAQ/coverage tabs. Settings UI lives on the existing workspace Settings page (`apps/web/app/workspaces/[id]/settings/page.tsx`), not the Insights page — a Slack webhook + email toggle read like workspace configuration, not a read-model tab. Preview shows the plain-text Slack payload (`digestPreviewText`), not the raw HTML — deliberately avoids `dangerouslySetInnerHTML` so the preview stays safe even if `DigestContent` is later extended to include free text (e.g. a topic-gap label). |
| Appointments          | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Standard | TODO: Fill after repository analysis. Do not treat as verified.                                |
| Intake / Booking      | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Standard | TODO: Fill after repository analysis. Do not treat as verified.                                |
| Automations           | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Deep     | TODO: Fill after repository analysis. Do not treat as verified.                                |
| Messaging             | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Deep     | Includes messaging webhooks when applicable. Verify in source.                                 |
| Imports               | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Standard | TODO: Fill after repository analysis. Do not treat as verified.                                |
| Insights / Analytics  | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Standard | TODO: Fill after repository analysis. Do not treat as verified.                                |
| Platform Admin        | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | TODO: Fill after repository analysis. Do not treat as verified. | Deep     | Admin workflows may affect billing, plans, roles, or platform operations. Verify risk by task. |
| Settings / Onboarding | `apps/web/app/workspaces/[id]/settings/page.tsx` | `apps/api/src/workspaces/workspaces.controller.ts#update` (rename half — see Workspaces row for the rest of this controller); `apps/api/src/auth/auth.controller.ts#changePassword` (password half — owned by Auth/Permissions domain, surfaced on this page's UI only) | `workspaces.name` (rename); `users.passwordHash`, `refresh_tokens.revokedAt` (password change) | none | none | `apps/api/src/workspaces/workspaces.service.spec.ts`, `apps/api/test/workspaces.e2e-spec.ts`, `apps/web/src/lib/api/workspaces.spec.ts`, `apps/web/app/api/workspaces/[id]/route.spec.ts`, `apps/web/app/workspaces/[id]/settings/page.spec.ts`, `apps/api/src/auth/dto/auth-dtos.spec.ts`, `apps/api/src/auth/auth.service.spec.ts`, `apps/api/test/change-password.e2e-spec.ts`, `apps/web/src/lib/api/auth.spec.ts`, `apps/web/app/api/auth/change-password/route.spec.ts` | Standard (rename) / Deep (password) | Added 2026-07-04: first real settings — workspace rename via `PATCH /workspaces/:workspaceId`, owner/admin only (mirrors `Invite Member`'s role tier, not `Remove Member`'s owner-only tier, since rename is cosmetic/reversible). Downgraded from the Workspaces domain's blanket-Deep default — evidenced isolated/low-risk per `risk-register.md` (cosmetic field, no FK/role/tenant-boundary change, reuses `CreateWorkspaceDto`'s exact validation shape and the already-tested guard chain). Change password (`POST /auth/change-password`, added 2026-07-04 after explicit Deep approval) verifies current password via bcrypt, re-hashes, and revokes all of the caller's active refresh tokens (theft-cascade precedent from `refresh()`) — frontend logs the user out and redirects to `/login` on success. This row intentionally does NOT flatten rename+password to one risk value — see the split in the Risk column. 2026-07-08: page redesigned from a flat stacked-forms layout to card-per-section (`@repo/ui` `Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardContent`, one card each for workspace name, change password, and — new this pass — weekly digest settings), pure layout/markup change, no field/validation/request-shape change to the two existing sections. The digest-settings card is this page's first UI for the F6 digest feature (`GET/PATCH .../digest-settings`, owner/admin only) — see the Insights / Scheduler row's F6 entry for the backend; it lives here rather than on the Insights page because a Slack webhook + email toggle is workspace configuration, not a read-model tab. |
| Infrastructure / Deployment | none | none | none | none | Docker, GitHub Actions, Caddy, Hetzner VPS | operational verification only, see `docs/ai/testing-strategy.md` | Deep | `docker-compose.yml` (local dev, full stack: postgres/redis/seaweedfs/api/web, all hot-reload, api/web healthchecks, host web/API default to `3100`/`3101` to avoid Suki's `3000`/`3001`, API `S3_*` overrides pinned to the local SeaweedFS identity so prod `.env` keys do not break local auth), `docker-compose.prod.yml` (prod, healthchecked, api/web env_file only, app/internal service ports not host-published, bundled Caddy opt-in via `COMPOSE_PROFILES=public`), `apps/api/Dockerfile`/`apps/web/Dockerfile` (real Node installed on Bun Alpine for Node-shebang CLIs, filtered Bun installs, BuildKit cache mounts, prod Turbo cache mounts), `docker/api-dev-entrypoint.sh` + `docker/web-dev-entrypoint.sh` (repair stale named `node_modules` volumes from lockfile/package-manifest stamps; API also conditionally rebuilds db/ai packages + runs `db:migrate` before API dev), `.github/workflows/deploy.yml` (push-to-main auto-deploy via SSH, Postgres backup from container env, creates container-readable `s3.prod.json`, removes stale `mnemra-prod-caddy` unless `COMPOSE_PROFILES=public`, starts with `--force-recreate`, then checks api/web health and S3 round-trip inside containers), `scripts/ensure-seaweedfs-s3-config.sh` (rewrites gitignored prod SeaweedFS identity JSON from `.env` S3 keys with container-readable mode), `scripts/deploy.sh`+`deploy-remote.sh` (manual fallback paths; cache-preserving build, optional Caddy profile handling, forced container recreation, startup migration, internal health/S3 checks), `docker/Caddyfile` (optional public traffic goes to Next.js `web:3000`; same-origin `/api/*` proxy route handlers forward server-side to `api:3001`), `docker/init-db.sql`, `docker/seaweedfs/s3.prod.json.example`. No frontend/backend/DB-schema code paths — pure infra. `apps/api/src/health/health.controller.ts` (`GET /health`, dependency-free liveness check) is the one real application-code addition, consumed by Docker `HEALTHCHECK` directives and the CI health-poll steps. Verified source as of 2026-07-04 (Docker dev/prod cache/perf hardening + Mnemra rebrand + CI/CD). Shared-VPS deploy (2026-07-04): `docker-compose.prod.yml`'s `web` service now publishes `127.0.0.1:3100:3000` so a host-level Caddy (systemd, not a Docker container, on a VPS already running Tyvera on 3000/3001) can reach it — `api` intentionally stays unpublished since Next.js's own `/api/*` route handlers proxy to it server-side over the Docker-internal network (`api:3001`), verified via source read: no client-side `NEXT_PUBLIC_API_URL` usage anywhere, `apiFetch()` always calls same-origin relative paths. VPS-level `/etc/caddy/Caddyfile` (outside this repo) gets one new site block for `mnemra.tyvera.app` that sends ALL traffic to `127.0.0.1:3100` — no `/api/*` split, unlike Tyvera's own Caddy block, which does split `/api/*` directly to its NestJS container because Tyvera's Next.js is not a BFF. Do not copy Tyvera's split pattern here if this ever needs re-deriving. 2026-07-08: `api` service in both `docker-compose.yml` and `docker-compose.prod.yml` gained an explicit `platform: linux/amd64` pin (V2 F1 DuckDB fix, see `docs/ai/risk-register.md` "Structured SQL Execution") — no-op on the Hetzner CX21 VPS (native x86_64, `DEPLOYMENT.md`), forces amd64 emulation on Apple Silicon dev machines so `duckdb`'s Linux prebuilt binary loads. |

### Branding Sync Notes

- 2026-07-05: Marketing/Landing brand chrome now uses the 19A folded-page `M` mark through `apps/web/src/components/brand-mark.tsx`, backed by `apps/web/public/mnemra-mark.svg`; `apps/web/app/page.tsx`, `apps/web/app/loading.tsx`, and `apps/web/app/chat/loading.tsx` no longer use the generic Sparkles icon as the logo.
- 2026-07-05: Auth pages (`login`, `register`, `verify-otp`) reuse the same `BrandMark` in their brand link. This is display-only and does not change auth form behavior, redirects, cookies, or API calls.
- 2026-07-05: Static brand image assets were regenerated from the same folded-page mark: `apps/web/app/icon.png`, `apps/web/app/favicon.ico`, `apps/web/app/apple-icon.png`, and `apps/web/app/opengraph-image.png`.
- 2026-07-06: Replaced the folded-page `M` mark with a new six-petal bloom mark (user-supplied raster art, no vector source, embedded as a raster `<image>` inside `apps/web/public/mnemra-mark.svg`). Internal identifier renamed `mnemra-folded-page` → `mnemra-bloom` everywhere it appears (`BrandMark`'s `data-brand-mark`, the SVG's `<title>`/`data-mark`, and every spec that asserted the old string: `brand-mark.spec.tsx`, `brand-images.spec.ts`, `page.spec.ts`, `loading.spec.tsx`, `chat/loading.spec.tsx`, and the three `(auth)/*/page.spec.ts` files). New mark's dominant color (`#5663e0`) is within rounding of the existing brand primary (`#5661df` in the old SVG, `#525edc` manifest theme color) — no palette/token changes made. Flat presentation chosen (no white card/fold frame) since the new mark isn't letter-based: `icon.png`/`favicon.ico` render the mark alone on a transparent canvas, `apple-icon.png` renders it on a solid white canvas (iOS renders transparent apple-touch-icons as black), and `opengraph-image.png` had its old white icon-card region patched to plain white and the flat mark composited in the same position — headline/tagline/description copy and the amber accent line were left untouched. All 4 static raster dimensions/formats (32×32, ICO, 180×180, 1200×630) are unchanged.
