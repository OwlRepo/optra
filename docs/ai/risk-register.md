# Risk Register

Purpose:

Map high-risk project areas.

This file is map only.

It is not proof of behavior.

If task touches listed high-risk area, default to Deep.

Only downgrade Deep if repository evidence proves task is isolated and low-risk.

Verify risk against source code and related contracts.

If risk area is missing, mark `UNMAPPED RISK`.

---

## Risk Areas

| Risk Area              | Why Risky                                    | Default Task Size | Required Checks                        | Manual QA              | Notes                                    |
| ---------------------- | -------------------------------------------- | ----------------- | -------------------------------------- | ---------------------- | ---------------------------------------- |
| Billing                | Revenue impact, customer trust               | Deep              | DB invariants, mutation paths, tests   | Full billing flow      | Verify against DB contracts              |
| Payments               | Financial transactions, compliance           | Deep              | Transaction safety, rollback, tests    | Full payment flow      | Verify against DB contracts              |
| SMS Credits            | Resource billing, cost control               | Deep              | Credit balance invariants, tests       | Credit flow            | Verify against DB contracts              |
| Plan Upgrades          | Subscription state, feature access           | Deep              | Plan state invariants, tests           | Upgrade/downgrade flow | Verify against DB contracts              |
| Auth / Permissions     | Security, access control                     | Deep              | Auth flows, permission checks, tests   | Full auth flow         | Verify against auth middleware           |
| OTP / Invite Email Delivery | `NotificationsService` wraps `resend@6.16.0`, which never rejects on API-level failures (bad key, unverified domain, rate limit) — it always resolves `{data, error}`. A caller that doesn't check `error` gets a false-positive success with no email ever sent and no error anywhere. | Deep | `error` field checked and thrown (`InternalServerErrorException`, logged) in both `sendOtp` and `sendInvite`; unit coverage in `notifications.service.spec.ts` stubs the SDK's `{data, error}` contract directly (no live network in tests) | Register a real account with `EMAIL_OTP_ENABLED=true` and confirm the email actually arrives, not just that the request doesn't error | Previously `UNMAPPED RISK` — discovered 2026-07-05 while investigating "OTP not sending, no error anywhere." Fixed the silent-swallow bug in code (see `module-ownership-map.md` Auth/Permissions row). **Open operational item, not fixable in code:** live-reproduced against the real account — Resend returns `The tyvera.com domain is not verified. Please, add and verify your domain on https://resend.com/domains`. Registration will keep 500ing for real users until `tyvera.com` is verified in the Resend dashboard (or `RESEND_FROM_EMAIL`/`RESEND_API_KEY` in `.env` point at a domain/account that already is verified). User + OTP DB rows are still written before the send attempt, so a real send failure leaves an unverified, un-retryable user (`ConflictException` on re-register) — no resend-OTP endpoint exists yet; flagged here, not built, out of scope for this fix. |
| Change Password        | Mutates live user credential (`users.passwordHash`); incorrect revocation logic could either leave a stolen session alive or unexpectedly lock out legitimate other-device sessions | Deep | `bcrypt.compare` current-password verification before any mutation, `bcrypt.hash` re-hash with the same `BCRYPT_ROUNDS` constant as register/login, all-active-refresh-token revocation scoped to exactly the caller's `userId`, server-side `mnemra_rt` cookie clear, tests | Change password in one browser, confirm a second already-logged-in browser is force-logged-out on its next refresh attempt, confirm the new password logs in and the old one does not | Added 2026-07-04. Reuses the exact theft-cascade revocation precedent already established in `auth.service.ts#refresh()`'s reuse-detection branch — a password change is at least as strong a credential-security signal as detected token reuse, so it gets the same "revoke everything" treatment. Frontend logs the user out and redirects to `/login` on success so the current tab never retries a token the backend just revoked. |
| Workspaces / RBAC      | Tenant boundary, member removal, invite misuse | Deep            | Membership invariants, guard order, tests | Invite/member flow  | Enforce RBAC server-side; keep ≥1 owner per workspace; single-use invite token with expiry. 2026-07-04: workspace **rename** (`PATCH /workspaces/:workspaceId`) is downgraded to Standard for that slice only — cosmetic `varchar(255)` field, no FK/role/tenant-boundary change, reuses `CreateWorkspaceDto`'s exact validation shape and the already-tested `JwtAuthGuard`+`WorkspaceMemberGuard`+`RolesGuard` chain (owner/admin, mirrors `invite` not the owner-only `removeMember`). This is a scoped, evidenced exception, not a blanket downgrade of the domain — member removal/invite/RBAC stay Deep. |
| Post-Login Redirect Target | User-facing consistency only — wrong redirect confuses/strands users but touches no auth decision, token, or permission check | Deep by domain default (Auth/Permissions), downgraded to Standard for this slice | Two client-side `router.push()` target-string assertions (`login/page.spec.ts`, `verify-otp/page.spec.ts`), plus `refreshMock` call assertions added 2026-07-08 to both specs | Login with existing verified user and with fresh register+OTP user; confirm both land on `/chat` then auto-forward into `/workspaces/:id/chat` (or `/workspaces` if the user has zero workspaces). As of 2026-07-08, also test the prefetch scenario: visit the marketing homepage while logged out (letting a `<Link href="/chat">` prefetch), then log in and confirm no bounce-back to `/login` | Added 2026-07-05: fixed inconsistent post-login landing page — `login/page.tsx:36` and `verify-otp/page.tsx:40` previously pushed straight to `/workspaces`; now both push `/chat`, reusing `/chat`'s pre-existing auto-route-to-first-workspace-chat logic (`apps/web/app/chat/page.tsx`, untouched). Invite-accept (`invite/[token]/page.tsx`) intentionally still pushes `/workspaces/:id/chat` directly — it already knows the destination workspace, out of scope for this fix. Downgraded from Auth's Deep default the same way the 2026-07-04 workspace-rename slice was: pure client-side routing-string change, no middleware/guard/token/backend logic touched, existing test coverage already pinned the old target so drift is caught immediately. **Logged testing exception:** this is a cross-layer, user-facing flow, and CLAUDE.md's testing rule normally requires e2e coverage with no exceptions — but this repo has no Playwright/Cypress infra at all yet (see `testing-strategy.md`), and the user explicitly chose Vitest-only for this slice rather than standing up e2e infra first. Recommend a follow-up task to add Playwright and backfill real browser e2e for this and other auth flows. **Follow-up fix added 2026-07-08:** RCA found the actual root cause of "inconsistent" (works sometimes, not others) — the marketing page has several `<Link href="/chat">` (prefetch-eligible, no `prefetch={false}`) that, when scrolled into view by a logged-out visitor, prefetch `/chat` through `middleware.ts`, which redirects to `/login` since there's no `mnemra_rt` cookie yet; Next.js's client Router Cache stores that redirect. A subsequent successful login/OTP-verify then does a soft `router.push('/chat')` that can serve that stale cached redirect instead of re-checking middleware with the freshly-set cookies, bouncing the user back to `/login`. Fixed by calling `router.refresh()` immediately before `router.push('/chat')` in both `login/page.tsx` and `verify-otp/page.tsx` — `router.refresh()` invalidates the client Router Cache so the navigation re-runs through middleware fresh. No cookie-commit race was found (`Set-Cookie` headers are already committed by the Fetch spec before `login()`/`verifyOtp()` resolves in JS). A separate, related bug was found and intentionally deferred to its own follow-up (see the "Client-Side Session Refresh" row below): the 15-minute `mnemra_at` cookie has no client-side refresh path for API calls (`refreshAccessToken()` in `apps/web/src/lib/api/auth.ts:25-27` is defined but never called), causing ~15-minute silent session drops independent of this fix. |
| Client-Side Session Refresh | Silent mid-session logout: `mnemra_at` (access token) expires after 15 min; `middleware.ts` transparently refreshes it on page navigation, but client-side data fetches from an already-open page (`apiFetch`/`uploadFile` in `apps/web/src/lib/api/client.ts`) bypass middleware entirely — a bare 401 there used to bounce straight to `/login` even with a valid 7-day `mnemra_rt` cookie | Deep by domain default (Auth/Permissions) — not downgraded, change is cross-cutting (all 14 files under `src/lib/api/*.ts` call through `apiFetch`) | New `apps/web/src/lib/api/client.spec.ts` (client.ts previously had zero test coverage) — covers happy path, 401→refresh-success→retry-success, 401→refresh-fail→throw-original, retry-still-fails→throw-retried-error, refresh-endpoint excluded from its own retry loop, `/login`/`/register`/`/verify-otp` excluded (unauthenticated, a 401 there means bad credentials not an expired session, not a session to refresh), concurrent 401s share one in-flight refresh, `uploadFile` mirrors the same behavior | Log in, wait >15 minutes without navigating (or manually expire `mnemra_at` via devtools), then trigger a data fetch (e.g. open a workspace, send a chat message, upload a document) and confirm it succeeds silently instead of redirecting to `/login`; separately, fully log out (or let `mnemra_rt` expire/get revoked) and confirm a subsequent data fetch still redirects to `/login` as before | Added 2026-07-08, follow-up to the Post-Login Redirect Target RCA above. `apiFetch`/`uploadFile` now retry once through `POST /api/auth/refresh` on a 401 (except for the unauthenticated auth endpoints and the refresh endpoint itself, to avoid infinite loops / spurious refreshes on failed logins), sharing a single in-flight refresh promise across concurrent 401s. If refresh fails, the original error is thrown unchanged, so all 12 existing `isUnauthorized()` → `/login` redirects across the app keep working for genuine logouts — this only eliminates the *false-positive* logout case. `client.ts`'s internal refresh call is a small standalone `fetch('/api/auth/refresh')`, not a call to the existing-but-unused `refreshAccessToken()` in `auth.ts`, specifically to avoid a circular import (`auth.ts` already imports `apiFetch` from `client.ts`); `refreshAccessToken()` remains as-is, unused, for any future explicit "manually refresh" call site. **Logged testing exception (same rationale as the row above):** no Playwright/Cypress infra exists yet, so the manual QA steps above substitute for real-browser e2e coverage of the token-expiry timing behavior. |
| Automations            | Background behavior, side effects            | Deep              | Job safety, idempotency, tests         | Automation trigger     | Verify against job queue contracts       |
| Jobs                   | Background processing, retry logic           | Deep              | Queue safety, idempotency, tests       | Job execution          | Verify against job queue contracts       |
| Webhooks               | External integrations, failure handling      | Deep              | Webhook safety, retry, tests           | Webhook flow           | Verify against integration contracts     |
| Database Migrations    | Schema changes, data integrity               | Deep              | Migration safety, rollback, data check | Pre/post migration     | Verify against migration tooling         |
| Transactions           | Data consistency, atomicity                  | Deep              | Transaction boundaries, rollback       | Transactional flow     | Verify against DB contracts              |
| External Integrations  | Third-party dependencies, failure modes      | Deep              | Error handling, retry, tests           | Integration flow       | Verify against integration documentation |
| Production Deployment  | Availability, rollback, monitoring           | Deep              | Deploy safety, rollback plan           | Smoke test             | Verify against deployment docs. **2026-07-08:** RCA'd a production-only 404 on `GET /api/workspaces/:id/insights/coverage` (worked on localhost, HTML 404 from Next.js in prod). Root cause: root `.dockerignore`'s bare `coverage`/`**/coverage` patterns match *any* path segment named "coverage" at any depth, not just test-coverage-report output — they were also excluding the real app route folder `apps/web/app/api/workspaces/[id]/insights/coverage/` from the Docker build context (`docker-compose.prod.yml` web build context is repo root; `apps/web/Dockerfile` does `COPY . .` before `next build`), so the route never made it into `.next/standalone` and 404'd with no error logged anywhere. Fixed by scoping the patterns to `/apps/*/coverage` and `/packages/*/coverage` (anchored to build-context root). **Watch-out for future deploys:** the same file still has equally bare `dist`/`**/dist` and `build`/`**/build` patterns — no tracked app path currently sits under a directory literally named `dist` or `build`, so they're not live bugs today, but any future route/module/folder named `dist`, `build`, or `coverage` will silently vanish from every Docker build with no error, only a runtime 404 in production. The existing route-handler unit tests (e.g. `route.spec.ts` files importing `GET` directly) cannot catch this class of bug — they exercise the handler as a plain function, bypassing Next.js's build-time file-system route resolution entirely. |
| Workspace Events       | Shared cross-job write hooks plus per-member unread state | Standard | Job terminal-state safety, membership-row scoping, tests | Upload/crawl/extract then inspect feed/badge | Event recording must never fail or retry ingest/scrape/ticket jobs; `events_seen_at` updates only caller row |
| Search                 | Cross-workspace retrieval leaks, mixed retrieval mechanisms | Standard | Workspace scoping, grouped response shape, tests | Search docs/tickets/chat in 2 workspaces | Documents use vector chunk search while tickets/chat use full-text; scores are not comparable and must stay in separate arrays |
| Marketing SEO / Metadata | Public-facing only (static config: `metadataBase`/canonical/OG/robots/sitemap/manifest/JSON-LD) — no auth, DB, or API surface, but a wrong domain silently breaks every social share/canonical/crawl signal in production with no runtime error | Standard | `metadataBase`/canonical domain must match the live `WEB_URL` on every future domain change; share-preview validators (Facebook/Twitter/LinkedIn) after any OG copy or image change; `/robots.txt` + `/sitemap.xml` manual fetch check | Post-deploy: paste live URL into a share-preview validator, confirm image/title/description; submit sitemap to Search Console | Added 2026-07-05. Purely additive/static — no runtime state, no migration. `next/og` `ImageResponse`-based dynamic icon/OG routes are known-broken on Node v25 in this repo (`ERR_MODULE_NOT_FOUND` in Next 14.2.35's metadata-image route pipeline); use pre-rendered static PNGs at `app/{icon,apple-icon,opengraph-image}.png` instead — see Marketing/Landing row in `module-ownership-map.md` for full detail. |
| Brand Assets / Logo | Public visual identity only; wrong assets create inconsistent favicon/social/auth/landing presentation but do not touch auth, DB, API, or tenant data | Tiny | Static asset dimensions, SVG source existence, `BrandMark` render tests, page/auth/loading brand render tests, visual review of favicon/apple/OG images | Open landing page, auth pages, loading shell, browser tab favicon, and a share-preview validator | Added 2026-07-05. The 19A folded-page mark is stored as `apps/web/public/mnemra-mark.svg`; static file-convention images are pre-rendered at `apps/web/app/{icon.png,favicon.ico,apple-icon.png,opengraph-image.png}`. Updated 2026-07-06: swapped for a six-petal bloom mark (identifier `mnemra-bloom`, replacing `mnemra-folded-page`); same file paths and dimensions, flat presentation (no card/fold frame), brand color unchanged (`#5663e0` vs prior `#5661df`/`#525edc`, within rounding). |
| Unbounded List Responses | Every list-returning endpoint (workspaces, knowledge bases, documents, chat sessions, chat messages, scrape runs, tickets) returns full unbounded arrays with no limit/offset/cursor; documents can approach the 5000/workspace quota, tickets/chat-sessions/scrape-runs have no cap at all | Deep for Documents/Chat/Tickets/Scraping (matches their existing domain risk), Standard for Workspaces/Knowledge Bases | Keyset/offset correctness under concurrent insert/delete, response-shape contract change on list endpoints, proxy query-string forwarding | Paginate through a seeded large list and confirm no skipped/duplicated rows across pages | Found during 2026-07-01 production-readiness audit. Workstream A shipped on 2026-07-01: all 7 endpoints used keyset cursor pagination. 2026-07-04 workspace UX slices began the offset contract shift: members, documents, and scrape runs now return `{items,page,pageSize,total,totalPages}` with query-string passthrough. Remaining keyset rows are intentional until their slices land. |
| Document Download Streaming | Member-readable document list now has matching single/bulk download endpoints that read object storage and stream bytes/zip through API + web proxy | Deep | Route-scope verification before storage read, missing object handling, zip response headers, raw proxy passthrough, memory pressure on large selected sets | Download one file and two selected docs from a seeded KB as member; verify foreign IDs are skipped/not leaked | Slice 1 (2026-07-04) adds `StorageService.getBuffer()`, `GET .../documents/:documentId/download`, and `POST .../documents/download`. Current implementation buffers file bodies before response/zip; acceptable for current upload cap but revisit streaming/temp-file zip if bulk download size grows. |
| Document Bulk Deletion | Owner/admin selected-document delete mutates storage, DB rows, chunk cascade, and workspace chat-cache version | Deep | Route-scope verification before delete, owner/admin RBAC, stale/foreign id skipping, storage-delete failure handling, single cache bump after any row delete | Select two docs as owner/admin, bulk delete, confirm rows disappear; verify a member can still bulk download but cannot bulk delete | Added 2026-07-04: `POST .../documents/delete` accepts 1–200 UUIDs, deletes only docs in route workspace+KB, skips stale/out-of-scope ids, and returns `{deleted, skipped}`. Object-storage delete remains best-effort; DB row deletion drives chunk cascade and cache invalidation. |
| Refine / Saved Messages | Per-user daily-capped LLM rewrite tool plus a personal saved-drafts library, added to an existing Deep domain (Chat/RAG) | Standard | New-endpoint RBAC/IDOR tests, daily rate-limit 429 coverage, `RefineEmptyError`/`RefineRefusalError`→422 mapping with "try rephrasing" copy, workspace+user scoping tests on saved-message reads, additive-migration apply check | Refine a message as a member, confirm the daily counter decrements and the status endpoint reflects it; save a refined message and confirm another workspace member cannot see it | Added 2026-07-04. Downgraded from the repo's migrations-Deep-by-default rule because the migration (`0012_little_blink.sql`) is a pure additive table with two cascading FKs and no data migration, and the three hard components (LLM chain `packages/ai/src/chains/refine.ts`, rate limiter `RateLimitService.checkRefineDaily`, DB schema) were pre-built and pre-tested before this slice — this slice is wiring + UI only, following the exact `chat.controller.ts`/`ChatRateLimitGuard` pattern. Required checks verified in `apps/api/src/refine/refine.service.spec.ts` and `apps/api/test/refine.e2e-spec.ts`: `RefineRateLimitGuard` runs before the LLM call so a user at their daily cap never triggers an OpenAI request; `GET .../refine/status` is read-only (`redis.get`, never `incr`) so polling the badge cannot itself consume quota; saved-message reads always filter both `workspaceId` AND `userId` (personal-not-shared, mirrors `chat_sessions`); `RefineEmptyError`/`RefineRefusalError` map to `422`, never a raw 500, with "try rephrasing" copy so the frontend can show actionable text. |
| Vendor Catalog Ingestion / Image Storage / Vision Matching | Brand-new domain: first persisted-image storage in this app, a new outbound image-fetch SSRF surface (distinct from the crawler's HTML-only fetch), vision-LLM-as-comparator (not embedding similarity), 2 new Bull queues, and reuse of the crawler for a new capability (`extractProductImages`) | Deep | Workspace-isolation on all 4 new tables, `assertPublicUrl`+size-cap+content-type check before every image fetch, `CATALOG_MATCH_MAX_CANDIDATES` cap enforced + logged when truncated, delete-prior-then-insert idempotency on both parse and match, tests | Upload a PDF/CSV catalog for one vendor, search matches against a seeded PO line item, verify a second vendor's items are excluded when `vendorId` scope is given, verify a foreign-workspace query line item 404s | Added 2026-07-10 (Optra Track A, step A3 of 3, final slice). See `module-ownership-map.md`'s Vendor Catalog / Vision Matching row and the dedicated A3 note below for full detail — candidate cap, SSRF/size-cap image fetch, page-image PDF photo granularity, and the Jest/`import.meta` environment gap discovered (not a regression) while wiring the e2e suite. |

## Current Notes

- Docker/CI hardening + Mnemra rebrand (2026-07-04): local dev is now fully containerized
  (`docker compose up` runs postgres/redis/seaweedfs/api/web with hot reload, replacing the
  former host-based `bun run dev` + infra-only-Docker split via the now-deleted `scripts/dev.sh`).
  Production `docker-compose.prod.yml` fixed three real bugs found during discovery, not just
  documentation drift: (1) `apps/api/Dockerfile`/`apps/web/Dockerfile` referenced a nonexistent
  `bun.lockb` instead of the repo's actual `bun.lock`, silently breaking every prod image build;
  (2) `docker/seaweedfs/s3.prod.json` was bind-mounted but never existed on disk, breaking a fresh
  `docker compose -f docker-compose.prod.yml up`; (3) `env_file:` pointed at a template rather than a
  real-secrets file, risking a deploy running with placeholder values (`CHANGE_ME_STRONG_PASSWORD`,
  `your-domain.com`). Env model has since been unified: dev and prod compose both `env_file: .env`
  (single gitignored file, `cp .env.example .env`), which Compose also auto-reads for `${VAR}`
  interpolation; the deploy workflow reads `.env` for `DOMAIN` without shell-sourcing it, so the former
  `DEPLOY_DOMAIN` GitHub Secret is no longer used. Added
  `depends_on: condition: service_healthy` wiring for api/web/caddy (previously only infra services
  had healthchecks) plus a dependency-free `GET /health` endpoint
  (`apps/api/src/health/health.controller.ts`) consumed by both the new prod `HEALTHCHECK`
  directives and the new `.github/workflows/deploy.yml` auto-deploy healthcheck-poll. Also
  completed the in-progress "Second Brain" → "Mnemra" rebrand across browser UI, docs, and
  Docker/DB naming (`support_brain` → `mnemra`).
  Docker cache/perf follow-up (2026-07-04): `.dockerignore` now recursively excludes nested
  build outputs and dependency folders (`**/node_modules`, `**/.next`, `**/dist`, `**/build`,
  `**/.turbo`, `**/*.tsbuildinfo`, `.gstack`) so Docker does not send stale local artifacts in
  the build context. Web's build graph is source-verified as `@repo/web` + `@repo/ui`; API's graph
  is `@repo/api` + `@repo/ai` + `@repo/db`. Dockerfiles use Bun install cache mounts, Turbo build
  cache mounts, and filtered installs. Dev images no longer prebuild package `dist` files that
  bind mounts hide at runtime; API/Web dev entrypoints now repair stale named `node_modules`
  volumes by rerunning filtered `bun install` when lockfile/package manifests are newer than
  app-specific stamp files, or when required runtime deps such as `archiver`/`next` are missing.
  API dev also conditionally rebuilds only stale/missing `@repo/db`/`@repo/ai` package output
  before `db:migrate`. The Bun Alpine base image has no real
  `node`; container repro showed `nest build`/`swc` hanging under Bun's Node shim, so API/Web
  Dockerfiles install `nodejs` and production uses Node for API/Next standalone startup. Web runner
  copies the filtered web `node_modules` from the deps stage because Bun workspace installs do not
  leave `next` inside `.next/standalone`, and `server.js` requires it at runtime. Bun and Turbo cache
  mounts use locked app-specific ids so a corrupt package tarball in one cache does not poison another service build. Prod compose keeps `api`/`web` ports internal to Compose,
  scopes `env_file: .env` to app services only. Bundled Caddy is now opt-in with
  `COMPOSE_PROFILES=public` so the app can deploy on a shared VPS where another service already
  owns host ports `80`/`443`.
  Deploy scripts preserve cache, do not `docker compose down` before replacement, and use
  `--remove-orphans --force-recreate` so regenerated env/config bind mounts are actually reloaded
  by long-running containers.
  Local `docker-compose.yml` overrides API `S3_*` values to match `docker/seaweedfs/s3.json`
  (real generated openssl-style credentials as of the 2026-07-09 Optra rebrand, not the placeholder
  `mnemra-local` / `mnemra-local-secret` strings this note originally described) so production S3
  keys in `.env` cannot break local SeaweedFS authentication.
  Prod compose must never publish app/internal service host ports (`3000`, `3001`, `5432`, `6379`,
  `8333`, `8888`, `9333`) because Suki/Tyvera already uses `3000`/`3001` on the same target class;
  bundled Caddy must stay disabled unless this app owns host `80`/`443`. **CONTEXT DRIFT fixed
  2026-07-09:** local dev now publishes web/API on `3300`/`3301` by default (`OPTRA_WEB_PORT`/
  `OPTRA_API_PORT`, was `MNEMRA_WEB_PORT`/`MNEMRA_API_PORT` on `3100`/`3101`) — see the new
  "PO ↔ Invoice Comparison" note below for the full port table and why this changed.
  Required checks:
  - Turbo dry graph for web is exactly `@repo/web`, `@repo/ui`; Turbo dry graph for API is exactly `@repo/api`, `@repo/ai`, `@repo/db`
  - `docker compose build api web` and `docker compose -f docker-compose.prod.yml build api web` both succeed locally as dry runs before trusting a live VPS deploy
  - `docker compose config --quiet` and prod `docker compose ... config --quiet` with required env set both pass
  - `apps/api`'s `/health` returns `200 {"status":"ok"}` with zero dependencies reachable (verifies it never false-fails the container `HEALTHCHECK` during startup race windows)
  - GitHub Actions `deploy.yml` requires `VPS_HOST`/`VPS_USER`/`VPS_SSH_KEY`/`VPS_PORT` secrets configured on `OwlRepo/mnemra` before the auto-deploy path can run; it reads `DOMAIN` from the VPS `.env`, not a separate domain secret
  - prod API/Web health checks must run inside containers (`docker compose exec -T api/web wget ...`) because their ports are not host-published
  - when `COMPOSE_PROFILES=public` enables bundled Caddy, public Caddy routes must send all browser traffic to `web:3000`; Next.js owns same-origin `/api/*` proxy route handlers and forwards server-side to `api:3001`
  - when `COMPOSE_PROFILES` does not include `public`, deploy scripts must remove any stale `optra-prod-caddy` container before `up` so a previous Caddy run cannot keep colliding with shared host ingress
  - `scripts/ensure-seaweedfs-s3-config.sh` must rewrite `docker/seaweedfs/s3.prod.json` from non-placeholder `S3_ACCESS_KEY`/`S3_SECRET_KEY` in `.env` before prod `up`, and the deploy path must force-recreate services plus run an S3 round-trip from inside `api`, or the `seaweedfs` service bind-mount/auth can drift from the API env
  - generated prod SeaweedFS identity JSON must remain container-readable (`0644` today) because the `chrislusf/seaweedfs` image runs the service as uid/gid `1000` and Docker Desktop may mount host files as `root:root`
  - no live production data existed anywhere under the old `support_brain` name at the time of this change (confirmed with the project owner) — if that assumption is ever wrong for a specific deploy target, stop and reconcile via `pg_dump`/restore before cutting over, per the rollback notes in the implementation plan

- Shared-VPS deploy under `mnemra.tyvera.app` alongside Tyvera (2026-07-04). Full plan: `/Users/romeoangelesjr/.claude/plans/users-romeoangelesjr-downloads-mnemra-v-structured-tower.md`.
  Required checks:
  - `web`'s host port binds `127.0.0.1` only, never `0.0.0.0` — Tyvera's own containers bind `0.0.0.0:3000`/`3001` on this VPS (a pre-existing choice on Tyvera's side, not replicated here)
  - `api` stays unpublished on the host — nothing outside the Docker `internal`/`web` networks needs to reach it; verified no client-side code references `NEXT_PUBLIC_API_URL` or calls the API directly, only Next.js route handlers via `API_URL=http://api:3001`
  - VPS-level Caddy (`/etc/caddy/Caddyfile`, not in this repo) must send ALL traffic for `mnemra.tyvera.app` to `127.0.0.1:3100` with no `/api/*` split — Mnemra's Next.js is a full BFF (unlike Tyvera's Caddy block, which does split `/api/*` straight to a NestJS container)
  - VPS `.env` `DOMAIN`/`WEB_URL` must be the real subdomain (`mnemra.tyvera.app` / `https://mnemra.tyvera.app`), not a placeholder — `WEB_URL` doubles as the NestJS CORS allowlist origin (`apps/api/src/main.ts`), so a wrong value is a live security-config bug, not just cosmetic
  - DNS for `mnemra.tyvera.app` must resolve (verified externally, not from the VPS itself) before any Caddy reload, to avoid burning Let's Encrypt ACME issuance-failure rate limits
  - Caddyfile edits on the VPS get a timestamped backup before editing and a graceful `caddy reload` (never `systemctl restart`), validated first with `caddy validate`, since it's a single unversioned file serving both apps
  - secrets typed in plaintext into an agent chat session (API keys, sudo passwords) should be treated as exposed and rotated regardless of whether they were ever committed to git

- **Bug found and fixed 2026-07-04**: `apps/web/Dockerfile`'s `runner` stage copied `public/` and `.next/static/` to `./apps/web/public` / `./apps/web/.next/static`, but the standalone `server.js` lands flat at `/app/server.js` and expects those directories directly beside it (`/app/public`, `/app/.next/static`) — the standard Next.js standalone layout. Effect: the homepage HTML rendered fine (SSR doesn't need the asset files), so every existing healthcheck/smoke test (`GET /` only) passed, but every real page was served with zero CSS and broken JS (`_next/static/*` 404). Not caught until manually loading the deployed site in a browser.
  Required checks going forward:
  - after any `apps/web/Dockerfile` change, actually fetch a `/_next/static/*` asset path from the running container (not just `/`) and confirm `200`, e.g. `curl -o /dev/null -w '%{http_code}' http://127.0.0.1:<port>/_next/static/css/<hash>.css` — a healthcheck hitting only `/` cannot catch this class of bug
  - `docker compose exec web sh -c "find / -maxdepth 4 -name server.js"` then confirm `public/` and `.next/static/` exist as siblings of wherever `server.js` actually landed, not nested under `apps/web/`

- SeaweedFS / S3-compatible storage is a live external-integration risk as of Slice 3A.
  Required checks:
  - container comes up and S3 endpoint responds
  - `StorageService.ensureBucket()` is idempotent
  - save/get/delete round-trip passes against the real endpoint
  - `StorageService.getBuffer()` returns exact stored bytes for download paths

- Migration `0001_panoramic_spiral.sql` changes document storage semantics.
  Required checks:
  - `documents.storage_key` exists after apply
  - `chunks.document_id` FK includes `ON DELETE CASCADE`
  - rerunning schema generation shows no diff

- Documents / ingest queue is a live jobs risk as of Slice 3B.
  Required checks:
  - upload creates SeaweedFS object + `documents.status='pending'`
  - enqueue failure after row insert marks the document terminal `failed` with `lastError`
  - Bull job uses retry `attempts=3` with exponential backoff
  - deterministic Bull job id is `ingest:{documentId}`
  - processor always cleans temp files in `finally`
  - processor writes terminal `done` or `failed`, never leaves stuck `processing` on handled errors
  - startup reconciliation fails stale `pending` rows after 2 minutes and stale `processing` rows after 30 minutes when Bull no longer has the job
  - `syncChunks()` receives tenant metadata and document/workspace ids

- OpenAI embeddings are now in the document ingest critical path.
  Required checks:
  - upload path does not require a working OpenAI key to return `201 pending`
  - embed failure isolates to the document row (`status='failed'`) and does not crash the worker
  - retry remains safe because chunk sync is content-hash diff based

- Priority 2 workspace/knowledge-base/document pages now poll document status client-side.
  Required checks:
  - `setInterval` is created only while at least one document is `pending` or `processing`
  - the interval is cleared on unmount/navigation so route changes do not leak background polling
  - 401s from any workspace/KB/document page route back to `/login`

- Web crawling for knowledge-base sources is a live jobs + external-integration + quota risk as of 2026-06-30.
  Required checks:
  - crawler stays same-origin, honors robots.txt, sets explicit User-Agent, throttles requests, and caps depth/pages
  - seed URL is rejected before queueing when hostname/IP/DNS resolution points to private or internal targets
  - when `includePrefixes` is omitted, crawl scope defaults to the seed subtree and `/.../home` seeds widen to their parent section
  - enqueue failure after row insert marks the scrape run terminal `failed` with `error`
  - deterministic Bull job id is `scrape:{runId}`
  - each fetched page URL is revalidated against the same SSRF guard before any network fetch
  - crawler tests never hit live network (`fetchImpl` injected)
  - `scrape_runs` always reaches terminal `completed` or `failed`, never hangs in `running` on handled errors
  - startup reconciliation fails stale `queued` rows after 2 minutes and stale `running` rows after 30 minutes when Bull no longer has the job
  - startup reconciliation also fails `running` rows after 5 minutes with no `lastProgressAt` heartbeat
  - page-level crawl persistence failures increment `pagesFailed` without aborting whole run
  - live crawl progress updates persist `pagesFound/pagesSucceeded/pagesFailed` during the run instead of only at the end
  - workspace doc quota clamps `maxPages` before queueing
  - duplicate in-flight crawl requests for the same workspace + KB + seed URL reuse the existing run instead of inserting a second queued row
  - recrawl of same page upserts one `documents` row by `(knowledge_base_id, source_url)` and reuses ingest safely
  - web page polls crawl runs every 3 seconds only while a run is `queued` or `running`
  - web page disables crawl submit while the POST is in flight and surfaces duplicate-run reuse clearly
  - crawl-row UI must keep terminal status separate from page-level counts (`Found` / `Queued` / `Page errors`) so ongoing runs are not mistaken for failed runs
  - scrape modal must autofocus URL input and keep focus while typing through rerenders
  - document queue UI must show truthful in-flight counts (`pending`/`processing`) rather than total documents as “in queue”

- Document upload ingress is a live DoS + parser-safety risk as of 2026-07-01.
  Required checks:
  - multipart uploads reject payloads above `MAX_UPLOAD_MB` before storage/ingest
  - upload allowlist matches supported loader extensions/MIME types only
  - unsupported extensions/MIME return `400`; oversized uploads return `413`
  - small allowed uploads still return `201 pending`

- Workspace chat / RAG is a live tenant-isolation + history-privacy risk as of 2026-06-30.
  Required checks:
  - backend route stays nested under `:workspaceId` with `JwtAuthGuard` + `WorkspaceMemberGuard`
  - retrieval only uses guarded route `workspaceId`, never body-supplied tenant identifiers
  - ticket chunks below `TICKET_SLOT_MIN_SCORE` never get force-included, reserved ticket chunks never exceed `TICKET_SLOT_RESERVE`, and total retrieved chunk count never exceeds caller `limit`
  - session/message reads filter by both `workspaceId` and owning `userId`
  - web proxy never forwards raw OpenAI credentials or calls OpenAI from browser-facing API routes
  - plain-text stream keeps citations out of token body; sources persist on assistant message for reload-safe history
  - citations correctly discriminate document vs ticket sources in both straight-line and LangGraph paths
  - legacy persisted `sources` without `sourceType` still render as document citations in FE history/cache reads
  - markdown rendering stays safe (no raw HTML execution) while preserving basic formatting such as bold text, lists, links, and code blocks
  - full-width chat rows still cap inner text width for readability rather than stretching prose edge-to-edge

- Answer caching on workspace chat is a live tenant-isolation + staleness risk as of 2026-06-30.
  Required checks:
  - exact cache keys include `workspaceId` and current version
  - semantic cache lookup filters by both `workspaceId` and `version`
  - semantic cache rows older than `SEMANTIC_CACHE_TTL_HOURS` are never served as hits even when version still matches
  - first cache version bootstraps to `1`; invalidation bumps move to `2+` so old exact keys go dark
  - document ingest `done` and document single/bulk delete both call `cache.bumpVersion(workspaceId)` when rows actually change
  - ticket-driven chunk changes (`syncTicketChunk` outcomes `embedded`/`deleted`) also bump workspace cache version, including backfill path once per changed workspace
  - fallback/insufficient-info responses (`isFallback: true`) never get written to either cache layer, in both straight-line and LangGraph paths
  - 2026-07-05 prompt wording change: partial-context answers may hedge and cite supported evidence, but only the two structural no-context paths set `isFallback: true`; cache-skip invariant for true fallbacks re-verified unchanged
  - semantic-cache writes opportunistically delete that workspace's expired rows without blocking successful cache writes if cleanup fails
  - Redis/cache failures fail soft to normal chat answers; cache outage must not 500 chat
  - optional `X-Chat-Cache` header reflects `exact|semantic|miss` for observability/tests

- RAG performance changes (2026-07-03) carry answer-quality regression risk; each is behind an env flag defaulting to prior behavior, roll out one at a time.
  Required checks:
  - streaming vs grading: confident answers stream and skip self-grade; only low-confidence (top score < `SELF_GRADE_MIN_SCORE`) take buffered generate→grade→regenerate. Unset `SELF_GRADE_MIN_SCORE` = grade every answer (legacy)
  - query routing: `classifyQuery()` must not send troubleshooting/procedural questions down the light path; simple path uses `SIMPLE_QUERY_CHUNK_LIMIT` and skips rewrite/grade
  - question integrity: generation/grading use `originalQuestion`; only `retrievalQuery` is rewritten
  - embedding reuse: `precomputedEmbedding` is only reused for the first retrieval; a rewrite re-embeds the new query
  - context budget: `buildEvidencePack()` output token count stays within `RAG_CONTEXT_TOKEN_BUDGET`, highest-scored chunks kept
  - migration `0011_sticky_scream` is additive (columns + btree/hnsw indexes + backfill), reversible by dropping them; hnsw requires pgvector ≥ 0.5 (installed 0.8.3)
  - retrieval metadata filters are NOT yet exposed via `ChatDto` because the exact/semantic cache key does not include filters — must be added to the cache key before exposing per-request filters

- Chat rate/budget controls are a live abuse + cost risk as of 2026-07-01.
  Required checks:
  - per-user and per-workspace counters key by minute bucket and caller/workspace ids
  - rate-limit guard runs on every chat POST, including cache hits
  - monthly token budget applies only to miss-path generations, never cache hits
  - Redis counter failures fail open and log warnings instead of blocking support agents
  - `429` and `402` response behaviors stay covered in unit/e2e tests

- History-aware chat (condense + history-in-answer) is a live answer-quality + cost risk as of 2026-07-05.
  Required checks:
  - `chat.service.ts#answer()` skips history fetch entirely (zero extra DB query) when both `HISTORY_CONDENSE_ENABLED` and `HISTORY_IN_ANSWER_ENABLED` are `false`
  - `condenseQuestion()` never calls the LLM when history is empty (session's first turn) or when `HISTORY_CONDENSE_ENABLED=false` — verified in `packages/ai/src/chains/condense.spec.ts`
  - exact/semantic cache keys and `embedQuery()` operate on the condensed standalone question, not the raw follow-up, once history exists — prevents unrelated conversations that ask the same literal short follow-up ("find more like that") from colliding in cache
  - `retrieveNode`/`rewriteNode` never receive raw conversation history, only the already-condensed `retrievalQuery` — condensing and the existing low-confidence recall-rewrite loop are sequential, not overlapping
  - bounded history (`HISTORY_MAX_MESSAGES` default 12, `HISTORY_TOKEN_BUDGET` default 600, trimmed oldest-first) is threaded into the final generation prompt in both the light path and every graph generation call site (confident-stream, generate, regenerate) when `HISTORY_IN_ANSWER_ENABLED=true`
  - `gradeAnswerNode` never receives history — grading checks groundedness against retrieved context only
  - monthly usage accounting (`UsageService.addUsage`) folds in the condensed-question token length only on turns where condensing actually changed the text; no-history turns are unaffected
  - both flags default `true` (explicit product decision overriding this repo's usual dark-ship default for new RAG behavior — see `HISTORY_CONDENSE_ENABLED`/`HISTORY_IN_ANSWER_ENABLED` in `.env.example`), can be flipped to `false` independently to roll back either behavior without a code or migration rollback
  - `chat_messages(session_id, created_at)` index (migration `0013_condemned_silver_surfer.sql`) supports the new per-turn history read; additive, safe rollback via `DROP INDEX`
  Manual QA: ask a real KB-grounded question, then a pronoun-style follow-up ("what about that?") in the same session, and confirm the answer stays topically coherent; ask the identical literal follow-up in two unrelated sessions in the same workspace and confirm they resolve to different (correctly disambiguated) answers, not a cache collision.

- Conditional LangGraph on chat miss path is a live quality + cost risk as of 2026-07-01.
  Required checks:
  - `LANGGRAPH_ENABLED` defaults `false` and disables agentic path instantly
  - high-score retrieval path does not add rewrite/self-grade calls
  - low-score path caps rewrites with `MAX_QUERY_REWRITES` then falls back cleanly
  - `SELF_GRADE_ENABLED` stays off by default because it can double generation cost
  - retrieval and sources remain scoped to `workspaceId` through every branch

- Offline RAGAS harness is a live measurement-process risk as of 2026-07-01.
  Required checks:
  - eval harness stays outside app runtime and never runs per request
  - dataset rows keep `question`, `answer`, `contexts`, `ground_truth` schema
  - weekly run records JSON output and lowest metric so prompt/retrieval fixes have evidence

- Ticket copilot is a live tenant-isolation + queue-reliability + extraction-quality risk as of 2026-07-01.
  Required checks:
  - backend routes stay nested under `:workspaceId` with `JwtAuthGuard` + `WorkspaceMemberGuard`
  - exact transcript dedup scopes by both `workspaceId` and `transcriptHash`, with DB unique enforcement to close concurrent-create races
  - create path stores transcript before enqueue and job payload carries only `ticketId`
  - queue enqueue failure after row insert marks ticket terminal `failed` with `lastError`
  - startup reconciliation fails stale `pending` rows only after pending grace exceeds the 5-minute job timeout, and fails stale `processing` rows after 30 minutes when Bull no longer has the job
  - extraction completion never overwrites a row that already left `pending|processing` due to human review or future retries
  - extraction failures surface as ticket `failed` state and web failure banner, never raw 500 to user
  - patch/save stamps `reviewedBy` and `reviewedAt`, preserving auditability for copied Linear tickets
  - review-save embedding sync only qualifies `done + reviewedBy + usefulness='useful'` tickets, and useful→not_useful transitions remove the ticket chunk
  - `syncTicketChunk` failures never fail the review-save request; backfill re-run stays idempotent
  - PATCH validators cap large free-text fields before they can bloat row size or API memory
  - usefulness/edit-state feedback stays queryable for dashboard usefulness-rate observability

- Workspace Events are a live jobs + tenant-state risk as of 2026-07-02.
  Required checks:
  - every terminal processor hook (`ingest`, `scrape`, `ticket-extraction`) wraps `events.record(...)` in `.catch(...)`
  - event-record failure never changes job terminal DB update or retry behavior
  - unread count uses caller membership row only and treats `events_seen_at IS NULL` as "all unread"
  - viewing Overview marks events seen once per visit and clears the sidebar badge on next load

- Search is a live tenant-isolation + relevance-shape risk as of 2026-07-02.
  Required checks:
  - `GET /workspaces/:workspaceId/search` stays behind `JwtAuthGuard` + `WorkspaceMemberGuard`
  - document results fetch `documents` rows scoped to route `workspaceId`, even if a chunk hit references another workspace document id
  - ticket full-text search filters `tickets.workspace_id = :workspaceId`
  - chat full-text search joins `chat_sessions` because `chat_messages` has no `workspace_id`
  - response shape stays `{documents, tickets, chatMessages}` and never becomes one merged ranking
  - shared search modal autofocuses query input on open and never steals focus back while results rerender

- Chat Query Metrics (`chat_query_metrics`) is a live hot-path-write risk as of 2026-07-08 (V2 slice S3, `docs/ai/planning/v2-features.md`).
  Required checks:
  - `ChatService#recordQueryMetrics()` is called as `await ...recordQueryMetrics(...).catch(...)` from every `onComplete` branch — a metrics-write failure must never propagate into the chat response
  - `questionEmbedding` is `null` on exact-cache hits by design (no re-embed cost on the cheapest path) and populated on semantic/miss hits by reusing the already-computed embedding — never a fresh `embedQuery()` call
  - this table is write-only in this slice; any future reader (F6 digest, F7a coverage dashboard) must filter `workspaceId`, same as every other tenant table
  - the pgvector `vector(1536)` column in a hand-authored/edited migration must stay unquoted (`vector(1536)`, not `"vector(1536)"`) — drizzle-kit generated the quoted form here, which Postgres rejects as an unknown type

- Structured SQL Execution (datasets/DuckDB) is a Deep, live untrusted-code-execution risk as of 2026-07-08 (V2 slice S1+F1, `docs/ai/planning/v2-features.md`).
  Required checks:
  - `DuckDbQueryService.runReadOnlyQuery()` ALWAYS loads the CSV via trusted code (`CREATE TABLE ... AS SELECT * FROM read_csv_auto(trustedPath)`) BEFORE calling `SET enable_external_access=false` — never load untrusted-path input after lockdown
  - the keyword-based `assertReadOnlySelect()` filter (single-statement, SELECT/WITH-only, forbidden DDL/DML list including `set`) runs on every call — it is defense-in-depth against statements that only touch the already-materialized in-memory table, since `enable_external_access=false` does not stop those
  - every ephemeral DuckDB connection is `.close()`d in a `finally` block; every downloaded temp CSV is `unlink`ed in a `finally` block, mirroring the ingest-processor temp-file discipline
  - dataset selection (`StructuredQueryService.selectCandidates()`) is a hand-written pgvector query and must keep the `WHERE workspace_id = ...::uuid` guard — no RLS backstop exists (A7)
  - structured answers bypass BOTH the exact and semantic chat caches unconditionally (decided in `ChatService#answer()` before either cache lookup) — never let a computed answer over a mutable dataset get cached
  - DuckDB aggregate results (`SUM`, etc.) return as JS `bigint` from the Node driver and must be sanitized to `Number` before leaving `DuckDbQueryService` — otherwise `JSON.stringify` throws on the very first real query
  - XLSX uploads are converted to CSV once at profiling time (`DatasetProfilingProcessor`, first sheet only) and the SAME storageKey is overwritten with the CSV bytes — `DuckDbQueryService` and the query engine never need to know the original format
  - ticket-trend queries (V2 F2) reuse this exact engine via `StructuredQueryService#answerFromTickets()`: trusted Drizzle code exports the caller's own workspace tickets to a temp CSV (mandatory `workspaceId` filter, `status='done'` only) — LLM-generated SQL still never touches Postgres directly, same isolation model as file-based datasets
  - Docker gap (infra, not app code) RESOLVED 2026-07-08: `duckdb@1.4.4` has no Linux ARM64 prebuilt binary and no in-image build toolchain to compile one. Base image moved Alpine→Debian and a missing `ca-certificates` package was fixed first. Root fix: pinned `platform: linux/amd64` on the `api` service in both `docker-compose.yml` and `docker-compose.prod.yml` — the Hetzner CX21 VPS (`DEPLOYMENT.md`) is native x86_64, so this is a no-op in production; on an Apple Silicon dev machine it forces amd64 emulation, which resolves the dlopen failure since duckdb's amd64 Linux prebuilt loads correctly under emulation. `bun run dev` locally remains a valid, unaffected fallback path.
  - Docker gap #2 (infra, not app code) RESOLVED 2026-07-08: even on amd64, `apps/api/Dockerfile`'s `base` stage installed Debian bookworm's default `nodejs` apt package (pinned at 18.20.4, Node ABI 108) to run duckdb's `node-pre-gyp` postinstall script. `duckdb@1.4.4` no longer ships a prebuilt Linux x64 binary for ABI 108 (verified: `npm.duckdb.org/.../duckdb-v1.4.4-node-v108-linux-x64.tar.gz` 404s; ABI 115/Node 20 and ABI 127/Node 22 both 200) — install fell through to compiling from source, which then failed too since the image has no Python for node-gyp. Root fix: install Node 20.x from NodeSource instead of Debian's default, so the postinstall script downloads a real prebuilt binary instead of trying to compile one. `base` is shared by both the `dev` and `prod` build targets, so this single Dockerfile change covers `docker-compose.yml` and `docker-compose.prod.yml` identically — no compose-file-specific fix was needed. Verified: `docker build --target dev` and `--target prod` both succeed, and `require('duckdb')` executes a real query (`SELECT 1`) inside the built prod image.
  - Docker gap #3 (infra, not app code) RESOLVED 2026-07-08, found live-verifying gap #2's fix in the running `docker-compose.yml` dev stack: the `api` service's `node_modules` is a persistent named volume (`api_node_modules`), refreshed only by `docker/api-dev-entrypoint.sh`'s `needs_install()` staleness check, which previously compared only `package.json`/`bun.lock`/etc mtimes against an install stamp — it had no way to detect a Dockerfile-only change (like the Node 18→20 bump), so an already-running dev environment kept a stale volume with the old, broken `@swc/core`/`duckdb` native bindings even after rebuilding the image. Root fix: added `apps/api/Dockerfile` itself to `needs_install()`'s watched-file list, so any Dockerfile edit forces a fresh `bun install` on next container start. One-time recovery for an already-corrupted volume still requires a manual reset (`docker compose down api && docker volume rm mnemra_api_node_modules && docker compose up --build -d api`) — the entrypoint fix prevents this from happening again on *future* Dockerfile changes, it does not retroactively repair a volume that's already in a bad state.
  - Docker gap #4 (infra, not app code) RESOLVED 2026-07-08, found in the same live-verification pass: the `api` service healthcheck in both `docker-compose.yml` and `docker-compose.prod.yml`, plus the `prod` stage's own `HEALTHCHECK` in `apps/api/Dockerfile`, all shelled out to `wget` — which was never installed in the `base` stage (confirmed: `which wget` → not found). The api container has therefore been reporting `unhealthy` for its entire life regardless of whether the app itself was working, and in production this would block any service with `depends_on: condition: service_healthy` on `api` from ever starting. Root fix: switched all three healthchecks to `curl` (already installed in `base` for the NodeSource setup script in gap #2's fix, so no new package needed). Verified: dev container now reports `healthy` within one interval of starting; prod image confirmed to have `curl` at `/usr/bin/curl`.
  - cross-file comparison (V2 F5) reuses the exact same trusted-load-then-lockdown sequence via `runReadOnlyMultiTableQuery()` — EVERY table in the batch is loaded via trusted code before the single `enable_external_access=false` call for that connection; there is no per-table lockdown, so a bug that skipped loading one table before lockdown would silently break that table's data, not create a new injection vector (`assertReadOnlySelect()` still gates the final joined SQL exactly once, same as the single-table path)
  - `answerAcrossDatasets()` only ever selects from `selectCandidates()`'s pgvector results (same `WHERE workspace_id = ...::uuid` guard as the single-dataset path) — comparison mode cannot be used to pull in a dataset from another workspace; it just changes how many of THIS workspace's already-scoped candidates get loaded

- Scheduled Jobs / Freshness Detector (`background_runs`, `document_review_flags`) is a new Deep risk area as of 2026-07-08 (V2 S2+F3, `docs/ai/planning/v2-features.md`) — this repo's first scheduler substrate.
  Required checks:
  - `FreshnessTickProcessor` registers its repeatable job with a fixed `jobId: 'freshness-tick'` on every `onModuleInit()` — Bull treats re-registering the same jobId+cron as a no-op, so this must stay idempotent across every API boot/restart, never spawn a second repeatable timer
  - the tick fans out per-workspace jobs with `jobId` including the ISO week (`freshness-check:${workspaceId}:${weekStamp}`) so a redeploy mid-week cannot double-enqueue that workspace's check for the same week
  - `TicketDocCoverageService.findGaps()` filters `tc.created_at >= now() - make_interval(days => FRESHNESS_LOOKBACK_DAYS)` and both `chunks` rows it joins are already `WHERE workspace_id = ...` scoped on both sides of the LATERAL join — no cross-workspace document can ever be suggested as the "nearest" match for a ticket
  - `findGaps()`'s `NOT EXISTS` guard against existing `status = 'open'` flags for the same `(document_id, ticket_id)` pair is the only de-dup mechanism — reruns after a flag is dismissed WILL re-flag the same pair if the underlying content still mismatches, by design (dismissal is not a permanent suppression)
  - `FreshnessCheckProcessor` always writes a `background_runs` row (`start`/`succeed`/`fail`) around the primitive call and rethrows on failure — Bull's own retry (`attempts: 2`) still applies on top, so `lastError` reflects the most recent attempt, not necessarily the first
  - embedding-distance is a heuristic, not ground truth: a low cosine score means "no good documented match found," which can mean either a stale doc OR a genuinely undocumented topic — the flag `reason` is generic (`ticket-mismatch`) and does not claim to distinguish the two; a human reviews via the dismiss action

- Auto-FAQ Generation (`faq_drafts`) is a new Deep risk area as of 2026-07-08 (V2 F4, `docs/ai/planning/v2-features.md`) — the first slice in this batch where LLM-authored content can enter the retrieval corpus.
  Required checks:
  - a `faq_drafts` row can NEVER become a `documents` row without passing through `FaqDraftsController#approve()`, which is owner/admin-gated (`RolesGuard` + `@Roles('owner', 'admin')`) — this is a hard invariant, not a default-on convenience; no code path auto-approves
  - `generateFaqDraft()`'s prompt is built ONLY from already-extracted, reviewer-approved ticket fields (`title`/`issueSummary`/`nextAction`) — never the raw `transcript` column, which is untrusted free-form customer input; this is the PII/prompt-injection mitigation the plan flagged as an open point for this slice
  - `FaqDraftsService#approve()` mirrors `scrape.processor.ts`'s exact document-creation pattern (storage write, then `documents` insert, then `IngestService.queueDocument()`) — approval is indistinguishable from any other document entering the corpus to every downstream system (chunking, embedding, cache invalidation); zero retrieval-side special-casing to keep in sync
  - `FaqClusterProcessor` dedupes against tickets already present in any `pending`/`approved` `faq_drafts.ticketIds` array before clustering — without this, every weekly run would re-draft near-identical FAQs for the same unresolved ticket cluster
  - `getOrCreateFaqKnowledgeBase()` matches by exact name (`"Generated FAQs"`) with no uniqueness constraint on `knowledge_bases.name` — if a user renames or duplicates that KB, a second one could be silently created on the next approval; documented simplification, not a correctness bug, but worth knowing if KB counts look off
  - clustering (`FaqClusterService.cluster()`) is pure/synchronous and unit-tested in isolation from the DB and the LLM call — `FAQ_CLUSTER_SIMILARITY_THRESHOLD`/`FAQ_MIN_CLUSTER_SIZE` are tunable via env without touching either

- Coverage Dashboard (V2 F7a, `docs/ai/planning/v2-features.md`) is Standard risk — read-only, no new table, but shares the scheduler substrate and an LLM label call.
  Required checks:
  - `TopicGapProcessor` writes its result to Redis (`insights:topic-gaps:${workspaceId}`), NOT Postgres — a Redis flush or eviction silently empties the dashboard's third panel until the next weekly run; this is accepted as a cache, not a source of truth, per the plan's explicit "no migration" call for this slice
  - both `TOPIC_GAP_MAX_CANDIDATES` (rows fetched) and `TOPIC_GAP_MAX_LABELED_CLUSTERS` (LLM calls) are hard caps per run — a workspace with a huge fallback volume still only ever costs a bounded number of label calls per week, never scales with raw query volume
  - `generateTopicLabel()` reuses the existing `'grade'` model role instead of a new one — deliberate, not an oversight: this is a cheap, cost-capped classification-style call, the exact tier `'grade'` already exists for
  - `getSummary()`/`getLowScoreQueries()` are plain `WHERE workspace_id = ...` scoped reads over `chat_query_metrics` — same tenant-isolation pattern as every other read in this table, no new guard logic to get wrong
  - **Gotcha (git):** the web proxy route folder `apps/web/app/api/workspaces/[id]/insights/coverage/` collides with the bare `coverage` line in `.gitignore` (intended for generated test-coverage-report dirs, matches ANY path segment named `coverage`) — `git status`/`git add -A` silently never see these files. Committed via `git add -f`. Any FUTURE route/folder literally named `coverage` anywhere in this repo hits the same trap — check `git status --short` actually lists a new route file before assuming a plain `git add -A` caught it
  - `FaqClusterService.cluster()` is reused as-is for topic-gap clustering by mapping `{id, embedding}` into its `{ticketId, embedding, score}` shape — this is intentional reuse of a generic pure function, not a naming mistake; changing `FaqClusterService`'s field names would require updating both call sites

- Digest (V2 F6, `docs/ai/planning/v2-features.md`) is Deep — two external integrations (Resend email, user-supplied Slack webhook) plus a cross-workspace weekly fan-out.
  Required checks:
  - the Slack webhook URL is validated with `assertPublicUrl()` (SSRF guard) at BOTH save time (`DigestSettingsService.update()`) and send time (`DigestProcessor`) — a URL safe when saved could resolve to a private/internal address later (DNS rebinding, infra changes on the far end); re-validating at send time is not redundant
  - `DigestProcessor` treats email and Slack as fully independent channels — a Slack webhook rejection or a Resend API error is caught PER CHANNEL and recorded in `background_runs.stats`, never allowed to prevent the other channel from being attempted or to fail the whole per-workspace job
  - `DigestSettingsController` requires `owner`/`admin` for BOTH read and write (not just write) — the Slack webhook URL is workspace-sensitive configuration (an external destination for internal aggregate data), treated with the same access bar as approving FAQ drafts into the corpus
  - digest content (`DigestContentService`) makes zero LLM calls — pure aggregation over `workspace_events`/`chat_query_metrics`/`document_review_flags`/`faq_drafts`/`tickets` — so there is no token-budget interaction to get wrong for this feature, unlike F4/F7a
  - recipient scope for v1 is the workspace OWNER only (`workspaces.ownerId` joined to `users.email`) — never all workspace members; a documented scope cut resolving the "who receives it" question the plan left open, not an oversight
  - Resend confirmed live 2026-07-08 (`RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`DOMAIN` set, `EMAIL_OTP_ENABLED=true`) — the plan's original "Resend domain verification still open" caution (written during planning, before this was checked) no longer applies; do not assume it's still open without checking `.env` again if this surfaces later
  - a "quiet week" (nothing notable happened) renders a first-class message in both renderers, not an empty `<ul>` or blank Slack post — an empty-looking digest reads as broken to a user, a quiet-week message reads as working correctly

- PO ↔ Invoice Comparison (`purchase_orders`, `invoices`, `po_line_items`, `invoice_line_items`, `discrepancy_flags`) is a new Deep risk area as of 2026-07-09 (Optra Track A, step A1 of 3) — the first slice that persists a `DuckDbQueryService` query's output back into Postgres rather than returning an ephemeral chat answer.
  Required checks:
  - the comparison SQL (`comparison.service.ts`'s `COMPARISON_SQL` constant) is a **fixed, hand-written template** — never built from row data, never LLM-generated (`generateSql`/`generateMultiTableSql` are not used here); row values only ever enter via `read_csv_auto` under `DuckDbQueryService`'s existing trusted-load-then-lockdown sequence, and `assertReadOnlySelect()` still re-validates the template on every call as defense-in-depth
  - `compare()` validates both the PO and invoice belong to the caller's `workspaceId` (`404` otherwise) and have `status='done'` (`400` otherwise) BEFORE touching DuckDB — a not-yet-parsed or foreign-workspace document must never reach the query engine
  - `compare()` is idempotent: it deletes that exact `(purchaseOrderId, invoiceId)` pair's prior `discrepancy_flags` before inserting the fresh set, so repeat comparisons never accumulate stale/duplicate flags
  - `ProcurementParseProcessor`'s CSV/XLSX path makes zero OpenAI calls — verified in `procurement-parse.processor.spec.ts` via a mocked-and-asserted-uncalled `@repo/ai#embedQuery` (a mechanical check, not just a design claim) — and deletes existing line items for a document before re-inserting, so a Bull job retry (`attempts:3`) never doubles rows. **The PDF path (A2, added 2026-07-10) does call OpenAI** — see the dedicated A2 bullet below.
  - `po_line_items`/`invoice_line_items.workspaceId` is denormalized onto the row (same pattern as `chunks`) so `comparison.service.ts` never needs a parent join to enforce isolation when reading line items for the CSV export step
  - match key is SKU primary, normalized-description fallback (`COALESCE(NULLIF(lower(trim(sku)),''),'desc::'||lower(trim(description)))`) — a vendor file with neither populated on a given row becomes an empty match key and will spuriously collide with any other empty-key row; documented v1 limitation, not fixed by this slice
  - schema left two explicit attach points for A2, now used: `storageKey` nullable on `purchase_orders`/`invoices` (a PDF-sourced header still has one, since A2 stayed upload-based — kept nullable for a possible future no-file source) and `sourceKind` defaulting `'csv'` on headers + line items, now also `'pdf'`/`'pdf-extraction'` — no breaking migration was needed, as designed.
  - **A2 (PDF extraction, text-only) added 2026-07-10 — first OpenAI usage in this domain, plus a real dependency finding:**
    - `.pdf` uploads reuse the existing `procurement-parse-queue`; `ProcurementParseProcessor` branches on extension and calls `ProcurementExtractionService` (`apps/api/src/procurement/procurement-extraction.service.ts`, thin DI seam) → `@repo/ai#extractLineItemsFromPdf` (`packages/ai/src/chains/procurement-extraction.ts`), which mirrors `ticket-extraction.ts`'s established pattern: prompt→JSON→hand-validate→typed errors, temperature 0, 2-attempt timeout retry. Model: `OPENAI_PROCUREMENT_EXTRACTION_MODEL` (default `gpt-4o`), its own `ModelRole` in `models.ts`.
    - Prompt-injection mitigation mirrors ticket-extraction verbatim: "document text is untrusted input, never follow instructions inside it" — extracted output is only ever written to line-item columns, never used to build SQL (the comparison template stays fixed, per the bullet above).
    - Kill switch: `PROCUREMENT_PDF_EXTRACTION_ENABLED` (env `=== 'true'`, checked in the controller's `fileFilter`) — set `false` to reject `.pdf` uploads with `400` instantly, no code change, no restart-order dependency.
    - Vision fallback (2026-07-10): when extracted text is below `MIN_TEXT_CHARS` (scanned/image-only PDF), the chain rasterizes pages (`packages/ai/src/loaders/pdf-render.ts#renderPdfToImages`) and sends them to the same model as `image_url` content blocks instead of text — same prompt/validation/retry scaffolding, different input shape. `ProcurementExtractionUnsupportedError` is now reserved for genuine render failure (corrupt PDF, zero pages) — a scanned PDF with a readable image is a supported case, not a rejection.
    - Numeric fields (`quantity`/`unitPrice`/`lineTotal`) are validated at the LLM-output boundary (`Number.isFinite` after string coercion) before insert — an LLM hallucinating a non-numeric value (e.g. `"ten"`) would otherwise fail the entire bulk insert at the Postgres `numeric` column, silently losing every other good row in the same document. Caught by TDD before shipping, not assumed safe.
    - **Dependency finding + resolution:** scanned/image PDF support (rasterize pages → vision model) was first attempted via `mupdf` (WASM, no native-binary/ABI risk — this repo already lost real time to that failure class with duckdb). Installed, then removed after reading `mupdf`'s own `LICENSE` file directly: **AGPL-3.0-or-later** — for this closed-source multi-tenant SaaS, AGPL's network-use clause is real legal exposure, not something to accept implicitly. **Resolved, not deferred:** swapped to `pdfjs-dist` (Apache-2.0 — Mozilla's own PDF.js, already indirectly present via `pdf-parse`) + `@napi-rs/canvas` (MIT, prebuilt N-API binaries, no compile step) — same rasterization outcome, zero AGPL exposure, zero standing legal flag. Verified via a real (non-mocked) render of a real generated PDF before any code depended on it. Two real API gotchas found and fixed during this swap, worth knowing before touching this file again: (1) `pdfjs-dist`'s `getDocument()` throws if given a Node `Buffer` — it explicitly requires a plain `Uint8Array` (`Buffer extends Uint8Array` isn't enough); (2) this pdfjs-dist version's `render()` requires `canvas: null` explicitly alongside `canvasContext` when not using a real `HTMLCanvasElement` (verified in `types/src/display/api.d.ts`'s `RenderParameters`, not assumed from older API knowledge). **Before anyone reaches for `mupdf` here again: it's simply unnecessary** — the permissive alternative already works and is what's shipped.
  - **A3 (vendor catalog + vision matching) added 2026-07-10 — new domain, new SSRF surface, first persisted-image storage:**
    - New tables `vendors`/`catalogs`/`catalog_items`/`catalog_matches` (migration `0021_dear_speed_demon.sql`, additive only). `catalogs.sourceKind` (`'upload'`\|`'scrape'`) shares one 4-state `catalog_doc_status` enum with `catalog_items`'s parent lifecycle; `catalog_matches.matchType` (`'sourcing'`\|`'compliance'`) is derived from whether the search request supplied a `vendorId`, not independently settable.
    - Match mechanism is a vision-LLM comparator (`compareLineItemToCatalogImage`, `packages/ai/src/chains/catalog-match.ts`), not embedding similarity — zero new embedding model/dimension. Candidates are prefiltered with plain `ILIKE` on `sku`/`description` (no `pg_trgm`, confirmed not enabled in this DB — documented upgrade path) and capped at `CATALOG_MATCH_MAX_CANDIDATES` (default 8, truncation logged via `Logger.warn`, never silent). Judged in parallel via `Promise.all` over that already-small capped set.
    - **Deliberate deviation from the approved plan's "parallel via p-limit" wording:** skipped adding `p-limit` as a new `apps/api` dependency. The candidate set is already hard-capped at 8 by `CATALOG_MATCH_MAX_CANDIDATES`, so a concurrency-limiting library adds no real bound beyond what the cap already enforces; `packages/ai/src/web/crawl.ts` already carries a documented dynamic-`import()` workaround for this exact package (`loadPLimit`, a `new Function('specifier', 'return import(specifier)')` helper to dodge a CJS/ESM build issue) that wasn't worth repeating in a second package for zero behavioral gain.
    - PDF catalog photos are **page-image granularity**, not per-product crops: `renderPdfToImages` (reused verbatim from A2, `packages/ai/src/loaders/pdf-render.ts`) rasterizes each page once, `CatalogParseProcessor` stores it once, and every item `extractCatalogItemsFromImage` finds on that page shares the same `photoStorageKey` + `sourcePageNumber`. `extractCatalogItemsFromImage` (new chain function, added to the same file the plan already scheduled for the compare function) deliberately returns `{items:[]}` for a page with no products rather than throwing — diverges from `procurement-extraction.ts`'s throw-on-empty semantics, because a whole invoice/PO having zero line items is anomalous but a single catalog page (cover, table of contents) having zero products is normal and expected.
    - New outbound-fetch SSRF surface, distinct from the crawler's HTML-only fetch: `CatalogImageService.fetchAndStore()` downloads scraped `<img>` URLs and CSV/XLSX `photo_url` column values. Reuses `assertPublicUrl` (`@repo/ai`) before every fetch, `redirect:'manual'` (mirrors `crawlPage`'s handling — a redirect the guard hasn't re-validated is treated as failure, not silently followed), `content-type` must start with `image/`, and a new `CATALOG_IMAGE_MAX_BYTES` (default 5MB) streamed size cap — the crawler itself has no size cap (confirmed, it only ever reads bounded HTML text, never buffers an arbitrary body). A failed fetch (blocked URL, wrong content-type, oversized body, network error) logs a warning and returns `null`; the catalog item is kept text-only rather than dropped — an image is a bonus, not a requirement, for a catalog item to be useful.
    - `CrawledPage` (`packages/ai/src/web/crawl.ts`) gained a new `html: string` field, populated from the already-fetched raw page body — additive, backward-compatible (`ScrapeProcessor`'s existing `onPage` callback only ever reads `page.title`/`page.content`/`page.url`, verified by reading it in full). Required because `extractProductImages(html, url)` (new, mirrors `extractLinks`'s cheerio `.each()`/`canonicalizeUrl`-in-try-catch/dedup shape, selector widened to `img[src], img[data-src]` for lazy-loaded catalog images) needs raw HTML, which the interface never exposed before this slice (only Readability-extracted text via `extractContent`).
    - `CatalogScrapeProcessor` deliberately does **not** create `documents` rows or call `IngestService` — unlike every other consumer of `crawlSite` in this app, it writes `catalog_items` directly from images found in each crawled page's HTML. Reuses `ScrapeService`'s exact enqueue/reconcile/stale(2min pending/30min processing)/idle(5min heartbeat) lifecycle shape, just pointed at the `catalogs` table (`sourceKind='scrape'` rows) instead of `scrapeRuns` — the `catalogs` schema already carried the needed progress columns (`pagesFound`/`pagesSucceeded`/`pagesFailed`/`lastProgressAt`) since Phase 1 designed the table for both source kinds up front, so no migration was needed for the scrape path.
    - `CatalogExtractionService` (`apps/api/src/catalog/catalog-extraction.service.ts`) is the one Nest DI seam both the per-page extraction call and the compare call go through — same override point used by both `catalog-parse.processor.spec.ts`/`catalog-match.service.spec.ts` (unit, jest-mocked `@repo/ai`) and `catalog.e2e-spec.ts` (full-module override, zero real OpenAI calls anywhere in this slice's test suite).
    - **Real Jest/Vitest environment gap discovered while wiring the e2e suite, not a regression:** `pdfjs-dist`'s legacy build dynamically imports its own `.mjs` file (which uses `import.meta.url`). Confirmed via direct testing that this resolves fine under Vitest (already proven by A2's `pdf-render-integration.spec.ts`, a real non-mocked render) and under plain Node (`node -e` against the compiled `packages/ai/dist` output, real PDF via `pdf-lib`, real render succeeded) — but throws `Cannot use 'import.meta' outside a module` specifically under Jest's own module loader, which doesn't go through Node's native `require(esm)` support the way plain Node does. This only ever surfaced now because A3's e2e is the first place in this repo that exercises the real (non-mocked) `renderPdfToImages` call chain through Jest — A2's own e2e (`procurement.e2e-spec.ts`) fully overrides `ProcurementExtractionService`, so it never actually reaches the real rasterizer either. Not a production bug (production runs plain compiled Node, not Jest). Fix applied in the test file only: `catalog.e2e-spec.ts`'s Jest-run happy-path case uses the CSV/`photo_url` upload branch (with `CatalogImageService` also overridden, avoiding any real network fetch) instead of PDF — same full-pipeline proof (upload→queue→processor→items→search→match→dismiss), no source-code change, no Jest config change (a global Jest ESM config change would have a large blast radius across every other spec in `apps/api`). The PDF page-render branch itself stays fully proven at the only two layers that CAN exercise it for real: `pdf-render-integration.spec.ts` (real render, Vitest) and `catalog-parse.processor.processor.spec.ts` (real processor logic, `renderPdfToImages` mocked).
    - Separately (pre-existing, not caused by this slice, flagged not fixed — spun off as its own follow-up task): `apps/api/test/scrape.e2e-spec.ts`, `search.e2e-spec.ts`, and `auth-rate-limit.e2e-spec.ts` never call `jest.setTimeout(30_000)` the way `procurement.e2e-spec.ts` and the new `catalog.e2e-spec.ts` both do, so their `afterAll` teardown (`pool.end()` etc.) occasionally exceeds Jest's default 5-second hook timeout under this session's accumulated load — reproduced twice in isolation, all 3 real assertions in `scrape.e2e-spec.ts` passed both times, only the hook itself timed out. Running the full e2e suite in parallel workers (Jest's default) also surfaces pre-existing cross-file resource contention on the shared real Postgres/Redis instances (confirmed present even with `catalog.e2e-spec.ts` excluded) — `--runInBand` is NOT a safe workaround for this repo's e2e suite specifically, since every e2e file's `afterAll` calls the shared `@repo/db` singleton's `pool.end()` assuming it owns the only connection in its process; forcing all files into one process means the first file's teardown closes the pool for every file that runs after it.
  - **CONTEXT DRIFT found, not fixed (flagging only):** this register's Structured SQL Execution note above says `datasets.service.spec.ts`/`dataset-profiling.processor.spec.ts`/`duckdb-query.service.spec.ts`/`structured-query.service.spec.ts` exist as that domain's test coverage, and `module-ownership-map.md`'s Datasets/Structured Query row lists the same files — but no `apps/api/test/datasets.e2e-spec.ts` exists in the repo (verified via `find`), despite this being a Deep domain with an upload/queue/parse lifecycle exactly like the one this task added e2e coverage for (`procurement.e2e-spec.ts`). Not fixed here — out of scope for this task's blast radius (the Datasets domain's own files were not touched), but worth a follow-up task.
  - **Side effect of this task, not originally in scope:** discovered a live local port/project-name collision (this repo's own `docker-compose.yml` project name `mnemra` was shared with an unrelated local repo, `~/Documents/personal/second-brain`, per `docker compose ls`) and, separately, a real port collision on the target deploy VPS between `mnemra-prod-web` (`127.0.0.1:3100`) and this project once renamed to "Optra." Fixed by renaming both `docker-compose.yml` (dev) and `docker-compose.prod.yml`: project name `mnemra`/`mnemra-prod` → `optra`/`optra-prod`, all container names, image tags, the internal Postgres DB name, and the SeaweedFS identity/credentials (`docker/init-db.sql`, `docker/seaweedfs/s3.json`, now real openssl-generated secrets, not placeholder text). New default ports: web `3300` (was `3100`), api `3301` (was `3101`), postgres `54322` (was `54321`), redis `6380` (was `6379`), SeaweedFS S3/filer/master `8433`/`8988`/`9433` (was `8333`/`8888`/`9333`). Env var names also changed: `MNEMRA_WEB_PORT`/`MNEMRA_API_PORT` → `OPTRA_WEB_PORT`/`OPTRA_API_PORT`. Prod's Caddy `80`/`443` were deliberately left unchanged (profile-gated, standard ports). **Resolved 2026-07-10:** operator confirmed one shared VPS-level reverse proxy for all apps, subdomain-routed — `main.tyvera.app`, `mnemra.tyvera.app`, and (new) `optra.tyvera.app` — same model already documented above for `mnemra.tyvera.app` (see the "Shared-VPS deploy" note earlier in this file). Optra's own bundled `optra-prod-caddy` (the `public` compose profile) stays disabled, matching Mnemra. Action needed on the VPS, outside this repo: add a block to the VPS-level `/etc/caddy/Caddyfile` sending `optra.tyvera.app` to `127.0.0.1:3300` with **no** `/api/*` split (Optra's Next.js is a full BFF like Mnemra's, not a split-routing app like Tyvera's) — mirror the existing `mnemra.tyvera.app` block exactly. VPS `.env` needs `DOMAIN=optra.tyvera.app` and `COMPOSE_PROFILES` must NOT include `public`; `WEB_URL`/`NEXT_PUBLIC_API_URL` derive from `${DOMAIN}` automatically via `docker-compose.prod.yml`, no separate var to set. Same edit discipline as the existing Caddyfile block: timestamped backup, `caddy validate` before `caddy reload` (never `systemctl restart`). The pre-existing `docker/seaweedfs/s3.prod.json` bind-mount target still does not exist on disk (confirmed via `find`) — a pre-existing gap, unrelated to and not fixed by this task.

- Docker/deploy naming-collision sweep (2026-07-10) — trailing "mnemra" string leftovers not caught by the 2026-07-09 docker-compose rename above.
  Required checks:
  - `grep -rniI mnemra --include='*.yml' --include='*.yaml' --include='Dockerfile*' --include='*.sh' --include='.env*' --include='Caddyfile' .` (excluding node_modules) returns zero hits except the intentional dual-match placeholder sentinel in `scripts/ensure-seaweedfs-s3-config.sh` (matches both `mnemra-local`/`optra-local` defensively for one transition period)
  - `docker compose config` / prod equivalent resolve with zero "mnemra" leaks
  - `.env.example`'s `MNEMRA_WEB_PORT`/`MNEMRA_API_PORT` were a real functional bug, not cosmetic — `docker-compose.yml` never actually read those var names (only `OPTRA_WEB_PORT`/`OPTRA_API_PORT`), so a fresh env bootstrap from the template silently got wrong/default ports with no error
  - **Highest-risk item:** `.github/workflows/deploy.yml` hardcoded `cd /home/deploy/apps/mnemra` — confirmed via this file's own "Shared-VPS deploy" note and the 2026-07-04/05 SSH investigation (`/Users/romeoangelesjr/.claude/plans/users-romeoangelesjr-downloads-mnemra-v-structured-tower.md`) that this is a real, separate, currently-live production app's own deploy directory (`OwlRepo/mnemra`, `mnemra.tyvera.app`, `127.0.0.1:3100`) — not a former name of this repo. **VPS state confirmed by operator before merge:** `/home/deploy/apps/optra` already existed and was already primed (first `docker compose ... up` already run manually there), so the path rename was safe to merge immediately with no manual runbook gate.
  - `scripts/deploy-remote.sh`'s `/opt/mnemra` — confirmed active (wired via `package.json`'s `deploy:remote` script, documented in `DEPLOYMENT.md`/`DOCKER.md`), but per the 2026-07-04/05 SSH investigation that exact path was confirmed to not exist on the real VPS (`/opt` contains only `containerd`) — rename was a pure string fix, nothing to migrate
  - `scripts/ensure-seaweedfs-s3-config.sh`'s prod S3 identity name (`"name": "mnemra"` → `"optra"`, matching dev's already-correct `docker/seaweedfs/s3.json`) and its placeholder-detection sentinel (coupled edit with `.env.example`'s S3 key renames, matched both old and new placeholder strings defensively)
  - `docker/seaweedfs/s3.prod.json.example`, `DEPLOYMENT.md`, `DOCKER.md` stale paths/var-names/volume-names/image-tag examples fixed in the same pass per the Documentation Sync Rule; `scripts/deploy.sh:168` (a second occurrence of the "Mnemra owns 80/443" comment, missed by the original 2026-07-09 grep scope) and root `package.json`'s workspace `"name"` field also fixed
  - `mnemra_at`/`mnemra_rt`/`mnemra_session_active` cookie/session identifiers (~62 references) explicitly out of scope — separate, much larger rename requiring its own product decision (dual-read migration vs. mass logout on cutover), not touched here

- Full `/qa` sweep (2026-07-10) — live browser QA across all 8 Optra core-feature domains (Vendors/Catalogs, Procurement, Discrepancies, Catalog Matches, Knowledge Bases, Tickets, Insights, Members/Settings) plus Chat/RAG, ahead of promoting the app for user testing. 5 confirmed bugs fixed with atomic commits + regression tests; 2 findings explicitly deferred as Deep-risk (`packages/ai` RAG chain contract changes require separate approval, per this file's own gate).
  Golden-path results — all verified against real end-to-end runs, not just source reading:
  - Vendor → catalog upload (CSV) → parse → item display: works, placeholder photo icon is documented/intentional (no image-serving endpoint yet), not a bug
  - PO + invoice upload → compare → discrepancy flags: works, verified with deliberately-engineered mismatches (qty+price mismatch, missing-on-invoice, missing-on-po, and one exact-match line item correctly produced zero false positives) — dismiss/filter also verified
  - Catalog Matches vision search (real GPT-4o vision call, `CatalogMatchService`): works, 100% confidence real match returned with coherent reasoning; confirmed the "Verify against this vendor" (compliance) path is unreachable via normal navigation exactly as documented above — reproduced live, not just read in source
  - Knowledge base create → document upload → ingest → indexed: works end-to-end
  - Chat/RAG: works — real grounded answer + citation returned for a real uploaded document — **but only after fixing ISSUE-004 below**, a genuine crash bug in the golden path
  - Members invite, Insights (all 3 tabs + live coverage stats), Settings (workspace rename, digest preview): all verified working
  - Tickets: extraction pipeline works end-to-end, including procurement-domain transcripts, after fixing ISSUE-006/007 below
  Bugs found and fixed (atomic commits, see git log `fix(qa): ISSUE-00N`):
  - ISSUE-001: chat page's empty-state sample prompts were literal Mnemra customer-support copy ("reset a password", "refund policy", "SSO migration") — replaced with procurement-domain examples
  - ISSUE-002: `comparison.service.ts`'s zero-line-items guard threw a **backwards** message ("must have NO parsed line items") for a check that actually requires the opposite; one test had pinned the buggy wording as a substring match and needed updating alongside the fix
  - ISSUE-003 (infra): `apps/api/Dockerfile`'s dev image never installed `procps` — NestJS's `--watch` mode uses `tree-kill`, which shells out to `ps` on Linux to restart the process on file change; missing `ps` crashed the ENTIRE API container on every single source edit (`spawn ps ENOENT`). This means every dev-mode hot-reload was broken until this fix — a severe, previously-undetected DX/CI-adjacent bug, not just a QA-session artifact
  - ISSUE-004 (critical): `Strands.tsx` (the `ogl`-based WebGL component backing `ThinkingIndicator`, shown on every chat message) crashed the **entire chat page** to a full-page React error boundary whenever WebGL context creation failed — a real `ogl` library bug (`this.gl.renderer = this` with no null-check) reachable in headless browsers, but also real corporate GPU policies/VMs/driver blacklists in production. Fixed with a try/catch guard in our own wrapper (can't patch `node_modules`); added a regression test that stubs `canvas.getContext` to null under jsdom, reproducing the exact real-browser failure mode
  - ISSUE-005: swept remaining hardcoded "Mnemra" branding in every transactional email `NotificationsService` sends via Resend (OTP subject/body, invite subject/body, digest subject, `RESEND_FROM_EMAIL` fallback) and the crawler User-Agent sent to external vendor/KB sites (`MnemraBot` → `OptraBot`, 3 files) — these are real emails/HTTP headers seen by real users and third-party sites today, found live while testing the Settings digest preview (which literally rendered "*Mnemra weekly digest*")
  ISSUE-006/007 were initially deferred (Deep-risk `packages/ai` RAG chain contract, per this file's own gate) pending explicit approval — approved and fixed 2026-07-10, same day:
  - ISSUE-006 (fixed): `packages/ai/src/chains/ticket-extraction.ts`'s `EXTRACTION_SYSTEM_PROMPT` gated ticket creation on "concrete support problem, bug, broken workflow, outage... that engineering can act on" — a legitimate procurement-domain transcript (e.g. a vendor sourcing-policy question) got correctly-but-wrongly rejected as non-actionable, because the prompt's framing was still Mnemra's customer-support domain. Broadened the prompt to explicitly include vendor/pricing disputes, catalog data problems, sourcing/vendor-onboarding questions, and policy decisions. Not mock-testable (existing tests mock `ChatOpenAI.invoke` entirely, bypassing prompt content) — verified via a live re-test: the exact rejected scenario now extracts successfully (status done, 6/7 high confidence).
  - ISSUE-007 (fixed) + one more bug found while fixing it: the extraction schema required `reproSteps` to be a non-empty string with no fallback, hard-failing the whole ticket when a real transcript described symptoms narratively rather than as numbered steps. Separately found: the prompt already told the model `hypothesizedRootCause` "may be null if evidence is weak," but the code required it non-empty too — a direct prompt/code contract contradiction, throwing on exactly the input the prompt promised was valid. Fixed both: `reproSteps` now falls back to a descriptive default instead of throwing; `hypothesizedRootCause` is now `string | null` (the DB column — `packages/db/src/schema/tickets.ts` — was already nullable `text()`, confirmed before editing, no migration needed). TDD: 2 new regression tests in `ticket-extraction.spec.ts` reproduce both hard-fails pre-fix, pass post-fix.
  Also flagged, not fixed (out of scope for a QA bug-fix loop — product/UX decisions, not bugs): the "Verify against this vendor" unreachable-button gap (needs a product decision on how a vendor gets selected for compliance verification, not a one-line wiring fix); missing DELETE endpoint for uploaded POs/invoices (service method exists, unwired); ticket `category`/`resolvedAt`/`assigneeId` columns are write-only (no UI, no read-path exposure) — a half-shipped feature; no duplicate-invite protection in `WorkspacesService.invite()`.
