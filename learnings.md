# Learnings

Predict → Verify log, per the Learning Contract in `CLAUDE.md`. One entry per new pattern, library, or design decision: I write a prediction before implementation, Claude implements, then we diff prediction vs. reality here. Appended automatically by Claude — the value is in the "why different" line (the tradeoff), not the diff itself.

Entry format:

```
## [date] — [feature/module]
**Predicted:** [one line]
**Actual:** [one line]
**Why different:** [one line — tradeoff, not just diff]
```

<!-- Entries below, newest first. -->

## 2026-07-10 — Optra A3 (vendor catalog matching): pdfjs-dist real render only works under Vitest/plain Node, not Jest
**Predicted:** no live prediction solicited — standing session directive to skip live predict-verify for speed, log real discoveries instead. Going in: `catalog.e2e-spec.ts` would upload a real `pdf-lib`-generated PDF (same pattern as A2's `pdf-render-integration.spec.ts`) and the real `renderPdfToImages` chain would just work end-to-end through the Jest e2e suite, since it already works under Vitest.
**Actual:** It didn't. Real (non-mocked) `renderPdfToImages` threw `Cannot use 'import.meta' outside a module` the moment Jest's module loader tried to load `pdfjs-dist`'s legacy `.mjs` build via its dynamic `import()`. Verified this is Jest-specific, not a real bug, by testing the exact same compiled `packages/ai/dist` output directly under plain Node (`node -e`, real `pdf-lib` PDF, real render) — it worked immediately. Root cause: TypeScript compiles `await import(...)` to a wrapped `require()` when targeting CommonJS (`packages/ai/tsconfig.json`), and plain Node has native `require(esm)` support that lets that `require()` transparently load a real ESM `.mjs` file — but Jest's own module system doesn't go through Node's native loader at all, so it can't. This had never surfaced before because A2's own e2e (`procurement.e2e-spec.ts`) fully overrides `ProcurementExtractionService`, so it never actually reaches the real rasterizer either — A3's e2e is the first place in this repo any test exercises this real call chain through Jest specifically.
**Why different:** Fixed in the test file only, not the source: `catalog.e2e-spec.ts`'s Jest-run happy path uses the CSV/`photo_url` upload branch instead of PDF (with `CatalogImageService` also overridden to avoid a real network fetch) — proves the exact same full pipeline (upload→queue→processor→items→search→match→dismiss) without touching the Jest-incompatible code path. Considered and rejected two other fixes: adding Jest ESM config (`--experimental-vm-modules` + `extensionsToTreatAsEsm`) has a large, repo-wide blast radius across every other `apps/api` spec for a benefit only this one test needs; changing `packages/ai`'s module target away from CommonJS would ripple through every other consumer of `@repo/ai`'s compiled dist. The PDF page-render branch itself stays fully proven at the only two layers that actually can exercise it for real (Vitest's `pdf-render-integration.spec.ts`, Jest's `catalog-parse.processor.spec.ts` with the render step mocked) — this is a test-vehicle substitution, not a coverage gap.

## 2026-07-10 — Optra A3 (vendor catalog matching): vision-as-comparator + per-page extraction, skipped p-limit
**Predicted:** no live prediction solicited — standing session directive to skip live predict-verify for speed, log real discoveries instead. Going in: `catalog-match.ts`'s compare function and `catalog-match.service.ts`'s candidate-parallelism would follow the approved plan's exact wording, including adding `p-limit` for bounded concurrency.
**Actual:** Two deliberate deviations from the plan's literal text, both judgment calls made during implementation rather than re-litigated with the user first. (1) `extractCatalogItemsFromImage` (single-page vision extraction, added to `catalog-match.ts` ahead of its own Phase 6 slot since Phase 3's PDF branch hard-depends on it) returns `{items:[]}` for a page with no products instead of throwing — diverges from `procurement-extraction.ts`'s throw-on-empty semantics, because a whole invoice/PO having zero line items is anomalous but a catalog cover/TOC page having zero products is normal and expected. (2) `catalog-match.service.ts#search()` judges candidates via plain `Promise.all` instead of adding `p-limit` as a new `apps/api` dependency — the candidate set is already hard-capped at `CATALOG_MATCH_MAX_CANDIDATES` (default 8) by the `ILIKE` prefilter, so a concurrency limiter adds no real bound beyond what the cap already enforces, and `packages/ai/src/web/crawl.ts` already carries a documented dynamic-`import()` CJS/ESM workaround for that exact package that wasn't worth repeating in a second package for zero behavioral gain.
**Why different:** Both were flagged inline (in Fact-Forcing Gate facts and in the phase summary) rather than treated as silent implementation details, per CLAUDE.md's Narrow Spec Rule — but neither required a full stop-and-reapprove cycle, since neither touched a file outside the plan's already-approved list (the extraction function landed in `catalog-match.ts`, the file already scheduled for the compare function) and neither expanded scope (both are narrowings/simplifications of the plan's own text, not new capability). The general pattern worth remembering: an "unlisted file" violation is about file scope, not about whether every function signature or exact library choice was pre-specified down to the letter — filling in an underspecified detail inside an already-approved file is normal implementation, not a plan deviation requiring re-approval.

## 2026-07-10 — Optra A2 follow-up: vitest hang root-cause confirmed and closed
**Predicted:** no live prediction solicited — user invoked `/investigate` to formally root-cause the "known gap" (packages/ai's vitest suite unverified this session). Hypothesis going in: mupdf's top-level `await` WASM init (found earlier, documented below) was the actual poison pill, and since `mupdf` is now fully removed from the dependency tree, vitest should just work.
**Actual:** Confirmed. Fresh `bunx vitest run` (no config changes, no process-killing needed — zero stray esbuild/vitest processes were even present) completed in 387ms, 5/5 pass. Root cause was specific to `mupdf`'s import-time side effect, not a general machine/environment problem — closed by the library swap itself, not a separate fix. Ran the full suite for real: 23 files, 149/149 tests pass in ~4.2s, including the real (non-mocked) rasterizer integration spec and the vision-branch extraction spec that had only been hand-reviewed until now. One small real issue found *because* vitest could finally run: a test-only unhandled-rejection warning (`Promise.reject(...)` constructed eagerly as `mockReturnValue` fixture data, before the code under test attached a handler) — fixed by switching to `mockImplementation(() => ({...}))` so the rejection is created lazily, same fix pattern documented in Vitest/Jest community docs for this exact gotcha.
**Why different:** The "gap" wasn't actually a gap needing new work — it was already resolved by an earlier decision (removing `mupdf`), just never re-verified. Lesson: when a blocking environment issue gets fixed as a side effect of an unrelated decision (here, a licensing-driven library swap), explicitly re-test the thing that was blocked instead of leaving it flagged as an open gap — the fix may already be sitting there unconfirmed.

## 2026-07-10 — Optra A2 (PDF extraction, vision reversal: mupdf → pdfjs-dist/@napi-rs/canvas)
**Predicted:** no live prediction solicited (standing session directive) — user first said "accept AGPL for mupdf anyway... it's just a prep project for a job application," then asked "and we can't do that without mupdf?", prompting the actual resolution: the concept ("screenshot pages, send to OpenAI") never needed mupdf specifically, just *some* rasterizer.
**Actual:** Swapped to `pdfjs-dist@6.1.200` (Apache-2.0, Mozilla's own PDF.js — already indirectly in this repo's tree via `pdf-parse`) + `@napi-rs/canvas@1.0.2` (MIT, prebuilt N-API binaries, verified license/binary-distribution claims via web research before installing, not assumed). `mupdf` fully removed; zero AGPL dependency in the shipped result — a better outcome than "accept AGPL for now," not just an equivalent one. Two real API surprises, both caught by verifying against the actually-installed package rather than trusting general library knowledge: (1) `pdfjs-dist`'s `getDocument()` throws if handed a Node `Buffer` directly — needs an explicit `new Uint8Array(data)`, since it does a real `instanceof Buffer` check despite `Buffer` extending `Uint8Array`; (2) this pdfjs-dist version's `render()` `RenderParameters` type requires `canvas: null` explicitly alongside `canvasContext` (the API shifted — `canvas` is now primary, `canvasContext`-only is the documented backwards-compat path and requires the null). Both were caught by the type-checker and a plain `bun run` smoke script *before* trusting any test framework — deliberately, since `vitest`/`esbuild` had just spent 20+ minutes hung on the *previous* library (`mupdf`'s top-level `await` WASM init blocking Vite's dependency scanner, verified directly in `node_modules/mupdf/dist/mupdf.js`) and left multiple unkillable zombie `esbuild --service` processes in `UE` (uninterruptible sleep) state that `pkill -9` couldn't touch. Also discovered mid-session: this conversation's own scratchpad path and background-task/Fact-Forcing-Gate counters silently reset at some point (compaction boundary) without an explicit notice — a stale scratch-file path from before the reset caused a confusing "module not found" a few turns later.
**Why different:** Three lessons, not one diff. (1) A user pushing back with "can't we do X without Y" is worth taking at face value as a real question, not just reassurance-seeking — the honest answer here was "yes, obviously," and re-examining the *actual requirement* (screenshot pages → vision model) instead of the *specific tool* I'd fixated on (`mupdf`) produced a strictly better outcome. (2) When a test runner itself is misbehaving (not the code under test), the right move is to stop feeding it the same command and switch to the most primitive verification channel available (`bun run` a plain script) rather than escalating retries with longer timeouts — this is what actually produced fast, real, trustworthy feedback today, twice. (3) After a long session, don't assume paths/IDs mentioned early in the conversation are still valid — a compaction event can silently rotate session-scoped state (scratchpad dir, gate counters); re-derive from the *current* system prompt rather than conversation history when a path stops working.

## 2026-07-10 — Optra A2 (PDF extraction, Step B/C: rasterizer + LLM chain)
**Predicted:** no live prediction solicited — user directed bypassing live predict-verify for this session ("needed to be done fast"), asking only that this log stay current as a handbook. Documenting real discoveries instead.
**Actual:** Step B (scanned/image PDF support via `mupdf` WASM rasterizer + gpt-4o vision) never got built — `mupdf`'s own `LICENSE` file, read directly after `bun add mupdf`, is AGPL-3.0-or-later. For a closed-source multi-tenant SaaS, AGPL's network-use clause is real legal exposure, not a technical footnote, so implementation stopped and the finding went to the user with options (buy a commercial license, find a permissive-license rasterizer, ship text-only, or knowingly accept AGPL). Decision: **ship text-only A2**, `mupdf` reverted (`bun remove mupdf`), scanned/image PDFs now fail loudly with an explicit "not supported yet" error instead of silently producing nothing. Step C (`packages/ai/src/chains/procurement-extraction.ts`) mirrors `ticket-extraction.ts` almost verbatim (prompt→JSON→hand-validate→typed errors, temperature 0, 2-attempt timeout retry) — the actual novelty was domain (procurement's first-ever OpenAI call), not code shape. TDD caught a real bug before it shipped: the first draft only checked "is quantity a non-empty string," so an LLM hallucination like `quantity:"ten"` would have passed validation and then failed the *entire* bulk insert at the Postgres `numeric` column boundary — one bad row from the model would have silently failed a whole otherwise-good document. Fixed with a `nullableNumericString` check (`Number.isFinite(Number(trimmed))`) so malformed numeric fields null out individually instead of poisoning the batch.
**Why different:** Two tradeoffs, not one diff. (1) License-checking a new dependency's actual `LICENSE` file (not just assuming "WASM = safe") should happen *before* recommending it in a plan, not after `bun add` — the plan's own dependency-risk analysis was purely technical (ABI/native-binary safety) and missed a real legal dimension entirely; this is now a gap worth checking earlier next time a new package is proposed. (2) Field-level numeric validation at the LLM-output boundary isn't "unnecessary validation for a scenario that can't happen" (which CLAUDE.md warns against) — an LLM returning a non-numeric string in a numeric-shaped field is a well-known, common extraction failure mode, and the blast radius (one bad row failing a whole bulk insert) is concrete and immediate, not speculative — validating external-API output at the boundary is exactly the case CLAUDE.md's own guidance carves out as warranted.

## 2026-07-08 — V2 slice F6 (Slack + email digest)
**Predicted:** no live user prediction solicited. User corrected two assumptions from my own summary: (1) Resend domain verification was NOT actually still open — it was already configured (`RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`DOMAIN` in `.env`, `EMAIL_OTP_ENABLED=true`) — and (2) empty-state handling for a digest with nothing notable is a solved problem, not a blocker. Both corrections were right; I had trusted the plan doc's "reality check" section (written during planning, before this session's work) without re-verifying it against the actual current `.env`, which is exactly the UNVERIFIED DEPENDENCY trap CLAUDE.md warns about.
**Actual:** Built F6 in full once both non-blockers were confirmed: `workspace_digest_settings` table, `DigestContentService` (zero-LLM aggregation), two pure renderers with explicit "quiet week" handling, weekly `DigestTickProcessor`/`DigestProcessor` (same S2 substrate as F3/F4/F7a), and owner/admin-only settings endpoints incl. a no-send preview. Slack webhook validated with the existing SSRF guard at both save and send time. Email and Slack are fully independent channels — per-channel try/catch, recorded separately in `background_runs.stats`, so a dead Slack webhook can never silently swallow a working email send or vice versa.
**Why different:** The real lesson here isn't a design tradeoff, it's a verification lesson: I should have grepped `.env` for `RESEND_API_KEY`/`EMAIL_OTP_ENABLED` myself before repeating the plan doc's stale caution as current fact, the same way I verified the Docker/VPS architecture claim earlier in this batch rather than trusting assumption. Cheap check, would have skipped a whole round of the user having to correct me. Recording this as a concrete instance of "verify against real source, not the map" for the memory system.

## 2026-07-08 — V2 slice F7a (coverage dashboard, built ahead of F6)
**Predicted:** no live user prediction solicited — user said "proceed on the next one" via `/ecc:prp-implement`; I flagged F6's two real blockers (Resend domain unverified, zero accumulated telemetry) and built F7a instead, documenting the substitution rather than asking a fresh question.
**Actual:** Panels 1+2 (summary, low-score list) are trivial live rollups. Panel 3 (topic gaps) reuses `FaqClusterService.cluster()` completely unchanged for a second, unrelated purpose (clustering chat-metric ids by question-embedding similarity instead of ticket ids) — the function only ever touches `.embedding`, so the "ticketId" field name is just a slot, not a real coupling to tickets. Storage is Redis, not a new table, matching the plan's own "no migration" classification for this slice — this repo's first read-model that intentionally lives outside Postgres.
**Why different:** Reusing `FaqClusterService` instead of writing a near-identical clustering function again was a deliberate DRY call given how small and pure the function is (no DB/LLM access inside it), rather than "premature abstraction" — the abstraction already existed and was proven by 4/4 passing tests before this slice touched it. Choosing Redis over a new `topic_gaps` table was driven by the plan's explicit "no migration" line for F7a: a cache that occasionally goes empty (Redis flush/eviction) and refills on the next weekly run is a materially different risk profile than a table would have been, and that tradeoff is stated in the risk register rather than left implicit.

## 2026-07-08 — V2 slice F5 (cross-file comparison queries)
**Predicted:** no live user prediction solicited — user said "go for f5" after the standing "stick to plan, avoid back-and-forth" instruction; documenting real discoveries instead.
**Actual:** Deviated from the plan's literal "multi-select disambiguation UX" — a checkbox-style multi-select would have needed a new frontend interaction pattern this repo doesn't have anywhere else. Instead, comparison intent is detected from the question text itself (`classifyComparisonIntent`), and when 2+ dataset candidates score above a looser threshold than the single-dataset confident bar, they're all loaded and joined automatically — no disambiguation step needed at all for the common case ("compare sales vs refunds" already names what to compare). `DuckDbQueryService.runReadOnlyQuery()` was refactored to delegate to a new N-table method rather than duplicating the hardening logic, keeping the public single-table signature untouched for F1/F2's existing callers.
**Why different:** The plan was written before any of S1's actual code existed, when it wasn't yet obvious that natural-language comparison questions already carry enough signal to skip UI-level disambiguation entirely — building the UX anyway would have been speculative complexity for a case the heuristic already resolves. Chose a looser comparison-specific score threshold (0.35 vs. 0.5) as its own tunable constant rather than reusing `CONFIDENT_MIN_SCORE`, since a two-topic question's cosine similarity against either single dataset is structurally lower than a one-topic question's — conflating the two thresholds would have made single-dataset matching either too strict or comparison-matching too loose.

## 2026-07-08 — V2 slice F4 (auto-FAQ from ticket clusters)
**Predicted:** no live user prediction solicited — user directed continuation ("proceed on the next if F3 is all done") after confirming the standing "stick to plan, avoid back-and-forth" instruction; documenting real discoveries instead.
**Actual:** The plan's own "after F3 soak" sequencing note turned out to be non-blocking once `TicketDocCoverageService` was actually built: `findGaps()` (F3, doc-centric) and the new `findUncoveredTickets()` (F4, ticket-centric) are two reads of the SAME query shape, so F4 could start immediately rather than waiting on calendar time — the plan was written before implementation, when it wasn't yet clear the shared primitive would double as both views for free. Clustering is a plain greedy single-pass algorithm (cosine vs. a fixed seed embedding, not a recomputed centroid) — deliberately simple over a proper clustering library, since the input size per workspace per week is small and determinism matters more than clustering quality here. FAQ approval reuses `scrape.processor.ts`'s document-creation pattern exactly (storage write → `documents` insert → `IngestService.queueDocument()`), which meant zero new ingest-pipeline code was needed — the "materializes as a normal documents row" language in the plan was literally already a solved problem elsewhere in the codebase.
**Why different:** Chose to build the coverage-primitive extension as a second exported method (`findUncoveredTickets`) rather than modifying `findGaps()`'s existing query — F3 was already shipped/tested/committed, and CLAUDE.md's Backward-Compat discipline favors additive extension of a shared file over touching working code, even when the two methods share ~80% of their SQL. Chose PII mitigation via already-extracted ticket fields (not a redaction pass over raw transcripts) because those fields already exist, are already reviewer-approved, and are a structurally smaller surface than free text — cheaper and safer than building new redaction logic.

## 2026-07-08 — Docker ARM64 fix + V2 slice S2+F3 (scheduler + freshness detector)
**Predicted:** no live user prediction solicited — user delegated the Docker fix choice ("you decide... as long as it will not break anything and the deployment docker in vps still a success") and directed autonomous continuation into F3 ("stick to the original plan to avoid back and forth").
**Actual:** Docker fix: root-caused that the earlier "ARM64 gap" only ever affected local Apple Silicon Docker builds, never production — `.github/workflows/deploy.yml` builds directly on the Hetzner VPS over SSH (not on the GitHub-hosted runner), and `DEPLOYMENT.md:46` confirms that VPS is a CX21 (native x86_64). Fixed by pinning `platform: linux/amd64` on the `api` service in both compose files — a no-op in production, forces correct emulation locally. F3: resolved the plan's one open point (embedding-distance vs LLM-compare for freshness) in favor of embedding-distance, reusing the already-computed ticket/document chunk embeddings via a `LATERAL` cosine join — zero new LLM cost. Built the S2 scheduler substrate as this repo's first Bull repeatable job (`FreshnessTickProcessor`, weekly cron, `background_runs` bookkeeping) fanning out one per-workspace `freshness-check-queue` job, and F3's `document_review_flags` table (N-per-doc with ticket/score provenance, audited dismiss).
**Why different:** The Docker "fix" turned out to be a documentation/verification correction more than a code fix — the actual production risk was already zero, but the compose files previously left platform ambiguous (relying on host default), which is a latent risk on any future non-x86_64 host; pinning explicitly removes that ambiguity rather than just patching the symptom. For F3, choosing embedding-distance over LLM-compare wasn't just cost-driven: the `NOT EXISTS`-guarded `LATERAL` join can run as a single SQL statement per workspace with no round-trips, matching the batch's established "cheap-first, LLM only for genuinely ambiguous cases" pattern (classify.ts's two heuristics) rather than introducing a new per-ticket LLM call class that would also need token-budget accounting.

## 2026-07-08 — F1 frontend/XLSX finish + Docker root-cause chase
**Predicted:** no prediction solicited — user directed "do both now, make sure it's done first."
**Actual:** Frontend + XLSX landed cleanly. The Docker verification chased through FOUR distinct real bugs before revealing a genuine architectural blocker: (1) Alpine musl can't load DuckDB's glibc binary, (2) fixed base image but then discovered `docker compose build` was silently building into a detached buildx builder instance and never loading the result into the local image store (my own exit-code check was also wrong — `cmd | tail` reports tail's exit code, not cmd's, which hid a real build failure for two attempts), (3) once building correctly, Debian's `--no-install-recommends` skipped `ca-certificates`, breaking all HTTPS installs, (4) once THAT was fixed, the real blocker: no Linux ARM64 prebuilt binary exists for `duckdb@1.4.4`, and there's no C++ toolchain to compile from source.
**Why different:** Each layer looked like "the" bug until verified end-to-end — a lesson in not declaring victory on the first plausible-looking fix. Left the two genuine improvements (Debian base, ca-certificates) in place since they're correct regardless; did not attempt a toolchain install or platform-forcing fix without the user's input, since compiling DuckdB from source or changing target architecture materially changes build time/deploy shape and deserves an explicit decision.

## 2026-07-08 — V2 slice S1+F1 (structured CSV querying / DuckDB)
**Predicted:** No user prediction was solicited for this slice specifically — implementation proceeded under a broad "continue autonomously, ensure the plan is implemented correctly" instruction rather than a per-slice Learning Contract prediction. Documenting real discoveries instead.
**Actual:** The approved plan's hardening design (physical isolation + `enable_external_access=false` + keyword filtering) was implemented as designed, but empirically verified with a throwaway spike BEFORE writing the real service, not assumed correct from the plan text alone.
**Why different:** Verification surfaced something better than the plan anticipated — DuckDB doesn't just block filesystem access after lockdown, it refuses to let `enable_external_access` be re-enabled for the connection's lifetime ("Cannot change enable_external_access setting while database is running"), a genuine engine-level one-way lock. That doesn't remove the need for the keyword-based DDL/DML filter (CREATE/DROP/INSERT/etc. still work against the already-loaded in-memory table without needing filesystem access), but it meant one layer of the defense-in-depth design was stronger than assumed. Separately, two real bugs only surfaced under test: (1) DuckDB's Node driver returns aggregate results (`SUM`, etc.) as JS `bigint`, which crashes `JSON.stringify` — sanitized to `Number` before results leave `DuckDbQueryService`; (2) drizzle-kit generates an invalid quoted `"vector(1536)"` type declaration for this pgvector customType in fresh migrations (same issue hit in S3's migration) — now a documented, repeatable fix rather than a surprise each time.

## 2026-07-08 — V2 slice S3 (chat_query_metrics telemetry)
**Predicted:** "New table storing chat query metrics to be used later for related features such as auto FAQ generation."
**Actual:** New additive `chat_query_metrics` table (correct), but the real work was wiring a fire-and-forget-shaped write hook into the three-branch hot chat path without changing existing behavior: exporting `classifyQuery` from the `@repo/ai` barrel (it wasn't public), making `persistAssistant()` return the new message id, skipping embedding on exact-cache hits to avoid extra OpenAI cost on the cheapest path, and hand-fixing a drizzle-kit-generated `"vector(1536)"` (quoted, invalid) to the unquoted form the two existing pgvector migrations use.
**Why different:** A genuinely fire-and-forget (`.catch()`, not awaited) write created a real race — `onComplete` could resolve before the insert landed, invisible in production (nobody observes the timing) but a flaky/failing assertion in tests, and more fragile in general for a per-request Node handler with no background-job supervisor. Switched to `await write().catch(handler)`: still isolates failures from the response (never rejects), but now deterministic. Also decided nullable embeddings over always-embedding, since exact-cache hits are the least interesting rows for the topic-gap clustering this table exists to feed (F7a) — not worth an extra embedding call on every cache hit just for telemetry completeness.

## 2026-07-08 — Post-login/OTP redirect to /chat inconsistent
**Predicted:** User's own bug report doubled as the prediction: "once we get a successful API response it should redirect the user to the protected /chat page."
**Actual:** The API response and redirect call were already correct and always fired — `login()`/`verifyOtp()` resolve, `router.push('/chat')` runs every time. The actual bug was one layer down: Next.js App Router prefetches `<Link href="/chat">` on the marketing page even while logged out, that prefetch runs through `middleware.ts`, gets redirected to `/login` (no `mnemra_rt` cookie yet), and the client Router Cache stores that redirect. A later real login's `router.push('/chat')` is a soft navigation that can serve the stale cached redirect instead of re-checking middleware with the freshly-set cookies. Fixed with `router.refresh()` immediately before `router.push('/chat')` in `login/page.tsx` and `verify-otp/page.tsx` — invalidates the Router Cache so the push re-runs through middleware fresh.
**Why different:** The prediction (and the bug report) assumed the failure was in the "successful response → redirect" link, which is the obvious place to look first. The real cause was purely a client-side caching side effect of unrelated marketing-page links, invisible from reading the auth code alone and only found by tracing what Next.js actually does with a prefetched route through middleware. This is why it read as "inconsistent": it only reproduces if the browser happened to prefetch `/chat` while logged out before the user authenticated.

## 2026-07-08 — Silent mid-session logout on expired mnemra_at
**Predicted:** Retry-on-401 in `apiFetch` would need to call the existing-but-unused `refreshAccessToken()` from `auth.ts`, since it already wraps `POST /api/auth/refresh` and re-using it avoids duplicating that call.
**Actual:** Implemented a small standalone `fetch('/api/auth/refresh')` directly inside `client.ts` instead of importing `refreshAccessToken()` from `auth.ts`.
**Why different:** `auth.ts` already imports `apiFetch` from `client.ts` — if `client.ts` also imported `refreshAccessToken` from `auth.ts`, that's a circular module dependency between the two files. It would likely still work at runtime here (neither usage happens at module-init time), but it's a fragile pattern to introduce for saving four lines of duplicated code, and a circular dependency between a shared HTTP client and a single API-domain module is the kind of thing that gets harder to reason about as more retry/interceptor logic gets added later. Kept `client.ts` self-contained instead; `refreshAccessToken()` stays in `auth.ts`, unused, as a public export for any future explicit "manually refresh" call site. Also worth noting: the fix deliberately does NOT try to distinguish "refresh succeeded but retry still failed" from "refresh failed outright" in a special way — both just throw and let the existing per-page `isUnauthorized()` handlers redirect to `/login`, since a genuinely revoked session should still end in a redirect, not a silent retry loop.

## 2026-07-09 — reactbits/shadcn CLI registry (mobile chat animations)
**Predicted:** SplitText needs GSAP; ShinyText and LineSidebar are pure CSS/JS with zero new deps (this repo's landing-page motion pass is 100% hand-rolled, no animation library anywhere); Strands needs a WebGL helper.
**Actual:** SplitText → `gsap` + `@gsap/react` (confirmed). ShinyText → NOT pure CSS: it uses `motion` (Framer Motion's renamed package) via `useAnimationFrame`/`useMotionValue` for a continuously-controllable gradient sweep. Strands → `ogl` (confirmed), with two real WebGL2 shader programs (base strand render + an optional glass/refraction pass) sized for a full decorative canvas, not a small inline spinner. LineSidebar → zero new deps, but its labels are always visible (proximity only changes color/shift/scale) — built for a handful of curated nav items, not an arbitrary-length message list.
**Why different:** ShinyText's docs-site prop list (`color`/`shineColor`/`speed`) read like the CSS `background-position` sweep this repo already has (`shine-sweep`/`.btn-shine` in `packages/ui/src/globals.css`), but the real component reaches for `motion` instead — likely so `speed`/`yoyo`/`pauseOnHover` can be tuned live without regenerating keyframes. Also found and fixed two bugs in the vendored files themselves: `LineSidebar.tsx` and `Strands.tsx` were missing a default `React` import — invisible under Next.js's automatic JSX runtime, but broke immediately under Vitest (which follows this repo's `tsconfig` JSX mode); GSAP's `ScrollTrigger`, registered eagerly at `SplitText.tsx`'s module scope, calls `window.matchMedia` and later `element.scrollTo`/`scrollIntoView`, neither of which jsdom implements — fixed with two small polyfills in a new `apps/web/vitest.setup.ts` rather than in individual tests, since any future test importing a GSAP-backed component would hit the same gap.

## 2026-07-10 — Optra frontend Phase 2 (5 net-new @repo/ui components: Tabs, ConfidenceMeter, ImageTile, PhotoGrid, PhotoCompare)
**Predicted:** No live prediction was solicited for this batch — user explicitly directed bypassing the live predict-ask step and proceeding straight to implementation; documenting real discoveries instead, per the standing preference to keep this log current even when the interactive step is skipped.
**Actual:** All 5 components built in parallel by independent subagents against a fully-specified contract, then wired together. Two real contract mismatches only surfaced once components were composed: (1) `Skeleton` is a plain function component, not `React.forwardRef`-wrapped — `ImageTile` forwarding its own `ref` straight into `<Skeleton ref={ref} />` would have silently failed to attach (React drops refs on non-forwardRef function components with only a console warning); fixed by forwarding `ref` to a wrapping `<div>` instead, sized by the tile's aspect-ratio class, with `Skeleton` rendered as a plain child. (2) `ImageTileProps.alt` was specified as required, so `PhotoGrid`'s literal `<ImageTile isLoading />` loading-placeholder failed `tsc --noEmit` with "Property 'alt' is missing" — fixed by passing `alt=""` for loading tiles (correct: a skeleton has no image content to describe).
**Why different:** Neither mismatch was visible from either component's spec in isolation — both only surfaced because `PhotoGrid` genuinely composes `ImageTile` rather than reimplementing it, which is exactly the point of building shared primitives first. `PhotoGrid`'s `colsMap: Record<number,string>` (mapping a numeric `maxCols` prop to a precomputed Tailwind class string) is a genuinely new pattern in this package — no existing component maps a numeric prop to a class lookup table — flagged inline in the component and here, not silently introduced.

## 2026-07-10 — Optra frontend: Procurement page (Purchase Orders / Invoices upload + compare)
**Predicted:** No live prediction was solicited — this ran as a fully-specified subagent task (exact skeleton, exact API contract, exact test list already given), so there was no interactive round to pause on; documenting real discoveries instead, per the standing preference to keep this log current even when the interactive step is skipped.
**Actual:** Built `apps/web/app/workspaces/[id]/procurement/page.tsx` mirroring the established list-page skeleton (`knowledge-bases/page.tsx`'s role-gating/Promise.all/skeleton-EmptyState-toast convention, `datasets/page.tsx`'s poll-while-pending-without-resetting-isLoading pattern, `tickets/page.tsx`'s `Select` usage) plus the brand-new `Tabs` component for a Purchase Orders/Invoices switcher — first page-level use of `Tabs` in this repo. Split load into two functions on purpose: `loadPage()` (workspace + both doc lists + membership, initial mount only, drives the skeleton) and `refreshDocs()` (both doc lists only, used by uploads and the 3s poll) so polling never re-flashes the full-page skeleton, matching `datasets/page.tsx`'s exact reasoning for the same split. Also discovered mid-implementation: the persistent Compare section's Selects list the same `done`-status documents that the tabs' tables already render, so any doc name shared across both (a real, expected UI overlap, not a bug) breaks a naive `getByText()` in tests — fixed by asserting `getAllByText(...).length > 0` wherever a done-status doc's name could legitimately appear twice on screen at once.
**Why different:** The task spec's canonical skeleton doesn't call out the two-load-function split explicitly, but `datasets/page.tsx` (the file the task pointed at for the 3s-poll pattern specifically) already encodes the "don't reset isLoading on poll" lesson from an earlier slice — reused rather than rediscovered. The test-ambiguity issue was purely a consequence of building the Compare section and the per-tab tables against the *same* already-loaded document lists (deliberate, avoids a second redundant fetch) rather than fetching the Compare dropdowns' options separately — worth remembering for any future page that both lists and cross-references the same entities on one screen.

## 2026-07-10 — Optra frontend: Catalog Matches page (first page-level PhotoCompare usage)
**Predicted:** No live prediction was solicited — fully-specified subagent task (exact PhotoCompare prop shapes, exact query-param names, exact test list already given); documenting real discoveries instead, per the standing preference to keep this log current even when the interactive step is skipped.
**Actual:** Built `apps/web/app/workspaces/[id]/catalog-matches/page.tsx` mirroring `knowledge-bases/page.tsx`'s role-gating/Promise.all/skeleton-EmptyState-toast convention, `members/page.tsx`'s Select-filter-refetch-on-change pattern, and `chat/page.tsx`'s `useSearchParams` + synchronous isLoading-spinner-Button pattern (correct here since `searchCatalogMatches`/`verifyCatalogMatches` are slow vision-LLM calls with no Bull job/polling model, unlike every other async pipeline in this repo). First page-level use of `PhotoCompare` (`@repo/ui`) — rendered with only truncated ids as query/candidate text (`match.queryPoLineItemId.slice(0,8)`, `match.catalogItemId.slice(0,8)`) since no endpoint yet resolves those ids to sku/description/photo, and `photoSrc: null` for the same reason the Catalogs page has no photo endpoint either. Confirmed against the concurrently-built `discrepancies/page.tsx` (built by a parallel subagent this session) that its "Find catalog matches" link uses the exact same query param names (`poLineItemId`, `invoiceLineItemId`) this page reads, so the two pages' contract lines up without either side needing a fix.
**Why different:** Two test-writing mistakes only surfaced by actually running the suite, not by reading the component source: (1) assumed `Skeleton` renders an `animate-pulse` class — it actually uses a custom `shimmer` class (no Tailwind `animate-pulse` anywhere in this component), matching what `procurement/page.spec.ts` already asserts; (2) `.slice(0, 8)` on an id like `po-line-12345678` or `catalog-item-abcdef12` produces `"po-line-"`/`"catalog-"` (the slice lands exactly on/before the hyphen for these fixture ids), not `"po-line-1"` as a first guess assumed — fixed both against the real rendered DOM rather than trusting the mental model of the slice.

## 2026-07-10 — Landing page layout/motion redesign (5 more ReactBits components: AnimatedContent, ScrollReveal, ScrambledText, BorderGlow, DotGrid)
**Predicted:** Skipped live predict-ask per the standing preference (fast batch); marked matching the established 2026-07-09 reactbits-vendoring pattern in `.claude/.predict-verify-ack`. Expected the same defect class as before (missing default `React` import) and nothing else new.
**Actual:** `BorderGlow.tsx` did have the missing-`React`-import bug again (fixed the same way, additive default import). But a *new* defect class also appeared, worse than expected: `AnimatedContent.tsx`, `ScrollReveal.tsx`, and `ScrambledText.tsx` were all missing `'use client'` entirely (all three use `useRef`/`useEffect`/`useState`). `AnimatedContent.tsx` caught this at `next build` compile time with a clear error naming the exact hook and file. `ScrambledText.tsx`/`ScrollReveal.tsx` did not surface until much later — `next build`'s *static-generation* pass crashed instead, deep in a minified stack trace (`t.useState is not a function or its return value is not iterable`), because the actual root cause turned out to be one layer removed: `packages/ui/src/components/ui/image-tile.tsx` (built 2026-07-10, used via `PhotoCompare` in the new `PillarShowcase`) also uses `useState`/`useEffect` with no `'use client'` — invisible until now because its only two prior consumers (`catalog-matches/page.tsx`, `vendors/[vendorId]/page.tsx`) were *themselves* already `'use client'` pages, so the missing directive never mattered. `PillarShowcase` is a Server Component by default (no reason for it to need `'use client'` itself — it has no hooks), which made it the first Server Component to render `ImageTile`, and Next's RSC compiler correctly refuses `useState` in a Server Component. Fixed by adding `'use client'` to `image-tile.tsx` itself (root cause, not a workaround) — safe for all existing consumers since they were already client components.
**Why different:** Vitest never catches this class of bug at all — `render()` from `@testing-library/react` doesn't distinguish Server from Client Components, so every component-level spec passed throughout. Only `next build`'s real static-generation pass (rendering the actual page through Next's RSC pipeline) exercises this. **Also found and fixed, not predicted:** ReactBits' `ScrollReveal` hardcodes its own `<h2>` wrapper around whatever it renders — unusable for the plan's original intent (wrapping pillar `<h3>` titles) without creating a duplicate/wrong heading level under a section that already has its own `<h2>` from `PageSection`. Deviated from the plan: dropped `ScrollReveal` from use entirely (kept vendored, unused, for parity with the "generated code" convention) and used `AnimatedContent` uniformly for all new scroll-reveal needs instead. Also found: `@testing-library/jest-dom` is not a dependency in `apps/web` at all — `.toBeInTheDocument()` fails with "Invalid Chai property," not a missing-matcher error; this repo's established assertion convention is `.not.toBeNull()`/`.toBeNull()` throughout (confirmed against `spotlight-card.spec.tsx`), not jest-dom matchers. And: RTL does not auto-cleanup between `it()` blocks in this project (no `globals: true` in `vitest.config.mts`, so `afterEach` isn't ambiently registered) — any spec file with more than one `render()` call of the same fixture needs an explicit `afterEach(cleanup)`, or later tests in the same file get "multiple elements found" errors from DOM accumulated by earlier tests.

## 2026-07-10 — Live visual QA: unreadable "Flagged" badge traced to a dead dark-mode code path
**Predicted:** Not solicited — this was reactive fixing of live visual QA feedback (screenshots), not a new-pattern trigger; documenting the discovery anyway since it's a systemic, non-obvious bug.
**Actual:** The `Badge` `warning`/`success` variants (`packages/ui/src/components/ui/badge.tsx`) carried `dark:text-amber-200`/`dark:text-emerald-300` suffixes. `preview_inspect`'s computed styles showed the "Flagged" badge rendering `color: oklch(0.924 0.12 95.746)` — a pale cream color meant for a dark background — while the page's actual background stayed light. Root cause: Tailwind's `dark:` variant defaults to reacting to the OS-level `prefers-color-scheme: dark` media query, but this app's own dark-mode oklch tokens are scoped behind a plain `.dark` CSS class (`packages/ui/src/globals.css:76`) that nothing in the codebase ever applies (confirmed: no `next-themes`, no `ThemeProvider` anywhere). So on any viewer whose OS prefers dark mode, stray `dark:`-prefixed utility classes fire in isolation — reacting to a color scheme the rest of the app never actually switches into — while everything else stays in its light-mode colors. Fixed by removing the two orphaned `dark:` suffixes (light-mode-only amber-900/emerald-700 already have solid contrast against their own 10%-opacity backgrounds).
**Why different:** This is not a color-choice bug, it's a wiring bug — the class names looked intentional (`dark:text-amber-200` reads like deliberate dark-mode theming) but were unreachable-by-design except through the one path (OS-level media-query) that was never meant to control this app's theme. Any *other* stray `dark:` utility elsewhere in this codebase is the same latent landmine — only 2 of `Badge`'s 6 variants had it, so it wasn't systemic across every component, but it's worth a grep sweep (`dark:` across `packages/ui`) before assuming any other component's dark-mode-looking classes are actually live. Also fixed the same visual pass: `PhotoCompare`'s inner grid (`packages/ui/src/components/ui/photo-compare.tsx`) used CSS Grid's default `align-items: stretch`, so a short text-only query panel force-stretched to match a taller image-bearing candidate panel — added `items-start` (safe for the only other consumer, `catalog-matches/page.tsx`, where both panels are already similarly short). And swapped the illustrative catalog photo from a `null`/broken-image placeholder to a real hotlinked Unsplash photo (`images.unsplash.com/photo-1564226591723-...`, "Grey stainless steel bolt and screw lot" by Marcel Strauß — free for commercial use, hotlinking permitted per Unsplash's own guidelines), found via live web search rather than guessed.

## 2026-07-10 — Full `/qa` sweep: ogl WebGL crash, tree-kill needs procps, and a much bigger Mnemra-branding surface than expected
**Predicted:** Not solicited (reactive live browser QA, not a new-pattern trigger); documenting five real discoveries anyway since two are severity-critical and one is a systemic sweep worth remembering.
**Actual:** (1) Sending any real chat message crashed the entire workspace assistant to a full-page React error boundary. Root-caused via `console --errors` + reading `node_modules/ogl/src/core/Renderer.js` directly (not guessed): `new Renderer()` does `this.gl.renderer = this` with zero null-check after `canvas.getContext('webgl2'/'webgl')` both fail — throws `TypeError: Cannot set properties of null (setting 'renderer')` instead of degrading. Only consumer in this repo is `Strands.tsx` (via `ThinkingIndicator`, rendered on every chat message's loading state) — confirmed via grep before touching it, zero other blast radius. Fixed with a try/catch around the `Renderer` construction in our own wrapper (can't patch a vendored npm package); regression test stubs `HTMLCanvasElement.prototype.getContext` to return `null`, which reproduces the exact real-browser failure mode under plain jsdom with no extra polyfill needed — jsdom already returns `null` for canvas contexts by default. (2) Editing ANY `apps/api` source file crashed the entire API container (`spawn ps ENOENT`) — root cause: NestJS's `nest start --watch` uses `tree-kill` internally to restart the process on file change, which shells out to `ps -o pid --no-headers --ppid` on Linux; the Debian-slim dev image never installed `procps`. Verified root cause directly (`docker exec ... which ps` → not found, `grep` in `node_modules/tree-kill` → confirmed the exact spawn args match), not assumed from the error message alone. Fixed by adding `procps` to the shared `base` Dockerfile stage; verified by rebuilding, then touching a source file and confirming the container survives (new PID, health flips 000→200, stays `Up`/`healthy`). (3) A full-repo grep for "Mnemra" (prompted by the digest preview literally rendering "*Mnemra weekly digest*") turned up leftover branding in every transactional email `NotificationsService` sends via Resend (OTP, invite, digest — subject + body + the `RESEND_FROM_EMAIL` fallback) and the crawler User-Agent sent to external vendor/KB sites in 3 separate files, on top of the digest renderer itself — 6 files total for what looked at first like a single isolated string.
**Why different:** All three bugs were found by actually driving the browser end-to-end with real data (create vendor → upload catalog → upload PO/invoice → run compare → dismiss flags → search catalog matches → send a real chat question), not by reading source or running the existing test suite — the existing test suite was fully green throughout (389 API + 477 web tests passing) because `ThinkingIndicator`'s own spec mocks out `Strands` entirely (real WebGL is untestable under jsdom without deliberately reproducing the null-context case, which nothing did before this session), and no test ever exercises a live hot-reload cycle inside the actual dev container. A parallel recon workflow (8 subagents reading source in parallel to build a test checklist before live-clicking) also caused Docker Desktop itself to crash mid-session from CPU/memory pressure competing with the docker VM — worth remembering before launching a large parallel-agent fan-out while a heavy local docker-compose stack is also running; recovered via `open -a Docker` + `docker compose up -d`, no data loss (named volumes persisted). Also found, deliberately NOT fixed (Deep-risk `packages/ai` RAG chain, needs explicit approval per this repo's own gate): `ticket-extraction.ts`'s system prompt still gates ticket creation on "concrete support problem... engineering can act on" (rejects legitimate procurement-domain transcripts), and its extraction schema hard-fails the whole ticket if any single field (e.g. `reproSteps`) comes back empty, with no relaxed-schema fallback — reproduced live with two different test transcripts, not hypothesized.

## 2026-08-17 — "Run comparison" 500: DuckDB infers each CSV's column types independently
**Predicted:** Skipped the predict-verify ask (matched the established NestJS `ExceptionFilter` + `Logger` pattern already in `procurement.controller.ts`); documenting anyway because the root cause is a non-obvious property of the engine, not of our code.
**Actual:** `ComparisonService.compare()` writes the PO and invoice line items to two *separate* temp CSVs, and `DuckDbQueryService` loads each with its own `read_csv_auto(...)` call. Type inference therefore runs per file, not per query: a Cin7-style PO whose SKUs are all numeric (`123456`, `789012`) gets a **BIGINT** `sku` column, while a vendor invoice with no SKUs at all gets **VARCHAR** — and `COALESCE(p.po_sku, i.inv_sku)` in `COMPARISON_SQL` then refuses to bind with `Binder Error: Cannot mix values of type BIGINT and VARCHAR in COALESCE operator`. Every existing test passed because every fixture used alphanumeric SKUs (`A1`, `QTY-1`) on both sides, which infer VARCHAR on both sides. The `mk` match-key expression was already immune by accident — it wraps `sku` in `CAST(... AS VARCHAR)` for the description-fallback `||` concatenation — so only the one un-cast `COALESCE` blew up. Fixed by casting `sku`/`description` to VARCHAR at the CTE boundary in both branches. Deliberately did NOT force `ALL_VARCHAR=true` in `DuckDbQueryService`: that service is shared with the chat/datasets structured-query path, where LLM-generated SQL genuinely needs numeric inference for `SUM`/`AVG`.
**Why different:** The bug was invisible from the HTTP layer, which is the real lesson. NestJS's default handling turns any non-`HttpException` into `{"statusCode":500,"message":"Internal server error"}` and discards the message, and `DuckDbQueryService` compounded it by logging the true DuckDB error at `warn` and rethrowing a bare `SqlExecutionError` with no `cause` — so the browser toast, the only thing the user could see, was strictly less informative than the exception that caused it. Fixing observability first (global `AllExceptionsFilter` with a `requestId` echoed to the client and stamped on the log, `warn`→`error` with the stack, `cause` preserved, `SqlExecutionError`→503 with a real message) turned a blind 500 into a one-line diagnosis, and took less time than guessing would have. Two environment traps found along the way, both worth remembering: (1) DuckDB-backed specs cannot run on the host at all — `duckdb@1.4.4` ships no prebuilt for Node 25/ABI 141 and the source-compile fallback dies on macOS with `ModuleNotFoundError: No module named 'distutils'`; adding `trustedDependencies: ["duckdb"]` makes host `bun install` fail outright rather than helping, so these suites must run inside the dev container (Node 20/ABI 115). (2) `bun run test:e2e` fails with `The specified key does not exist` whenever the dev API container is running, because both processes watch the same Redis and the dev worker (real SeaweedFS storage) steals Bull jobs whose files only exist in the e2e's in-memory storage mock — stop the `api` service first, or run e2e in a `docker compose run --rm` one-off.

## 2026-08-18 — demo seeder (scripts/seed)
**Predicted:** seeder belongs in `packages/db/scripts/` next to `migrate.ts`, using drizzle directly.
**Actual:** it must live at repo root — `@repo/ai` (needed for embeddings) already depends on `@repo/db`, so a seeder inside `packages/db` that imports `@repo/ai` is a dependency cycle. Followed the `scripts/backfill-ticket-embeddings.ts` precedent instead.
**Why different:** package-graph direction, not convenience — the embedding step is what pulls the seeder up out of the DB package.

## 2026-08-18 — catalog item photos
**Predicted:** seeding `catalog_items.photoStorageKey` would be enough to show product images.
**Actual:** both pages hardcoded `src: null` / `photoSrc: null` and no endpoint served an item photo, so the column was inert. Needed a `GET .../catalog-items/:itemId/photo` route, a Next `proxyRaw` route (an `<img>` cannot send a bearer token), and resolved `catalogItem`/`queryItem` on the matches response.
**Why different:** the column existed but nothing downstream consumed it — a schema field is not a feature until something reads it.

## 2026-08-18 — DuckDB native binding / Node ABI pin
**Predicted:** the 4 failing API suites need a Node version whose ABI DuckDB supports, because a native addon built for one Node ABI cannot load under another.
**Actual:** half right. The Node pin (`.nvmrc` = 22, ABI 127; Dockerfile `setup_20.x` → `setup_22.x`) is the correct fix and took the suites from 4-failed/0-tests to 4-passed/58-tests with zero source edits — but the ABI is an **install-time** constraint only, not a runtime one. `duckdb.node` is N-API (verified: 71 `napi_*` symbols), so once the file exists it loads under any Node — an ABI-127 binding was confirmed running real queries under Node 25. What `node_abi` actually keys is `node-pre-gyp`'s *download URL* (`duckdb-v1.4.4-node-v{abi}-{platform}-{arch}.tar.gz`), which 404s for Node 23/25 regardless of the artifact's portability.
**Why different:** the failure mode reads like an ABI mismatch but is really a *fetch* miss — the install script ran fine (duckdb is not in `bun pm untrusted`; the earlier `trustedDependencies` theory was a red herring), found no artifact, fell through to a source compile that also failed, and left `lib/binding/` empty. Distinguishing the two matters for the guardrail: `scripts/verify-env.sh` now reports "works now, but a reinstall on this Node would leave lib/binding/ empty" (⚠) separately from "missing" (✗), because on a wrong Node with a populated `node_modules` everything passes right up until someone re-clones.

**Corrects the 2026-08-17 entry above:** DuckDB-backed specs no longer "must run inside the dev container (Node 20/ABI 115)" — `nvm use` on the host is now sufficient and is what `.nvmrc` enforces.

**e2e: RESOLVED 2026-08-18, and my own diagnosis above was wrong.** The A/B holds — e2e is Node-independent, 11/14 failing identically on Node 22 and 25. But I wrote that stopping the dev `api` container "did **not** help," and that conclusion was **confounded**: the 5s hook timeout was failing those same suites regardless, so the container fix was invisible underneath it. Two independent causes were stacked, and I read the combined symptom as one. Fixing the timeout first, then re-testing, separated them cleanly. Details in the 2026-08-18 e2e entry below.

## 2026-08-18 — e2e: three stacked causes, and why the first hid the other two
**Predicted:** the 11 failing e2e suites shared one root cause, and the demo seed data loaded the day before had made `cleanupUsers()`'s prefix scan slow enough to blow jest's 5s hook timeout.
**Actual:** three unrelated causes, and the seed-data theory was simply false. `users` holds 147 rows and `EXPLAIN ANALYZE` puts the `LIKE 'prefix%'` scan at **0.051 ms** — cleanup was never the cost. (1) **Hook timeout.** Teardown is ~6.6s because it closes a whole Nest app with Bull + Redis clients, not because of any query; jest's 5s default is just unrealistic for that. Set `testTimeout: 30000` in `test/jest-e2e.json` → 8 suites went green immediately. (2) **Bull job theft.** Every spec boots the full `AppModule`, so every parallel jest worker registers real queue consumers — and so does a running `optra-api` dev container. Any of them can consume a job whose uploaded file exists only in the *enqueueing* worker's in-memory `StorageService` stub, so the victim times out waiting for `status='done'` while the thief logs a real S3 `The specified key does not exist`. Fixed properly with `prefix: process.env.BULL_PREFIX || 'bull'` in `app.module.ts` plus a per-pid `BULL_PREFIX` in `jest-e2e.setup.ts` — `'bull'` is Bull's own default so production keys are byte-identical. (3) **Incomplete test double.** `chat.e2e-spec.ts`'s `jest.mock('@repo/ai')` factory omitted `classifyQuery` and `classifyStructuredIntent`, both of which `chat.service.ts` destructures. Result: 14/14 suites and 40/40 tests pass **in parallel, with the dev container running** — no `--runInBand`, no `docker stop`.
**Why different:** stacked causes invert the usual debugging order. The 5s timeout was the *cheapest* to fix and the *least* interesting, but until it was gone every experiment about the other two returned the same "11 failed" and looked like a null result — which is exactly how I mis-concluded that the container theory was disproven. The lesson is to fix the outermost, dumbest failure first and only then re-run the A/B, rather than A/B-ing a symptom that two causes can both produce. Two smaller notes worth keeping: `chat.controller.ts` swallows any non-`HttpException` into `res.write(...) + res.end()`, which yields **201 with no headers at all**, so a `TypeError` deep in `ChatService` surfaced as `expect(undefined).toContain('text/plain')` — the assertion pointed at headers while the real error was a missing mock export. And `globalTeardown` runs outside `setupFiles` and outside Nest's `ConfigModule`, so it must load the root `.env` itself or it defaults to Redis 6379 while this repo publishes 6380.

## 2026-08-18 — removing p-limit: the hack that made its own code untestable
**Predicted:** the 10 failing `packages/ai` tests were a Node-version problem — `new Function('specifier','return import(specifier)')` needed a newer Node, so pinning `.nvmrc` to 25 (or setting Vitest `pool: 'vmForks'`) would fix them.
**Actual:** both would have worked and both would have been wrong. Plain Node 22 executes that helper fine — I checked directly rather than inferring. The gap is **Vitest's module runner**, which gives `new Function`-compiled code no host dynamic-import callback; `--pool=vmForks` passes while `forks`/`threads` fail. The real defect was upstream of all of that: an **ESM-only dependency (`p-limit@7`) inside a package that compiles to CommonJS**, worked around by hiding the import from the compiler. Only `pLimit(n)` and `limit(fn)` were ever used, so `p-limit` was deleted and replaced by a ~30-line in-house `createLimit`. Result: 173/173 on Node 22, 24 **and** 25, and the `transformIgnorePatterns` shim in `apps/api/test/jest-e2e.json` — which existed solely to transpile `p-limit`/`yocto-queue` — could be deleted too, with e2e still 14/14.
**Why different:** a workaround that its own test runner cannot execute is not a workaround, it is an untested production path wearing one. The tell was that the hack lived in `dist/` and ran in production on every scrape job while ten tests covering that exact function had never once run. Pinning the Node version would have made the suite green and left that property intact — which is the difference between "tests pass" and "the code is verified". Two things fell out of fixing it properly rather than around it: the `.nvmrc` 22-vs-25 tension disappeared entirely (22 was blocked *only* by these tests), and the inverted packaging guard in `crawl.spec.ts` now fails if anyone reintroduces `new Function` or an ESM-only import into that CommonJS module.

**Two traps found while verifying, both worth remembering.**
(1) `apps/api/src/storage/storage.service.spec.ts` gates on `process.env.S3_ENDPOINT`. That var IS in the root `.env`, but the unit Jest config has no `setupFiles` and nothing loads dotenv before collection — so the suite reported a tidy "1 skipped" on every run and had **never executed**. It passes 3/3 the moment the endpoint is visible. A conditional skip whose condition is never satisfiable is indistinguishable from a passing suite in the summary line; the fix loads `.env` inside that one spec, not globally, because a global load would also hand 58 other suites live credentials including `EMAIL_OTP_ENABLED`.
(2) An incremental `bun install` left `node_modules/vite/node_modules/esbuild/bin/esbuild` SIGKILLed on exec (exit 137) with no matching `@esbuild/darwin-arm64@0.28.1` installed, breaking `packages/ai`, `packages/db` and `scripts/seed` at config load with `The service was stopped: write EPIPE`. That error reads like a test failure and is not — it is the same class as the DuckDB binding: a platform-specific package install failed to materialise. `bun install --force` repairs it. Worth checking a native binary actually *executes* before believing a sudden multi-package failure.

## 2026-09-20 — S0a compare integrity: row lock, ParseUUIDPipe, and two STATIC-ONLY guesses settled by tests
*No live prediction solicited (owner chose "skip live, log after" for this batch). "Predicted" is the plan's own written expectation in `docs/OPTRA-DEVELOPMENT-PLAN.md` rev 4 §4.*
**Predicted:** a `db.transaction` around delete+insert would be enough to stop a compare from leaving an empty or doubled flag set. The DuckDB "empty cell → NULL?" and "BIGINT drops leading zeros?" questions were both open (STATIC-ONLY), and a bad `flagId` "reaches Postgres".
**Actual:** the transaction alone fixes the *failed insert* case (prior set survives), but not concurrency: under READ COMMITTED, four parallel compares interleaved their deletes and inserts and left **3** flag sets in the test. Serializing needs a lock, so each transaction first takes `SELECT … FOR UPDATE` on the PO row (drizzle `.for('update')`, first use in this repo). Tests settled both DuckDB questions, with opposite outcomes. An empty cell **is** read as NULL: a blank-SKU, blank-description PO line was labeled `missing_on_po`. A `00123` SKU was **not** re-typed: DuckDB keeps a leading-zero column as VARCHAR, and that test passed on the old code. A non-UUID `flagId` returned **500**, not 400 (e2e red with the controller fix stashed). `ParseUUIDPipe` (also first use here) turns it into a 400. That only worked after moving `UploadExceptionFilter` off the controller class, because the class-level `@Catch(BadRequestException)` swallowed every non-upload 400 and flattened its body.
**Why different:** atomicity and isolation are separate guarantees. A transaction makes one writer all-or-nothing, but two writers still interleave unless something orders them. The row lock is the cheapest ordering available without a schema change; S1's append-only runs remove the need for it. The DuckDB outcomes show why the plan labels guesses STATIC-ONLY instead of fixing them blind. One guess was a real bug and the other was not, and the fix (build the match key in TypeScript, read `sku` from the stored row) makes the answer independent of type inference either way. A class-level exception filter is a scope decision, not a style choice: it applied to routes that never upload anything.

## 2026-09-20 — S0b parse integrity: retries that actually retry, and reconcile that actually recovers
*No live prediction solicited (owner chose "skip live, log after" for this batch). "Predicted" is the plan's own written expectation in `docs/OPTRA-DEVELOPMENT-PLAN.md` rev 4 §4 B9/B10.*
**Predicted:** rethrowing from the processor's catch block is enough to turn Bull's `attempts: 3` back on, and adding a periodic reconcile fixes stuck documents.
**Actual:** rethrowing *everything* would have been wrong in both directions. An unreadable PDF or a CSV with a broken quote fails identically on every attempt, and for the PDF case each attempt is a paid model call. So the processor now classifies failures. Document problems (`ProcurementParseInputError`, extraction Unsupported/Empty/Refusal, `CatalogParseInputError`) fail at once and return, so Bull records no failure and never retries. Everything else is rethrown. Only the final attempt (`attemptsMade + 1 >= opts.attempts`) writes `failed`, so a row the queue will retry stays `processing` instead of flickering to failed. Periodic reconcile alone would not have recovered anything either. It could only re-add a missing job under the **same** deterministic jobId, and Bull's `addJob` returns the existing job for an id it still holds (`removeOnFail: false` keeps failed ones). Requeueing therefore uses a fresh id (`…:r1`, `…:r2`), and the count is parsed back out of `queueJobId`, which bounds requeues at 2 with no schema change. The schedule is a Bull repeatable job with `repeat.every` (the insights ticks use `cron`). It runs on the same queue under a named `@Process('reconcile')`, so queue event logging has to skip that job because its data has no `kind`/`id`.
**Why different:** "retry" is only safe once you know which failures are about the world and which are about the input. Retrying input errors wastes money and delays the real message, and not retrying world errors turns a network blip into a failed document. The jobId trap is the same class of bug as the old dead retries: code that looks like it recovers but is silently a no-op underneath. The tell is to ask what the queue does with the *exact* id you hand it, not what the method name promises.

## 2026-09-20 — S0c LLM budget: measure what the provider bills, not what we guess
*No live prediction solicited (owner chose "skip live, log after" for this batch). "Predicted" is the plan's written expectation (rev 4 §4 B12, §5 S0c).*
**Predicted:** giving each chain a token count means changing its return type to `{ result, usage }`, and the budget check can simply be added next to each call site.
**Actual:** neither. Changing the return types would have broken 14 `jest.mock('@repo/ai')` factories and every caller at once. Each chain instead takes an optional `TokenMeter` and calls `meter.record(response)` right after every `invoke`. `@langchain/openai` 0.2.11 already sets `usage_metadata` on each invoke result, so retry attempts that got a response count too. A per-call-site check would have repeated the same three steps nine times: check, run, charge. It would also have missed the key detail, which is charging **in `finally`**, because a chain that gets a response and then fails to parse it has still spent the tokens. So there is one helper, `UsageService.metered(workspaceId, run)`, and every path goes through it. The job side needed its own decision. `metered` throws a 402 `HttpException`, and each processor now treats `isBudgetExceeded` as a final outcome (procurement/catalog permanent failure, FAQ/topic-gap stop without rethrow), since a Bull retry would be refused the same way. Catalog parse also had to rethrow it out of its per-page catch, or it would have skipped every page one at a time. One existing chat test asserted the old behaviour (condensed-question length added to the estimate). It changed on purpose, because condense is now charged from the provider count and adding the estimate too would double-charge.
**Why different:** an optional collector keeps the chain's public shape stable, so the measurement can be added without breaking every consumer. That tradeoff matters more than the few characters an extra argument costs. The "record in finally" rule falls out of asking what the provider actually bills: the request, not whether our parsing succeeded.

## 2026-09-20 — S0d CI gate: the second workflow file is the bug
*No live prediction solicited (owner chose "skip live, log after" for this batch). "Predicted" is the plan's written expectation (rev 4 §4 B21, §5 S0d).*
**Predicted:** add `.github/workflows/ci.yml` running type-check, lint and unit tests, and make `deploy.yml` depend on it.
**Actual:** one file, not two. GitHub has no "this workflow needs that workflow" edge; the only cross-workflow link is `workflow_run`, and a `workflow_run`-triggered run checks out and executes the **default branch's** copy of the workflow, not the pushed commit's — so a PR that changes the gate would be merged under the old gate, and the two runs report independently. Putting the `ci` job in `deploy.yml` makes `needs: ci` the whole contract, and `deploy` additionally carries `if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'` so a branch push runs the gate and can never reach the VPS. Concurrency had to split per job: `ci` cancels superseded runs (nothing outside the runner changed), `deploy` must not, because `up -d --force-recreate` cancelled halfway leaves the production stack partly replaced. The env was the fiddly part, not the YAML. Compose publishes Postgres on **54322** and Redis on **6380**, and `.env.example` still says 6379; api/web declare `env_file: .env`, so the file must exist even though only the three backing services start. Both are handled by writing `.env` from the example and then overriding in the job's `env:`, which wins because dotenv never overrides an already-set variable. The SeaweedFS keys are read out of `docker/seaweedfs/s3.json` at run time instead of being pasted into the workflow, so that file stays the single place they are written down — and without them `storage.service.spec.ts` would go on silently skipping in CI exactly as it did locally for months.
**Verified, and the first run paid for itself.** On throwaway branch `ci/s0d-verify`: run `35489182169` failed at `type-check` with `Cannot find module '@repo/db'`, run `35489278525` failed exactly where the deliberate probe test was, and run `35489426121` passed every step. `deploy` reported **skipped** all three times. The type-check failure was not in the local dry run, which had passed 9/9: `@repo/db` and `@repo/ai` point `main`/`types` at `dist`, a clean checkout has none, and turbo's `type-check` task depends on `^type-check` rather than `^build`, so nothing builds them. It only passed on my machine because an earlier e2e build had left `dist` behind. Fixed by running `turbo run build --filter=@repo/db --filter=@repo/ai` before `type-check`.

**Why different:** "make X depend on Y" is a dependency between *jobs*, and jobs only see each other inside one workflow. Reaching for a second file imported an ordering problem that did not need to exist. The general tell: when a CI feature seems to need cross-file coordination, check whether the platform resolves that file from the commit or from the branch tip — if it is the branch tip, the coordination is a race.

## 2026-09-20 — S0e env + deps: the bug was in the question, not the code
*No live prediction solicited (owner chose "skip live, log after" for this batch). "Predicted" is the plan's written expectation (rev 4 §4 D8/B11/B18/B19, §5 S0e).*
**Predicted:** align two `.env.example` values, declare `papaparse`, upgrade `xlsx`, and re-run B11 under `node dist/main` on Node 22 to see whether the old Bull crash still happens.
**Actual:** three of the four were as described, one was mis-framed at the root. The dependency work was wider than the register said — `xlsx` is declared in **two** packages (`apps/api` and `packages/ai`), so pinning one would have left two versions resolving, and `papaparse` is imported by **five** API files, not the two the row listed. The npm package is abandoned at 0.18.5, so the fix is a URL dependency on `cdn.sheetjs.com`; that clears both CVEs with no code change and all three XLSX specs passed untouched, but it costs something the plan's mitigation assumed we'd keep — `bun.lock` records **no integrity hash** for a tarball URL, and the CDN becomes a build-time dependency for CI and Docker. B11 was the real surprise. The question "does it still crash under Node?" was unanswerable as asked, because **production has never run Node**: `apps/api/Dockerfile:99` says `exec node dist/main`, but the base image `oven/bun:1.2.22` symlinks `/usr/local/bun-node-fallback-bin/node` to `/usr/local/bin/bun`, and through it `process.version` reports `v24.3.0` while `typeof Bun !== 'undefined'` is `true`. Bun spoofs a Node version, so the word `node` in the CMD had been hiding the runtime for months. Tested both: the compiled build ran `scrape-queue` and `ticket-extraction-queue` to `completed` with zero stacktrace entries on Node 22 **and** on Bun 1.2.22, in each case after real Postgres writes — well past the 20–30 ms crash point. B11 retired; a new risk row replaces it, because CI tests on Node 22 while production runs Bun, so no suite touches the runtime that serves users.
**Why different:** a bug report names the environment it was found in, not necessarily the one that matters. The TODO said "compiled build" and blamed Node 25, and a year of readers — me included, until I ran one `docker run` — carried that forward without checking what `node` resolved to inside the image. The cheap move was to verify the runtime before designing an experiment about it: one command reframed the whole item and turned a suspected live production bug into a retired one plus a real, previously invisible risk. When a report blames a version, confirm that version is the one actually running before testing anything else.

## 2026-09-20 — S0f catalog integrity: a delete is only as safe as the thing it claims to replace
*No live prediction solicited (owner chose "skip live, log after" for this batch). "Predicted" is the plan's written expectation (rev 4 §4 B13–B16, §5 S0f).*
**Predicted:** wrap the catalog search's delete+insert in a transaction, preserve dismissed matches, validate `vendorId`, order and escape the prefilter, fix the image content types, and scope the matches page to its query line.
**Actual:** mostly that, with two corrections that only showed up against the code. The delete's real defect was not "it isn't transactional" but "it isn't scoped to what the search re-judged": it filtered on workspace and line item alone, so a compliance verify for vendor A deleted vendor B's sourcing rows and every dismissal. Scoping it to `status='open'` + same `matchType` + same vendor fixes all three at once, and the transaction is the smaller half. Writing that test taught me the semantics I had not settled: vendor A legitimately ends up with **two** rows — a sourcing verdict and a compliance verdict — because `matchType` exists to distinguish "somewhere in our catalogs" from "this vendor carries it". My first assertion said one row; the assertion was wrong, not the code, so I changed the test and said so rather than narrowing the delete to make it green. The prefilter had a worse bug than the missing `orderBy` the register named: with a blank SKU *and* description the text filter was skipped entirely, so an arbitrary 8 catalog items went to a paid vision model with no signal at all. B15 flipped from a correctness fix to a security decision — `catalog-image.service.ts` accepts any remote `image/*`, which includes `image/svg+xml`, and serving the *true* stored type would have turned a harmless mislabel into stored XSS, so the fix is a raster allowlist, not "trust what we stored". And B16's prescription, "link `vendorId`", is not implementable: `purchase_orders` has no vendor column, so a discrepancy flag has no vendor to pass. The verify control is driven by the vendor dropdown already on the page instead, and the deep link is deferred to S3 with the schema evidence.
**Why different:** "make it atomic" and "make it correct" are different fixes, and the register named the first because it is the one you can see from the call shape. The scope of a delete is invisible until you ask what the matching insert is allowed to replace — which is also the question that settles the UI semantics. Second lesson: when a fix makes previously-mislabelled data render for the first time, re-ask what the data can do. Serving bytes truthfully is only an improvement if you have decided which truths are safe to serve.

## 2026-09-20 — S1 comparison runs: the delete was the feature, until it wasn't
*No live prediction solicited (owner chose "skip live, log after" for this batch). "Predicted" is the plan's written expectation (rev 4 §4 B1/D5/B6/B8, §5 S1).*
**Predicted:** add a `comparison_runs` table and a nullable `comparison_run_id`, stop deleting on re-compare, make "current" mean the latest succeeded run, and align the seed's delta with the engine.
**Actual:** the model work landed as planned; two things did not. First, **the plan had the delta fix backwards.** It said align the seed to the engine, but the engine computed `delta = PO − invoice` and wrote NULL for `poValue`/`invoiceValue`/`delta` on `missing_on_*` rows, so an overbilling displayed as negative and a missing line rendered three blank columns. The seed had it right all along. Neither side had a single test — `grep delta comparison.service.spec.ts` returned nothing — which is exactly why two opposite conventions could coexist for months. The engine moved to match the seed and both are now pinned. Second, **two existing tests had to change, and both were asserting the bug.** `re-comparing … replaces prior flags` and `serializes concurrent compares into exactly one flag set` were true only because of the delete that destroyed dismissals. Rewriting them was the point of the slice, not a concession to it: the property worth keeping is that the *user* sees one set, which the current-run query now provides, while the stored rows accumulate as evidence. I renamed both and left a comment saying what they used to assert and why that changed. A smaller discovery: the NULL-run fallback removed the need for a data migration entirely — a pre-S1 flag counts as current for its pair until that pair is compared again, so nothing vanished from the demo on deploy and no production rows had to be rewritten.
**Why different:** "align A with B" presumes B is right. The register named the seed as the deviant because the engine is the thing that runs — but the engine had no test, and the seed's convention was the one a human had reasoned about, in prose, in its `reason` strings. Untested code is not the reference implementation just because it is the live one. And when a change makes an existing test fail, the question is which of the two encodes the behaviour you actually want; deleting the test and weakening the test are both ways of avoiding that question.

## 2026-09-20 — S2 decisions: the audit table is defined by what it cannot do
*No live prediction solicited (owner chose "skip live, log after" for this batch). "Predicted" is the plan's written expectation (rev 4 §4 B5, §5 S2, POLICY v1 #7).*
**Predicted:** add an append-only `discrepancy_decisions` table, keep `PATCH dismiss` working as a `false_positive` decision, and add a history API.
**Actual:** as planned, with three decisions the plan left to implementation. **The note requirement collided with the existing route.** POLICY #7 says a decision carries a required note, but `PATCH dismiss` takes no body and the web client calls it that way. Rather than break that contract or quietly let notes be optional, the explicit endpoint demands a real note and the legacy route records an authored system note on the caller's behalf — so the policy holds for every decision, and the difference is visible in the data rather than hidden. **The outcome vocabulary did not fit the flag's status enum**, and the tempting fix was to teach `discrepancy_flag_status` four new values. That would have been this repo's first `ALTER TYPE`, on a forward-only deploy with no down migration, to express a distinction that is already captured in `outcome`. Every decision closes the flag; the status stays two-state. **The actor's role had nowhere to come from.** `CurrentUserContext` carries only `userId` and `email`, and `RolesGuard` checks the membership without attaching it to the request. Threading the role through the decorator would have touched every controller, so the service looks it up — and stores it, because the role at decision time is part of the record and memberships change. Idempotency fell out of S0a's earlier fix: `dismissFlag` only appends a decision when its guarded UPDATE actually transitions an open flag, so a re-dismiss adds nothing.
**Why different:** an append-only table is not a schema shape, it is the absence of code. Nothing enforces it at the database level here — the guarantee is that no update or delete path exists, which is why the risk register now says "do not add one" rather than describing the columns. The related lesson is that a policy written for new code has to be reconciled with the callers that already exist; "required" and "backward compatible" were both satisfiable, but only by deciding who supplies the value when the caller cannot.

## 2026-09-20 — S4 source evidence: pick the precedent that matches the threat, not the one the plan names
*No live prediction solicited (owner chose "skip live, log after" for this batch). "Predicted" is the plan's written expectation (rev 4 §4 D4, §5 S4).*
**Predicted:** copy `getItemPhoto` plus its `@Res()` route plus `proxyRaw`, and add a BFF route and client function.
**Actual:** the named template was the wrong one, and noticing that was the whole slice. `getItemPhoto` serves bytes **inline** for an `<img>`, which is why it carries a raster-only allowlist that refuses SVG — the allowlist exists precisely because nothing stops the browser rendering what it returns. A purchase order is `.csv`/`.xlsx`/`.pdf`, served from the web app's own origin through the BFF, so copying that shape would have meant deciding which uploaded types are safe to render. The knowledge-base download answers the question by not asking it: hard-coded `application/octet-stream`, always `attachment`, stored content type never consulted. No allowlist needed because nothing is ever rendered. I added `X-Content-Type-Options: nosniff` on top, after finding that the repo's only `nosniff` comes from `docker/Caddyfile` — so it is absent in local dev and in any topology without that proxy, and the header the API sets is doing the real work. Two smaller findings. `storageKey` is nullable and e2e fixtures insert rows without one, so "no bytes" had to be a distinct 404 from "no such document", and the list needed a derived `hasSourceFile` boolean rather than the key itself, which is an internal S3 path. And the filename sanitizer that stops quote-breakout and CRLF injection existed as **two independent copies** — defined in `documents.controller.ts`, inlined in `tickets.controller.ts`. Adding a third would have been the easy path; it is now one module with its own unit tests, and the two existing controller specs proved the move was behaviour-preserving.
**Why different:** a template is a set of decisions someone already made for *their* threat model. `getItemPhoto` and the KB download differ by one property — whether the browser renders the result — and every other difference between them follows from it. The plan named the template that was closest in mechanism (read a key, stream bytes, proxy) rather than closest in consequence. When reusing a pattern that touches security, the question is not "what does this code do" but "what was it allowed to assume".

## 2026-09-20 — S3a provenance: a column you cannot fill is the bug, not the fix
*No live prediction solicited (owner chose "skip live, log after" for this batch). "Predicted" is the plan's written expectation (rev 4 §5 S3).*
**Predicted:** add nullable provenance columns — source sheet, row, page — plus typed `extraction_confidence`, `extractor_version` and `uom`, and write them from the parse worker.
**Actual:** five of the six, and dropping the sixth was the real decision. **`source_page` cannot be filled honestly.** The digital-text PDF path flattens the document to a single string before prompting, and the vision path sends every page in one message with the index discarded — only a per-page loop like the catalog parser yields a page number, at one model call per page. A slice whose purpose is killing dead columns should not ship a sixth one, so it is deferred with the reason recorded. **`source_row` was not what it looked like either.** The obvious implementation — the array index inside the parsed rows — is the position among *surviving* rows, not the line in the file, because Papa's `skipEmptyLines` had already collapsed the array. My first test asserted rows 3 and 5 and got 2 and 3, which is exactly the assertion earning its keep: the fix was turning `skipEmptyLines` **off** so the index maps to the file again, and letting the existing `isEmptyLineItem` filter drop blanks downstream. Same output set, correct coordinates. **The `uom` alias list is a loaded gun.** `units` is already a quantity alias, so the intuitive list — `unit`, `units`, `uom` — would silently reclassify every "Units" column from how-many to what-kind and corrupt quantities on those documents. The list excludes both, and a test pins that `Units` still means quantity. Two smaller things: making `MappedLineItem` gain a required field broke eight fixtures at type-check but zero at runtime, because the runtime guard treated `undefined` as a value — fixed by making the guard nullish-safe *and* the fixtures explicit, not by weakening the type. And the hand-written backfill needed a `jsonb_typeof` guard: a `raw_row` whose confidence is the string `"high"` casts to numeric with an error, which I only saw because I tested the SQL against real rows rather than trusting it.
**Why different:** "add a column for X" assumes X is knowable. Three of these six fields turned out to have a different answer to "where would the value come from": one had it sitting unused in a blob, one needed a parser flag changed to stay truthful, and one had no source at all. Checking that before writing the migration is cheaper than discovering it after the column exists — a nullable column nobody writes looks identical to one that is merely not populated yet, and the repo already had four of those to prove it.

## 2026-09-21 — S3b header linkage: the validator that passes is not the same as the value that is stored
*Live prediction captured before implementation (Learning Contract triggered: no route in this repo parsed a non-`file` multipart field before this slice).*
**Predicted:** `@Body()` alongside `FileInterceptor` works under the existing `ValidationPipe({whitelist: true})` with no `transform`, since all six fields are strings. What bites is supertest `.field()`/`.attach()` ordering — attach first and multer has already streamed the file, so `req.body` is empty and the failure reads as "vendorId must be a UUID", looking like a validation bug rather than an ordering bug. Second: with no `forbidNonWhitelisted`, a typo'd field name silently produces a null column instead of a 400.
**Actual:** the first half held and is now pinned by an e2e test that posts no fields at all and asserts a per-field 400 — worth keeping, because "does ValidationPipe even run on a multipart body" was genuinely unverified here. The ordering trap never fired, only because I wrote `.field()` before `.attach()` from the start and said why in both the test and `uploadFile`; it remains the sharp edge for anyone adding a field later. The typo prediction was **moot**: making every field required means a missing field 400s loudly rather than nulling quietly — requiredness closed the hole I was worried about. The real surprise was elsewhere. **`IsISO4217CurrencyCode` is case-insensitive.** I chose a strict validator specifically so S6 could compare currencies without `usd` and `USD` reading as a mismatch — and the validator accepts both, so the column would have stored both and manufactured exactly the false discrepancy the choice was meant to prevent. Validation had to be followed by normalization, in the service, because the global pipe runs without `transform` and a `@Transform` would never fire. Two more. **A pinning test earned its place**: my first pass at loosening `UploadExceptionFilter` checked `typeof body === 'object'`, and the test written beforehand caught that Nest wraps a plain-string `BadRequestException` into an object too — the change would have silently added an `error` key to a response shape clients already read. The condition is `Array.isArray(body.message)` instead. **And widening both list projections broke the method that served them**: once the two shapes diverged, one `list(workspaceId, kind)` returned a union no caller could narrow by a literal `kind`, so it split into `listPurchaseOrders` / `listInvoices`.
**Why different:** "validated" is not a property of a value, it is a property of a *check* — and a check that accepts more than you assumed leaves the surplus in your database. The decision record said "validated 3-letter code" and I read that as "normalized 3-letter code"; only the e2e test that stored a lowercase value and read it back showed the gap. The same shape recurs in the filter: `getResponse()` returning an object is not evidence that the object is the one you meant.

## 2026-09-22 — S5 goods receipt: an exhaustiveness guard only guards what you converted
*Live prediction captured before implementation (Learning Contract triggered: no `assertUnreachable`, no `assertNever`, no `switch (kind)` and no `never` check existed anywhere in the repo).*
**Predicted:** converting the 18 `kind === 'purchase_order' ? … : …` sites to `switch` + `assertUnreachable` would be mechanical and behaviour-preserving, and the suite would stay green with zero test edits. The trap I expected: the guard only protects sites I actually convert, so a plain `if/else` I missed would keep compiling after the union widened — meaning the compiler is not the safety net I was claiming unless every site is converted first. I predicted a post-widening grep would find sites my list of 18 had missed.
**Actual:** the mechanical half held exactly — 15 API sites converted, full suite green with no test changes, and widening `ProcurementDocKind` then produced **12 `not assignable to parameter of type 'never'` errors**, each naming a site to fill. The prediction's second half was right in substance and wrong in shape. There were no missed `if/else` branches — the grep came back clean. **The gap was a site that was never a branch at all.** `ProcurementParseService.reconcile` is a *sequence* of per-table scans: select stale purchase orders, reconcile them, select stale invoices, reconcile them. Nothing there tests `kind`, so nothing narrows to `never`, so the compiler is silent — and a goods receipt whose parse job vanished would have sat `pending` forever with no stuck-document recovery, which is invisible until someone asks why a document never finished. The same shape applies to the controller's per-kind routes: six hard-coded route methods, no branch, no error. I added the third scan by hand and left a comment saying no compiler error will remind the next person. Three smaller findings. **`validateLineItem` needed a return type, not just wider input** — the three receipt quantities are optional going in (a PO caller builds the literal without them) but always present coming out, and without saying so every consumer spreading the result got three possibly-undefined fields. **Four existing `toEqual` assertions had to grow**, which is not the same as weakening them: they assert the complete mapped shape and the shape genuinely gained three keys. **And `isEmptyLineItem` was the quiet killer** — it tested five fields, so a receipt row stating only "8 received" read as empty and was filtered out before insert, meaning a GRN CSV would have parsed to zero rows while the upload reported success.
**Why different:** I framed the risk as "a branch I forget to convert" when the real risk was "code that never branched in the first place". Exhaustiveness checking secures the places where a type is *discriminated*; it says nothing about places where the same distinction is expressed by repetition — two queries, six routes, two cleanup blocks. Those are exactly the places a reader assumes are covered because the rest of the file now is. The lesson for the next document kind: after the compiler goes quiet, grep for the *table names*, not for the kind.

## 2026-09-22 — S6 three-way comparison: the new input broke the old path
*Live prediction captured before implementation (Learning Contract triggered: the repo had no `ALTER TYPE` anywhere — `grep -rn "ALTER TYPE" packages/db/drizzle/` returned nothing across 27 migrations).*
**Predicted:** hand-editing the generated migration to `ADD VALUE IF NOT EXISTS` would be safe, because drizzle diffs *snapshots* rather than SQL text, so `0027_snapshot.json` stays authoritative and the edit cannot desync the next diff. The real trap would be elsewhere: `COMPARISON_SQL` classifies with a single `CASE`, which returns the **first** matching branch, and a line can be simultaneously short-received and over-billed (ordered 10, accepted 7, billed 10) — so one of the two exceptions would be silently swallowed by branch order.
**Actual:** the migration half held exactly, and both verification paths passed — the already-at-0026 database (production's path) and from scratch. The `CASE` prediction was *right about the mechanism and wrong about the consequence*: branch order does pick one label, but because this slice also adds `received_value`, one flag row now carries ordered, received and billed together. The branch chooses the **label**, not the information, so nothing is hidden — a better outcome than the prediction feared, arrived at by accident rather than design. **The actual failure was in a place I never considered: the old path.** `Papa.unparse([])` returns an empty string, so a comparison with no receipts wrote a `grn.csv` with no header at all; `read_csv_auto` then built a table with zero columns and the new CTE failed to bind `mk`. That is `Binder Error: Referenced column "mk" not found in FROM clause` on **every** comparison — two-way included — because the third CSV is written on every run whether or not receipts exist. Twenty-odd previously-green tests went red at once.
**Why different:** I reasoned carefully about the semantics of the branch I was adding and not at all about the shape of the input I was adding. A new optional data source is not optional to the query that names it: the query references `grn_items` unconditionally, so the file must exist with its columns even when it holds nothing. The lesson generalises past CSV — any time a fixed query template grows a source, the empty case is a structural requirement, not a degenerate one.

*Process note, recorded because it changed the outcome.* I wrote the engine before its tests, which violates the Testing Requirement in CLAUDE.md. The owner said "strictly follow the rules", and the remedy was to back the implementation out, write the failing tests, confirm red, and re-apply. Two things came from that which would not otherwise have: three of the new tests turned out to pass against the *old* engine (they asserted "no flags", which a two-way run also returns for a matching pair) and were rewritten to assert the run mode instead; and the retention-guard test was validated by deliberately breaking the guard to watch it fail. Both are now written up in `docs/ai/testing-strategy.md`. Test-after produces tests shaped like the code; test-first produces tests shaped like the requirement.
## 2026-09-22 — S6 commit 2: the demo data was wrong and the engine said so
*Prediction captured before implementation (Learning Contract triggered: a run-level flag computed in TypeScript with no line reference is a shape this repo has never had — every flag until now came out of the engine keyed to a matched line).*
**Predicted:** the two needs-review types would split cleanly by where they are decided — UOM per line inside the SQL, currency per run outside it — and the work would be mostly plumbing: a column through the CSV, a branch in the `CASE`, a value object built in TypeScript. The trap I expected was ordering: `uom_mismatch` has to outrank every numeric branch, because a quantity difference measured across different units is not a number anybody can act on, and a `CASE` returns its first match.
**Actual:** the ordering held and cost nothing. Two things I did not predict. First, **the "absent is not a value" rule from commit 1 turned out to govern this half too, and more sharply.** A NULL accepted quantity must not read as zero; equally, an unstated *unit* must not read as a different unit — and almost no row in the system carries a unit, since `uom` only arrived in S3a and only a source with a unit column fills it. Reading absent as different would have put essentially every comparison into review. The same reasoning applied to currency, where it also protects every pre-0025 document. One rule, three columns. Second, **a document can contradict itself**: each CTE sums duplicate lines for one item, so two boxes plus one each is "3" of nothing. That is the same defect as two documents disagreeing, and it earns the same flag — which POLICY v1 #4 does not literally cover, since it speaks of "two compared lines". I extended it rather than leave the engine comparing a number the policy forbids comparing.
**Why different:** I predicted the risks of the code I was about to write, and the real find came from the data. The seeded `discrepancy_flags` are hand-written, so nothing forced them to agree with what `compare()` actually produces on the same rows. Running the engine over the demo seed once, with a throwaway spec, and diffing the two showed a receipt line's `uom` was keyed on its row number *within that receipt* — so when a PO's two deliveries held different line counts, the same item carried two different units and the engine flagged the demo data. **The engine was right and the seed was wrong**, which is the good direction for a surprise to run, but I only learned it because I looked. The golden fixtures pin the engine; nothing pins the seed to the engine. That check is worth making permanent.

## 2026-09-23 — S7 commit 1: the column that looked like an ordering key
*Prediction captured before implementation (Learning Contract triggered: `selectDistinctOn` appears nowhere in this repo, and this is the first offset-paginated endpoint in the procurement domain).*
**Predicted:** the hard part would be the current-run scope. It keeps a flag when its run is its pair's latest succeeded run, *or* when it has no run and its pair has never been compared — and that second branch ran in JavaScript, after the query. A filter that runs after the query cannot be paginated, so it had to become SQL without changing what it means. I expected the risk to be semantic drift in that rewrite, and planned to prove it by requiring the four S1 current-run tests to pass with zero edits.
**Actual:** that half went exactly as planned and the proof worked — the rewrite landed, the suite ran, 58 tests green, no test touched. **The real defect was in a column I had read a dozen times and never thought about.** `discrepancy_flags.created_at` is `defaultNow()`, and `compare()` inserts every flag of a run inside one transaction, so `now()` is evaluated once and all of them share it to the microsecond. `listFlags` ordered by `created_at` alone. That is not a total order, so `LIMIT`/`OFFSET` over it is free to return a different sequence per page — repeating some flags on page 2 and never showing others. It is the kind of bug that produces no error, no warning, and a queue a reviewer simply works incorrectly.
**Why different:** I reasoned about the predicate I was rewriting and treated the `ORDER BY` beside it as scenery. The tell was there in the repo the whole time: all five existing offset services end their `orderBy` with `desc(id)`, and I had read `documents.service.ts:59-97` as the pattern to copy without asking why the last clause was there. A convention you copy without understanding is a convention you will drop the first time you improvise. The generalisable form: **before paginating, ask what makes the sort key unique — and if nothing does, the pagination is already broken, whatever the LIMIT says.**

*Process note.* Two findings came from having a design agent pressure-test the approved plan rather than implementing it straight. It caught that paginating the procurement document lists would truncate the `<Select>` pickers feeding S3b's required upload links — a correctness regression strictly worse than the unbounded read the plan set out to fix — and that fixing `Modal`'s missing focus trap inside a feature slice would make both changes unreviewable. Both are now deferrals with reasons in `docs/ai/risk-register.md`. The plan was approved and still wrong in two places; approval is not verification.

## 2026-09-23 — S7 commit 2: the routes existed, the callers never did
*Prediction captured before implementation (Learning Contract triggered: first endpoint in this repo that lists `comparison_runs`, and the first join of `users` into a procurement read).*
**Predicted:** mostly assembly. The decision model shipped in S2 — four outcomes, a required note, an append-only trail, routes, guards, a BFF proxy, and client functions — so commit 2 would be a modal plus one new read endpoint. The risk I expected was overlay accessibility, since `packages/ui` has no Sheet and `Modal` has no focus trap.
**Actual:** the assembly was assembly. Two things were not. **First, `apps/web/src/lib/api/procurement.ts` had exported `listDiscrepancyDecisions` and `recordDiscrepancyDecision` since S2 with literally zero callers** — a repo-wide grep returned only their definitions. The entire audit spine was reachable by nothing, and the only affordance a reviewer had was Dismiss, which silently writes a `false_positive` decision the UI never showed. Nothing was broken; the feature simply had no way in, and no test could have caught that because every layer worked. **Second, the role gating was never tested.** Every 403 in the procurement e2e suite came from `WorkspaceMemberGuard`, because the fixture was always an *outsider* — someone not in the workspace at all. `RolesGuard` had never once run in that file. A member able to dismiss and decide would have passed 52 e2e tests.
**Why different:** I was looking for defects in what I was about to write, and both findings were about what was already there and unexercised. They share a shape: a thing that works, connected to nothing that uses it. Coverage counts lines executed, not paths a user can reach — and "the route has a test" is not "a role can be refused", any more than "the client function exists" is "the feature ships".

*Correction to my own approved plan, recorded because it was wrong twice.* The plan said to paginate the three procurement document lists. `apps/web/app/workspaces/[id]/procurement/page.tsx:804,878` renders `purchaseOrders` as the `<Select>` options for the invoice→PO and GRN→PO links that S3b made required, so paging that list to twenty would make any PO past the first page unlinkable — a correctness regression worse than the unbounded read it set out to fix. The plan also put a `Modal` focus-trap fix in this slice; that touches six-plus consumers and would have made both diffs unreviewable. Both are now deferrals with reasons in `docs/ai/risk-register.md`. An approved plan is a hypothesis, not a specification.

## 2026-09-23 — S8 commit 1: the guard that was on two sides of three
*Predict-verify skipped — `eq(goodsReceipts.status, 'done')` is the same guard `loadReadyPo`/`loadReadyInvoice` have applied since S0a. Recorded anyway, because the reason it was missing generalises.*

Planning S8's automation surfaced a bug in the code automation would drive. `compare()` reads goods receipts filtered by workspace and by purchase order, and not by the receipt's own `status` — while the PO and the invoice sides both refuse anything that is not `done`. Line rows live independently of their header: a re-parse replaces them wholesale, and a **failed** parse leaves the previous attempt's rows sitting there while the header reads `failed`. So a receipt the system knows it could not read was still contributing accepted quantities.

What makes it worth an entry is the direction of the error. The stale quantity is usually *low* — a partial parse — and a low accepted quantity is precisely what produces `short_receipt` and `invoice_exceeds_received`. The failing test printed the sentence the old code was willing to write: `Invoice bills more than was accepted for A1: accepted 7, invoiced 10`, against a supplier who delivered all ten. A silent bug that reads as an accusation is not the same class of bug as one that drops a row.

Two reusable tells. **First: a lifecycle field checked on some siblings and not others.** There was no compile error and could not be — the guard is expressed by repetition across three reads, and `strictNullChecks: false` in `apps/api/tsconfig.json` means nothing about the API side would ever have complained. The risk register already warned that `comparison.service.ts` expresses the kind distinction by repetition; this is the same shape, one field over. **Second: automation is a magnifier, not a cause.** This has been wrong since S5 and nobody hit it, because a human presses Compare when they believe the paperwork is ready. Auto-compare fires exactly while documents are changing. Before automating a path, the question is not "does this work?" — it is "what did the human's judgement used to supply that nothing will now?"

*Process note.* This landed as its own commit ahead of the auto-compare work, on a design agent's argument: it is a latent defect on the manual path today, provable in isolation, and folding it into the slice that made it urgent would have made both unreviewable. The proof that it narrowed nothing else is that all seven existing three-way tests passed **unedited** — `seedGoodsReceipt` hard-codes `status: 'done'`, so they were always describing the filtered world.

## 2026-09-23 — S8 commit 2: the id that would have swallowed the change
*Prediction captured before implementation (Learning Contract triggered: the repo's first comparison queue, the first processor handing work to another module's queueing service since `ScrapeProcessor`, and a freshness skip that is the only thing bounding append-only run growth).*

**Predicted:** the dangerous part would be the stop condition. A comparison run is permanent evidence and POLICY v1 #9 then refuses to hard-delete any document it referenced, so automation with no skip does not waste CPU — it permanently enlarges the set of things nobody can delete. I planned the guard as "skip when the pair's latest succeeded run is newer than every document feeding it", and expected the deduplication to come from a deterministic Bull job id plus a short delay, the way `procurement-parse:{kind}:{id}` already works.

**Actual:** the stop condition was right in shape and wrong in three details, all of which only appear when you ask *which instant* a run represents. **It has to compare against `started_at`, not `finished_at`.** A run that starts at T0, reads its lines, and finishes at T2 saw nothing of a re-parse at T1 — comparing against T2 calls that run current when it is a torn read of a document that changed underneath it. **It has to include `strategy_version`**, or an engine change leaves every pair pinned to a verdict the current rules would no longer produce; and that column had shipped in S1 as a DB default with no writer, so making it mean anything meant writing it. **It has to include the receipts**, which are in neither of the job's two named documents and still change the answer.

**And the deterministic id was actively unsafe.** A *fixed* id plus a delay does not merely deduplicate — while a job sits delayed, Bull hands that same job back to the next `add`. So a second document finishing its parse would be handed the job that had already resolved the old state, and its change would be silently swallowed: not a duplicate run, a *missing* one. The id now carries the newest input `updated_at`, so only an enqueue for the identical state is ever coalesced, which is the only kind that is safe to drop. The delay survives as an optimisation; the freshness check is the correctness mechanism.

**Why different:** I reasoned about the job as a *thing to deduplicate* and not as a *claim about a moment*. Every one of the four corrections is the same question asked again — what state did this job read, and when? `finished_at` answers "when did we stop", `created_at` answers "when were we asked"; only `started_at` answers "what did we see". A deterministic id says "this work is already queued", which is true right up until the work changes. The generalisable form: **an idempotency key must identify the input, not the intention.** Keying on the intention is how a system loses an update while looking perfectly idempotent.

*Process note.* A design agent found all four before a line was written, against a plan the owner had already approved — the same pattern as S7. It also argued the goods-receipt status filter out of this commit into its own, which turned out to be right: that defect was latent on the manual path and provable in isolation. Approval is not verification, and the second reading is where the corrections live.

## 2026-09-23 — S8 commit 3: three lists that all claimed to be complete
*Predict-verify skipped — the enum migration follows the rule 0027 established, and the event write copies the six existing `EventsService.record` callers verbatim. Recorded for the pattern the consumers exposed.*

Adding two values to `workspace_event_type` meant finding everything that had quietly hard-coded the old six. There were three, and **not one of them would have failed a build**:

- `apps/web/app/workspaces/[id]/page.tsx` duplicates the union by hand, because the API's shape does not reach that file as a type — and the icon `switch` beside it has a `default`, so a new event renders the generic alert glyph and nothing complains.
- `digest-renderers.ts` keys labels off `Record<string, string>` with a `?? type` fallback, so a missing label emails a customer "3 comparison_flagged".
- `scripts/seed/__tests__/data.test.ts` asserted the seed covered every value by comparing against a **hand-copied list of six**. A test whose expected value is a copy of the thing it is checking is not a test; it agreed with the seed and both were wrong together.

The fix in each case was to derive from `workspaceEventTypeEnum.enumValues` rather than to add two more strings. The digest now renders one of every value and asserts no raw enum name survives into the HTML. The seed test compares its coverage set against the enum. The web union stays hand-written — the type genuinely does not cross that boundary — so it gets a test that renders an unknown type alongside the new ones and asserts they do not resolve to the same icon, which is the only way to catch a `default` swallowing a case.

**The generalisable form: a fallback and a hand-copied list fail the same way — silently, and only in front of a user.** `?? type` and `default:` are both written to be defensive, and both convert "someone forgot" into "the product says something slightly wrong forever". Where a list must be duplicated, the duplicate needs a test that reads the original; where it need not be, derive it.

*Two smaller things worth keeping.* Seeding `comparison_failed` honestly required a failed run in the demo data, which the seed had never had — so S7's run history had shipped a `lastError` rendering path that no demo could show. And the event is named `comparison_flagged` on the owner's call: it fires only when a run finds something, and `comparison_completed` would have been a name that promised every completion. Enum values cannot be dropped in Postgres, so that was a one-way door worth stopping at.

## 2026-09-23 — S9 commit 1: the branch chose the label and quietly chose the evidence
*Predict-verify skipped — this is S6's `received_value` move applied to a second pair of columns. Recorded because the shape of the defect generalises past this repo.*

`COMPARISON_SQL` classifies a line with a `CASE`, which returns its first match, and `quantity_mismatch` is tested before price. Everyone knows a `CASE` picks one label. What nobody had noticed is that it was also picking what got **stored**: the engine computed `po_price_min/max` and `inv_price_min/max` on every row and always had, but `toFlagValues` read them only when the winning label happened to be `price_mismatch`. So a line where the vendor changed both the quantity and the unit price — the single commonest real case — was recorded as a quantity problem, and the price difference existed nowhere in the system. A reviewer fixes the quantity, closes the flag, and pays the new price without ever seeing it.

No test failed, and none could have. Every test asserted the label, and the label was right.

Two things made it findable. The data was already in the row — `joined` selects all four price aggregates and `ComparisonRow` types them — so the fix touched no SQL at all, which is what let the whole change be reviewed as "the engine's verdict cannot have moved". And S6 had already solved this exact problem once, for the receiving trio, and written the reason down in a comment three lines above the bug: *"nothing is hidden by the branch order that picked between them."* The rule was in the file. It had been applied to three columns and not the fourth.

**The generalisable form: a branch that selects a label must never also select what is recorded.** Ranking is a presentation decision — which single word best describes this row. Persistence is an evidence decision — what did we know when we looked. Collapsing the two means the losing branches take their findings with them, silently, and the only symptom is a downstream human making a worse decision much later.

The tell to grep for: a computed value read inside a conditional whose condition is the classification. If `isPrice ? price : null` appears next to a value the engine produced unconditionally, the `null` branch is throwing away something real.

## 2026-09-23 — S9 commit 2: the constraint that compiled, generated, and meant the opposite
*Learning Contract triggered (first partial index, first expression index, first auto-supersession in this repo). No live prediction solicited — the approved plan stands as the prediction, and the diff below is against it.*

**Predicted (from the approved plan):** a partial unique index on `(workspace, vendor, lower(sku), coalesce(uom,''), currency) WHERE effective_to IS NULL` would be the cheap database-level backstop under the real mechanism, which is the service closing the previous open term inside the insert's transaction. The plan had already reasoned that the *general* constraint — no two windows overlapping at any instant — needs `btree_gist` and `EXCLUDE USING gist`, which drizzle-kit cannot emit, and that hand-writing it into a generated migration would drift from the snapshot.

**Actual:** the partial index is not merely unavailable. drizzle-orm 0.30's builder **accepts `.where()`** — it is in the type, the compiler is happy, the schema file reads exactly as intended — and drizzle-kit 0.20 then **drops the clause while generating**, emitting a plain `CREATE UNIQUE INDEX` over the same columns. A full unique index on `(workspace, vendor, sku_key, currency)` would have rejected every superseded term, which is precisely the history the table exists to hold. The failure mode is not "the constraint is missing" but "the constraint means the opposite of what was written." The `sql\`coalesce(...)\`` expression in the column list was worse still: it emitted syntactically invalid SQL, quotes and all, which at least would have failed loudly.

I only saw it because I read the generated `.sql` before applying it. Had I trusted the schema file, the first symptom would have been a legitimate price change rejected at runtime, months later, with a unique-violation pointing at an index nobody remembered asking for.

**Why different:** I reasoned about what drizzle-kit *could express* and not about what it does when it cannot. A generator that silently narrows its input is more dangerous than one that refuses it, because the type system sits on the wrong side of the gap — the builder's types describe drizzle-orm's runtime query API, not drizzle-kit's DDL emitter, and nothing connects the two. **A schema DSL is a claim about intent; only the emitted DDL is a claim about the database.** The general rule now in the risk register: after adding any index that is partial, expression-based, or a constraint beyond a plain unique/FK, generate and read the SQL, and if the generator changed its meaning, delete it from the schema rather than patch the migration — a hand-written constraint lives outside the snapshot and drifts on the next generate.

The invariant moved into `VendorPriceTermsService.create`, which is defensible here for a specific reason worth stating: the service is the *only* writer. That is what makes a service-level invariant equivalent to a database one. The moment a second writer appears — a bulk import, a migration backfill — it stops being equivalent, and this note is the thing to re-read.
